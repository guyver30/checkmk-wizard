---
context: default
phase: none-active (last completed: 13-wizard-parents-support-and-topology-map)
task: 0
total_tasks: 0
status: paused
last_updated: 2026-09-24T01:23:42.114Z
---

<current_state>
No phase is mid-execution. This session was operational quick work around rebuilding a blank Checkmk site on the deployment host (`~/checkmk-stack/app/checkmk-wizard/deploy`, podman-compose project `deploy`). Site volume wiped, wizard run successfully (18 hosts, automation user created from the env secret). The Mosquitto volume was wiped at 01:16 UTC after a first attempt had not actually removed it; dashboard confirmation is still pending.
</current_state>

<completed_work>

- quick 260924-azn: `CMK_REST_SECRET` pre-seed (wizard creates/updates the `automation` user to match; never prints an env-sourced secret) — merge `c06658a`, docs `5b42e82`
- `CMK_LIVESTATUS_TCP=on` on the checkmk compose service + docs — `7045f96`
- Podman doc §8.5 "Starting over with a blank site" — `96e7182`
</completed_work>

<remaining_work>

- Sync the deployment host with the latest commits (its compose.yaml lacks `CMK_LIVESTATUS_TCP=on`), then `podman compose up -d`
- Confirm the dashboard shows only current hosts after one poll cycle (hard refresh)
- Live-verify: `CMK_LIVESTATUS_TCP` env on the next wipe; the PUT update path for the pre-seeded secret
- Optional: quick task so the poller clears retained `lan/devices/*` topics with no matching Checkmk host
- Still outstanding from before: `/bm:secure-phase 13`; Phase 14 not planned
</remaining_work>

<decisions_made>

- Blank site = remove BOTH `deploy_checkmk_data` and `deploy_mosquitto_data` (dashboard reads only retained MQTT; tombstones only fire for devices leaving a running site)
- Env is read in `wizard.py`, passed to `bootstrap_automation_user(secret=...)`; empty string = unset
- `.env.example` `CMK_REST_SECRET` now empty rather than `REPLACE_ME`
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/quick/260924-azn-pre-seed-fixed-automation-user-rest-secr/260924-azn-SUMMARY.md` — what shipped and what is unverified
2. `docs/Podman setup for checkmk, minio, mosquitto, worker.md` §5 and §8.5 — current operational procedure

## Infrastructure State
- Deployment host: all compose services up as of the last paste; mosquitto volume recreated 2026-09-24 01:16 UTC, checkmk volume kept (fresh site, wizard already run)
- The dashboard dev server was run with `npm run dev` in `node:22-alpine` on the host network (`CHECKMK_PROXY_TARGET=http://localhost:8080`)
- Credentials (`CMK_REST_SECRET`, poller password `poller`) were printed by podman-compose and pasted in chat; disposable, rotate before wider exposure

<context>
Unrelated dirty state left alone on purpose: untracked `docs/resize.gif` and the empty Phase 15 scaffold directory. Ruff has 7 findings from before this work.
</context>

<next_action>
Start with: sync the deployment host to the latest commits, `podman compose up -d`, then check the dashboard shows only the 18 current hosts.
</next_action>
