---
phase: 12-agent-metrics-and-service-status
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/src/components/TreeNode.tsx
  - dashboard-react/src/components/Tree.test.tsx
  - .planning/PROJECT.md
autonomous: true
requirements: [DASH-11]

must_haves:
  truths:
    - "Clicking (or keyboard-activating) a device row in the fleet tree navigates to that device's drill-down at /details?id={id}"
    - "The tree's group rows keep their existing expand/collapse behaviour, unchanged"
    - "PROJECT.md's Out of Scope entry no longer forbids the read-only per-service drill-down this phase ships"
  artifacts:
    - path: "dashboard-react/src/components/TreeNode.tsx"
      provides: "device-row navigation into the details route"
      contains: "/details?id="
    - path: ".planning/PROJECT.md"
      provides: "amended Out of Scope entry recording the partial reversal"
      contains: "read-only"
  key_links:
    - from: "dashboard-react/src/components/TreeNode.tsx"
      to: "dashboard-react/src/routes/DetailsRoute.tsx"
      via: "react-router Link to /details?id={id}"
      pattern: "details\\?id="
---

<objective>
Make the per-device drill-down reachable: device rows in the fleet tree navigate to
`/details?id={id}` (D-15). Then amend PROJECT.md's Out of Scope entry that currently forbids what
this phase deliberately ships (ROADMAP Phase 12 scope item 6).

Purpose: without this, every gauge and service row Phase 12 builds is reachable only by hand-typing a
URL. It is deliberately a separate, dependency-free plan so it can run in parallel with the poller
and data-layer work.
Output: one navigable tree row plus its test, and one corrected documentation entry.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-metrics-and-service-status/12-CONTEXT.md
@.planning/phases/12-agent-metrics-and-service-status/12-UI-SPEC.md
@dashboard-react/src/components/TreeNode.tsx
@dashboard-react/src/components/Tree.tsx

<interfaces>
<!-- Existing contracts. -->

// dashboard-react/src/components/TreeNode.tsx
export interface TreeNodeProps { node: TreeGroupNode; depth: number; isOpen: boolean;
  onToggle: (key: string) => void }
// Device children are rendered at lines ~76-100 as a plain div with role="treeitem",
// tabIndex={-1}, depth-derived paddingLeft, className "flex items-center gap-2 py-1.5 pr-3 text-sm"
// and aria-label `${device.label} — ${displayState}`. Each child exposes: id, label, state,
// stale, typeIcon, tagGroupMissing.

// react-router (v8, already a dependency and already used in App.tsx)
import { Link } from "react-router";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device rows navigate to the drill-down</name>
  <files>dashboard-react/src/components/TreeNode.tsx, dashboard-react/src/components/Tree.test.tsx</files>
  <read_first>
    - dashboard-react/src/components/TreeNode.tsx in full (the group-row button pattern at lines 37-46 and the device-row div at lines 76-100 being made navigable)
    - dashboard-react/src/components/Tree.tsx (how TreeNode is composed and what the tree's roles/aria structure expects)
    - dashboard-react/src/components/Tree.test.tsx (existing render helpers and whether a router wrapper is already present)
    - .planning/phases/12-agent-metrics-and-service-status/12-UI-SPEC.md Scope Boundary, D-15 bullet ("reuse the row's existing hover/interactive styling; no new visual contract")
  </read_first>
  <action>
Import `Link` from `react-router` (the same import path `App.tsx` already uses) and turn each device
child row into a navigation target to `/details?id={device.id}`, with `encodeURIComponent` applied to
the id so a host name containing a reserved URL character still resolves.

Keep the row's existing structure and classes: same `role="treeitem"`, same `aria-label`, same
depth-derived `paddingLeft` inline style, same `flex items-center gap-2 py-1.5 pr-3 text-sm` class
list, same icon / tag-warning / label / `StateBadgeForState` children in the same order. Add only what
navigation needs: render the row's content inside a `Link`, and add
`hover:bg-bg-subtle-hover` so the device row gets the same hover affordance the group button already
has. Do not add a new colour, border, radius or spacing token — UI-SPEC explicitly says this row
needs no new visual contract.

Accessibility: the row must remain reachable and activatable by keyboard. A `Link` is natively
focusable, so drop the row's `tabIndex={-1}` rather than leaving a focus trap, and keep exactly one
accessible name for the row — do not end up with both the div's `aria-label` and the link text
announcing the device twice; put the `aria-label` on whichever element is the treeitem and leave the
inner link's text as the visible label.

Group rows are untouched: `onToggle` and `aria-expanded` behaviour stays exactly as it is.

Tests in `Tree.test.tsx` (add a `MemoryRouter` wrapper to the existing render helper if one is not
already present — every test in the file must keep passing): assert a device row renders a link whose
`href` is `/details?id=web1`; assert an id needing encoding (for example `web 1`) produces
`/details?id=web%201`; and assert clicking a group row still toggles it rather than navigating.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/components/Tree.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "encodeURIComponent" dashboard-react/src/components/TreeNode.tsx` returns 1
    - `grep -n "from \"react-router\"" dashboard-react/src/components/TreeNode.tsx` matches
    - A test asserts a rendered device row link has `href` equal to `/details?id=web1`
    - A test asserts a device id containing a space produces `href` `/details?id=web%201`
    - A test asserts activating a group row still calls its toggle handler
    - `grep -c "tabIndex={-1}" dashboard-react/src/components/TreeNode.tsx` returns 0
    - `npm --prefix dashboard-react test` passes in full and `npm --prefix dashboard-react run typecheck` exits 0
  </acceptance_criteria>
  <done>Every device row in the fleet tree is a keyboard-reachable link into that device's drill-down, with the group rows' behaviour unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Amend PROJECT.md's per-service drill-down Out of Scope entry</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 30-45 (the Out of Scope list, including the entry at line 37 being amended)
    - .planning/REQUIREMENTS.md Out of Scope table (already amended during planning on 2026-09-21 — this task makes PROJECT.md say the same thing, so read it first and match its reasoning rather than inventing new wording)
    - .planning/phases/12-agent-metrics-and-service-status/12-CONTEXT.md, the Phase Boundary paragraph on partially reversing this entry
  </read_first>
  <action>
Replace the Out of Scope bullet reading "Duplicating Checkmk's own per-service drill-down UI in the
new dashboard — link out to Checkmk's UI for full service-level detail instead" with an entry that
narrows the exclusion to write/administration actions rather than read-only status, matching the
already-amended wording in `.planning/REQUIREMENTS.md`'s Out of Scope table: what remains out of scope
is Checkmk's per-service configuration and administration UI — rule editing, downtime scheduling,
acknowledgement, service discovery and every other write action — while a read-only per-service
status list and agent-metric gauges are in scope as of Phase 12 (DASH-08 through DASH-10), because
"why is this host red" could not be answered without them. Note in the same entry that the external
deep link into Checkmk's own UI remains unbuilt (D-17) and that `CHECKMK_BASE_URL` already exists in
`dashboard-react/src/lib/config.ts` for whenever it is.

Change only that bullet. Do not reflow, reorder or reword any other Out of Scope entry, and do not
touch PROJECT.md's Key Decisions table — decisions are logged there by the phase-completion flow, not
by this plan.
  </action>
  <verify>
    <automated>grep -c "per-service drill-down UI in the new dashboard — link out" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "per-service drill-down UI in the new dashboard — link out" .planning/PROJECT.md` returns 0 (the old absolute prohibition is gone)
    - `grep -n "read-only" .planning/PROJECT.md` matches within the Out of Scope section
    - `grep -n "D-17\|deep link" .planning/PROJECT.md` matches, recording that the Checkmk link stays unbuilt
    - `git diff --stat .planning/PROJECT.md` shows changes confined to the Out of Scope section (no Key Decisions or Context edits)
  </acceptance_criteria>
  <done>PROJECT.md no longer forbids the read-only per-service view this phase ships, and records precisely what is still excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkmk host name → URL query string | Device ids originate from Checkmk and are interpolated into a client-side route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Tampering (URL injection) | TreeNode device link | mitigate | `encodeURIComponent` on the id before interpolation, asserted by the space-containing-id test; the poller has already rejected topic-unsafe ids upstream via `is_publishable_device_id` |
| T-12-15 | Elevation of Privilege | navigation target | accept | `/details` is a read-only client-side route on a static, backend-less dashboard; no action can be triggered by reaching it |
| T-12-SC | Tampering | package installs | accept | No new packages; `react-router` is an existing dependency already used by `App.tsx` |
</threat_model>

<verification>
- `npm --prefix dashboard-react test` passes in full.
- `npm --prefix dashboard-react run typecheck` exits 0 and `npm --prefix dashboard-react run lint` reports no new findings.
- PROJECT.md's Out of Scope section matches REQUIREMENTS.md's amended entry in substance.
</verification>

<success_criteria>
A device row in the fleet tree navigates to its own drill-down, and the project's own scope
documentation no longer contradicts the feature Phase 12 delivers.
</success_criteria>

<output>
Create `.planning/phases/12-agent-metrics-and-service-status/12-05-SUMMARY.md` when done
</output>
