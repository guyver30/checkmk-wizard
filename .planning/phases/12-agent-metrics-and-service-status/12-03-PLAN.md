---
phase: 12-agent-metrics-and-service-status
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/src/lib/types.ts
  - dashboard-react/src/lib/serviceSort.ts
  - dashboard-react/src/lib/serviceSort.test.ts
  - dashboard-react/src/lib/gauges.ts
  - dashboard-react/src/lib/gauges.test.ts
  - dashboard-react/src/store/mqttClient.ts
  - dashboard-react/src/store/mqttClient.test.ts
  - dashboard-react/src/store/useAppStore.ts
  - dashboard-react/src/store/useAppStore.test.ts
autonomous: true
requirements: [DASH-08, DASH-09, DASH-10]

must_haves:
  truths:
    - "The dashboard subscribes to `lan/devices/+/services` and `lan/devices/+/service_history`"
    - "A services payload lands in the store keyed by device id, and a malformed one leaves the previous value intact"
    - "A zero-length retained payload on either new topic clears that device's slice, matching the existing tombstone behaviour"
    - "Gauge ring colour is derived from a metric's own warn/crit numbers, in one shared helper, never from a Checkmk service state"
    - "Service rows sort CRIT, WARN, UNKNOWN, OK, ties broken by name"
  artifacts:
    - path: "dashboard-react/src/lib/types.ts"
      provides: "DevicePayload gauge fields, ServiceEntry, ServiceHistoryEntry"
      contains: "ServiceEntry"
    - path: "dashboard-react/src/lib/gauges.ts"
      provides: "gaugeColor(), smartBadge(), otherMountsLabel()"
      contains: "export function gaugeColor"
    - path: "dashboard-react/src/lib/serviceSort.ts"
      provides: "SEVERITY_ORDER, compareServices()"
      contains: "export function compareServices"
    - path: "dashboard-react/src/store/useAppStore.ts"
      provides: "services and serviceHistory state slices plus their handleMessage branches"
      contains: "serviceHistory"
  key_links:
    - from: "dashboard-react/src/store/mqttClient.ts"
      to: "lan/devices/+/services"
      via: "SUBSCRIBE_TOPICS array entry"
      pattern: "lan/devices/\\+/services"
    - from: "dashboard-react/src/store/useAppStore.ts"
      to: "ServiceEntry[]"
      via: "handleMessage branch on parts[3] === 'services'"
      pattern: "\"services\""
---

<objective>
Extend the dashboard's data layer to carry Phase 12's new payloads: gauge fields on the existing
device status shape, two new store slices fed by two new subscribed topics, and the two pure
helpers (`gaugeColor`, `compareServices`) the detail route will render from.

Purpose: keep every computation and every defensive parse out of the route component, so plan 12-04
is pure presentation.
Output: typed payload shapes, subscribed topics, store branches and tested helpers. No visible UI
change yet.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-agent-metrics-and-service-status/12-CONTEXT.md
@.planning/phases/12-agent-metrics-and-service-status/12-UI-SPEC.md
@.planning/phases/12-agent-metrics-and-service-status/12-PATTERNS.md
@dashboard-react/src/lib/types.ts
@dashboard-react/src/store/useAppStore.ts
@dashboard-react/src/store/mqttClient.ts

<interfaces>
<!-- Existing contracts the new code must match. -->

// dashboard-react/src/lib/types.ts
export interface DevicePayload { id?, alias?, state?, host_state_raw?, device_type?, folder?,
  timestamp?, staleness?: number | null, downtime?: boolean, acknowledged?: boolean, services?: unknown }
export interface HistoryEntry { state?: string; timestamp?: string; [key: string]: unknown }

// dashboard-react/src/store/useAppStore.ts
export interface AppState { devices: Record<string, DevicePayload>; history: Record<string, HistoryEntry[]>;
  events: EventEntry[]; topology: TopologyPayload | null; pollerStatus: PollerStatusPayload | null;
  lastKnownPollerTimestamp: string | null; connection: ConnectionState;
  handleMessage: (topic: string, payload: Uint8Array) => void; setConnection: (s: ConnectionState) => void }
function isPlainObject(value: unknown): value is Record<string, unknown>
function parsePayload(buf: Uint8Array | null | undefined): { ok: true; value: unknown } | { ok: false }

// kone-design-system
export type ProgressColor = "brand" | "success" | "warning" | "danger";
</interfaces>

<payload_contract>
<!-- Published by scripts/mqtt_poller.py after plan 12-02. Field names are the wire contract. -->

lan/devices/{id}/status adds: cpu_percent, cpu_warn, cpu_crit, ram_percent, ram_warn, ram_crit,
  disk_percent, disk_warn, disk_crit, disk_other_worst_percent, disk_other_worst_warn,
  disk_other_worst_crit, disk_other_worst_mount, smart_total, smart_failing
  (numbers or null; disk_other_worst_mount is a string or null)

lan/devices/{id}/services         = [{"description": str, "state": str, "plugin_output": str}, ...]
lan/devices/{id}/service_history  = [{"timestamp": str, "description": str, "from": str, "to": str}, ...]
</payload_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payload types for gauges, service rows and service history</name>
  <files>dashboard-react/src/lib/types.ts</files>
  <read_first>
    - dashboard-react/src/lib/types.ts in full (its header comment states the "every field optional, loosely typed, runtime guards remain load-bearing" convention this task must follow)
    - .planning/phases/12-agent-metrics-and-service-status/12-PATTERNS.md, the `types.ts` section
  </read_first>
  <action>
Extend `DevicePayload` with the fifteen gauge keys from the payload contract above, each typed
`number | null` except `disk_other_worst_mount` which is `string | null`, and all optional (`?`) —
matching this file's stated convention that every payload field is optional because the shape
describes untrusted JSON crossing the MQTT boundary. Replace the existing placeholder
`services?: unknown` field: delete it, since the service list now lives on its own topic and store
slice rather than inside the status payload, and leaving a same-named field of a different meaning
would be a trap. Add a short comment above the gauge block naming its producer
(`scripts/mqtt_poller.py::classify_host_services`) and stating that `null` means "this host has no
such service" — the signal the hide-on-absence rules D-03/D-06 key off.

Add two interfaces next to `HistoryEntry`/`EventEntry`, following their exact optional-fields-plus-
index-signature shape:
- `ServiceEntry` — `description?: string; state?: string; plugin_output?: string; [key: string]: unknown`
- `ServiceHistoryEntry` — `timestamp?: string; description?: string; from?: string | null; to?: string | null; [key: string]: unknown`
  </action>
  <verify>
    <automated>npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cpu_percent\|smart_failing\|disk_other_worst_mount" dashboard-react/src/lib/types.ts` matches all three
    - `grep -c "services?: unknown" dashboard-react/src/lib/types.ts` returns 0
    - `grep -n "export interface ServiceEntry\|export interface ServiceHistoryEntry" dashboard-react/src/lib/types.ts` matches both
    - `npm --prefix dashboard-react run typecheck` exits 0
  </acceptance_criteria>
  <done>The dashboard's type layer describes every Phase 12 payload field by its exact wire name.</done>
</task>

<task type="auto">
  <name>Task 2: Subscribe the two new topics and route them into the store</name>
  <files>dashboard-react/src/store/mqttClient.ts, dashboard-react/src/store/mqttClient.test.ts, dashboard-react/src/store/useAppStore.ts, dashboard-react/src/store/useAppStore.test.ts</files>
  <read_first>
    - dashboard-react/src/store/mqttClient.ts lines 1-100 (the module header's choke-point rationale and the SUBSCRIBE_TOPICS array)
    - dashboard-react/src/store/useAppStore.ts in full (the AppState interface, parsePayload/isPlainObject guards, and the existing status/history branches this task mirrors)
    - dashboard-react/src/store/useAppStore.test.ts (the INITIAL_STATE reset pattern and encode() helper)
    - dashboard-react/src/store/mqttClient.test.ts (how SUBSCRIBE_TOPICS is asserted today)
  </read_first>
  <action>
In `mqttClient.ts`, append `"lan/devices/+/services"` and `"lan/devices/+/service_history"` to the
`SUBSCRIBE_TOPICS` array. Nothing else in that module changes — `connect()` already subscribes the
whole array.

In `useAppStore.ts`, add two slices to `AppState` next to `history`:
`services: Record<string, ServiceEntry[]>` and `serviceHistory: Record<string, ServiceHistoryEntry[]>`,
both initialised to `{}` in the store factory, and import the two new types.

Add two `handleMessage` branches, placed after the existing `parts[3] === "history"` branch and
written as literal mirrors of it — same `parsePayload` guard, same `Array.isArray` shape check, same
"malformed means keep last-known-good, never throw" contract:
- `parts[1] === "devices" && parts[3] === "services"` → on `null` (zero-length tombstone) set that id
  to `[]`; on a non-array drop the message; otherwise replace `services[id]`.
- `parts[1] === "devices" && parts[3] === "service_history"` → identical, against `serviceHistory[id]`.

Also extend the existing `status` tombstone branch (which currently deletes `devices[id]` and
`history[id]`) to delete `services[id]` and `serviceHistory[id]` too, so a device removed from
Checkmk leaves no orphaned slice behind if its status tombstone arrives before the others.

Tests in `useAppStore.test.ts`, mirroring the existing history-branch tests: a well-formed services
array landing under the right id; a malformed (non-JSON) payload leaving the previous array intact; a
JSON object (wrong shape) being dropped; a zero-length payload emptying the slice; the same four for
`service_history`; and a status tombstone clearing all four per-device slices. In
`mqttClient.test.ts`, assert `SUBSCRIBE_TOPICS` contains both new wildcard topics.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/store/useAppStore.test.ts src/store/mqttClient.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lan/devices/+/services\|lan/devices/+/service_history" dashboard-react/src/store/mqttClient.ts` returns 2
    - A test asserts `handleMessage("lan/devices/h1/services", encode([{description:"PING",state:"OK",plugin_output:"ok"}]))` puts a one-element array at `useAppStore.getState().services.h1`
    - A test asserts a following malformed payload on the same topic leaves `services.h1` unchanged (identical array contents)
    - A test asserts `handleMessage("lan/devices/h1/services", new Uint8Array())` sets `services.h1` to `[]`
    - A test asserts a zero-length `lan/devices/h1/status` payload removes `h1` from `devices`, `history`, `services` and `serviceHistory`
    - No `try`-less `JSON.parse` is introduced: `grep -c "JSON.parse" dashboard-react/src/store/useAppStore.ts` remains 1 (inside `parsePayload`)
  </acceptance_criteria>
  <done>Both new topics are subscribed once at the MQTT choke point and land in dedicated store slices under the same defensive contract as every existing topic.</done>
</task>

<task type="auto">
  <name>Task 3: Pure helpers for gauge colour, SMART/other-mount badges and service sort order</name>
  <files>dashboard-react/src/lib/gauges.ts, dashboard-react/src/lib/gauges.test.ts, dashboard-react/src/lib/serviceSort.ts, dashboard-react/src/lib/serviceSort.test.ts</files>
  <read_first>
    - dashboard-react/src/lib/staleness.ts (the small pure-function lib module shape and header-comment style to mirror)
    - dashboard-react/src/lib/stateMapping.ts (the one existing severity table — these helpers must not duplicate it)
    - .planning/phases/12-agent-metrics-and-service-status/12-UI-SPEC.md "Color" and "Copywriting Contract" (the locked colour rule and the exact badge wording)
    - .planning/phases/12-agent-metrics-and-service-status/12-RESEARCH.md "Code Examples" (the drafted compareServices)
  </read_first>
  <action>
Create `dashboard-react/src/lib/gauges.ts` exporting three pure functions, with a header comment
stating that gauge colour comes from a metric's OWN warn/crit (D-04) and never from a Checkmk service
state, and that `stateMapping.ts` remains the single source of truth for state→badge colour — these
two colour systems are deliberately separate and must not be merged:
- `gaugeColor(value: number | null | undefined, warn: number | null | undefined, crit: number | null | undefined): ProgressColor`
  — returns `"danger"` when `crit` is a finite number and `value >= crit`; `"warning"` when `warn` is
  a finite number and `value >= warn`; otherwise `"success"`. A null/undefined/non-finite `value`
  also returns `"success"`. Document why the no-threshold fallback is `"success"` and not `"brand"`:
  UI-SPEC's Color section reserves brand blue for focus/interactive affordances and explicitly bars it
  from gauge rings.
- `smartBadge(total: number | null | undefined, failing: number | null | undefined): { text: string; color: ProgressColor } | null`
  — returns `null` when `total` is null/undefined/0 (D-06's hide-entirely rule, never an "N/A" badge);
  otherwise `{ text: "SMART: Fail (1/2)", color: "danger" }` when `failing > 0` and
  `{ text: "SMART: Pass (2/2)", color: "success" }` when it is 0. The exact wording (count in BOTH
  cases) is locked by UI-SPEC's Copywriting Contract.
- `otherMountsLabel(percent: number | null | undefined): string | null` — returns `null` when
  `percent` is null/undefined (hide, per D-01/D-03), otherwise `"Other mounts: {percent}% used"` with
  the number rounded to a whole percent.

Create `dashboard-react/src/lib/serviceSort.ts` exporting `SEVERITY_ORDER` (`{ CRIT: 0, WARN: 1,
UNKNOWN: 2, OK: 3 }`) and `compareServices(a: ServiceEntry, b: ServiceEntry): number` — severity rank
first (an unrecognised or missing state ranks with UNKNOWN, i.e. 2), then
`description.localeCompare(description)` for a stable order (D-09).

Tests for both modules: `gaugeColor` at, below and above each boundary (79/80/89/90 against warn 80
crit 90), with null thresholds, and with a null value; `smartBadge` for the hidden, pass and fail
cases including the exact strings; `otherMountsLabel` for null and for a rounded value; and
`compareServices` sorting a mixed array to CRIT, WARN, UNKNOWN, OK with two same-state rows ordered
by name, plus an entry with a missing `state` landing in the UNKNOWN band.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/lib/gauges.test.ts src/lib/serviceSort.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `gaugeColor(90, 80, 90) === "danger"`, `gaugeColor(85, 80, 90) === "warning"`, `gaugeColor(79, 80, 90) === "success"`, `gaugeColor(99, null, null) === "success"`
    - `smartBadge(null, null) === null` and `smartBadge(0, 0) === null`
    - `smartBadge(2, 0).text === "SMART: Pass (2/2)"` and `smartBadge(2, 1).text === "SMART: Fail (1/2)"` with colors `"success"` and `"danger"`
    - `otherMountsLabel(91.4) === "Other mounts: 91% used"` and `otherMountsLabel(null) === null`
    - A test sorts `[OK, CRIT, UNKNOWN, WARN]` rows with `compareServices` to states `["CRIT","WARN","UNKNOWN","OK"]`
    - `grep -c "brand" dashboard-react/src/lib/gauges.ts` counts only comment lines (`grep -v '^\s*//' dashboard-react/src/lib/gauges.ts | grep -c '"brand"'` returns 0)
  </acceptance_criteria>
  <done>Every Phase 12 computation the detail route needs exists as a tested pure function outside the component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mosquitto (WebSockets) → browser store | Retained JSON on the two new topics is untrusted input; any tab can receive a malformed or oversized payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering / DoS | `useAppStore.handleMessage` new branches | mitigate | Reuse `parsePayload()` + `Array.isArray()` verbatim; a malformed payload keeps last-known-good and never throws out of the handler (asserted by the malformed-payload tests) |
| T-12-09 | Denial of Service | store growth | mitigate | Both slices REPLACE per device id and are already bounded by the poller (`service_history_max_entries`); no client-side appending is introduced — the store header comment's existing rule |
| T-12-10 | Elevation of Privilege | MQTT subscription | accept | The WS listener is read-only by Phase 8's `BRK-03` ACL and both new topics fall under the existing `lan/#` read pattern; no publish capability is added |
| T-12-SC | Tampering | package installs | accept | No `npm install` this phase; `ProgressColor` and every component used come from the already-vendored `kone-design-system` tarball |
</threat_model>

<verification>
- `npm --prefix dashboard-react run typecheck` exits 0.
- `npm --prefix dashboard-react test` passes in full (no regression in existing store/route tests).
- `npm --prefix dashboard-react run lint` reports no new findings.
</verification>

<success_criteria>
The store exposes `services` and `serviceHistory` per device id, `DevicePayload` carries every gauge
field by its wire name, and `gaugeColor`/`smartBadge`/`otherMountsLabel`/`compareServices` are tested
pure functions ready for the route to render.
</success_criteria>

<output>
Create `.planning/phases/12-agent-metrics-and-service-status/12-03-SUMMARY.md` when done
</output>
