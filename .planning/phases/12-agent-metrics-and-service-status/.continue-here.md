---
context: phase
phase: 12-agent-metrics-and-service-status
task: 0
total_tasks: 14
status: planned
last_updated: 2026-09-21T13:49:30.860Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting a paraphrased Checkmk service name without a live probe | CONTEXT.md's original D-05 claimed a `"Smart <device>"` service name from memory; source-level research found the real Checkmk 2.4.0 plugin registers `"SMART <device> Stats"` / `"Temperature SMART <device>"` instead. No live host was reachable to settle it during planning. | advisory | Fixed via `SMART_HEALTH_SERVICE_RE`, one clearly-commented constant in `scripts/mqtt_poller.py` (plan 12-01/12-02), plus a `--dump-service-names` CLI diagnostic — if a live agent-test run shows a different name, correcting that one regex is the entire fix. Do not re-litigate this as a planning gap; it's a known, contained risk. |
| Republishing `lan/devices/{id}/services` on `plugin_output` drift | A chatty check's embedded numbers (e.g. "load average: 1.23") could force a republish nearly every cycle if the change-detection signature includes `plugin_output`, defeating the whole reason D-12 split gauges from the service list (~420 rows/cycle problem ROADMAP flagged). | advisory | `services_signature()` in plan 12-02 explicitly excludes `plugin_output` from its tuple; a unit test asserts a plugin_output-only diff does not trigger republish. |

<current_state>
Phase 12 (Agent Metrics and Service Status) planning is **fully complete and verified** — this
is a clean phase-boundary pause, not a mid-plan interruption. Nothing has been executed yet.
All 5 PLAN.md files exist, are committed, and passed `gsd-plan-checker` verification. The very
next action for this phase is `/bm:execute-phase 12`.
</current_state>

<completed_work>

Completed (this planning session, all committed):
- CONTEXT.md gathered and amended twice: once to fold in tree-row navigation (D-15) and the
  DASH-03 history strip (D-16, excluding the Checkmk deep-link per D-17), once to correct D-05's
  SMART service name after RESEARCH.md's source-level finding.
- UI-SPEC.md created and verified (5/6 design dimensions PASS, 1 non-blocking spacing FLAG on
  the gauge-row-to-table gap — cosmetic, not fixed, not blocking).
- RESEARCH.md: verified CPU (`util`) and RAM (`mem_used_percent`) perfdata field names against
  Checkmk's own 2.4.0 GitHub source, cross-checked against this project's own deployed
  `_create_threshold_rules()`. Found and flagged the D-05 SMART-naming conflict.
- PATTERNS.md: mapped all 11 code files this phase touches to existing codebase analogs.
- 5 PLAN.md files created (12-01..12-05), 2 waves, 14 tasks, all `autonomous: true` (no
  checkpoints).
- REQUIREMENTS.md: minted PLR-09..PLR-12 and DASH-08..DASH-11; DASH-03 marked `Partial` (history
  strip only, per D-16 — its Checkmk-link clause per D-17 is explicitly not covered).
- `gsd-plan-checker` verification PASSED with 2 warnings; the actionable one (no plan synced
  `docs/Podman setup for checkmk, minio, mosquitto, worker.md`'s MQTT topic contract table) was
  fixed by adding Task 4 to plan 12-02.
- SEED-001 planted (`.planning/seeds/`): future need for an MQTT-subscriber-backed history
  database when the dashboard moves to cloud / needs long-range reporting.
- Todo captured (`.planning/todos/pending/`): pre-seed a fixed Checkmk automation-user REST
  secret from env instead of the current generate-then-hand-copy flow — unrelated to Phase 12,
  parked for later.
</completed_work>

<remaining_work>

Execute Phase 12 via `/bm:execute-phase 12`. Wave structure:

**Wave 1 (parallel-safe, no file overlap):**
- 12-01 — Poller: `GET services` column-availability triad, Nagios `perf_data` parser,
  `ServiceSnapshot`, `--dump-service-names` diagnostic. Files: `scripts/mqtt_poller.py`,
  `tests/test_mqtt_poller.py`.
- 12-03 — Dashboard data layer: gauge fields + `ServiceEntry`/`ServiceHistoryEntry` types, 2 new
  subscribed MQTT topics + store slices, `lib/gauges.ts`/`lib/serviceSort.ts` helpers.
- 12-05 — Tree-row navigation to `/details?id=` (D-15) + PROJECT.md Out of Scope amendment
  (ROADMAP scope item 6).

**Wave 2 (depends on Wave 1):**
- 12-02 (depends on 12-01) — Classification into gauge/SMART/service-list, 2 new topics,
  change-only publish, bounded service history, cycle wiring, **plus the Podman-doc sync task**.
- 12-04 (depends on 12-03) — `DetailsRoute.tsx` fill-in: gauge row, SMART badge, service `Table`
  worst-first, per-device history strip.
</remaining_work>

<decisions_made>

- Folded tree-row navigation and DASH-03's history strip into Phase 12 (both touch the same
  `DetailsRoute.tsx`/`TreeNode.tsx` this phase already modifies); explicitly excluded the
  Checkmk external deep-link (user declined it).
- Built the SMART-service filter against the source-verified name now rather than blocking on
  live verification (none reachable) — isolated to one constant, with a diagnostic CLI flag as
  the correction mechanism if wrong.
- Minted new REQUIREMENTS.md IDs during planning rather than running a separate
  requirements-update cycle first (RESEARCH.md's recommendation, low-risk/mechanical).
- Added a doc-sync task to plan 12-02 in response to the plan-checker's warning, rather than
  accepting the gap — this project has a consistent per-phase convention of keeping the Podman
  setup doc's MQTT topic contract table current (Phase 9/10/11 all did this).
</decisions_made>

<blockers>
None blocking execution. One non-blocking human action noted below.
</blockers>

## Required Reading (in order)
1. `.planning/phases/12-agent-metrics-and-service-status/12-CONTEXT.md` — locked decisions D-01..D-17 (read D-05/D-08's amendment notes and D-15/D-16/D-17 carefully, they postdate the original discuss-phase session)
2. `.planning/phases/12-agent-metrics-and-service-status/12-RESEARCH.md` — especially "Critical Finding" (SMART naming) and "Pattern 3" (verified perfdata fields)
3. `.planning/phases/12-agent-metrics-and-service-status/12-UI-SPEC.md` — design contract, approved
4. `.planning/phases/12-agent-metrics-and-service-status/12-PATTERNS.md` — codebase analogs cited by each plan's `read_first` blocks
5. The 5 `12-0N-PLAN.md` files themselves — each carries a `<payload_contract>` block; 12-02 and 12-03's must stay byte-identical (do not let the two waves' field names drift)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Assuming a Checkmk service name from memory/discussion without a live probe → structural mitigation already applied (isolated regex constant + `--dump-service-names`); do not add a second, different SMART-name assumption elsewhere in the codebase without the same isolation.

## Infrastructure State
- No running services relevant to this pause — no dev server, no live Checkmk/agent-test host was reachable during planning (no `podman`/`docker` on this machine either).
- Working tree is clean aside from this handoff; all planning artifacts are committed (see HANDOFF.json's `completed_tasks` for commit hashes).

## Human Actions Pending (non-blocking)
- When a live agent-test/Checkmk host becomes reachable, run
  `uv run python scripts/mqtt_poller.py --dump-service-names` (built in plan 12-01) and confirm
  the SMART service is actually `"SMART <device> Stats"`. If it differs, the fix is a one-line
  change to `SMART_HEALTH_SERVICE_RE` in `scripts/mqtt_poller.py`.

<context>
This was a full `/bm:plan-phase 12` run start to finish in one session: UI-SPEC generation +
verification, a user-directed scope amendment (tree nav + history strip in, Checkmk link out),
full research (which surfaced and the user resolved a real conflict in a previously "locked"
decision), pattern mapping, planning, and plan-checker verification with one fix applied. Two
side conversations were also captured as forward-looking artifacts unrelated to Phase 12's
execution: SEED-001 (future MQTT-to-database history persistence for cloud) and a todo
(pre-seeding the automation-user's REST secret from env). Neither blocks or relates to
executing Phase 12 — they're parked for whenever their own trigger conditions arrive.
</context>

<next_action>
Start with: `/clear` then `/bm:execute-phase 12`
</next_action>
