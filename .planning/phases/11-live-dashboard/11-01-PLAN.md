---
phase: 11-live-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mqtt_poller.py
autonomous: false
requirements: [DASH-04]
must_haves:
  truths:
    - "`uv run python scripts/mqtt_poller.py --check-columns` prints a `staleness ... (optional)` line (D-17)"
    - "The live `dmc` site's answer for the `staleness` column is recorded in-repo as a dated live-verification comment, following the 2026-09-08/2026-09-11 column-probe precedent (D-17)"
    - "An absent `staleness` column degrades to a logged warning and an omitted column, never a crash (D-17)"
  artifacts:
    - path: "scripts/mqtt_poller.py"
      provides: "`staleness` appended to OPTIONAL_HOST_COLUMNS with a dated live-verification comment"
      contains: "staleness"
  key_links:
    - from: "scripts/mqtt_poller.py::OPTIONAL_HOST_COLUMNS"
      to: "scripts/mqtt_poller.py::main() --check-columns loop"
      via: "the existing `for name in OPTIONAL_HOST_COLUMNS: print(...)` loop"
      pattern: "OPTIONAL_HOST_COLUMNS"
    - from: "scripts/mqtt_poller.py::OPTIONAL_HOST_COLUMNS"
      to: "scripts/mqtt_poller.py::select_host_columns()"
      via: "present/absent branch with _logger.warning on absent"
      pattern: "select_host_columns"
---

<objective>
Resolve RESEARCH.md Open Question 1 / Pitfall 4 before any code depends on it: does the live
Checkmk `dmc` site (2.4.0p36.cre) actually expose a `staleness` column on the Livestatus
`hosts` table?

Purpose: D-17 makes the dashboard prefer Checkmk's own `staleness` value over
timestamp-age staleness. RESEARCH.md rates that column MEDIUM confidence — community-documented,
never live-probed against this site. Every other optional column in this poller
(`parents`, `tags`, `alias`) was resolved by exactly this probe, on a dated comment, before
being trusted. Plan 11-04 (the payload extension) and plan 11-06 (`staleness.js`) both depend
on this answer, so it lands first and alone.

Output: `staleness` present in `OPTIONAL_HOST_COLUMNS`, its live-probe result recorded as a
dated comment in the same style as the existing `alias` note.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-dashboard/11-CONTEXT.md
@.planning/phases/11-live-dashboard/11-RESEARCH.md
@.planning/phases/11-live-dashboard/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contract the executor extends. Extracted from scripts/mqtt_poller.py — use directly. -->

scripts/mqtt_poller.py:140-151
  REQUIRED_HOST_COLUMNS = ("name", "state")
  OPTIONAL_HOST_COLUMNS = ("scheduled_downtime_depth", "acknowledged",
                           "worst_service_state", "parents", "tags", "alias")
  # lines 77-139 above it hold the dated live-verification comment block
  # (2026-09-08 probe, plus the 2026-09-11 `alias` follow-up note)

scripts/mqtt_poller.py:494  def available_host_columns(host: str, port: int, timeout: float) -> set[str]
scripts/mqtt_poller.py:513  def select_host_columns(available: set[str]) -> list[str]
scripts/mqtt_poller.py:1191 def main() -> int      # --check-columns branch iterates OPTIONAL_HOST_COLUMNS verbatim
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `staleness` to OPTIONAL_HOST_COLUMNS with a probe-pending comment</name>
  <files>scripts/mqtt_poller.py</files>
  <read_first>
    - scripts/mqtt_poller.py lines 1-35 (module docstring and its "do not copy from docs/src/*" convention)
    - scripts/mqtt_poller.py lines 77-155 (the dated live-verification comment block, REQUIRED_HOST_COLUMNS, OPTIONAL_HOST_COLUMNS, _SERVICE_STATE_NAMES)
    - scripts/mqtt_poller.py lines 494-541 (available_host_columns, select_host_columns, build_hosts_query)
    - scripts/mqtt_poller.py lines 1191-1228 (main()'s --check-columns branch)
    - .planning/phases/11-live-dashboard/11-PATTERNS.md section "`scripts/mqtt_poller.py` (service, event-driven) — D-17 additive fields" (the copy-exact column-probe comment convention)
  </read_first>
  <action>
Append the literal string `"staleness"` as the last entry of the `OPTIONAL_HOST_COLUMNS`
tuple in scripts/mqtt_poller.py. Do not touch `REQUIRED_HOST_COLUMNS`, do not add an
argparse flag, and do not restructure the `--check-columns` branch — PATTERNS.md establishes
that the existing `for name in OPTIONAL_HOST_COLUMNS: print(...)` loop in `main()` is the
entire CLI change needed.

Directly above the new entry, add a comment in the exact style of the existing `alias`
comment (which cites "Added by Phase 10 (D-10) ... live-verified present and populated on a
real 2.4.0p36.cre site on 2026-09-11 (plan 10-06)"). The new comment must state:
- Added by Phase 11 (D-17), consumed by the dashboard's `staleness.js` in preference to
  timestamp-age staleness
- that it is NOT covered by the 2026-09-08 or 2026-09-11 probes
- a `PROBE PENDING` marker to be replaced by the dated result in Task 2

Add nothing else: `select_host_columns()`'s existing present/absent branch already emits
`_logger.warning("Livestatus hosts table does not expose optional column %r; degrading to a
safe default for that field", name)` for an absent column, and `query_devices()` is extended
in plan 11-04, not here. Adding the column name alone must not change any published payload.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mqtt_poller.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'staleness' scripts/mqtt_poller.py` matches inside the `OPTIONAL_HOST_COLUMNS` tuple definition
    - `grep -c 'PROBE PENDING' scripts/mqtt_poller.py` returns 1
    - `uv run python -c "import importlib.util,sys;s=importlib.util.spec_from_file_location('p','scripts/mqtt_poller.py');m=importlib.util.module_from_spec(s);sys.modules['p']=m;s.loader.exec_module(m);print(m.OPTIONAL_HOST_COLUMNS[-1])"` prints `staleness`
    - `uv run pytest tests/test_mqtt_poller.py -q` passes with zero failures (existing suite unchanged — `test_select_host_columns_omits_unavailable_optional_columns` asserts `["name","state"]` and must still pass because `staleness` is optional)
    - `git diff --stat scripts/mqtt_poller.py` shows no change to `REQUIRED_HOST_COLUMNS`, `publish_device_status`, `DeviceSnapshot`, or `main()`'s argparse block
  </acceptance_criteria>
  <done>`staleness` is an optional column the `--check-columns` diagnostic will report on, and the existing test suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Probe the live site for `staleness` and record the dated result</name>
  <files>scripts/mqtt_poller.py</files>
  <read_first>
    - scripts/mqtt_poller.py lines 77-155 (the dated comment block this task appends to)
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md §5 (Livestatus-over-TCP; Livestatus is NOT published to the LAN, so the probe must run inside the container network) and §7's "Poller smoke test" subsection (which documents `--check-columns` as the standalone column-confirmation tool)
    - .planning/phases/11-live-dashboard/11-RESEARCH.md "Pitfall 4" and "Open Questions" #1
  </read_first>
  <what-built>
`staleness` is now in `OPTIONAL_HOST_COLUMNS`, so the poller's existing `--check-columns`
diagnostic will report it as `present` or `missing`. Nothing yet reads its value.
  </what-built>
  <how-to-verify>
On the deployment host, from the repo checkout's `deploy/` directory (the poller container
already has `LIVESTATUS_HOST=checkmk` set and `paho-mqtt==2.1.0` installed, and Livestatus
6557 is reachable only inside `cmk_net`):

1. Ensure the stack is up: `podman compose ps` shows `checkmk` and `mqtt-poller` running.
2. Run the probe against the live site:
   `podman compose exec poller python /scripts/mqtt_poller.py --check-columns`
3. Read the line for `staleness`. Expect either:
   - `present: staleness (optional)` — Checkmk exposes it; the dashboard can prefer it.
   - `missing: staleness (optional)` — it does not exist under that spelling; the dashboard
     must use the timestamp-age fallback only.
4. If `missing`, also capture the full available column set so an alternate spelling can be
   found rather than guessed:
   `podman compose exec poller python -c "import sys;sys.path.insert(0,'/scripts');import mqtt_poller as p;print(sorted(p.available_host_columns('checkmk',6557,10.0)))"`
   and look for any name containing `stale`.
5. Paste the full `--check-columns` output (and the column list if step 4 ran) back here.

Alternative if `podman compose exec` is unavailable: run
`uv run python scripts/mqtt_poller.py --check-columns` from the repo checkout with
`LIVESTATUS_HOST` pointed at a host that can reach Checkmk's 6557, per CLAUDE.md's uv-only
rule (never bare `python`).
  </how-to-verify>
  <action>
After the operator returns the probe output, replace the `PROBE PENDING` marker from Task 1
with a dated live-verification note in the exact style of the existing `alias` note, stating:
the date the probe ran (today's date), the site id (`dmc`) and version (`2.4.0p36.cre`), the
plan id (`11-01`), and the verbatim result (`present` or `missing`).

If the result is `missing`, the comment must additionally state, in one sentence, that
`staleness` is unavailable on this site and that `dashboard/js/staleness.js` (plan 11-06)
therefore relies on the `timestamp`-age fallback against the 3x factor (D-12). Do NOT remove
`staleness` from `OPTIONAL_HOST_COLUMNS` in that case — the graceful-degradation path is the
designed behavior and the column may appear on a future Checkmk version or a differently
configured site.

If step 4 surfaced a differently-spelled column containing `stale`, record that exact
spelling in the comment and stop: this becomes an input to plan 11-04 rather than a silent
substitution here.
  </action>
  <acceptance_criteria>
    - `grep -c 'PROBE PENDING' scripts/mqtt_poller.py` returns 0
    - The comment above `"staleness"` in `OPTIONAL_HOST_COLUMNS` contains today's date, the string `dmc`, the string `2.4.0p36.cre`, the string `11-01`, and one of the literal words `present` or `missing`
    - The plan SUMMARY records the verbatim `--check-columns` line for `staleness` so plans 11-04 and 11-06 can read the result without re-probing
    - `uv run pytest tests/test_mqtt_poller.py -q` still passes
  </acceptance_criteria>
  <done>The live `dmc` site's answer for the `staleness` column is recorded as a dated comment in `scripts/mqtt_poller.py`, and the `PROBE PENDING` marker is gone.</done>
  <resume-signal>Paste the `--check-columns` output, then type "approved"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poller container → Checkmk Livestatus (`checkmk:6557`) | The poller reads untrusted-shaped text/JSON from a socket it does not control; a malformed response must not crash the poll loop |
| Livestatus column schema → poller code | Assuming a column exists that does not is a silent-degradation vector (RESEARCH.md Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | `select_host_columns()` / the poll loop | mitigate | Adding an unavailable optional column must not raise. The existing `select_host_columns()` present/absent branch (logged warning, column omitted) is reused verbatim; Task 1 adds no new error path and `REQUIRED_HOST_COLUMNS` is untouched, so a missing `staleness` cannot abort a cycle |
| T-11-02 | Tampering | assumed-but-unverified `staleness` column | mitigate | Task 2's live `--check-columns` probe replaces an assumption with a dated, recorded fact before any downstream code reads the value; a `missing` result is recorded in-repo rather than silently shipping dead code |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — D-17 needs no new Python dependency (RESEARCH.md "Installation": "no `uv add` required"). Nothing to gate |

`[ASSUMED]`/`[SUS]` packages: none introduced by this plan.
</threat_model>

<verification>
- `uv run pytest tests/test_mqtt_poller.py -q` — full existing poller suite green
- `podman compose exec poller python /scripts/mqtt_poller.py --check-columns` on the deployment host prints a `staleness (optional)` line
- `git diff scripts/mqtt_poller.py` is confined to the `OPTIONAL_HOST_COLUMNS` tuple and its comment block
</verification>

<success_criteria>
- `staleness` is in `OPTIONAL_HOST_COLUMNS` (D-17)
- A dated live-verification comment records the real `dmc`-site answer, in the same shape as the 2026-09-08 and 2026-09-11 precedents (D-17)
- Zero behavior change to any published MQTT payload
- Existing poller test suite unbroken
</success_criteria>

<output>
Create `.planning/phases/11-live-dashboard/11-01-SUMMARY.md` when done. It MUST include the
verbatim `staleness` line from the `--check-columns` output — plans 11-04 and 11-06 read it
from there.
</output>
