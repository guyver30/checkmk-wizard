---
phase: 11-live-dashboard
plan: 09
type: execute
wave: 5
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06", "11-07", "11-08"]
files_modified:
  - docs/Podman setup for checkmk, minio, mosquitto, worker.md
  - README.md
  - dashboard/README.md
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06]
must_haves:
  truths:
    - "An operator who has never seen this repo can bring the dashboard up from the deployment doc alone: start the service, open port 8090, and know which single file to edit first"
    - "The vendored asset inventory is documented — what was copied, from where, and why nothing is fetched at runtime"
    - "All five ROADMAP Phase 11 success criteria have been observed against the live stack by a human"
  artifacts:
    - path: "docs/Podman setup for checkmk, minio, mosquitto, worker.md"
      provides: "dashboard service startup, port 8090 endpoint, config.js edit-before-deploy step"
      contains: "8090"
    - path: "dashboard/README.md"
      provides: "directory layout, vendored-asset provenance, no-build-step rationale"
      contains: "config.js"
  key_links:
    - from: "docs/Podman setup for checkmk, minio, mosquitto, worker.md"
      to: "dashboard/js/config.js"
      via: "a documented edit-before-first-use step naming CHECKMK_BASE_URL"
      pattern: "config\\.js"
---

<objective>
Document the dashboard for deployment, and verify the whole phase against the live stack with a
human in the loop.

Purpose: this repo's rule is that documentation is updated when a feature lands, not later. And
the dashboard's entire verification story is manual — there is no JS test harness in this repo by
convention, so the five ROADMAP success criteria are confirmed by an operator looking at the
running system.

Output: updated deployment and repo docs, and a recorded live-verification pass.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-dashboard/11-CONTEXT.md
@.planning/phases/11-live-dashboard/11-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the dashboard in the deployment doc and the repo READMEs</name>
  <files>docs/Podman setup for checkmk, minio, mosquitto, worker.md, README.md, dashboard/README.md</files>
  <read_first>
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md — §2 Directory Structure, §3 Configuration Files (and its "First-time credential setup"), §4 Deployment, §6 Endpoints & Network Access and its "MQTT topic contract (poller)" subsection, §7 Verification & Pipeline Testing
    - README.md — the existing project overview, to match its tone and depth
    - deploy/compose.yaml as edited by plan 11-02 (the dashboard service block)
    - dashboard/js/config.js (plan 11-02) — the exact constant names an operator edits
    - .planning/phases/11-live-dashboard/11-UI-SPEC.md "Asset Vendoring Manifest" — the inventory to document
  </read_first>
  <action>
Update three documents. Match each file's existing heading depth, tone and comment density — do
not restructure sections that already work.

`docs/Podman setup for checkmk, minio, mosquitto, worker.md`:
- §2 Directory Structure: add the `dashboard/` tree — `index.html`, `devices.html`,
  `details.html`, `css/`, `js/` (including `js/vendor/`), `fonts/`, `icons/`, `images/`.
- §3 Configuration Files: add an edit-before-first-use step for `dashboard/js/config.js`, naming
  `CHECKMK_BASE_URL` (must be changed from the `<HOST_IP>` placeholder to a URL a LAN browser can
  actually resolve, because the poller's `checkmk:5000` is container-internal), `CHECKMK_SITE`,
  and the `wsreader`/`wsreader` disposable defaults with the same rotate-before-exposure framing
  §3 already uses for the other default credentials.
- §6 Endpoints: add `http://<host>:8090` for the dashboard, in the same table/list shape the
  other endpoints use, and state that the browser talks to mosquitto's WebSockets listener on
  9002 directly — nginx serves static files only and proxies nothing.
- §6 MQTT topic contract: confirm the `staleness` and `host_state_raw` keys plan 11-01 added are
  documented there; add them if plan 11-01's edit did not land.
- §7 Verification: add a short dashboard check — start the stack, open port 8090, confirm the
  connection indicator reads "Connected" and the device list populates from retained messages
  without restarting the poller.
- Add a note that the dashboard has no build step: there is no `npm install`, no bundler, and no
  `package.json` — every asset is committed, and deployment is a bind mount.

`dashboard/README.md` (new): the directory layout, the module list with one line each on what it
owns, the vendored-asset inventory (mqtt.js 5.15.2 with its source URL and pin rationale; the
four KONE fonts, the 25 icons and the trimmed logo with their `web_assets/` provenance and the
one-time trim step), and the no-CDN/no-egress rationale. State that `config.js` is the only file
an operator edits per deployment, and that it is the browser-side equivalent of a compose
`environment:` block.

`README.md`: a short section pointing at the dashboard, its port, and `dashboard/README.md`.

Do not document anything this phase did not build. In particular the topology map, per-device
CPU/RAM/disk gauges, SMART readouts and per-service status belong to Phases 12 and 13 — if a doc
sentence would promise them, leave it out.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && D="docs/Podman setup for checkmk, minio, mosquitto, worker.md" && grep -q '8090' "$D" && grep -q 'config.js' "$D" && grep -q 'CHECKMK_BASE_URL' "$D" && test -f dashboard/README.md && grep -q 'mqtt@5.15.2\|mqtt.min.js' dashboard/README.md && grep -q 'dashboard' README.md && test $(grep -Eic 'vis-network|CPU gauge|SMART|per-service drill' "$D" dashboard/README.md README.md | awk -F: '{s+=$2} END {print s+0}') -eq 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 and prints `ok`
    - `grep -c '8090' "docs/Podman setup for checkmk, minio, mosquitto, worker.md"` returns at least 2 (the endpoint and the directory/service mention)
    - `grep -c 'host_state_raw' "docs/Podman setup for checkmk, minio, mosquitto, worker.md"` returns at least 1
    - `dashboard/README.md` lists all three vendored asset groups: `grep -Ec 'fonts|icons|images' dashboard/README.md` returns at least 3
    - No deferred feature is promised: `grep -Eic 'vis-network|topology map|CPU gauge|SMART' dashboard/README.md` returns 0
    - No bare `python`/`pip` command appears in any added doc snippet: `grep -En '^\s*(python3?|pip) ' dashboard/README.md` returns nothing
  </acceptance_criteria>
  <done>An operator can deploy and configure the dashboard from the docs alone, and the vendored-asset inventory is recorded with its provenance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification of all five ROADMAP success criteria</name>
  <what-built>
    The complete dashboard: three pages behind one persistent shell, the vendored KONE-branded
    stylesheet, the MQTT-over-WebSockets connection with hand-rolled jittered backoff, the state
    store, the grouped tree and event history, the stats strip and overview, the sortable table,
    the detail panel, and the `dashboard` nginx service on host port 8090. The poller now also
    publishes `staleness` and `host_state_raw`.

    Everything automatable has been automated: syntax checks, asset-path resolution, grep gates
    on the locked copy strings and the icon classes, and node harnesses over every pure function.
    What cannot be automated here is looking at the running system — this repo has no browser test
    harness and this sandbox has no route to the live stack.
  </what-built>
  <how-to-verify>
    On the deploy host, from the repo checkout's `deploy/` directory:
    1. `podman compose up -d` then `podman compose ps` — the `dashboard` container is running.
    2. Open `http://<host>:8090/` from another LAN device (not the deploy host, to confirm D-02's
       `location.hostname` derivation works from any client).

    Then confirm each ROADMAP Phase 11 success criterion:
    3. **Criterion 1 (index):** the stats strip shows counts by state above a grouped fleet
       overview; hosts carry device-type icons; toggling the grouping control switches between
       folder and device type and the sidebar tree follows the same toggle; reloading the page
       keeps your choice.
    4. **Criterion 2 (devices):** the table lists every host, sorts on a header click, and the
       recent-events panel is visible in the sidebar on every page.
    5. **Criterion 3 (details):** clicking a host opens the detail panel in place — the URL becomes
       `details.html?id=<host>` with no page reload, the event panel keeps its scroll position,
       the 20-segment history strip renders, and "View in Checkmk →" opens that host's page in
       Checkmk.
    6. **Criterion 4 (staleness):** `podman compose stop poller`, wait past 3 minutes, and confirm
       the "Poller offline since HH:MM — data is N minutes old" banner appears, every row and tile
       gains the diagonal hatch, and last known values are still readable and visibly distinct
       from a red DOWN host. Then `podman compose start poller` and confirm it clears.
    7. **Criterion 5 (connection):** `podman compose stop mosquitto` and watch the indicator go
       from "Connected" to "Disconnected — retrying"/"Reconnecting in Ns…", with the countdown
       growing on successive attempts (1s, 2s, 4s… jittered, capped at 30s). Then
       `podman compose start mosquitto` and confirm it reconnects on its own and the fleet
       repopulates from retained messages.

    Brand and typography check (UI-SPEC):
    8. The KONE logo renders top-left on all three pages, as a small white rounded chip, not a
       large white square and not a broken image.
    9. Text renders in Inter (not a fallback system face) once loaded, with no lingering
       flash-of-invisible-text; the masthead wordmark renders in KONE Information, uppercase.
    10. Open devtools' Network tab and confirm zero requests to any external origin — every font,
        icon, image and script comes from port 8090.

    Report anything that fails, with what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" when all ten checks pass, or describe what failed (which criterion, what you saw instead).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator action | A doc that omits the config.js edit leaves a deployment with a dead Checkmk link and unrotated defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-26 | Information Disclosure | undocumented default credentials | mitigate | §3's edit-before-first-use step names `wsreader`/`wsreader` explicitly with the same rotate-before-exposure framing the existing default credentials already carry — the risk is an operator who does not know they exist |
| T-11-27 | Repudiation | live verification | mitigate | The blocking checkpoint records a human-observed pass or failure per ROADMAP criterion, including the external-request check that proves the no-egress posture holds at runtime |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; the doc explicitly states there is no package manager step for the dashboard |
</threat_model>

<verification>
- The doc verify command exits 0
- A human has confirmed all five ROADMAP Phase 11 success criteria plus the brand and no-egress checks against the live stack
</verification>

<success_criteria>
- Deployment and repo docs describe the dashboard, its port, its single config file and its vendored assets
- No deferred Phase 12/13 capability is promised anywhere in the docs
- Live verification recorded
</success_criteria>

<output>
Create `.planning/phases/11-live-dashboard/11-09-SUMMARY.md` when done
</output>
