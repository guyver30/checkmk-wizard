---
phase: 11-live-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/css/dashboard.css
  - dashboard/index.html
  - dashboard/devices.html
  - dashboard/details.html
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-06]
must_haves:
  truths:
    - "All three pages render a dark, dense, panel-based shell with a split left column — grouped tree over an always-visible event-history panel — when opened cold with no broker reachable (D-21, D-22)"
    - "The stale hatch composes on top of a state fill, so a CRIT-and-stale group renders red-and-hatched rather than one state replacing the other (D-15, DASH-04)"
    - "Every Checkmk state has both a color token and a non-color signal: icon, spelled-out label, or texture (D-10, D-11)"
    - "index.html's grouped-overview slot is a self-contained main-area module, so Phase 13's map is a drop-in replacement rather than a rewrite (D-24)"
  artifacts:
    - path: "dashboard/css/dashboard.css"
      provides: "Dark theme tokens, state palette, composable stale hatch, shell grid, page layouts"
      contains: "--state-stale"
      min_lines: 200
    - path: "dashboard/index.html"
      provides: "Stats-by-state strip and grouped fleet overview mount points (D-24)"
      contains: "stats-strip"
    - path: "dashboard/devices.html"
      provides: "Sortable device table skeleton with data-key headers (DASH-02)"
      contains: "data-key"
    - path: "dashboard/details.html"
      provides: "Per-device detail panel skeleton with history-strip container and Checkmk CTA (DASH-03)"
      contains: "history-strip"
  key_links:
    - from: "dashboard/index.html, devices.html, details.html"
      to: "dashboard/js/shell.js"
      via: "identical ordered classic script tag block on all three pages"
      pattern: "js/shell.js"
    - from: "dashboard/css/dashboard.css .is-stale"
      to: "the .state-* fill classes"
      via: "background-image gradient overlay, never a background-color replacement"
      pattern: "repeating-linear-gradient"
---

<objective>
Build the dashboard's static markup and stylesheet: one stylesheet implementing UI-SPEC's
tokens verbatim, and three real HTML files that each render the persistent shell correctly when
opened cold.

Purpose: D-21 requires three bookmarkable HTML files that behave as one app, and D-22 fixes the
shell layout. Fixing the DOM skeleton and the CSS contract first means every render module in
later plans writes into a known, already-styled mount point instead of inventing markup.

Output: three pages that render a complete empty-state shell with no JavaScript behavior yet
(the js/ files arrive in later plans; a missing script must not break the page).
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-dashboard/11-CONTEXT.md
@.planning/phases/11-live-dashboard/11-UI-SPEC.md
@.planning/phases/11-live-dashboard/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- DOM contract. Every id below is written into by a render module in plans 11-07/11-08/11-09
     and MUST be created with exactly these ids. -->

Shell markup, identical on all three pages:
  topbar                 (element id: topbar)
  connection-indicator   written by render-shell.js (plan 11-07), DASH-05
  poller-banner          written by render-shell.js, D-13/D-14, ships with the hidden attribute
  tag-group-banner       written by render-shell.js, D-16, dismissible, ships hidden
  sidebar-toggle         hamburger, only visible under the 900px breakpoint
  grouping-toggle        D-05 folder / device_type switch, in the sidebar tree header
  sidebar-tree           written by render-shell.js, grouped tree
  sidebar-events         written by render-shell.js, always-visible event history
  main                   swappable main area (D-23)

index.html main area:
  stats-strip            written by render-index.js (plan 11-08), DASH-01
  overview-grid          written by render-index.js, grouped fleet overview (D-24)

devices.html main area:
  device-table           written by render-devices.js (plan 11-08), DASH-02
    thead th carries data-key values: state, host, type, location, lastUpdate
    the Flags th carries NO data-key (UI-SPEC marks it not sortable)
    tbody rows are keyed by the tr attribute data-id set to the hostname

details.html main area:
  detail-panel           written by render-details.js (plan 11-09), DASH-03
  history-strip          written by render-details.js, 20 fixed-width segments
  checkmk-link           written by render-details.js, locked CTA text "View in Checkmk"

Ordered script block, identical on all three pages. Classic scripts only: no type=module,
no defer, no async. config.js defines globals every later file reads; shell.js must run last:
  js/config.js
  js/vendor/mqtt.min.js
  js/staleness.js
  js/grouping.js
  js/state-store.js
  js/mqtt-connection.js
  js/render-shell.js
  js/render-index.js
  js/render-devices.js
  js/render-details.js
  js/shell.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write dashboard/css/dashboard.css from UI-SPEC's token blocks</name>
  <files>dashboard/css/dashboard.css</files>
  <read_first>
    - .planning/phases/11-live-dashboard/11-UI-SPEC.md in full. Load-bearing sections: "Spacing Scale", "Typography", "Color" (the root custom-property block is literal), "State-color to meaning map", "Stale hatch pattern", "Layout" (the shell/sidebar grid CSS and the three layout variables), "Connection Indicator", "Responsive Behavior" (the single 900px block), "Accessibility", "Copywriting Contract"
    - .planning/phases/11-live-dashboard/11-CONTEXT.md decisions D-10, D-11, D-14, D-15, D-22
    - .planning/phases/11-live-dashboard/11-PATTERNS.md row for dashboard/css/dashboard.css, which instructs copying the root block verbatim and not re-deriving hex values
  </read_first>
  <action>
Create `dashboard/css/dashboard.css` implementing UI-SPEC's contract. Copy every declared token
value verbatim. Do not re-derive or improve hex values, spacing numbers, or layout fractions:
UI-SPEC already resolved every discretionary choice and was checker-verified.

Required contents:

1. Root custom properties copied exactly from UI-SPEC "Color": `--color-bg: #0b0d10`,
   `--color-surface: #16191d`, `--color-surface-raised: #1c2025`, `--color-border`,
   `--color-accent: #3b82f6`, `--color-accent-text`, `--color-text`, `--color-text-muted`,
   plus the eight semantic state tokens `--state-ok: #2ecc71`, `--state-warn: #f4c430`,
   `--state-crit: #ef4444`, `--state-down: #ef4444`, `--state-unknown: #f97316`,
   `--state-unreach: #f97316`, `--state-pend: #a1a1aa`, `--state-stale: #52525b`.
2. Spacing tokens `--space-xs: 4px` through `--space-3xl: 64px`, including the dense-layout
   `--space-sm2: 12px`. Typography tokens `--font-sans` and `--font-mono` with the exact
   system-font stacks UI-SPEC lists. Only the four declared sizes (12px, 14px, 18px, 24px)
   and two weights (400, 600) may appear anywhere in the file.
3. Layout tokens `--layout-sidebar-width: 280px`, `--layout-topbar-height: 48px`,
   `--layout-sidebar-tree-fraction: 58%`, and the shell/sidebar grid rules exactly as written
   in UI-SPEC "Layout".
4. A systematic state-class family, one class per state, each setting its fill from the
   matching state token: `.state-ok`, `.state-warn`, `.state-crit`, `.state-down`,
   `.state-unknown`, `.state-unreach`, `.state-pend`. Systematic naming matters because the
   render modules compute the class name from the state string.
5. `.is-stale` using UI-SPEC's exact declaration
   `repeating-linear-gradient(45deg, rgba(255,255,255,0.06) 0 6px, transparent 6px 12px)`
   applied through `background-image` ONLY. It must not set `background-color`. D-15 requires
   the hatch to compose over an existing red, amber or orange fill so that "CRIT and stale" is
   a single visual carrying both facts, which means `.state-crit.is-stale` keeps the red
   background-color from `.state-crit` and only adds the gradient on top.
6. Page-specific layout: the stats-strip row about 72px tall with 24px/600 numerals above 12px
   labels; the overview card grid using UI-SPEC's exact
   `grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))` with `gap: var(--space-md)`;
   group-card rules including the 4px worst-of left border and the member-host chip grid; dense
   device-table rules with 12px row padding, `--font-mono` on the Host and Last Update columns,
   and an active-sort indicator styled with `--color-accent`; history-strip segments at exactly
   12px wide, 24px tall, 2px gap.
7. Connection-indicator dot-plus-label rules for the four states (connecting, connected,
   reconnecting, offline); the poller banner as a full-width strip beneath the topbar using
   `--state-stale` at 20% opacity over `--color-bg`; the tag-group banner with its dismiss
   button.
8. Accessibility rules: `:focus-visible` with `outline: 2px solid var(--color-accent)` reaching
   every interactive element, and a `[hidden] { display: none }` safety rule so the two banners
   can be toggled with the `hidden` attribute.
9. The single `@media (max-width: 900px)` block exactly as UI-SPEC writes it (off-canvas
   sidebar drawer toggled by `.sidebar.is-open`), and nothing beyond it. Anything more is
   DASH2-01, explicitly deferred to v2.

Keep `--color-accent` out of every state rule: UI-SPEC locks the accent to nav, tab, sort,
focus and link use only, so a blue border can never be misread as a status color.

No web fonts, no `@import`, no `@font-face`: the monitoring LAN has no internet egress and
there is no build step to self-host one.

Add a short header comment stating that this file implements `11-UI-SPEC.md`'s contract and
that the token values are copied rather than derived, so a later reader knows where to change
them.
  </action>
  <verify>
    <automated>grep -c 'repeating-linear-gradient' dashboard/css/dashboard.css</automated>
  </verify>
  <acceptance_criteria>
    - All eight state tokens declared: `grep -Eo -- '--state-(ok|warn|crit|down|unknown|unreach|pend|stale)' dashboard/css/dashboard.css | sort -u | wc -l` returns 8
    - Exact UI-SPEC hex values present, one grep each returning at least 1: `#0b0d10`, `#16191d`, `#1c2025`, `#2ecc71`, `#f4c430`, `#ef4444`, `#f97316`, `#a1a1aa`, `#52525b`, `#3b82f6`
    - UNKNOWN is orange not grey (D-11): the line matching `--state-unknown` contains `#f97316`
    - `.is-stale` composes rather than replaces: `awk '/\.is-stale/,/}/' dashboard/css/dashboard.css | grep -c 'background-color'` returns 0, and the same awk range contains `repeating-linear-gradient`
    - Layout tokens exact: `grep -c -- '--layout-sidebar-width: 280px'`, `grep -c -- '--layout-topbar-height: 48px'`, `grep -c -- '--layout-sidebar-tree-fraction: 58%'` each return 1
    - Only the four declared font sizes appear: `grep -Eo 'font-size: *[0-9]+px' dashboard/css/dashboard.css | sort -u` yields exactly the set 12px, 14px, 18px, 24px
    - Overview grid matches UI-SPEC exactly: `grep -c 'repeat(auto-fill, minmax(240px, 1fr))' dashboard/css/dashboard.css` returns 1
    - Exactly one media query: `grep -c '@media' dashboard/css/dashboard.css` returns 1, and that line contains `max-width: 900px`
    - Focus outline present and accent-colored: the `:focus-visible` rule body contains `var(--color-accent)`
    - No web font loaded: `grep -Ec '@import|@font-face|fonts.googleapis' dashboard/css/dashboard.css` returns 0
    - History segments exact: `grep -c '12px' dashboard/css/dashboard.css` returns at least 1 and the history-strip segment rule sets width 12px and height 24px
  </acceptance_criteria>
  <done>The stylesheet implements every UI-SPEC token and layout rule, with the stale hatch composable over any state fill.</done>
</task>

<task type="auto">
  <name>Task 2: Write the three HTML pages with the shared shell and the identical script block</name>
  <files>dashboard/index.html, dashboard/devices.html, dashboard/details.html</files>
  <read_first>
    - dashboard/css/dashboard.css as written in Task 1 — every class and id referenced here must exist there
    - .planning/phases/11-live-dashboard/11-UI-SPEC.md sections "Layout" (the shell diagram plus the three page-specific main-area contracts), "Connection Indicator", "Copywriting Contract" (the exact locked strings), "Accessibility" (emoji title/aria-label rule, focus order, the no-innerHTML rule)
    - .planning/phases/11-live-dashboard/11-CONTEXT.md decisions D-05, D-10, D-19, D-21, D-22, D-23, D-24
    - .planning/phases/11-live-dashboard/11-PATTERNS.md row for the three HTML files (the head comment convention carried over from this repo's Python module docstrings)
    - docs/DMC-networkmap.png and docs/DMC-server.png for layout language only; the text in both is AI-generated and explicitly non-binding
  </read_first>
  <action>
Create `dashboard/index.html`, `dashboard/devices.html` and `dashboard/details.html`. All three
share identical shell markup and an identical script block; only the main-area contents differ.
Use semantic HTML (header, nav, aside, main, table) and no framework.

Shared shell markup, using exactly the ids listed in this plan's interfaces block:
- A `header` with id `topbar` containing the title, `sidebar-toggle` (hamburger for the 900px
  breakpoint), an in-app nav with three real links to `index.html`, `devices.html` and
  `details.html`, and a right-aligned `connection-indicator` holding a dot element plus a
  separate text element. Never dot-only: UI-SPEC's accessibility rule requires the paired
  label. Initial text is the locked string `Connecting…`.
- A `poller-banner` element shipping with the `hidden` attribute. D-14's persistent banner has
  no dismiss button. Do not hardcode the time or age values; the locked template that
  render-shell.js fills is `Poller offline since {HH:MM} — data is {N} minutes old`.
- A `tag-group-banner` element shipping with the `hidden` attribute, carrying the locked string
  `No device_type tag group on this site — run the wizard's tagging phase.` plus a dismiss
  button with `aria-label="Dismiss"`.
- An `aside` with class `sidebar`, split into `sidebar-tree` (whose header row contains the
  D-05 `grouping-toggle` control offering exactly the two modes `device_type` and `folder`) and
  `sidebar-events` (heading text `Event History`). Both panels exist on all three pages: the
  event panel is a shell fixture, not a devices.html widget, per UI-SPEC's "Recent-events panel
  placement" note. That is what makes D-22's "always on screen" true and D-23's in-place detail
  swap meaningful.
- A `main` element with id `main`. This is the only region that differs per page.

Per-page main contents:
- index.html: `stats-strip` (an empty container render-index.js fills with one stat per state)
  above `overview-grid` (the card grid). Include the locked empty-state copy inside
  overview-grid as static markup so a cold page with no broker shows the right words: heading
  `Waiting for device data…` and body `No devices have reported yet. Once the poller publishes
  topology, hosts will appear here automatically.`
- devices.html: a `device-table` table element with a real thead carrying `data-key` attributes
  valued `state`, `host`, `type`, `location`, `lastUpdate`, plus a Flags header cell with NO
  data-key. Header cells must be keyboard focusable and carry `aria-sort="none"` initially.
  An empty tbody, and the same locked empty-state copy as index.html in an adjacent element.
- details.html: a `detail-panel` containing a header row with slots for the device-type glyph,
  the display name, the hostname, the state badge, and the `checkmk-link` anchor carrying the
  locked CTA text `View in Checkmk →`. Plus `history-strip` as an empty container. Ship the
  anchor with no `href` attribute rather than a wrong one; render-details.js sets it. Include
  the locked empty-state copy for the case where the `?id=` query parameter is absent or names
  an unknown host.

Every page's head must carry `meta charset="utf-8"`, a viewport meta, a title, the stylesheet
link `css/dashboard.css`, and an HTML comment stating this file's D-19/D-21 routing role. That
comment is required by PATTERNS.md, which carries this repo's "every module states its purpose"
Python-docstring convention over to HTML. It must state: that this page renders correctly when
opened cold and is bookmarkable, that all three pages load the same `js/shell.js`, and that
in-app navigation is intercepted with `history.pushState` — which is why the nav links are real
hrefs that still work with JavaScript disabled.

The script block goes at the end of body in exactly the order listed in the interfaces section:
classic script src tags, no type=module, no defer, no async. Those js files do not exist yet;
later plans create them. That is expected and must not break the page: a 404'd script is a
console error, not a render failure, and the cold-open empty states above are what the operator
sees in the meantime.

Do not put any inline JavaScript, any inline style attribute, or any onclick attribute in any
of the three files. All behavior lives in js/ and all styling in css/.
  </action>
  <verify>
    <automated>for f in index devices details; do grep -q 'js/shell.js' "dashboard/$f.html" || exit 1; grep -o 'src="js/[^"]*"' "dashboard/$f.html" > "/tmp/dash-$f.txt"; done; diff /tmp/dash-index.txt /tmp/dash-devices.txt && diff /tmp/dash-index.txt /tmp/dash-details.txt</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and each contains the full shared shell id set: for each of `topbar`, `connection-indicator`, `poller-banner`, `tag-group-banner`, `sidebar-toggle`, `grouping-toggle`, `sidebar-tree`, `sidebar-events`, `main`, the command `grep -l "id=\"<id>\"" dashboard/index.html dashboard/devices.html dashboard/details.html | wc -l` returns 3
    - The script block is byte-identical across pages: the automated verify command above exits 0
    - The script order is exactly the eleven files listed in the interfaces block: `grep -o 'src="js/[^"]*"' dashboard/index.html` lists config.js first, vendor/mqtt.min.js second, and shell.js last
    - No module or deferred loading: `grep -Ec 'type="module"|defer|async' dashboard/index.html dashboard/devices.html dashboard/details.html` returns 0 for each file
    - Both banners ship hidden: `grep -c 'id="poller-banner" hidden' dashboard/index.html` returns 1 (attribute order may vary, but the `hidden` attribute must be present on both banner elements in all three files)
    - Locked strings present verbatim: `grep -c 'Waiting for device data' dashboard/index.html` returns 1; `grep -c 'No device_type tag group on this site' dashboard/index.html` returns 1; `grep -c 'View in Checkmk' dashboard/details.html` returns 1; `grep -c 'aria-label="Dismiss"' dashboard/index.html` returns 1
    - Device table headers correct: `grep -o 'data-key="[a-zA-Z]*"' dashboard/devices.html | sort` yields exactly `data-key="host"`, `data-key="lastUpdate"`, `data-key="location"`, `data-key="state"`, `data-key="type"` and nothing else; and `grep -c 'aria-sort="none"' dashboard/devices.html` returns 5
    - details.html ships no wrong link target: `grep 'id="checkmk-link"' dashboard/details.html | grep -c 'href=' ` returns 0
    - No inline behavior or styling: `grep -Ec 'onclick|style="|<script>[^<]' dashboard/index.html dashboard/devices.html dashboard/details.html` returns 0 for each file
    - Routing-role head comment present in all three: `grep -l 'pushState' dashboard/index.html dashboard/devices.html dashboard/details.html | wc -l` returns 3
    - Opened cold with no broker and no js files, each page still shows its empty state (verified in the plan 11-10 human-verify checkpoint; recorded here as the behavioral intent)
  </acceptance_criteria>
  <done>Three bookmarkable pages render the full shell and the correct locked empty-state copy with zero JavaScript loaded, and every mount-point id later plans write into exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN browser to nginx (8090) | Every file under dashboard/ is world-readable on the LAN; there is no authentication by design |
| Checkmk-sourced strings to the DOM | Hostnames, aliases, folder labels and event descriptions all originate outside this codebase and cross into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | static HTML markup | mitigate | The three pages contain zero inline script, zero inline style and zero event-handler attributes, so no Checkmk-sourced or query-string-sourced value can ever reach an inline execution context. Enforced by an acceptance criterion grepping for `onclick`, `style="` and inline script bodies |
| T-11-08 | Elevation of Privilege | DOM injection surface | mitigate | This plan ships only empty mount-point containers; no dynamic string interpolation exists here at all. The `textContent`-only obligation for the modules that fill these containers is carried by plans 11-07, 11-08 and 11-09, where the interpolation actually happens |
| T-11-09 | Information Disclosure | details.html query parameter | mitigate | `checkmk-link` ships with no `href`, so a cold page cannot be induced to advertise a link built from an unvalidated `?id=` value; href construction and its validation are owned by plan 11-09 |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. No web font, no CDN reference, no `@import` — enforced by an acceptance criterion — so this plan introduces no third-party fetch at page-load time |
</threat_model>

<verification>
- `grep -o 'src="js/[^"]*"' dashboard/index.html` matches the same output for devices.html and details.html
- `grep -Eo 'font-size: *[0-9]+px' dashboard/css/dashboard.css | sort -u` yields only 12px, 14px, 18px, 24px
- On the deployment host after plan 11-02's service is up: `curl -s http://localhost:8090/ | grep -c 'stats-strip'` returns 1, and `curl -sI http://localhost:8090/css/dashboard.css` returns 200
- `uv run pytest -q` — the Python suite is untouched by this plan and must stay green
</verification>

<success_criteria>
- Three real, bookmarkable HTML files load one identical shared script block (D-21)
- Persistent shell with a split left column: grouped tree over an always-visible event history (D-22)
- Every UI-SPEC token copied verbatim; stale hatch composes over state fills (D-11, D-15)
- Locked copy strings appear verbatim; every state carries a non-color signal (D-10, D-14, D-16)
- Zero inline script, zero inline style, zero web fonts, one media query
</success_criteria>

<output>
Create `.planning/phases/11-live-dashboard/11-03-SUMMARY.md` when done. Record the final id
inventory so plans 11-07 through 11-09 can target mount points without re-reading the HTML.
</output>
