---
phase: 11-live-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/css/dashboard.css
  - dashboard/index.html
  - dashboard/devices.html
  - dashboard/details.html
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-06]
must_haves:
  truths:
    - "All three pages render a dark, dense, KONE-branded shell — logo top-left, grouped tree over an always-visible event-history panel — when opened cold with no broker reachable (D-21, D-22, UI-SPEC Brand Masthead)"
    - "The stale hatch composes on top of a state fill, so a CRIT-and-stale group renders red-and-hatched rather than one state replacing the other (D-15, DASH-04)"
    - "Every Checkmk state has both a color token and a non-color signal: a masked KONE icon, a spelled-out label, or a texture (D-10 as superseded by UI-SPEC, D-11)"
    - "index.html's grouped-overview slot is a self-contained main-area module, so Phase 13's map is a drop-in replacement rather than a rewrite (D-24)"
  artifacts:
    - path: "dashboard/css/dashboard.css"
      provides: "KONE tokens, state palette, 25 icon mask classes, composable stale hatch, shell grid, page layouts"
      contains: "--state-stale"
      min_lines: 250
    - path: "dashboard/index.html"
      provides: "Stats-by-state strip and grouped fleet overview mount points (D-24)"
      contains: "stats-strip"
    - path: "dashboard/devices.html"
      provides: "Sortable device table skeleton with data-key headers (DASH-02)"
      contains: "data-key"
    - path: "dashboard/details.html"
      provides: "Per-device detail panel skeleton with history-strip container and Checkmk CTA (DASH-03)"
      contains: "history-strip"
  key_links:
    - from: "dashboard/index.html, devices.html, details.html"
      to: "dashboard/js/shell.js"
      via: "identical ordered classic script tag block on all three pages"
      pattern: "js/shell.js"
    - from: "dashboard/css/dashboard.css .is-stale"
      to: "the .state-* fill classes"
      via: "background-image gradient overlay, never a background-color replacement"
      pattern: "repeating-linear-gradient"
    - from: "dashboard/css/dashboard.css @font-face"
      to: "dashboard/fonts/"
      via: "same-origin relative url(), no CDN"
      pattern: "\\.\\./fonts/"
---

<objective>
Build the dashboard's stylesheet and its three real HTML pages: one stylesheet implementing the
KONE-branded UI contract verbatim, and three files that each render the persistent shell
correctly when opened cold.

Purpose: D-21 requires three bookmarkable HTML files that behave as one app, and D-22 fixes the
shell layout. Fixing the DOM skeleton and the CSS contract first means every render module in
later plans writes into a known, already-styled mount point instead of inventing markup.

Output: three pages that render a complete empty-state shell with no JavaScript behavior yet —
the `js/` modules arrive in later plans, and a missing script must not break the page.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-dashboard/11-CONTEXT.md
@.planning/phases/11-live-dashboard/11-UI-SPEC.md
@.planning/phases/11-live-dashboard/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- DOM contract. Every id below is written into by a render module in plans 11-06/11-07/11-08
     and MUST be created with exactly these ids. -->

Shell markup, identical on all three pages:
  brand-masthead         KONE logo img + .brand-title, leftmost child of #topbar (UI-SPEC Brand Masthead)
  topbar                 element id: topbar
  connection-indicator   written by render-shell.js (plan 11-06), DASH-05
  poller-banner          written by render-shell.js, D-13/D-14, ships with the hidden attribute
  tag-group-banner       written by render-shell.js, D-16, dismissible, ships hidden
  sidebar-toggle         hamburger (.icon-list), only visible under the 900px breakpoint
  grouping-toggle        D-05 folder / device_type switch, in the sidebar tree header
  sidebar-tree           written by render-shell.js, grouped tree
  sidebar-events         written by render-shell.js, always-visible event history
  main                   swappable main area (D-23)

index.html main area:
  stats-strip            written by render-index.js (plan 11-07), DASH-01
  overview-grid          written by render-index.js, grouped fleet overview (D-24)

devices.html main area:
  device-table           written by render-devices.js (plan 11-07), DASH-02
    thead th carries data-key values: state, host, type, location, lastUpdate
    the Flags th carries NO data-key (UI-SPEC marks it not sortable)
    tbody rows are keyed by the tr attribute data-id set to the hostname

details.html main area:
  detail-panel           written by render-details.js (plan 11-08), DASH-03
  history-strip          written by render-details.js, 20 fixed-width segments
  checkmk-link           written by render-details.js, locked CTA text "View in Checkmk →"

Ordered script block, identical on all three pages. Classic scripts only: no type=module,
no defer, no async. config.js defines globals every later file reads; shell.js must run last:
  js/config.js
  js/vendor/mqtt.min.js
  js/staleness.js
  js/display.js
  js/grouping.js
  js/state-store.js
  js/mqtt-connection.js
  js/render-shell.js
  js/render-index.js
  js/render-devices.js
  js/render-details.js
  js/shell.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write dashboard/css/dashboard.css from the KONE UI contract</name>
  <files>dashboard/css/dashboard.css</files>
  <read_first>
    - .planning/phases/11-live-dashboard/11-UI-SPEC.md in full. Load-bearing sections: "Typography" (the three @font-face blocks and the 4-size/2-weight table), "Iconography" (the .icon base rule and all 25 .icon-* mask selectors, written out literally), "Color" (the :root block is literal), "State-color to meaning map", "Stale hatch pattern", "Spacing Scale", "Radius Scale", "Brand Masthead" (the .brand-masthead/.brand-logo/.brand-title rules are written out literally), "Layout" (shell/sidebar grid CSS and the three layout variables), "Connection Indicator", "Responsive Behavior", "Accessibility"
    - .planning/phases/11-live-dashboard/11-CONTEXT.md decisions D-10, D-11, D-14, D-15, D-22
    - dashboard/fonts/ and dashboard/icons/ as vendored by plan 11-02 — confirm the exact on-disk filenames the url() references must match
  </read_first>
  <action>
Create `dashboard/css/dashboard.css` implementing UI-SPEC's contract. Copy every declared token
value verbatim. Do not re-derive, improve, or "modernize" any hex value, spacing number, radius
or layout fraction: UI-SPEC resolved every discretionary choice against KONE's own
`design-tokens.json` and was checker-reviewed.

Required contents:

1. Three `@font-face` blocks copied exactly from UI-SPEC Typography: `'KONE Information'`
   (woff2 + woff, weight 400), `'Inter'` weight 400 (`inter-regular.ttf`, format truetype) and
   `'Inter'` weight 600 (`inter-semibold.ttf`). Every `src` is a same-origin relative path under
   `../fonts/`. There must be zero `@import`, zero `fonts.googleapis.com`, zero `http`/`https`
   URL anywhere in the file — the no-CDN rule is hard, the no-web-font rule is not (UI-SPEC
   "Superseded: the old 'no web font' rule").

2. The `:root` block copied exactly from UI-SPEC Color: `--color-bg: #141414`,
   `--color-surface: #142041`, `--color-surface-raised: #142c6e`,
   `--color-border: rgba(20, 56, 155, 0.35)`, `--color-accent: #1450f5`,
   `--color-accent-text: #7296f9`, `--color-text: #ffffff`, `--color-text-muted: #a1b9fb`, and
   the eight state tokens `--state-ok: #1ed273`, `--state-warn: #ffe141`,
   `--state-crit: #ff5f28`, `--state-down: #f51414` (deliberately a DIFFERENT red from CRIT —
   see UI-SPEC "Noted deviation: D-11 red split"), `--state-unknown: #ffa023`,
   `--state-unreach: #ffa023`, `--state-pend: #959595`, `--state-stale: #727272`.

3. Spacing tokens `--space-xs: 4px` through `--space-3xl: 64px` including the dense-layout
   `--space-sm2: 12px`; radius tokens `--radius-sm: 4px`, `--radius-md: 8px`,
   `--radius-pill: 100px`; `--font-sans: 'Inter', -apple-system, "Segoe UI", Roboto,
   "Helvetica Neue", Arial, sans-serif` and the `--font-mono` system stack unchanged. Only the
   four declared sizes (12px, 14px, 18px, 24px) and two weights (400, 600) may appear as
   `font-size`/`font-weight` values anywhere in the file.

4. Layout tokens `--layout-sidebar-width: 280px`, `--layout-topbar-height: 48px`,
   `--layout-sidebar-tree-fraction: 58%`, plus the `.shell`, `.sidebar`,
   `.sidebar-tree`/`.sidebar-events` and `.topbar` rules exactly as UI-SPEC Layout writes them.

5. The `.brand-masthead`, `.brand-logo` and `.brand-title` rules exactly as UI-SPEC Brand
   Masthead writes them — including the white `.brand-logo` background chip at `--radius-sm`,
   the 24px logo height, and `.brand-title` at 14px/400 in `'KONE Information'` with
   `letter-spacing: 0.8px` and `text-transform: uppercase`.

6. The `.icon` base rule and all 25 `.icon-*` mask selectors exactly as UI-SPEC Iconography
   writes them: 16x16, `mask-repeat: no-repeat`, `mask-position: center`,
   `mask-size: contain`, `background-color: currentColor`, each selector carrying both the
   `-webkit-mask-image` and `mask-image` declaration pointing at `../icons/<name>.svg`. Use
   `mask-image`, never `<img>` and never inlined SVG — the vendored files hardcode
   `fill="#141414"` and cannot be recolored any other way.

7. A systematic state-class family, one class per state, each setting its fill from the matching
   token: `.state-ok`, `.state-warn`, `.state-crit`, `.state-down`, `.state-unknown`,
   `.state-unreach`, `.state-pend`. Systematic naming matters because the render modules compute
   the class name from the state string. Add the icon-recoloring pairing rule so
   `.icon.state-crit` (and each sibling) sets `background-color` to that state's token.

8. `.is-stale` using UI-SPEC's exact declaration
   `repeating-linear-gradient(45deg, rgba(255,255,255,0.06) 0 6px, transparent 6px 12px)`
   applied through `background-image` ONLY. It must not set `background-color`. D-15 requires
   the hatch to compose over an existing red/amber/orange fill, so `.state-crit.is-stale` keeps
   the red background-color from `.state-crit` and only adds the gradient on top.

9. The accessibility contract rule from UI-SPEC: DOWN and stale *text labels* must render in
   `--color-text` or `--color-text-muted`, never in `--state-down`/`--state-stale` — those two
   tokens may appear only in `background-color`, `border-color` or icon-color contexts, because
   both measure below 4.5:1 against `--color-surface`. Add a comment recording that measured
   contrast (≈3.8:1 and ≈3.3:1) next to the rule so a later edit does not silently undo it.
   Add `:focus-visible { outline: 2px solid var(--color-accent); }` on interactive elements.

10. Page-specific layout: the stats-strip row about 72px tall with 24px/600 numerals above
    12px labels; the overview card grid using the exact
    `grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))` with `gap: var(--space-md)`,
    cards at `--radius-md` with a 4px left border carrying the worst-of color; the device table
    at 14px with 12px `<th>` labels and `--space-sm` cell padding; the history strip's segments
    at 12px wide, 24px tall, 2px gap; count badges, state badges and the connection-indicator
    capsule at `--radius-pill`.

11. The single 900px breakpoint block exactly as UI-SPEC Responsive Behavior writes it
    (off-canvas `.sidebar`, `.sidebar.is-open` translate, `.shell` collapsing to one column).

Include an empty-state style for the copy strings the render modules will insert, so a cold page
with no broker looks deliberate rather than broken.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && test $(grep -c '@font-face' dashboard/css/dashboard.css) -eq 3 && test $(grep -Ec '@import|fonts\.googleapis|https?://' dashboard/css/dashboard.css) -eq 0 && test $(grep -c 'mask-image' dashboard/css/dashboard.css) -ge 50 && grep -q 'repeating-linear-gradient' dashboard/css/dashboard.css && for f in $(grep -o "\.\./icons/[a-z-]*\.svg" dashboard/css/dashboard.css | sort -u); do test -f "dashboard/css/$f" || { echo "MISSING $f"; exit 1; }; done && for f in $(grep -o "\.\./fonts/[a-z.-]*" dashboard/css/dashboard.css | sort -u); do test -f "dashboard/css/$f" || { echo "MISSING $f"; exit 1; }; done && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@font-face' dashboard/css/dashboard.css` returns exactly 3
    - `grep -Ec '@import|fonts.googleapis|https?://' dashboard/css/dashboard.css` returns 0
    - Every `url()` referenced under `../icons/` and `../fonts/` resolves to a file that exists (the verify command asserts this and exits non-zero on any miss)
    - All 25 `.icon-*` selectors are present: `grep -Ec '^\.icon-[a-z-]+ *\{|^\.icon-[a-z-]+ *\{' dashboard/css/dashboard.css` returns at least 25
    - Every KONE hex is present: `grep -Ec '#141414|#142041|#142c6e|#1450f5|#1ed273|#ffe141|#ff5f28|#f51414|#ffa023|#959595|#727272' dashboard/css/dashboard.css` returns at least 11
    - No superseded pre-KONE hex survives: `grep -Eic '#0b0d10|#16191d|#1c2025|#2ecc71|#f4c430|#ef4444|#f97316|#a1a1aa|#52525b|#3b82f6' dashboard/css/dashboard.css` returns 0
    - No emoji glyph is used as a state or device-type marker: `grep -c '⛔' dashboard/css/dashboard.css` returns 0
    - Only four font sizes appear: `grep -o 'font-size: *[0-9]*px' dashboard/css/dashboard.css | sort -u` lists only 12px, 14px, 18px, 24px
    - `.is-stale` sets `background-image` and not `background-color`: the `.is-stale` rule body contains `background-image` and `grep -A3 '\.is-stale' dashboard/css/dashboard.css | grep -c 'background-color'` returns 0
  </acceptance_criteria>
  <done>One stylesheet carries every KONE token, all 25 icon masks, the composable stale hatch, the shell grid and the three page layouts, with zero external URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Write the three HTML pages with the shared shell</name>
  <files>dashboard/index.html, dashboard/devices.html, dashboard/details.html</files>
  <read_first>
    - dashboard/css/dashboard.css as written in Task 1 — every class and id referenced from markup must exist there
    - .planning/phases/11-live-dashboard/11-UI-SPEC.md sections "Brand Masthead" (the exact masthead markup), "Layout", "Connection Indicator", "Copywriting Contract" (every locked string), "Accessibility" (the title + aria-label + role="img" rule for icon-only elements)
    - .planning/phases/11-live-dashboard/11-CONTEXT.md D-19, D-21, D-22, D-23, D-24
    - This plan's `<interfaces>` block — the id list and the ordered script block are a contract later plans depend on verbatim
  </read_first>
  <action>
Create `dashboard/index.html`, `dashboard/devices.html` and `dashboard/details.html`. All three
share identical `<head>`, topbar, sidebar and script block; only the `<main id="main">` content
differs. D-21 requires each to render correctly when opened cold, so every string a user sees
before MQTT connects must be in the static markup, not injected.

Shared parts, byte-identical across the three files:
- `<head>`: charset utf-8, viewport, a `<title>`, and exactly one stylesheet link to
  `css/dashboard.css`. No inline `<style>`, no `<link>` to any external origin.
- An HTML comment at the top of each file stating that file's routing role under D-19/D-21 —
  matching this repo's "every module states its purpose" convention.
- `#topbar` containing, in order: `.brand-masthead` (the `<img src="images/kone-logo.png"
  alt="KONE" class="brand-logo">` plus `<span class="brand-title">Live Network Dashboard</span>`,
  exactly as UI-SPEC writes it), the three in-app nav links (`index.html`, `devices.html`,
  `details.html`), `#sidebar-toggle` (a `<button>` wrapping `<span class="icon icon-list">` with
  `title` and `aria-label`), and `#connection-indicator` right-aligned containing a dot element
  and a text element pre-populated with the locked string "Connecting…".
- `#poller-banner` and `#tag-group-banner`, both shipping with the `hidden` attribute set.
  Each carries its leading icon span (`.icon-warning-circle-filled` and `.icon-info-filled`
  respectively) and a text element. The tag-group banner additionally carries a dismiss
  `<button>` wrapping `<span class="icon icon-close-cross">` with `aria-label="Dismiss"`.
- `.sidebar` containing a tree header with `#grouping-toggle` (a `<button>` carrying
  `.icon-filter`, `aria-pressed`, and a visible text label — never icon-only without a label),
  then `#sidebar-tree` and `#sidebar-events`. Each empty container holds its locked empty-state
  copy from the Copywriting Contract ("No events yet — state changes will appear here as they
  happen." for events).
- The ordered `<script src>` block exactly as listed in this plan's interfaces, all classic
  scripts, placed at the end of `<body>`. No `type="module"`, no `defer`, no `async`.

Page-specific `<main id="main">` content:
- `index.html`: `#stats-strip` (empty, styled) above `#overview-grid`, with the overview's
  locked empty-state heading "Waiting for device data…" and its body string. Wrap the overview
  in a single self-contained container element so Phase 13's map can replace it as a drop-in
  (D-24) — note that intent in an HTML comment.
- `devices.html`: `#device-table` as a real `<table>` with a `<thead>` row whose `<th>` elements
  carry `data-key` values `state`, `host`, `type`, `location`, `lastUpdate` and visible labels
  State / Host / Type / Location / Last Update, plus a final Flags `<th>` with NO `data-key`.
  Empty `<tbody>`, and the device-table empty-state copy rendered beneath or inside it.
- `details.html`: `#detail-panel` containing a header row (device name slot, hostname slot,
  state badge slot), `#history-strip`, and `#checkmk-link` as an `<a>` whose text is the locked
  CTA "View in Checkmk →" paired with `<span class="icon icon-pop-out">`. Ship the anchor with
  no `href` (or `href="#"`) — plan 11-08 computes the real target from config.js. Include the
  "Waiting for device data…" empty state for the case where no `?id=` is supplied.

Hard rules for all three files: no inline `style="` attributes, no inline `onclick`/`on*`
handlers, no inline script bodies. Every icon-only element carries `title`, `aria-label` and
`role="img"` (or is a `<button>` with an `aria-label`), per UI-SPEC Accessibility. Use no emoji
characters anywhere — the DOWN marker is `.icon-close-circle-filled`, not `⛔`.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && for f in index devices details; do p=dashboard/$f.html; test -f $p || exit 1; test $(grep -Ec 'onclick|onload|on[a-z]+="|style="' $p) -eq 0 || { echo "inline in $p"; exit 1; }; test $(grep -c 'type="module"\|defer\|async' $p) -eq 0 || { echo "module/defer in $p"; exit 1; }; test $(grep -c 'js/shell.js' $p) -eq 1 || exit 1; test $(grep -c 'brand-masthead' $p) -ge 1 || exit 1; test $(grep -c 'images/kone-logo.png' $p) -eq 1 || exit 1; test $(grep -Ec 'https?://' $p) -eq 0 || { echo "external url in $p"; exit 1; }; done && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and the verify command prints `ok`
    - Each page loads exactly 12 scripts in the interfaces-block order: `grep -c '<script src=' dashboard/index.html` returns 12, and the same for devices.html and details.html
    - The shell id set exists identically on all three pages: for each of topbar, connection-indicator, poller-banner, tag-group-banner, sidebar-toggle, grouping-toggle, sidebar-tree, sidebar-events, main — `grep -c 'id="<id>"'` returns 1 in every page
    - `grep -c 'hidden' dashboard/index.html` returns at least 2 (both banners ship hidden)
    - `dashboard/devices.html` has exactly five sortable headers: `grep -c 'data-key=' dashboard/devices.html` returns 5
    - `dashboard/details.html` contains `id="history-strip"`, `id="checkmk-link"` and the locked string `View in Checkmk`
    - Locked copy present verbatim: `grep -c 'Waiting for device data' dashboard/index.html` returns at least 1; `grep -c 'No events yet' dashboard/index.html` returns at least 1
    - No emoji glyph in any page: `grep -c '⛔' dashboard/*.html` returns 0 for each file
    - Opening `dashboard/index.html` in a browser with no broker running shows the KONE logo top-left, the split sidebar, and the empty-state copy — no blank page, no console-visible error from the missing js/ modules (they do not exist yet; a 404 on a script tag must not prevent the static shell from rendering)
  </acceptance_criteria>
  <done>Three bookmarkable pages render the complete KONE-branded shell cold, with every mount point later plans write into already present and styled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT payload → DOM | Checkmk-sourced strings (alias, folder, event text) will be rendered into these containers by later plans |
| LAN browser → dashboard origin | All CSS/font/icon/logo references must stay same-origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | static markup | mitigate | No inline `on*` handlers, no inline `<script>` bodies, no `style="` attributes — enforced as a grep acceptance criterion, so the later textContent-only rendering rule cannot be undercut by markup that already invites string injection |
| T-11-08 | Information Disclosure | external asset references | mitigate | Zero `https?://` in CSS or HTML — asserted by grep; nothing about the page reveals itself to an external origin, which also satisfies the no-egress LAN constraint |
| T-11-09 | Repudiation | state rendering | mitigate | Every state carries a non-color signal (icon mask, spelled label, or hatch texture), so a colorblind or monochrome-display operator cannot misread DOWN as CRIT |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; assets come from plan 11-02's already-audited vendoring |
</threat_model>

<verification>
- Open each of the three pages with `file://` or through any static server: the shell renders, the logo is visible, no layout is broken
- `grep` gates in the acceptance criteria all pass
- Every asset URL referenced from CSS resolves to a file committed by plan 11-02
</verification>

<success_criteria>
- The KONE masthead appears top-left on all three pages
- The DOM id contract in this plan's interfaces exists exactly as specified, so plans 11-06/07/08 need no exploration
- Zero external URLs, zero inline handlers, zero superseded pre-KONE hex values
</success_criteria>

<output>
Create `.planning/phases/11-live-dashboard/11-03-SUMMARY.md` when done
</output>
