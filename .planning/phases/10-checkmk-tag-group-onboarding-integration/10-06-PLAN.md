---
phase: 10-checkmk-tag-group-onboarding-integration
plan: 06
type: execute
wave: 4
depends_on: ["10-04", "10-05"]
files_modified:
  - scripts/mqtt_poller.py
  - tests/test_mqtt_poller.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: false
requirements: [TAG-01, TAG-02, TAG-03]
user_setup: []

must_haves:
  truths:
    - "A real host onboarded through the wizard carries a device_type tag visible in Checkmk"
    - "The key shape Livestatus uses for that tag is observed on the live site, not guessed"
    - "The poller publishes that host's real device_type, alias and folder to MQTT on a live stack"
    - "extract_device_type reflects the confirmed key shape and no longer carries an unreachable guess presented as a defensive necessity"
    - "ROADMAP.md, REQUIREMENTS.md and PROJECT.md no longer describe this phase in wording the user's own decisions superseded"
  artifacts:
    - path: "scripts/mqtt_poller.py"
      provides: "extract_device_type matching the live-confirmed Livestatus tag key shape, with a dated citation"
      contains: "Live-verified"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TAG-01/02/03 wording reconciled with CONTEXT.md decisions D-01, D-06 and D-07"
      contains: "TAG-03"
  key_links:
    - from: "src/checkmk_wizard/wizard.py Phase 4 device_type prompt"
      to: "lan/devices/{id}/status device_type field"
      via: "tag_device_type host attribute read back through Livestatus tags by the poller"
      pattern: "device_type"
---

<objective>
Prove the whole chain on a real deployment — wizard prompt, Checkmk tag attribute, Livestatus tags column, MQTT payload — then close the one assumption that could not be answered before a host actually carried the tag, and fix the upstream wording this phase's decisions superseded.

Purpose: everything in plans 10-02 through 10-05 was verified against mocks and against a probe that ran before any host carried a device type. 10-RESEARCH.md Assumption A3 (whether Livestatus keys custom tags by their bare id or with a `tag_` prefix) is explicitly deferred to this point, and Phase 9's own live-verification block in `scripts/mqtt_poller.py` says the same thing in the source: all 21 hosts on the live site returned `tags` with neither key set, because no host carried the tag yet. This plan is where that stops being true.
Output: a live-confirmed device-type key shape recorded in-source, a corrected `extract_device_type`, and planning docs whose wording matches what was actually built.

Two things this plan deliberately does NOT do. It does not rename the `folder` payload key — plan 10-03 resolved that discretion call in favour of keeping the existing generic key. And it does not add dashboard behaviour of any kind: preferring alias over hostname for display is Phase 11's job, and CONTEXT.md lists it under Deferred Ideas.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-CONTEXT.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-04-SUMMARY.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-05-SUMMARY.md
@scripts/mqtt_poller.py
@scripts/probe_checkmk_rest_shapes.py
@scripts/smoke_test_poller.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live end-to-end verification on the deployment host</name>
  <files>none — verification only, no files are modified by this task</files>
  <action>Pause and hand the verification steps below to the developer, who runs them on the real deployment host — this machine has no podman, no broker and no Checkmk site. Do not run the wizard, `podman compose`, `mosquitto_sub` or the smoke test locally, and do not simulate or infer the results. Collect four things before proceeding: the Phase 2 tag-group line the wizard printed (including the pre-existing-host default count), the probe's P5 `tags` key list for the newly tagged host, the verbatim retained `lan/devices/{id}/status` payload, and the smoke test's `[SUMMARY]` line. Task 2 consumes the P5 output to fix `extract_device_type`; Task 3 marks the requirements complete only if this verification passed.</action>
  <what-built>
The full Phase 10 chain:

- The wizard creates a `device_type` host tag group once, with `other` first so every pre-existing host defaults safely, and prints how many hosts that default touched (plan 10-02).
- Phase 4 asks a device type per host from `device_types.json`, plus an optional display alias; Phase 5 applies them as the `tag_device_type` and `alias` host attributes on all three monitoring paths (plan 10-04).
- The poller reads each host's folder from Checkmk's REST API instead of parsing the OMD filesystem path Livestatus reports, and carries `alias` through to the retained MQTT payload (plan 10-03).
- The poller service now has `CMK_REST_*` credentials and a smoke-test check that distinguishes working enrichment from a missing secret (plan 10-05).

One thing still cannot be answered from this machine. Checkmk's REST API names a tag attribute `tag_device_type`, but Livestatus's `hosts.tags` column has historically keyed tags by their bare group id. Nothing forces the two conventions to match, and when Phase 9 checked, no host on the live site carried a device type at all, so the poller still guesses both spellings. Once you onboard one real host with a device type, the answer becomes observable.

This development machine has no podman, no broker and no Checkmk site — same situation as Phases 8 and 9.
  </what-built>
  <how-to-verify>
Run these on the deployment host, from the repo checkout.

1. Pull the changes and provision the poller's REST credential if you have not already, following section 3's "First-time credential setup" in `docs/Podman setup for checkmk, minio, mosquitto, worker.md`: put the automation user's secret into the `poller` service's `CMK_REST_SECRET`, then:
   `cd <checkout>/deploy && podman compose up -d poller && podman compose logs --tail 40 poller`
   Expected: the poller starts, logs its selected Livestatus column list including `alias`, and logs no REST warning. Expected NOT to appear: the automation secret, or the MQTT password.

2. Run the wizard and onboard one host with a device type and an alias:
   `cd <checkout> && uv run checkmk-wizard`
   At Phase 2, note the line the wizard prints about the `device_type` tag group. If this is the first run against this site, it should report creating the group and say how many pre-existing hosts were defaulted to `other`. Report that line verbatim, and say whether the number matches roughly how many hosts you already had.
   At Phase 4, promote one host, pick a real device type (not `other` — pick something like `NetworkDevice` so it is distinguishable), and give it an alias. Finish through Phase 7 so changes are activated.

3. Confirm Checkmk stored it. In the Checkmk UI, open that host's properties and confirm both the Device Type tag and the Alias show your values. Also open Setup, Tags and confirm the `device_type` group exists with `other` listed first and no leftover `gsd_probe_device_type` group from plan 10-01's probe.

4. Answer the one open question — what key does Livestatus use? Run:
   `cd <checkout> && CMK_REST_HOST=localhost CMK_SITE_ID=dmc LIVESTATUS_HOST=localhost CMK_REST_USERNAME=automation CMK_REST_SECRET='<secret>' uv run python scripts/probe_checkmk_rest_shapes.py`
   Report the P5 section verbatim. The line that matters is the sorted `tags` key list for the host you just onboarded: does it contain `device_type`, or `tag_device_type`, or something else entirely?

5. Confirm the poller published it. Wait one poll interval (60s by default), then:
   `mosquitto_sub -h localhost -p 1883 -u poller -P poller -t 'lan/devices/<hostname>/status' -v -C 1 -W 10`
   Expected: a JSON payload whose `device_type` is the value you chose in step 2 (NOT `unknown`), whose `alias` is the alias you set, and whose `folder` is the Checkmk folder path for that host with no `wato` segment and no `/omd` prefix. Report the payload verbatim.

6. Run the full smoke test, including the new enrichment check:
   `uv run python scripts/smoke_test_poller.py --host localhost --livestatus-host localhost --compose-dir deploy`
   Expected: every check `[PASS]`, including the new enrichment check, and a `[SUMMARY]` line. This takes a few minutes.

Report back: the Phase 2 tag-group line from step 2, the step 4 P5 tags key list, the step 5 payload, and the step 6 summary. If `device_type` came back as `unknown` in step 5, say so loudly — that means the key shape guess is wrong in a way step 4's output will explain.
  </how-to-verify>
  <verify>
    <human-check>A real host's chosen device_type and alias appear in its retained lan/devices/{id}/status payload, the folder value contains no wato segment, and the smoke test reports all checks PASS</human-check>
  </verify>
  <resume-signal>Type "approved" with the step 2, 4, 5 and 6 outputs pasted, or describe the failures</resume-signal>
  <done>The full wizard-to-MQTT chain is observed on a real Checkmk 2.4.0p35 site, and the Livestatus tag key shape is known rather than guessed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the confirmed Livestatus tag key shape to extract_device_type</name>
  <files>scripts/mqtt_poller.py, tests/test_mqtt_poller.py</files>
  <read_first>
    - scripts/mqtt_poller.py lines 63-102 (the Phase 9 live-verification block, including its explicit statement that the device-type key shape remained unverified and should be re-confirmed once Phase 10 lands — this is the comment being closed out)
    - scripts/mqtt_poller.py lines 300-312 (`extract_device_type` and its dual-key guessing, plus `UNKNOWN_DEVICE_TYPE`)
    - The Task 1 checkpoint output (specifically the step 4 P5 tags key list and the step 5 published payload)
    - src/checkmk_wizard/site.py lines 185-205 (the dated `Bug fixed <date>` post-mortem comment convention for replacing behaviour rather than patching a symptom)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md (Common Pitfall 5 and Assumption A3 — the claim being closed, including its explicit note that a wrong guess in the other direction shows up as a persistent `unknown`)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-CONTEXT.md (Claude's Discretion: whether the dual-key guessing gets simplified was left for planning to resolve once the tag exists live — this task is that resolution point)
  </read_first>
  <action>
Apply what Task 1's step 4 actually showed.

If Livestatus keyed the tag by its bare group id (`device_type`), remove the `tag_device_type` branch from `extract_device_type` — it is confirmed-unreachable code, not a defensive necessity, and leaving it in would misrepresent an unverified guess as a deliberate safeguard. If Livestatus keyed it with the `tag_` prefix, keep that branch and remove the bare one instead. If both appeared, or the key was something else entirely, keep whatever the live site actually returns and say so.

Whichever way it went, replace the function's current docstring — which says the tag "doesn't exist until Phase 10" and treats `unknown` as the normal case — with one describing the confirmed reality: which key the live site uses, and that `UNKNOWN_DEVICE_TYPE` now means a genuinely untagged host rather than an unimplemented feature. Add a `Live-verified against a real Checkmk 2.4.0p35 CE site on <date of the Task 1 run>:` comment naming the observed key and the host it was observed on, following the convention in `api.py`.

Update the Phase 9 live-verification block above the column constants so it no longer says the device-type key shape is unverified and pending Phase 10. Do not delete that block's history — amend it with a dated follow-up line recording what Phase 10 confirmed, in the same style as `site.py`'s dated post-mortems. This block is the file's record of what was checked and when; rewriting it to look as if the ambiguity never existed would destroy that.

If step 5's payload showed `device_type` as `unknown` despite the host being tagged in the Checkmk UI, that is a real bug and this task fixes it before anything else — RESEARCH.md Pitfall 5 names this exact symptom and points at the key shape as the cause. Diagnose from step 4's key list, correct the lookup, and leave a dated post-mortem comment explaining what was expected versus what the site returned.

Update `tests/test_mqtt_poller.py`'s `extract_device_type` tests to match. Delete the test for whichever branch was removed rather than leaving it asserting behaviour the function no longer has, and add or amend a test whose name or comment records that the surviving key shape is live-confirmed, citing the date.
  </action>
  <verify>
    <automated>uv run pytest tests/test_mqtt_poller.py -q &amp;&amp; uv run pytest -q &amp;&amp; uv run ruff check scripts/ src/ tests/ &amp;&amp; grep -q 'Live-verified' scripts/mqtt_poller.py</automated>
  </verify>
  <acceptance_criteria>
    - `extract_device_type` looks up exactly the key shape Task 1's step 4 reported, and any confirmed-unreachable branch is gone
    - `extract_device_type`'s docstring no longer claims the tag "doesn't exist until Phase 10"
    - A `Live-verified against a real Checkmk 2.4.0p35 CE site on ` comment near `extract_device_type` names the observed Livestatus key and the date of the Task 1 run
    - The Phase 9 live-verification block above the column constants is amended, not replaced, with a dated line recording what Phase 10 confirmed about the device-type key and about the `alias` column's availability
    - `tests/test_mqtt_poller.py` has no test asserting a key branch that no longer exists
    - At least one `extract_device_type` test carries a comment citing the live-confirmation date
    - `uv run pytest -q` exits 0 for the full suite and `uv run ruff check scripts/ src/ tests/` exits 0
  </acceptance_criteria>
  <done>The poller reads the device-type tag by the key the live site actually uses, and the source records how and when that was established.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP, REQUIREMENTS and PROJECT wording with the decisions this phase implemented</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 10 section: its Goal line and Success Criteria 1-4, which say `unknown`, `VLAN` and "Phase 5 onboarding flow")
    - .planning/REQUIREMENTS.md (the Tagging section's TAG-01, TAG-02 and TAG-03 text, and the Traceability table rows for them)
    - .planning/PROJECT.md (the Active requirements list, which also uses the "VLAN" wording)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-CONTEXT.md (decisions D-01, D-02, D-06 and D-07 — the authoritative superseding wording, including the explicit "Follow-up needed" note asking for this reconciliation)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md (the "Terminology reconciliation the planner must handle, not silently pick one" paragraph)
  </read_first>
  <action>
Update the three planning documents so they describe what this phase actually built. CONTEXT.md's D-01 and D-02 raised this as an explicit follow-up; it is a wording gap in documents written before the discussion, not a scope change.

In `.planning/REQUIREMENTS.md`'s Tagging section: rewrite TAG-01 so the neutral default is `other` occupying first position in the tag group (which is the mechanism by which Checkmk auto-defaults pre-existing hosts) rather than a literal value named `unknown`, and so the choice list is described as config-driven and site-specific rather than illustrated with `server/switch/router/iot`. Rewrite TAG-02 so the prompt is attributed to the wizard's Phase 4 classification flow, with Phase 5 applying the collected value — the current "Phase 5 onboarding flow prompts" is simply wrong about where the prompt lives. Rewrite TAG-03 so it describes a generic location/group label derived from the host's Checkmk folder via the REST API's folder association, not "VLAN", and note that it is consumed by the poller rather than the wizard. Leave the requirement IDs, their ordering and the Traceability table structure untouched, and mark all three rows Complete only if the Task 1 verification passed.

In `.planning/ROADMAP.md`'s Phase 10 section: update the Goal line and Success Criteria 1, 2 and 3 with the same three corrections, keeping the numbering and the criteria count intact. Criterion 4 ("existing wizard Phase 5 onboarding tests still pass with the new tag prompt added") should say Phase 4 for the prompt, since that is where it went. Also fill in the Plans list for Phase 10 with the six plan files and their one-line objectives, replacing the `**Plans**: TBD` placeholder, and update the progress table row.

In `.planning/PROJECT.md`'s Active requirements list: apply the same VLAN-to-location/group-label correction wherever the Phase 10 items appear.

Add one short note, in whichever of the three files carries this phase's completion record most naturally, stating that the terminology was reframed during `/bm:discuss-phase` and citing D-01, D-02, D-06 and D-07 — so a future reader sees the reconciliation as a recorded decision rather than an unexplained edit. Do not rewrite history elsewhere in these documents: Phases 8, 9 and 11 wording is out of scope for this task, except that Phase 11's DASH-06 ("groups/colors by VLAN") should get the same location/group-label correction, because it consumes the field this phase defined.
  </action>
  <verify>
    <automated>grep -c 'VLAN' .planning/REQUIREMENTS.md; grep -n 'TAG-01\|TAG-02\|TAG-03' .planning/REQUIREMENTS.md; grep -q '10-01-PLAN.md' .planning/ROADMAP.md &amp;&amp; grep -q '10-06-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` TAG-01 describes the neutral default as `other` in first position and the choice list as config-driven, with no `unknown` default and no `server/switch/router/iot` illustration
    - `.planning/REQUIREMENTS.md` TAG-02 attributes the prompt to Phase 4 and the application to Phase 5
    - `.planning/REQUIREMENTS.md` TAG-03 describes a generic location/group label derived from the Checkmk folder via the REST API, with no `VLAN` wording, and attributes it to the poller
    - The requirement IDs TAG-01, TAG-02 and TAG-03 still exist, in the same order, and the Traceability table still maps each to Phase 10
    - `.planning/ROADMAP.md`'s Phase 10 Goal and Success Criteria 1-4 carry the same three corrections, with the criteria count unchanged at 4
    - `.planning/ROADMAP.md`'s Phase 10 `**Plans**: TBD` placeholder is replaced by a list naming all six plan files, and the progress table row for Phase 10 is updated
    - `.planning/PROJECT.md`'s Active requirements list no longer describes the Phase 10 label as VLAN
    - `.planning/REQUIREMENTS.md`'s DASH-06 carries the same location/group-label correction
    - A note citing D-01, D-02, D-06 and D-07 records that the terminology was reframed during phase discussion
    - No Phase 8, 9 or 11 content other than DASH-06 was modified
  </acceptance_criteria>
  <done>The planning documents describe the phase that was actually built, and the terminology reframe is recorded as a traceable decision rather than an unexplained edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator terminal -> session transcript | Live verification pastes poller logs, MQTT payloads and probe output into a transcript that becomes a SUMMARY file |
| Live Checkmk site -> poller topic construction | Real, operator-named hosts and real tag values cross into published payload fields for the first time |
| Wizard run on a real site -> production monitoring config | Task 1 creates a real tag group and modifies real host attributes on a live monitoring site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | Information Disclosure | Verification output pasted into the transcript | mitigate | Step 1 explicitly instructs confirming that neither the automation secret nor the MQTT password appears in the poller logs before pasting, exercising both `PollerConfig.__repr__`'s redaction of `cmk_rest_secret` (T-10-09) and `RestError`'s message construction (T-10-10) under real conditions |
| T-10-24 | Tampering | Real host attributes modified during verification | accept | Task 1 onboards one host through the wizard's normal supported flow on a site the operator already administers. The tag group's `other` default is non-destructive by construction, and step 3 has the operator confirm in the UI that pre-existing hosts were defaulted rather than mis-tagged |
| T-10-25 | Tampering | Leftover `gsd_probe_device_type` group from plan 10-01 | mitigate | Step 3 has the operator confirm in Setup, Tags that no probe group remains, catching a failed cleanup from the earlier plan |
| T-10-26 | Tampering | Topic construction against real host names and real tag values | mitigate | Step 5's payload inspection is the live check that `is_publishable_device_id` (T-09-01) still gates every topic segment, and that the `folder` value carries no path-like content — the check named in plan 10-05's smoke test asserts the same thing automatically |
| T-10-27 | Repudiation | Silent absorption of a live result that contradicts research | mitigate | Task 2 requires a dated post-mortem comment naming what was predicted versus what the site returned, and requires the Phase 9 verification block to be amended rather than rewritten, so the record of what was checked and when survives |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed by this plan; 10-RESEARCH.md's Package Legitimacy Audit records no new dependency for this phase |
</threat_model>

<verification>
- A real host's retained `lan/devices/{id}/status` payload carries the chosen `device_type`, the chosen `alias`, and a `folder` with no `wato` segment and no `/omd` prefix
- The Checkmk UI shows the `device_type` tag group with `other` first, and no leftover `gsd_probe_device_type` group
- `scripts/smoke_test_poller.py` reports all checks PASS, including the enrichment check added in plan 10-05
- `uv run pytest -q` and `uv run ruff check scripts/ src/ tests/` both exit 0 after Task 2's corrections
- `grep -q 'Live-verified' scripts/mqtt_poller.py` succeeds near `extract_device_type`
- No credential appears in the poller logs or in any published payload
</verification>

<success_criteria>
- ROADMAP.md Phase 10 success criterion 1 is observed live: the `device_type` tag group exists with a neutral first value and no pre-existing host was mis-tagged (TAG-01)
- ROADMAP.md Phase 10 success criterion 2 is observed live: the wizard prompted for a device type and the `tag_device_type` attribute shape was accepted by a real 2.4.0p35 site (TAG-02)
- ROADMAP.md Phase 10 success criterion 3 is observed live: the location/group label came from Checkmk's structured folder association, with no filesystem-path parsing (TAG-03)
- ROADMAP.md Phase 10 success criterion 4 holds: the full test suite passes with the new prompts in place
- 10-RESEARCH.md Assumption A3 is closed — confirmed or corrected with a dated in-source explanation
- The planning documents describe the phase as built, with the terminology reframe recorded as a traceable decision
</success_criteria>

<output>
Create `.planning/phases/10-checkmk-tag-group-onboarding-integration/10-06-SUMMARY.md` when done
</output>
