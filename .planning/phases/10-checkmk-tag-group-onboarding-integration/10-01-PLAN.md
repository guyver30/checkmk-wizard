---
phase: 10-checkmk-tag-group-onboarding-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - device_types.json
  - scripts/probe_checkmk_rest_shapes.py
autonomous: false
requirements: [TAG-01, TAG-02, TAG-03]
user_setup:
  - service: checkmk-rest-api
    why: "The probe script authenticates as a Checkmk automation user to POST/GET/DELETE a throwaway host tag group and read host_config — the poller and this probe have no existing REST credential path"
    env_vars:
      - name: CMK_REST_USERNAME
        source: "Checkmk site -> Setup -> Users -> the automation user the wizard bootstrapped (usually 'automation')"
      - name: CMK_REST_SECRET
        source: "Checkmk site -> Setup -> Users -> <automation user> -> Automation secret (or /omd/sites/<site>/var/check_mk/web/<user>/automation.secret on the checkmk host)"

must_haves:
  truths:
    - "The exact JSON body Checkmk 2.4.0p35 accepts for POST /domain-types/host_tag_group/collections/all is known from a real response, not from an Ansible module's source"
    - "Whether GET /domain-types/host_config/collections/all exposes extensions.folder, and what that value looks like, is known from a real response"
    - "Whether Checkmk materialises a newly created tag group's first-tag default as an explicit per-host attribute, or leaves it implicit, is known from a real response"
    - "The key shape Livestatus's hosts.tags column uses for custom tag groups is observed on a real site"
    - "The probe leaves the live site unchanged - its throwaway tag group is deleted and the real device_type group is not created"
  artifacts:
    - path: "device_types.json"
      provides: "D-05's checked-in device-type choice list with 'other' first (D-06)"
      contains: "other"
    - path: "scripts/probe_checkmk_rest_shapes.py"
      provides: "Reproducible REST/Livestatus shape probe plus the dated live-verified findings block downstream plans read"
      contains: "Live-verified"
  key_links:
    - from: "scripts/probe_checkmk_rest_shapes.py"
      to: "Checkmk REST /domain-types/host_tag_group/collections/all"
      via: "urllib.request POST with Bearer automation auth"
      pattern: "host_tag_group/collections/all"
    - from: "scripts/probe_checkmk_rest_shapes.py"
      to: "device_types.json"
      via: "json.loads of the repo-root config file"
      pattern: "device_types.json"
---

<objective>
Establish the two artifacts every downstream Phase 10 plan depends on: the checked-in `device_types.json` choice list, and a reproducible probe script whose live run resolves the REST/Livestatus payload shapes that 10-RESEARCH.md could only source from a forum post and an Ansible module.

Purpose: 10-RESEARCH.md Assumptions A1 (tag-group POST body shape), A2 (`extensions.folder` on `host_config`), and A3 (Livestatus `tags` key shape) are MEDIUM/LOW confidence. This repo's own rule is that Checkmk's live server behaviour, not its docs, is the source of truth (`api.py:200-206`, `scripts/mqtt_poller.py:69-90`). Writing `create_host_tag_group()` and the poller's REST folder client against unverified shapes would repeat exactly the class of failure PITFALLS.md Pitfall 9 documents.
Output: `device_types.json`, a checked-in probe script, and a dated live-verified findings block in that script's docstring that plans 10-02 and 10-03 read instead of re-guessing.

Terminology reconciliation (called out per 10-RESEARCH.md, not silently resolved): ROADMAP.md SC1 says the neutral default is `unknown` and SC3 says the folder-derived label is `VLAN`. CONTEXT.md D-06 supersedes the first (the neutral first-position value is `other`), and D-01/D-02 supersede the second (it is a generic location/group label, no tag group of its own). This plan implements CONTEXT.md's terms; plan 10-06 updates the upstream wording.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-CONTEXT.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-PATTERNS.md
@scripts/smoke_test_poller.py
@scripts/mqtt_poller.py

<interfaces>
<!-- Contracts the probe must exercise. Extracted from the codebase; use directly, no exploration needed. -->

Checkmk REST base URL construction (src/checkmk_wizard/api.py, `_site_base`):
  "{proto}://{host}:{port}/{site}/check_mk/api/1.0"  — port omitted when None.
  Container-mode default in deploy/compose.yaml worker service:
  CMK_REST_API=http://checkmk:5000/dmc/check_mk/api/1.0, CMK_SITE_ID=dmc

Checkmk REST auth header used everywhere in this repo (src/checkmk_wizard/api.py, CheckmkClient.__init__):
  Authorization: Bearer <username> <secret>

Livestatus LQL query shape the poller already uses (scripts/mqtt_poller.py, `build_hosts_query`):
  "GET hosts\nColumns: <space separated>\nOutputFormat: json\n\n"
  sent over a raw TCP socket to LIVESTATUS_HOST:6557, half-closed with shutdown(SHUT_WR).

Existing standalone-script conventions (scripts/smoke_test_poller.py):
  argparse with ArgumentDefaultsHelpFormatter, per-check functions returning bool,
  "[PASS]"/"[FAIL]"/"[SUMMARY]" prefixed prints, `def main() -> int` returning an exit code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add device_types.json and the REST/Livestatus shape probe script</name>
  <files>device_types.json, scripts/probe_checkmk_rest_shapes.py</files>
  <read_first>
    - scripts/smoke_test_poller.py (the standalone-diagnostic-script conventions this probe must match: module docstring stating purpose, argparse with ArgumentDefaultsHelpFormatter, `def main() -> int`, `[PASS]`/`[FAIL]`/`[SUMMARY]` print prefixes, env-var defaults)
    - scripts/mqtt_poller.py lines 52-115 (constants, `LivestatusError`, `_env_int`) and lines 313-386 (`_livestatus_request`, `available_host_columns`, `build_hosts_query` — the raw-socket LQL pattern this probe reuses for its tags probe)
    - src/checkmk_wizard/api.py lines 30-50 (`_site_base`, `CheckmkConnection`) and lines 81-103 (`CheckmkClient.__init__`'s `Authorization: Bearer <user> <secret>` header construction)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md (Architecture Pattern 1 and Pattern 3 — the exact candidate payload shapes this probe is testing; Assumptions Log A1/A2/A3)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
Create `device_types.json` at the repo root (D-05; path chosen to mirror `_SMARTMONTOOLS_DIR`'s `Path(__file__).resolve().parents[2]` repo-root resolution at `wizard.py:40`, which is how this repo already resolves bundled non-Python assets from a `uv run` checkout). Content is a single JSON array of strings, exactly: `other`, `E-link`, `ACS`, `Multimedia`, `NetworkDevice`, `GroupController`. `other` MUST be element 0 — D-06 makes first-position load-bearing because Checkmk auto-defaults every pre-existing host to the first tag in a newly created group.

Create `scripts/probe_checkmk_rest_shapes.py`: a stdlib-only (`urllib.request`, `urllib.error`, `json`, `socket`, `os`, `argparse`) diagnostic that runs against a live Checkmk 2.4.0p35 CE site and prints what its REST API and Livestatus actually return. No third-party imports — it must run with bare `python3` inside the poller container as well as under `uv run` on the host, matching `scripts/mqtt_poller.py`'s stated "standalone, dependency-light" constraint.

Configuration, all with env-var defaults overridable by argparse flags: `CMK_REST_HOST` (default `checkmk`), `CMK_REST_PORT` (default `5000`), `CMK_SITE_ID` (default `dmc`), `CMK_REST_USERNAME`, `CMK_REST_SECRET`, `LIVESTATUS_HOST` (default `checkmk`), `LIVESTATUS_PORT` (default `6557`). Build the base URL as `http://{host}:{port}/{site}/check_mk/api/1.0` and the auth header as `Bearer {username} {secret}`. Exit non-zero with a clear message if `CMK_REST_USERNAME` or `CMK_REST_SECRET` is empty.

Implement one `_rest(method, path, body=None)` helper that is the single choke point for every REST call — mirroring `_livestatus_request`'s "normalize every failure into one exception type" shape from `mqtt_poller.py`. It returns a `(status_code, parsed_json_or_raw_text)` tuple rather than raising on non-2xx, because non-2xx status codes are exactly what several probes below are measuring. Use `urllib.request.Request` with `Accept: application/json` and, for bodies, `Content-Type: application/json`; catch `urllib.error.HTTPError` to read its status and body, and `urllib.error.URLError`/`OSError` for connection-level failures.

Run these probes in order, each printing a labelled section header and its verbatim finding:

P1 `probe_real_group_absent`: GET `/objects/host_tag_group/device_type`. Print the status code. Expected 404 (the real group does not exist yet — plan 10-02 creates it). A 200 here means the group already exists on this site; print a loud warning that plan 10-02's backfill count (D-08) will not fire on this site, and continue.

P2 `probe_tag_group_create_shape`: POST `/domain-types/host_tag_group/collections/all` with a body built from `device_types.json`, using the throwaway group id `gsd_probe_device_type` (NOT `device_type` — the real group is plan 10-02's to create, and creating it here would suppress D-08's backfill count). First attempt the Checkmk >=2.4.0 shape: top-level keys `id`, `title` ("GSD probe device type"), `tags` where each entry is an object with `id`, `title` and `aux_tags: []`. Print the status code and the full response body. If the status is 4xx, retry once with the pre-2.4.0 shape (`ident` instead of `id` at both the group level and inside each tags entry) and print that status and body too. Print an explicit verdict line naming which key spelling the live site accepted, or that both failed.

P3 `probe_tag_group_readback`: GET `/objects/host_tag_group/gsd_probe_device_type`. Print the status code and the full response body, so the id/title/tags round-trip shape is on record.

P4 `probe_host_config_folder_shape`: GET `/domain-types/host_config/collections/all`. Take the first entry in the response's `value` array and print: the entry's `id`; the sorted key list of its `extensions` object; the value and Python type of `extensions.folder` if that key is present (this is Assumption A2 / Pattern 3 Candidate A); the sorted key list of `extensions.attributes`; and the `href` of every entry in the object's `links` array whose `rel` contains `folder_config` (Pattern 3 Candidate B fallback). Then print an explicit verdict line: whether `extensions.folder` exists, and whether `extensions.attributes` contains any key naming the probe tag group (`tag_gsd_probe_device_type`). That last check answers a question RESEARCH.md's own worked example gets wrong: whether Checkmk materialises a new tag group's implicit first-tag default as an explicit per-host attribute, or leaves it absent. Plan 10-02's backfill count depends on the answer.

P5 `probe_livestatus_tags_keys`: open a raw TCP socket to Livestatus and send `GET hosts` with `Columns: name tags` and `OutputFormat: json`, reusing `mqtt_poller.py`'s half-close-then-read-to-EOF pattern. For up to the first three rows, print the host name and the sorted key list of its `tags` dict. Print an explicit verdict line naming whether the keys are bare tag-group ids or `tag_`-prefixed (Assumption A3). Note in the output that this probe cannot fully close A3 until a host actually carries a device-type tag — plan 10-06 re-runs it after the wizard has set one.

P6 `cleanup_probe_tag_group`: DELETE `/objects/host_tag_group/gsd_probe_device_type` and print the status code. This MUST run in a `try`/`finally` (or equivalent) so it executes even if P3/P4/P5 raise, leaving the live site exactly as it was found. Print a loud `[FAIL]` line if the delete does not return 204 or 200, naming the group id the operator must remove by hand.

Never print `CMK_REST_SECRET`. Render the auth header in any diagnostic output as `Bearer <username> ***`, following `PollerConfig.__repr__`'s existing redaction precedent (`mqtt_poller.py:160-177`).

`main()` returns 0 when every probe produced a response (including expected 404s — this is an informational tool, not a pass/fail gate), and 1 when a connection-level failure or a missing credential prevented probing at all. Print a final `[SUMMARY]` line listing each probe name and whether it produced a finding.

Add a module docstring stating the script's purpose, that it targets Checkmk 2.4.0p35 CE, that it creates and deletes a throwaway tag group, and a `Findings` placeholder section reading `Findings: not yet run against a live site — see 10-01-PLAN.md Task 3`. Task 3 replaces that placeholder.
  </action>
  <verify>
    <automated>uv run python -c "import json,pathlib; c=json.loads(pathlib.Path('device_types.json').read_text()); assert isinstance(c,list) and c[0]=='other' and len(c)==6, c" &amp;&amp; uv run python scripts/probe_checkmk_rest_shapes.py --help &gt;/dev/null &amp;&amp; uv run ruff check scripts/ src/ tests/ &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `device_types.json` parses as a JSON list of exactly 6 strings whose element 0 is `other`
    - `python -c "import ast,sys; ast.parse(open('scripts/probe_checkmk_rest_shapes.py').read())"` exits 0
    - `grep -v '^#' scripts/probe_checkmk_rest_shapes.py | grep -c 'import httpx\|import paho\|import requests'` returns 0 (stdlib only)
    - `scripts/probe_checkmk_rest_shapes.py` contains the literals `gsd_probe_device_type`, `/domain-types/host_tag_group/collections/all`, `/domain-types/host_config/collections/all`, `extensions`, `folder_config` and `OutputFormat: json`
    - The script never references `device_type` as the id it POSTs (only `gsd_probe_device_type`); `grep -n 'id.*"device_type"' scripts/probe_checkmk_rest_shapes.py` finds no POST body usage
    - `uv run python scripts/probe_checkmk_rest_shapes.py --help` exits 0 and lists flags for rest host, rest port, site id, livestatus host and livestatus port
    - Running the script with `CMK_REST_SECRET` unset exits non-zero and prints a message naming the missing variable
    - `uv run ruff check scripts/ src/ tests/` exits 0
    - `uv run pytest -q` exits 0 (no existing test regressed)
  </acceptance_criteria>
  <done>`device_types.json` exists with `other` first, and a stdlib-only probe script is checked in that can answer Assumptions A1, A2 and A3 in a single run without leaving state behind on the live site.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the shape probe against the live Checkmk 2.4.0p35 site</name>
  <files>none — verification only, no files are modified by this task</files>
  <action>Pause and hand the verification steps below to the developer, who runs them on the real deployment host — this machine has no podman, no broker and no Checkmk site. Do not attempt to run `podman compose`, `mosquitto_sub` or the probe script locally, and do not simulate or infer the results. Collect four things from the developer before proceeding: the P2 verdict naming which key spelling the tag-group POST accepted, the P4 verdict on `extensions.folder` and on whether a new tag group's default materialises as an explicit host attribute, the P4 `folder_config` link href, and the P5 Livestatus `tags` key shape. Task 3 records that output in-source, and plans 10-02 and 10-03 build against it.</action>
  <what-built>
`device_types.json` (the D-05 device-type choice list, `other` first per D-06) and `scripts/probe_checkmk_rest_shapes.py`, a stdlib-only diagnostic that asks the live Checkmk site three questions this phase's research could not answer from documentation:

1. What JSON body does `POST /domain-types/host_tag_group/collections/all` actually accept on 2.4.0p35 — `id` or `ident`, at the group level and inside each tags entry?
2. Does `GET /domain-types/host_config/collections/all` expose a per-host `extensions.folder` field, and does a newly created tag group's first-tag default show up as an explicit `extensions.attributes` key or stay implicit?
3. Does Livestatus's `hosts.tags` column key custom tag groups by their bare id or with a `tag_` prefix?

The probe creates a throwaway tag group `gsd_probe_device_type` and deletes it again in a `finally` block. It deliberately does NOT create the real `device_type` group — that is plan 10-02's job, and creating it early would suppress the "N pre-existing hosts defaulted" count D-08 asks for.

This development machine has no podman, no broker and no Checkmk site — same situation as Phases 8 and 9, whose verification you ran on the real host.
  </what-built>
  <how-to-verify>
Run these on the deployment host, from the repo checkout.

1. Pull the changes and find the automation user's secret. Either read it from the Checkmk UI (Setup -> Users -> the automation user, usually `automation` -> Automation secret) or, on the checkmk host/container, from `/omd/sites/dmc/var/check_mk/web/automation/automation.secret`.

2. Run the probe from the repo checkout, pointing it at the site (adjust host/site if yours differ from the compose defaults):
   `cd <checkout> && CMK_REST_HOST=localhost CMK_REST_PORT=5000 CMK_SITE_ID=dmc LIVESTATUS_HOST=localhost CMK_REST_USERNAME=automation CMK_REST_SECRET='<secret>' uv run python scripts/probe_checkmk_rest_shapes.py`

   If port 5000 is not published to the host, run it from inside the worker container instead:
   `cd <checkout>/deploy && podman compose exec worker python3 /app/checkmk-wizard/scripts/probe_checkmk_rest_shapes.py`
   (the worker service already has `CMK_SITE_ID=dmc` and reaches `checkmk:5000` on `cmk_net`; you still need to pass `CMK_REST_USERNAME`/`CMK_REST_SECRET`).

3. Report the FULL verbatim output. The four lines that matter most:
   - P2's verdict line: which key spelling (`id` or `ident`) the site accepted, and the HTTP status.
   - P4's verdict: whether `extensions.folder` is present, what its value looks like for a host you know the folder of, and whether `extensions.attributes` gained a `tag_gsd_probe_device_type` key.
   - P4's `links` output: the `folder_config` href, so the Candidate B fallback is on record even if Candidate A worked.
   - P5's verdict: whether the Livestatus `tags` keys are bare or `tag_`-prefixed.

4. Confirm the cleanup succeeded: the last section must show the DELETE returning 204 or 200. Then confirm in the Checkmk UI (Setup -> Tags) that no `gsd_probe_device_type` group remains. If it does, delete it manually and say so.

Expected NOT to appear anywhere in the output: the automation secret value.
  </how-to-verify>
  <verify>
    <human-check>The probe printed a verdict for each of P1-P5, the throwaway tag group was deleted, and no secret appears in the output</human-check>
  </verify>
  <resume-signal>Type "approved" with the full probe output pasted, or describe what failed</resume-signal>
  <done>Assumptions A1 and A2 are answered by a real Checkmk 2.4.0p35 response, A3 is partially answered (fully closed in plan 10-06), and the live site is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Record the probe findings as a dated live-verified block</name>
  <files>scripts/probe_checkmk_rest_shapes.py</files>
  <read_first>
    - scripts/probe_checkmk_rest_shapes.py (its current module docstring with the `Findings: not yet run` placeholder)
    - The Task 2 checkpoint output (the operator's verbatim probe run)
    - src/checkmk_wizard/api.py lines 200-233 (this repo's live-verification citation convention: comments beginning "Live-verified against a real Checkmk 2.4.0p35 CE site: ...")
    - scripts/mqtt_poller.py lines 68-91 (the longer-form live-verification block Phase 9 wrote, including how it records what was NOT confirmed and why)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md (Assumptions Log — A1, A2, A3 are the claims being closed)
  </read_first>
  <action>
Replace the `Findings: not yet run against a live site` placeholder in `scripts/probe_checkmk_rest_shapes.py`'s module docstring with a findings block that begins `Live-verified against a real Checkmk 2.4.0p35 CE site on <date of the Task 2 run>:` and records, in prose, exactly what the operator's run reported:

- A1: which key spelling the tag-group POST accepted (`id` vs `ident`, at the group level and inside each tags entry), the exact top-level body keys that were accepted, and the HTTP status returned. State plainly if the >=2.4.0 `id` shape RESEARCH.md predicted was wrong.
- A2: whether `extensions.folder` exists on `host_config` collection entries, a verbatim example value, and therefore whether plan 10-03 implements Pattern 3 Candidate A (`extensions.folder`) or falls back to Candidate B (the tilde-encoded folder id from the `folder_config` link href). If Candidate B is required, record the exact href format observed so 10-03 does not have to re-derive it.
- Whether a newly created tag group's implicit first-tag default appears as an explicit `extensions.attributes` key on pre-existing hosts, or stays absent. State the consequence in one sentence: plan 10-02's backfill count must count hosts LACKING an explicit `tag_device_type` attribute if the default stays implicit, and count hosts whose attribute equals `other` if it is materialised.
- A3: the Livestatus `tags` key shape observed, and an explicit note that no host carried a device-type tag at probe time so the device-type key specifically is re-confirmed in plan 10-06.

Follow `scripts/mqtt_poller.py`'s existing live-verification block style: state what was confirmed, and state explicitly what was NOT confirmed and why, rather than implying full coverage.

If any probe reported a failure or a shape that contradicts RESEARCH.md, do not silently absorb it — record the contradiction in the findings block using the dated post-mortem convention from `src/checkmk_wizard/site.py:191-202`, naming what was predicted and what was actually observed. Also fix the probe script itself if the run exposed a bug in it (for example a body shape that needed a manual retry the script should have handled).
  </action>
  <verify>
    <automated>grep -q 'Live-verified against a real Checkmk 2.4.0p35 CE site' scripts/probe_checkmk_rest_shapes.py &amp;&amp; grep -qi 'extensions.folder\|folder_config' scripts/probe_checkmk_rest_shapes.py &amp;&amp; uv run ruff check scripts/ &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/probe_checkmk_rest_shapes.py`'s module docstring contains a line beginning `Live-verified against a real Checkmk 2.4.0p35 CE site on ` followed by the date of the Task 2 run
    - The findings block names, verbatim, which of `id` / `ident` the tag-group POST accepted
    - The findings block states whether `extensions.folder` is present and names which of Pattern 3 Candidate A or Candidate B plan 10-03 must implement
    - The findings block states whether a new tag group's default is materialised as an explicit `extensions.attributes` key, and names the counting rule plan 10-02 must use as a consequence
    - The findings block states the observed Livestatus `tags` key shape and explicitly notes that the device-type key is re-confirmed in plan 10-06
    - No placeholder text `not yet run against a live site` remains anywhere in the file
    - `uv run ruff check scripts/` exits 0 and `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>Downstream plans 10-02 and 10-03 can read a single checked-in file to learn the confirmed REST payload shapes instead of re-deriving them from a forum post.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell -> probe script | The Checkmk automation secret crosses into the process environment and into an HTTP Authorization header |
| Probe script -> live Checkmk REST API | The probe performs a real write (POST) and a real delete (DELETE) against a production-ish monitoring site |
| Probe script -> operator terminal | Probe output is pasted into a session transcript and may end up in a SUMMARY file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `CMK_REST_SECRET` in probe output / pasted transcript | mitigate | The probe never prints the secret; the auth header is rendered as `Bearer <username> ***`, following `PollerConfig.__repr__`'s existing redaction precedent. Task 2's checkpoint explicitly instructs the operator to confirm no secret appears in the output before pasting it |
| T-10-02 | Tampering | Live Checkmk tag configuration | mitigate | The probe writes only the throwaway id `gsd_probe_device_type`, never `device_type`, and deletes it in a `finally` block; a failed delete prints a loud `[FAIL]` naming the group the operator must remove. Task 2 requires a UI confirmation that no probe group remains |
| T-10-03 | Denial of Service | Live Checkmk site during probing | accept | The probe issues five read/write requests total against a LAN monitoring site the operator already administers; no loop, no fan-out, no load |
| T-10-04 | Spoofing | Probe's REST authentication | mitigate | Bearer automation-user auth, the exact scheme `CheckmkClient` already uses — no hand-rolled session/cookie flow is introduced (RESEARCH.md "Don't Hand-Roll") |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed by this plan. `device_types.json` and the probe script are stdlib-only; 10-RESEARCH.md's Package Legitimacy Audit records no new dependency, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `uv run python -c` parses `device_types.json` and confirms `other` is element 0
- `uv run python scripts/probe_checkmk_rest_shapes.py --help` exits 0
- The operator's live run produced a verdict for probes P1 through P5
- `grep -q 'Live-verified against a real Checkmk 2.4.0p35 CE site' scripts/probe_checkmk_rest_shapes.py` succeeds
- `uv run ruff check scripts/ src/ tests/` and `uv run pytest -q` both exit 0
</verification>

<success_criteria>
- `device_types.json` exists at the repo root, is valid JSON, and lists `other` first (D-05, D-06)
- The tag-group POST body shape is known from a real 2.4.0p35 response, not from an Ansible module's source (closes A1)
- The per-host folder source for the poller is decided from a real response, and the chosen candidate (A or B) is recorded (closes A2)
- Whether a new tag group's default materialises as an explicit host attribute is known, and the counting rule for D-08 is recorded
- The live site carries no leftover `gsd_probe_device_type` tag group
- No credential appears in any probe output or recorded finding
</success_criteria>

<output>
Create `.planning/phases/10-checkmk-tag-group-onboarding-integration/10-01-SUMMARY.md` when done
</output>
