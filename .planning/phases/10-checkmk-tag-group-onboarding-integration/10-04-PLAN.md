---
phase: 10-checkmk-tag-group-onboarding-integration
plan: 04
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/checkmk_wizard/wizard.py
  - tests/test_wizard.py
autonomous: true
requirements: [TAG-02]
user_setup: []

must_haves:
  truths:
    - "The wizard asks, once per promoted host, which device type it is, offering only the values from device_types.json"
    - "The wizard offers an optional display alias per host and treats a blank answer as 'use the hostname'"
    - "A host created by the wizard carries tag_device_type in its Checkmk attributes, for all three monitoring methods (snmp, ping, agent)"
    - "A host given an alias carries Checkmk's native alias attribute; a host without one sends no alias key at all"
    - "Every pre-existing Phase 4 and Phase 5 test still passes with the two new prompts in the loop"
  artifacts:
    - path: "src/checkmk_wizard/wizard.py"
      provides: "OnboardedHost.device_type/.alias, the Phase 4 prompts, and tag_device_type/alias on all three Phase 5 create_host call sites"
      contains: "device_type"
    - path: "tests/test_wizard.py"
      provides: "repaired answer iterators plus new coverage of the prompts and the attribute threading"
      contains: "tag_device_type"
  key_links:
    - from: "src/checkmk_wizard/wizard.py phase4_classification"
      to: "OnboardedHost.device_type"
      via: "questionary.select over _load_device_types()"
      pattern: "_load_device_types"
    - from: "src/checkmk_wizard/wizard.py _onboard_hosts"
      to: "Checkmk host attributes"
      via: "tag_device_type key in the create_host attributes dict"
      pattern: "tag_\\{DEVICE_TYPE_TAG_GROUP_ID\\}|tag_device_type"
---

<objective>
Collect a device type and an optional display alias per host during Phase 4, and apply both as real Checkmk host attributes during Phase 5.

Purpose: TAG-02, D-07, D-09. The `device_type` tag group provisioned by plan 10-02 is inert until hosts actually carry values; this plan is what makes a real device type reach Checkmk. `tag_device_type` follows the attribute shape this codebase already proves three times over (`tag_agent`, `tag_snmp_ds`, `tag_criticality`), and `alias` is one of Checkmk's own first-class host attributes rather than a custom tag.
Output: two new `OnboardedHost` fields, two new Phase 4 prompts, three updated Phase 5 attribute dicts, and a test suite that is green rather than crashing on a short answer iterator.

Terminology reconciliation: ROADMAP.md SC2 and REQUIREMENTS.md TAG-02 both say the prompt lives in "Phase 5 onboarding". CONTEXT.md D-07 supersedes that placement — the prompt lives in Phase 4 (Classification), exactly where `os_family`, `snmp_version` and `expected_open_ports` are already collected, and Phase 5 only applies what Phase 4 collected. The underlying requirement (the wizard prompts for and sets the device-type tag per host, with a live-verified attribute shape) is satisfied in full; only the phase label moves. Plan 10-06 corrects the upstream wording.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-CONTEXT.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-PATTERNS.md
@.planning/phases/10-checkmk-tag-group-onboarding-integration/10-02-SUMMARY.md
@src/checkmk_wizard/wizard.py
@tests/test_wizard.py

<interfaces>
<!-- Contracts to build against. Extracted from the codebase; use directly, no exploration needed. -->

src/checkmk_wizard/wizard.py — the dataclass being extended (fields appended, all with defaults):
  @dataclass class OnboardedHost:
      ip: str; hostname: str; folder: str; os_family: str
      snmp_version: str | None = None ; snmp_community: str | None = None
      expected_open_ports: list[int] = field(default_factory=list)
      expected_services: list[str] = field(default_factory=list)

src/checkmk_wizard/wizard.py — added by plan 10-02, reuse instead of new literals:
  DEVICE_TYPE_TAG_GROUP_ID = "device_type"
  def _load_device_types() -> list[str]      # validated, "other" guaranteed first

src/checkmk_wizard/wizard.py — the three Phase 5 attribute dicts to extend, inside _onboard_hosts:
  snmp branch  : {"ipaddress", "tag_agent": "no-agent", "tag_snmp_ds": snmp-v1|snmp-v2, "snmp_community"}
  ping branch  : {"ipaddress", "tag_agent": "no-agent", "tag_snmp_ds": "no-snmp"}
  agent branch : {"ipaddress", "tag_agent": "cmk-agent", "tag_snmp_ds": "no-snmp"}
  each already wrapped in try/except CheckmkAPIError printing a yellow warning

src/checkmk_wizard/wizard.py — existing prompt idioms in phase4_classification:
  questionary.select(prompt, choices=[questionary.Choice(label, value=v), ...]).ask_async()
  (await questionary.text(prompt, default="").ask_async()).strip()

tests/test_wizard.py — the answer-iterator driving idiom every phase4 test uses:
  answers = iter([...]) ; monkeypatch questionary.Question.ask_async to `return next(answers)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add device_type and alias to OnboardedHost and prompt for them in Phase 4</name>
  <files>src/checkmk_wizard/wizard.py, tests/test_wizard.py</files>
  <read_first>
    - src/checkmk_wizard/wizard.py lines 191-222 (`ScannedHost`, `OnboardedHost` with its inline "why this default exists" comment style, `WizardState`)
    - src/checkmk_wizard/wizard.py lines 797-886 (the whole `phase4_classification` per-host loop: the hostname re-prompt loop, the `os_family` select, the SNMP sub-prompts, the `expected_open_ports` validation loop, and the `OnboardedHost(...)` construction at the end — the new prompts append after the ports prompt and their values are passed into that constructor)
    - src/checkmk_wizard/wizard.py (the `DEVICE_TYPE_TAG_GROUP_ID` constant and `_load_device_types()` added by plan 10-02)
    - device_types.json (the actual choice values tests must use)
    - tests/test_wizard.py lines 820-832 and 1005-1050 (the four existing `phase4_classification`-driving tests and their fixed-length `answers = iter([...])` lists)
  </read_first>
  <behavior>
    - `OnboardedHost` accepts `device_type` and `alias` as keyword arguments and defaults them to `"other"` and `None`
    - `phase4_classification` asks a device-type select whose choices are exactly `_load_device_types()`'s return value, in order
    - `phase4_classification` asks an alias text prompt after the device-type prompt
    - A blank or whitespace-only alias answer yields `alias is None`, not `""`
    - A non-blank alias answer is stored stripped
    - The prompts run once per promoted host and their answers land on that host's `OnboardedHost`
  </behavior>
  <action>
Append two fields to `OnboardedHost` in `src/checkmk_wizard/wizard.py`: `device_type: str = "other"` and `alias: str | None = None`. Both need defaults and must be appended last, because existing call sites and tests construct `OnboardedHost` with keyword arguments and must keep working unchanged. Add inline comments in the file's existing style naming the locking decisions: `device_type` defaults to the same neutral value that occupies first position in `device_types.json` so an unprompted host matches what Checkmk's tag-group default would have given it (D-06/D-07); `alias` is Checkmk's own native host attribute, not a tag, and stays `None` when the operator wants the hostname used as-is (D-09).

Add two prompts at the end of `phase4_classification`'s per-host loop, after the `expected_open_ports` validation loop and before the `OnboardedHost(...)` construction. Order matters for the tests: device type first, then alias.

The device-type prompt is a `questionary.select` labelled `Device type for {hostname}:` whose choices are built one-per-entry from `_load_device_types()`, each `questionary.Choice` using the raw string as both label and value. Building the choices from the config loader rather than a literal list is what makes D-05 real — the taxonomy is site-specific (the operator's own example set is access-control/building-automation, not generic server/switch/router), and a hardcoded list would require a code change per site. No validation loop is needed: the value is validated by construction because the operator cannot type a free-form answer.

The alias prompt is a `questionary.text` labelled `Display name/alias for {hostname} (blank to use hostname):` with `default=""`. Strip the answer and convert an empty result to `None`, mirroring the blank-means-skip handling the `expected_open_ports` prompt already uses. No format validation: Checkmk validates `alias` server-side, and a rejected value surfaces through the existing `except CheckmkAPIError` warning at the Phase 5 call site, consistent with how `snmp_community` and hostname values are already handled.

Pass both values into the `OnboardedHost(...)` construction as `device_type=` and `alias=`.

Add tests to `tests/test_wizard.py` covering the six behaviours above, following the existing `answers = iter([...])` + monkeypatched `questionary.Question.ask_async` idiom. Monkeypatch `wizard._DEVICE_TYPES_PATH` to a `tmp_path` file for any test that asserts on the offered choice list, so the test does not break when the real `device_types.json` is edited. Include a test that captures the `questionary.select` choices actually offered and asserts they equal the loaded list in order.
  </action>
  <verify>
    <automated>uv run pytest tests/test_wizard.py -q &amp;&amp; uv run ruff check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `src/checkmk_wizard/wizard.py` contains `device_type: str = "other"` and `alias: str | None = None` inside the `OnboardedHost` dataclass, both after `expected_services`
    - `src/checkmk_wizard/wizard.py` contains the prompt strings `Device type for` and `Display name/alias for` inside `phase4_classification`
    - `grep -c '_load_device_types()' src/checkmk_wizard/wizard.py` is at least 2 (the ensure helper from 10-02 plus the Phase 4 prompt)
    - The `OnboardedHost(...)` construction at the end of `phase4_classification` passes both `device_type=` and `alias=`
    - `uv run pytest tests/test_wizard.py -q` exits 0 with at least 4 new tests whose names contain `device_type` or `alias`
    - A test asserts a blank alias answer yields `onboarded[0].alias is None`
    - A test asserts a `"  rack-2 door  "` alias answer yields `onboarded[0].alias == "rack-2 door"`
    - A test asserts the device-type select's offered choice values equal the monkeypatched config list in order
    - `uv run ruff check src/ tests/` exits 0
  </acceptance_criteria>
  <done>Phase 4 collects a config-driven device type and an optional alias per host, and both live on `OnboardedHost` ready for Phase 5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply tag_device_type and alias at all three Phase 5 host-creation call sites</name>
  <files>src/checkmk_wizard/wizard.py, tests/test_wizard.py</files>
  <read_first>
    - src/checkmk_wizard/wizard.py lines 1386-1500 (`_onboard_hosts`: the IP-placeholder delete, then the snmp branch's attribute dict, the ping branch's attribute dict, and the agent branch's attribute dict, each inside its own `try`/`except CheckmkAPIError`)
    - src/checkmk_wizard/wizard.py lines 891-916 (`_create_or_update_host` — how attributes reach `create_host`/`update_host_attributes`, including the update path's ETag handling, so the new keys work on re-runs too)
    - src/checkmk_wizard/wizard.py (the `DEVICE_TYPE_TAG_GROUP_ID` constant added by plan 10-02)
    - src/checkmk_wizard/api.py lines 193-224 (`create_host` and `update_host_attributes` — the `attributes` dict is passed through verbatim)
    - tests/test_wizard.py lines 745-800 (the three tests asserting `body["attributes"] == {...}` exactly for phase5 host creation — these assertions become stale the moment a new key is added)
    - tests/test_wizard.py lines 925-955 (the phase3 placeholder and phase2 folder attribute assertions — confirm these are NOT in scope; Phase 3 placeholders and folder defaults deliberately do not carry a device type)
  </read_first>
  <behavior>
    - The snmp branch's create/update attributes include `tag_device_type` set to the host's `device_type`
    - The ping branch's create/update attributes include `tag_device_type`
    - The agent branch's create/update attributes include `tag_device_type`
    - A host with `alias` set adds an `alias` key to the same attributes dict, in every branch
    - A host with `alias is None` sends no `alias` key at all (absent, not empty string)
    - Phase 3's IP-placeholder host creation and Phase 2's folder defaults are unchanged and carry no device type
  </behavior>
  <action>
In `_onboard_hosts`, extend all three `attributes` dicts (snmp, ping, agent) with the device-type tag. Derive the key from the constant rather than repeating the literal — build it once per host as `f"tag_{DEVICE_TYPE_TAG_GROUP_ID}"`, or assign it to a small local used by all three branches. 10-RESEARCH.md's anti-pattern list is explicit that hardcoding `tag_device_type` across multiple call sites is the shape to avoid; one derivation keeps the group id and the attribute key from drifting.

Add the alias conditionally in every branch: when `h.alias` is truthy, set an `alias` key on the same dict. Do not send `alias: ""` or `alias: None` when unset — an absent key means "Checkmk keeps whatever it has", while an explicit empty value would clear an alias an operator may have set in the UI. `alias` is a first-class Checkmk host attribute, not a tag, so it takes no `tag_` prefix (D-09).

Extract the shared construction so the three branches do not each grow a copy-pasted pair of lines — a small helper that takes an `OnboardedHost` and returns the device-type-plus-optional-alias fragment, merged into each branch's dict, is enough. Keep each branch's existing method-specific keys (`tag_agent`, `tag_snmp_ds`, `snmp_community`, `ipaddress`) exactly as they are, and keep each branch's existing `try`/`except CheckmkAPIError` warning behaviour unchanged — a rejected alias must warn and continue, not abort onboarding.

Do NOT add a device type to Phase 3's IP-named placeholder hosts or to Phase 2's folder default attributes. Those hosts are deliberately inert until Phase 5 knows their real monitoring method, and the tag group's own first-tag default (`other`) already covers them. Leave the corresponding tests at `tests/test_wizard.py` lines ~753 and ~951 asserting their exact original dicts.

Update the three existing Phase 5 attribute-equality assertions (`tests/test_wizard.py` lines ~753/~773/~797 — the phase5 ones, not the phase3/phase2 ones) to include the new key, and add tests for the six behaviours above. Include an explicit test asserting `"alias" not in body["attributes"]` for a host constructed without an alias — the absent-versus-empty distinction is the easiest thing here to get silently wrong.
  </action>
  <verify>
    <automated>uv run pytest tests/test_wizard.py -q &amp;&amp; uv run ruff check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"tag_device_type"' src/checkmk_wizard/wizard.py` is 0, and the attribute key is instead derived from `DEVICE_TYPE_TAG_GROUP_ID` at exactly one place
    - All three branches of `_onboard_hosts` (snmp, ping, agent) merge the device-type fragment into their attributes dict
    - `uv run pytest tests/test_wizard.py -q` exits 0 with at least 4 new tests whose names contain `device_type` or `alias`
    - A test per monitoring method (snmp, ping, agent) asserts the POST body's `attributes` contains `tag_device_type` with the host's value
    - A test asserts `"alias" not in body["attributes"]` when `OnboardedHost.alias is None`
    - A test asserts `body["attributes"]["alias"]` equals the supplied alias when it is set
    - The phase3 placeholder assertion at `tests/test_wizard.py` and the phase2 folder-default assertion still assert their original two-tag dicts unchanged
    - `uv run ruff check src/ tests/` exits 0
  </acceptance_criteria>
  <done>Every host the wizard creates or updates in Phase 5 carries its device type as a Checkmk tag attribute, and its alias as a native Checkmk attribute when one was given.</done>
</task>

<task type="auto">
  <name>Task 3: Repair every pre-existing test whose answer iterator the new prompts shortened</name>
  <files>tests/test_wizard.py</files>
  <read_first>
    - tests/test_wizard.py lines 820-832 (`test_phase4_offers_ping_monitoring_method` — the shortest iterator, and the clearest example of the failure mode)
    - tests/test_wizard.py lines 1000-1055 (`test_phase4_prompts_expected_open_ports_default_from_scan`, `test_phase4_expected_open_ports_blank_skips`, `test_phase4_rejects_out_of_range_port` — the remaining iterator-driven phase4 tests, including the one that deliberately answers the ports prompt twice)
    - src/checkmk_wizard/wizard.py lines 797-890 (the updated `phase4_classification` loop — the authoritative prompt order the iterators must match)
    - .planning/phases/10-checkmk-tag-group-onboarding-integration/10-RESEARCH.md (Common Pitfall 4 — why this fails as an opaque `StopIteration` rather than a readable assertion error)
  </read_first>
  <action>
Find every test that drives `phase4_classification` through a monkeypatched `questionary.Question.ask_async` backed by a fixed-length `answers = iter([...])` list, and append the two new answers in prompt order (device type, then alias) to each. Search the whole file rather than trusting the line numbers above — grep for `phase4_classification` and for `answers = iter(` and reconcile the two lists.

This is not optional cleanup deferred to a later step. These tests hardcode prompt count and order, so the moment Task 1 lands they raise `StopIteration` from inside `next(answers)` — an opaque crash, not a readable failure. ROADMAP.md's Phase 10 success criterion 4 ("existing onboarding tests still pass with the new tag prompt added") is exactly this task.

Use a real value from `device_types.json` for the device-type answer (not an arbitrary string), and `""` for the alias answer in tests that do not care about the alias, so the existing assertions keep testing what they were written to test. For `test_phase4_rejects_out_of_range_port`, which answers the ports prompt twice before succeeding, make sure the two new answers go after BOTH port answers.

Then run the full suite and fix any other test the two new prompts or the two new attribute keys broke, including any Phase 5 or snapshot-export test that compares an `OnboardedHost` by value or serialises it. Do not weaken an assertion to make it pass — if a test now sees a different value, either the production change is wrong or the expectation needs updating for a stated reason; say which in the test's comment.

Where a repaired test would otherwise silently re-break the next time a prompt is added, add a short comment above its `answers` list naming the prompt each element answers, following this repo's convention of explaining what a regression test guards (see `test_delete_site_choice_value_survives_as_sentinel`).
  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run ruff check src/ tests/ scripts/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0 for the entire suite with zero errors and zero skips introduced by this plan
    - `uv run pytest tests/test_wizard.py -q` reports no `StopIteration` and no test collection error
    - Every test that calls `phase4_classification` supplies an answer for both new prompts; `grep -c 'phase4_classification' tests/test_wizard.py` and the number of iterator-driven phase4 tests reconcile
    - No pre-existing assertion was deleted or loosened to a weaker comparison (for example an exact dict equality replaced by a subset check) without a comment stating why
    - At least one repaired test carries a comment naming which prompt each answer element corresponds to
    - `uv run ruff check src/ tests/ scripts/` exits 0
  </acceptance_criteria>
  <done>The suite is green with the two new prompts in the loop, and the repaired tests document their prompt ordering so the next prompt addition fails readably.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator terminal -> wizard prompts | Free-text alias input crosses into a Checkmk host attribute; the device-type answer does not, being constrained to a fixed choice list |
| Wizard -> Checkmk REST API | Authenticated per-host configuration writes carrying operator-supplied values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Tampering | device-type prompt input | mitigate | Validated by construction: a `questionary.select` over `_load_device_types()`'s validated list makes a free-form or unknown device type unreachable, so no regex guard is needed (contrast the hostname and CIDR prompts, which do accept free text and do validate) |
| T-10-16 | Tampering | free-text alias input reaching a Checkmk host attribute | accept | Checkmk validates `alias` server-side and rejects bad values with a 4xx, which surfaces through the existing `except CheckmkAPIError` warning at each Phase 5 call site. This matches how `snmp_community` free text is already handled. The alias never becomes an MQTT topic segment — the poller's `is_publishable_device_id` guard gates topics on the host name, not the alias (T-09-01, unchanged) |
| T-10-17 | Tampering | clearing an operator-set alias by sending an empty value | mitigate | The `alias` key is omitted entirely when `OnboardedHost.alias` is None, so a wizard re-run never overwrites an alias set directly in the Checkmk UI. Enforced by a test asserting the key is absent, not empty |
| T-10-18 | Information Disclosure | `CheckmkAPIError` printed when a host attribute is rejected | accept | The exception carries method/url/status/body, never the Bearer secret; identical exposure to every existing `except CheckmkAPIError` print site in this file |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed by this plan; 10-RESEARCH.md's Package Legitimacy Audit records no new dependency |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 for the whole suite
- `uv run pytest tests/test_wizard.py -q` exits 0 with no `StopIteration`
- `uv run ruff check src/ tests/ scripts/` exits 0
- `grep -c '"tag_device_type"' src/checkmk_wizard/wizard.py` returns 0 (the key is derived from `DEVICE_TYPE_TAG_GROUP_ID`)
- Phase 2 folder-default and Phase 3 placeholder attribute assertions are byte-identical to before this plan
</verification>

<success_criteria>
- The wizard prompts for device type per host from the config-driven list, and for an optional alias (TAG-02, D-07, D-09)
- Hosts created via all three monitoring methods carry `tag_device_type` in Checkmk
- A host given an alias carries Checkmk's native `alias` attribute; a host without one has no alias key sent
- Every pre-existing Phase 4/Phase 5 test passes unchanged in intent (ROADMAP.md Phase 10 success criterion 4)
- Phase 2 folder defaults and Phase 3 placeholder hosts are untouched
</success_criteria>

<output>
Create `.planning/phases/10-checkmk-tag-group-onboarding-integration/10-04-SUMMARY.md` when done
</output>
