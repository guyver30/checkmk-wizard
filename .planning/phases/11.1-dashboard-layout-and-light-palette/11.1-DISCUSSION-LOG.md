# Phase 11.1: Dashboard Layout and Light Palette - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-21
**Phase:** 11.1-dashboard-layout-and-light-palette (React pivot addendum — original
2026-09-16 discussion for this phase predates this log file)
**Areas discussed:** Initial framing (extract tokens vs. full rewrite), App structure & build
output, Cross-phase supersession scope, Missing components, State management & MQTT wiring

---

## Initial framing: how far to adopt the design system

Raised via `/bm:resume-work` session continuation, before any formal discuss-phase area.

| Option | Description | Selected |
|--------|-------------|----------|
| Extract tokens only | Pull colors/typography/spacing/icons into the existing vanilla CSS/JS dashboard, no React, no build step | |
| Full React rewrite | Rebuild the dashboard as a React + Tailwind + TypeScript app consuming the component library directly | ✓ |
| Not sure — inspect components first | Look at design-system/src/components before deciding | |

**User's choice:** Full React rewrite.
**Notes:** User stated the design system reflects "strictly my company's design rules" and
the dashboard "needs to follow it." Confirmed to be React + Tailwind + TypeScript, matching
`design-system/`'s actual stack (verified: `tsup` + Tailwind CLI build, React 18 peer dep).

## Procedural framing: how to formalize the pivot

| Option | Description | Selected |
|--------|-------------|----------|
| Re-discuss Phase 11.1 | Run /bm:discuss-phase on 11.1 to capture the decision, update CLAUDE.md's constraint, re-plan remaining waves | ✓ |
| Treat as a new phase | Leave 11/11.1 vanilla-JS output as-is, open a dedicated migration phase | |
| Just start coding | Skip formal re-scoping, edit CLAUDE.md's constraint directly, go straight to code | |

**User's choice:** Re-discuss Phase 11.1.

| Option | Description | Selected |
|--------|-------------|----------|
| Replace vanilla dashboard/ entirely | React app becomes dashboard/, old files deleted once parity reached | |
| Keep it until parity, then remove | Build React alongside (new path), verify parity, then delete old | ✓ |

**User's choice:** Keep it until parity, then remove. → Became D-42 (`dashboard-react/`
separate directory, cutover only at parity).

---

## App structure & build output

| Option | Description | Selected |
|--------|-------------|----------|
| SPA with client router | One Vite build, react-router drives routes; needs nginx try_files rewrite for cold-loads of non-root routes | ✓ |
| Vite multi-page build | Three separate real HTML entry points, preserves D-21 bookmarkability with zero nginx changes | |

**User's choice:** SPA with client router. → D-41.

| Option | Description | Selected |
|--------|-------------|----------|
| New source dir, build output replaces dashboard/ | dashboard-src/ builds into dashboard/, compose.yaml unchanged | |
| Separate directory, swap later | dashboard-react/ built/run separately, compose.yaml swapped over only at parity | ✓ |

**User's choice:** Separate directory, swap later. → D-42 (confirms the earlier "keep until
parity" answer at the file-layout level).

---

## Cross-phase supersession scope

| Option | Description | Selected |
|--------|-------------|----------|
| Supersede tech, keep product behavior | Drop vanilla-JS module list and hand-rolled palette engineering; keep three-pane layout, resizable panes, grouping select, event ordering, stale-flip behavior, truncation, deployment doc callout as requirements | ✓ |
| Re-open everything | Re-derive every product decision from scratch against what the design-system naturally supports | |

**User's choice:** Supersede tech, keep product behavior. → D-43.

| Option | Description | Selected |
|--------|-------------|----------|
| Trust the design-system | Drop the custom 3-tier state-token engineering and contrast-audit script; use the library's own status-bearing components | ✓ |
| Still verify contrast for custom state colors | Keep a lightweight contrast check for whatever custom state-color mapping gets built on top of the library's tokens | |

**User's choice:** Trust the design-system. → D-44 (with an open research item: confirm what
status/semantic-color primitives the library actually exposes for 5 device states).

---

## Missing components (tree, splitters, gauges, map)

| Option | Description | Selected |
|--------|-------------|----------|
| Custom-built on design-system primitives | Hand-roll Tree/Splitter/Gauge/Map-placeholder styled with the library's Tailwind tokens, no new runtime dependency | ✓ |
| Pull in small focused libraries | react-arborist (tree), react-resizable-panels (splitters), styled to match; gauge/map still custom | |

**User's choice:** Custom-built on design-system primitives. → D-45.

---

## State management & MQTT wiring

| Option | Description | Selected |
|--------|-------------|----------|
| Plain hooks + Context | A custom hook wraps mqtt.js, holds the store in useState/useReducer, exposed via Context | |
| Zustand | Small external state library, less boilerplate for cross-component reads | ✓ |

**User's choice:** Zustand. → D-46 (mqtt.js remains the WebSocket client; a single
hook/module owns connection lifecycle and writes into the Zustand store).

---

## Claude's Discretion

- Exact router library (react-router assumed but not mandated).
- Zustand store shape/slicing.
- Vite config specifics for `dashboard-react/`.
- Whether Tailwind config extends `kone-design-system`'s theme tokens directly or only
  imports its compiled `style.css`.
- Which design-system primitive (e.g. `Accordion.tsx`) the custom tree/splitter components
  build on.

## Deferred Ideas

- None new in this session — Phase 12 (agent metrics/gauges), Phase 13 (topology map,
  wizard `parents`), and Phase 14 (fleet intelligence) fences from the original 2026-09-16
  session are unchanged; D-45 just confirms the gauge/map *component pattern* now so those
  phases don't re-litigate it.

## Follow-up required before execution resumes

- Plans `11.1-04` through `11.1-09` were written against the vanilla-JS/hand-rolled-palette
  approach (contrast-audit script, CSS state tokens, DOM-manipulation view modules) and are
  now stale against D-40–D-46. They need to be replanned via `/bm:plan-phase 11.1` before
  wave 2 executes. Plans `11.1-01` through `11.1-03` already shipped vanilla-JS code that
  remains live in `dashboard/` per D-42 until the React rewrite reaches parity — not deleted
  by this session.
- `design-system/` and `web_assets/` are untracked in git — `git add` them before planning
  so the pivot is reproducible off this machine.
