---
phase: 11.1-dashboard-layout-and-light-palette
plan: 09
type: execute
wave: 7
depends_on: ["11.1-08"]
files_modified:
  - dashboard/README.md
  - docs/Podman setup for checkmk, minio, mosquitto, worker.md
autonomous: false
requirements: [DASH-01, DASH-06]

must_haves:
  truths:
    - "dashboard/README.md describes the three-pane layout, the resize/collapse controls and their persistence, not the superseded dark split-sidebar one (CLAUDE.md docs rule)"
    - "The deployment doc surfaces the CHECKMK_BASE_URL placeholder prominently enough that an operator cannot deploy past it and think the link is broken (D-37)"
    - "The operator sees the finished dashboard running against the live deployment and confirms it matches docs/dashboard1.png / docs/dashboard2.png"
    - "Event history reads newest-first against the LIVE poller (D-33, the confirmation deferred from 11.1-02 if the broker was unreachable then)"
  artifacts:
    - path: "dashboard/README.md"
      provides: "Operator-facing description of the 11.1 layout, controls and persisted preferences"
      contains: "Order by severity"
  key_links:
    - from: "docs/Podman setup for checkmk, minio, mosquitto, worker.md"
      to: "dashboard/js/config.js CHECKMK_BASE_URL"
      via: "a prominent pre-deployment edit step"
      pattern: "CHECKMK_BASE_URL"
---

<objective>
Update the two operator-facing docs to match what 11.1 actually ships, surface the
`CHECKMK_BASE_URL` placeholder properly (D-37), and put the finished dashboard in front of the
operator on the live deployment.

Purpose: CLAUDE.md requires docs be updated after any significant change, and this phase changed
the layout, the controls, and what the dashboard remembers between sessions. D-37 is explicitly a
documentation item, not a code change — the "View in Checkmk" link correctly disables itself while
the placeholder is present (D-20), which is exactly why it looks broken rather than announcing
itself.
Output: two updated docs and an operator sign-off.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard/README.md for the 11.1 layout, controls and persistence</name>
  <files>dashboard/README.md</files>
  <read_first>
    - `dashboard/README.md` in full — especially "Directory layout" (line ~15), "The one file you
      edit: `config.js`" (line ~47), "Vendored assets and no-CDN/no-egress rationale" (~68),
      "No build step" (~94) and "Out of scope" (~101).
    - The SUMMARYs from plans 11.1-05, 11.1-06 and 11.1-07 — the final element ids, the actual
      `localStorage` key names chosen, and the final control names. Take the values from the
      SUMMARYs, not from this plan, because storage key naming was Claude's Discretion.
  </read_first>
  <action>
Update `dashboard/README.md` to describe what now ships. Surgical edits — do not rewrite sections
that are still accurate (the no-CDN/no-egress rationale, the no-build-step section and the vendored
`mqtt.js` note are all unchanged and must be left alone).

Changes needed:
- **Layout description.** Replace any description of a dark theme or a vertically split sidebar
  with the three panes: tree left at full height, stats strip above the topology-map placeholder
  centre-top, event history centre-bottom. Say plainly that the map area is a placeholder until
  Phase 13 (DASH-07) and that it already holds the map's final geometry.
- **Pane controls.** Document that all three panes resize by dragging their splitter **or** by
  focusing it with Tab and using the arrow keys (Home/End jump to the limits), and that each pane
  has a collapse chevron that is a real button. Name the arrow-key step and the minimum pane size
  if the operator would want to know them.
- **What the dashboard remembers.** A short list of the persisted preferences — pane sizes, pane
  collapse states, the grouping mode and the severity-order checkbox — with their actual
  `localStorage` key names, and one sentence saying that a browser which refuses storage loses only
  the memory across reloads, never the controls themselves.
- **Grouping controls.** Replace any "Group by type" toggle-button description with the select
  (`Group by type` / `Group by folder`, extensible) plus the "Order by severity" checkbox, saying
  what severity ordering actually does: groups and hosts with the most severe state sort to the top.
- **`config.js` section (D-37).** Raise `CHECKMK_BASE_URL` in that section: state the checked-in
  placeholder `http://<HOST_IP>:8080` literally, and state the observable consequence of leaving
  it — the "View in Checkmk →" link **disables itself** (by design, D-20), so an unedited
  deployment looks broken rather than erroring. Add the `dashboard/README.md` directory-layout
  entry for `js/panes.js`.
- **Add `scripts/contrast_audit.py`** to whatever section documents dev tooling, with the exact
  command `uv run scripts/contrast_audit.py` and one line saying it is the committed proof of the
  palette's contrast (D-30) and must stay green after any colour change.

Match the file's existing heading style and tone. Do not add screenshots the repo does not have.
  </action>
  <acceptance_criteria>
    - `grep -q 'Order by severity' dashboard/README.md` succeeds
    - `grep -q 'panes.js' dashboard/README.md` succeeds
    - `grep -q 'uv run scripts/contrast_audit.py' dashboard/README.md` succeeds
    - `grep -qi 'arrow' dashboard/README.md` succeeds (keyboard resize is documented)
    - `grep -c 'Group by type' dashboard/README.md` — every remaining occurrence describes a `<select>` option, not a toggle button; verify by reading each hit
    - `grep -q 'CHECKMK_BASE_URL' dashboard/README.md` succeeds and the surrounding text contains the words `disable` (or `disables`) and the literal `http://<HOST_IP>:8080`
    - Every `localStorage` key named in the README matches a key literal actually present in `dashboard/js/panes.js` or `dashboard/js/render-shell.js` — verify each by grep and record the pairs in the SUMMARY
    - No description of a dark theme or a split sidebar survives: `grep -ci 'dark' dashboard/README.md` returns 0, or each hit is a deliberate historical reference
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Order by severity' dashboard/README.md &amp;&amp; grep -q 'panes.js' dashboard/README.md &amp;&amp; grep -q 'uv run scripts/contrast_audit.py' dashboard/README.md &amp;&amp; grep -q 'CHECKMK_BASE_URL' dashboard/README.md</automated>
  </verify>
  <done>The README describes the shipped layout, both resize modalities, the collapse buttons, every persisted preference with its real key, the new grouping controls, the contrast audit, and the `CHECKMK_BASE_URL` consequence.</done>
</task>

<task type="auto">
  <name>Task 2: Make CHECKMK_BASE_URL unmissable in the deployment doc (D-37)</name>
  <files>docs/Podman setup for checkmk, minio, mosquitto, worker.md</files>
  <read_first>
    - `docs/Podman setup for checkmk, minio, mosquitto, worker.md` — section 3 "Configuration
      Files" and its "First-time credential setup" / "Note on `dashboard/js/config.js` (dashboard,
      Phase 11)" block around lines 138–178, section 4 "Deployment" (~180), section 6 "Endpoints &
      Network Access" (~273) and section 7's "Dashboard check (Phase 11)" (~414).
    - `dashboard/js/config.js` — the literal `CHECKMK_BASE_URL = "http://<HOST_IP>:8080"`.
    - `dashboard/js/render-details.js` — the `UNCONFIGURED_MARKER` check that disables the CTA.
  </read_first>
  <action>
D-37 says the placeholder must be surfaced "more prominently than it currently is". Today it is a
**Note** buried inside section 3 — an operator following the deploy steps in order reaches a
running dashboard with a dead-looking link before they reach it.

Do three things:
1. Promote the existing note from a parenthetical into a clearly marked required pre-deployment
   step with its own heading in section 3, stating the file, the literal placeholder value, what to
   replace it with, and the observable symptom of skipping it (the "View in Checkmk →" link is
   **disabled, not broken** — D-20 does that deliberately so a misconfigured deep link can never be
   followed to a wrong host).
2. Add a one-line pointer to that step in section 4 (Deployment), at the point where the
   `dashboard` service first comes up, so the operator meets it in the order they actually work.
3. Extend section 7's "Dashboard check" with an explicit verification item: open a device's detail
   panel and confirm the "View in Checkmk →" link is enabled and resolves to the real site. A check
   that would have caught the live deployment's current state.

While in the file, update its Phase 11 dashboard descriptions where 11.1 changed them: the layout
(three panes) and the resize/collapse controls. Do not touch the Podman, Livestatus, ICMP, systemd
or broker sections — none of them changed.
  </action>
  <acceptance_criteria>
    - `grep -c 'CHECKMK_BASE_URL' "docs/Podman setup for checkmk, minio, mosquitto, worker.md"` returns at least 3 (section 3 step, section 4 pointer, section 7 check)
    - The section 3 occurrence is under its own markdown heading — verify the nearest preceding `#`-heading line and record it in the SUMMARY
    - `grep -qi 'disabled' "docs/Podman setup for checkmk, minio, mosquitto, worker.md"` succeeds in the context of the Checkmk link
    - The section 7 dashboard check contains the literal string `View in Checkmk`
    - `grep -qi 'three-pane\|three panes' "docs/Podman setup for checkmk, minio, mosquitto, worker.md"` succeeds
    - `git diff` on this file shows no hunk inside the Podman/systemd/Livestatus/broker sections
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'CHECKMK_BASE_URL' 'docs/Podman setup for checkmk, minio, mosquitto, worker.md')" -ge "3" &amp;&amp; grep -q 'View in Checkmk' 'docs/Podman setup for checkmk, minio, mosquitto, worker.md'</automated>
  </verify>
  <done>The placeholder is a named pre-deployment step, cross-referenced at deploy time, and verified in the dashboard check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification of the finished dashboard against the real deployment</name>
  <files>dashboard/ (verification only — no file is edited by this task unless a defect is found and fixed here)</files>
  <read_first>
    - `docs/dashboard1.png` and `docs/dashboard2.png` — the approved light mockups, this phase's
      visual contract (preserved by commit `ec6d832`).
    - `docs/live-dashboard1.png` and `docs/live-dashboard_offline.png` — the live screenshots that
      produced D-34 and D-35, so the before/after comparison is concrete.
    - The SUMMARYs of plans 11.1-01 through 11.1-08 — every "record in the SUMMARY" observation
      they gathered, so this checkpoint re-checks rather than re-derives.
  </read_first>
  <action>
Re-run every automated gate, deploy the current `dashboard/` to the live host, then hand the
verification steps below to the operator and WAIT for their response. Do not self-approve: the
keyboard path (step 5) and the visual match against the mockups are the two things this phase
cannot prove without a human.

If the operator reports a defect, record it against its step number and either fix it here (if it
is a small, in-scope correction to work this phase did) or log it as a follow-on naming the owning
phase. Nothing from Phase 12, 13 or 14 gets pulled in to satisfy a comment.
  </action>
  <what-built>
Phase 11.1 in full, running against the live Checkmk site and broker:
- KONE light palette, three token tiers, contrast-audited by computation (`uv run scripts/contrast_audit.py`)
- Three panes: tree left full height, stats strip above the topology-map placeholder centre-top,
  event history centre-bottom
- All three panes resizable by drag AND by keyboard, collapsible by real buttons, sizes and
  collapse states persisted
- Grouping `<select>` plus an "Order by severity" checkbox
- Stale flips that toggle classes instead of rebuilding
- Event rows that identify their device at any pane width
- Updated `dashboard/README.md` and deployment doc

Before pausing, re-run every automated gate one final time and paste the results into the SUMMARY:
`uv run scripts/contrast_audit.py` (exit 0), `uv run pytest -q` (green), and a cold load of all
three pages with no broker reachable (no console error).
  </what-built>
  <how-to-verify>
1. Deploy the current `dashboard/` to the host and open `http://<HOST_IP>:8090/` with the poller
   running. Compare against `docs/dashboard1.png` and `docs/dashboard2.png` — the approved light
   mockups, which are this phase's visual contract.
2. **Palette:** the page is light, text is readable everywhere, no white-on-white panel, the KONE
   logo reads correctly in the topbar, and the connection dot is visible with its ring.
3. **Layout (D-25/D-27):** the counts strip (`17 OK · 0 WARN · 3 DOWN · 0 STALE`) and the map
   placeholder are BOTH visible with no control touched. The tree runs the full height of the left
   column. The event history is along the bottom of the centre.
4. **Panes (D-26), mouse:** drag each of the two splitters; the panes resize and stop at a sensible
   minimum. Click each of the three collapse chevrons; each pane collapses to a rail and expands
   back to the size you had set, not to the minimum.
5. **Panes (D-26), keyboard — do this with your hands off the mouse:** press Tab until a splitter
   shows a focus ring, then arrow keys to resize it. Tab to each collapse button and press Enter.
   If any of this does not work, the phase has a WCAG 2.1.1 failure and must not close.
6. **Persistence:** resize and collapse things, then reload the page. Everything comes back as you
   left it.
7. **Grouping (D-32):** switch the select between `Group by type` and `Group by folder` — the tree
   regroups immediately, not after a pause. Tick "Order by severity" — the DOWN/CRIT groups and
   hosts jump to the top immediately. Collapse two groups first and confirm they are still
   collapsed after toggling severity order.
8. **Event ordering (D-33) — the check deferred from plan 11.1-02 if the broker was unreachable
   then:** force a real state change (stop a monitored host, or put one in downtime) and confirm
   the NEW event appears at the TOP of the event history, above the older ones.
9. **Event identity (D-35):** narrow the event pane to its minimum and confirm you can still tell
   your `192.168.0.x` hosts apart. Hover a row for the full name.
10. **Staleness (D-34):** stop the poller. Within ~12 seconds the offline banner appears and every
    row gains the hatch. While that happens, keep a tree row focused and the tree scrolled — focus
    and scroll must both survive. Restart the poller; the hatch clears.
11. **Checkmk link (D-37):** open a device's detail panel. If "View in Checkmk →" is disabled, your
    `dashboard/js/config.js` still has the placeholder — the deployment doc now tells you where to
    fix it. Fix it, reload, confirm the link resolves.
12. Take fresh screenshots for `docs/` if the current ones no longer represent the product.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what is wrong — naming the step number and, for anything visual, attaching a screenshot.</resume-signal>
  <acceptance_criteria>
    - All three automated gates re-run and their output pasted into the SUMMARY: `uv run scripts/contrast_audit.py` exit 0, `uv run pytest -q` green, three cold page loads with no console error
    - The operator has exercised steps 2–11 and responded
    - Every reported defect is recorded in the SUMMARY with the step number that surfaced it, and either fixed in this plan or logged as a follow-on with a named owner phase
    - A fresh screenshot of the finished dashboard is saved under `docs/` and its path recorded
  </acceptance_criteria>
  <verify>
    <automated>uv run scripts/contrast_audit.py &amp;&amp; uv run pytest -q</automated>
    <human-check>Operator confirms steps 2–11 against the live deployment and types "approved".</human-check>
  </verify>
  <done>The operator has seen the finished dashboard running live and approved it, or their defects are recorded and dispositioned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → deployment config | `dashboard/js/config.js` is hand-edited per deployment; the doc change is what makes that edit reliably happen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-20 | Spoofing | `CHECKMK_BASE_URL` deep link | mitigate | D-20's existing behaviour — the CTA disables itself while the placeholder is present — is retained and now documented, so a misconfigured link can never navigate an operator to an unintended host; the doc change removes the silent-confusion failure mode instead of removing the guard |
| T-11.1-21 | Repudiation | phase closure | mitigate | The live-verification checkpoint requires pasted gate output and an operator response, so "it works on the deployment" is evidenced rather than asserted — the same discipline that produced D-34 and D-35 from Phase 11's own live check |
| T-11.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed anywhere in this phase |
</threat_model>

<verification>
Both docs updated and grep-verified; all three automated gates green; operator sign-off recorded.
</verification>

<success_criteria>
The docs match what ships, D-37 is discharged, and the operator has verified the finished dashboard
on the live deployment — including the keyboard path, which is the one thing a screenshot cannot
show.
</success_criteria>

<output>
Create `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-09-SUMMARY.md` when done
</output>
