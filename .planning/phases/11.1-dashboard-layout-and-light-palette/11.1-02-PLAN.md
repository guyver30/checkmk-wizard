---
phase: 11.1-dashboard-layout-and-light-palette
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_mqtt_poller.py
autonomous: true
requirements: []
# No DASH requirement: this plan exists to discharge D-33 (the event-ordering
# contract renderEvents() depends on). It touches neither index.html nor
# devices.html, so tagging DASH-01/02/06 here would mislead a traceability audit.

must_haves:
  truths:
    - "A test proves lan/events/recent is published oldest-first across two run_cycle() calls (D-33)"
    - "A test proves oldest-first ordering survives the events_max_entries truncation (D-33)"
    - "renderEvents()'s .slice().reverse() is confirmed correct and is NOT changed (D-33)"
    - "scripts/mqtt_poller.py is not modified by this phase (scope fence)"
  artifacts:
    - path: "tests/test_mqtt_poller.py"
      provides: "Regression coverage pinning the event-ordering contract renderEvents() depends on"
      contains: "ordering"
  key_links:
    - from: "tests/test_mqtt_poller.py"
      to: "scripts/mqtt_poller.py"
      via: "run_cycle() -> publish_events() on lan/events/recent"
      pattern: "events/recent"
---

<objective>
Pin the event-ordering contract that `renderEvents()` trusts, at its producer. D-33 is explicit:
the shipped code is CORRECT — `mqtt_poller.py` appends chronologically and `render-shell.js`
reverses to compensate. The reported oldest-on-top fault was in a session-local preview harness
that pre-reversed its synthetic feed. **A plan that reverses the render again is wrong.**

Purpose: no existing test proves array *order*. Research read every events test in
`tests/test_mqtt_poller.py` (lines 734–1115): each asserts either "an event with device_id X exists
somewhere" or `entries[-1]`. Neither proves chronological order across cycles. That is the gap.
Output: two new pytest cases in the repo's existing harness. No new test runner, no JS tooling.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the ordering contract end to end, producer and consumer</name>
  <files>scripts/mqtt_poller.py, dashboard/js/render-shell.js</files>
  <read_first>
    - `scripts/mqtt_poller.py` lines 1040–1100 — the three `events_this_cycle.append(...)` call
      sites (tombstone, state-change, added-device) and the concatenation
      `state.events = (state.events + events_this_cycle)[-config.events_max_entries :]`.
    - `dashboard/js/render-shell.js` lines 552–569 — `renderEvents()` and its
      `.slice().reverse()` with the comment "Newest-first: the poller appends chronologically".
    - `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md` section 7.
  </read_first>
  <action>
Read-only confirmation. Establish and record three facts in the SUMMARY:

1. `events_this_cycle` is built by `.append(...)` in cycle order and concatenated onto the END of
   `state.events`, so the published `lan/events/recent` array is oldest-first.
2. `renderEvents()` reverses that array exactly once, producing newest-first rows in the DOM.
3. The two are self-consistent — no code change is required on either side.

Then confirm against the LIVE poller rather than by reading alone. Use the deployment's broker:
subscribe with `mosquitto_sub -h <broker-host> -p 1883 -t lan/events/recent -C 1` (or the WS
listener on 9002) and read back the retained payload's `timestamp` fields. Assert by inspection
that `timestamp` values are non-decreasing across the array — i.e. the array is oldest-first on the
wire. If no broker is reachable from the execution environment, say so plainly in the SUMMARY and
record the confirmation as deferred to plan 11.1-09's live-verification checkpoint; do NOT
substitute a synthetic feed for this check, because a synthetic feed is exactly what produced the
false report D-33 exists to correct.

**Do not modify `scripts/mqtt_poller.py`** — it is read-only for this phase per the scope fence.
**Do not modify `renderEvents()`'s reversal.**
  </action>
  <acceptance_criteria>
    - `git diff --quiet scripts/mqtt_poller.py` succeeds (file untouched)
    - `grep -c 'reverse()' dashboard/js/render-shell.js` is unchanged from the pre-task value (the reversal is still present exactly once in `renderEvents()`)
    - The SUMMARY names the three `.append(...)` line numbers in `mqtt_poller.py` and the `renderEvents()` line number of the `.reverse()`
    - The SUMMARY records either the live `mosquitto_sub` payload's first and last `timestamp` values (proving non-decreasing order) or an explicit "broker unreachable, deferred to 11.1-09" statement
  </acceptance_criteria>
  <verify>
    <automated>git diff --quiet scripts/mqtt_poller.py &amp;&amp; grep -q 'events.slice' dashboard/js/render-shell.js</automated>
  </verify>
  <done>Ordering contract confirmed at both ends; no production file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ordering regression tests to tests/test_mqtt_poller.py</name>
  <files>tests/test_mqtt_poller.py</files>
  <behavior>
    - Two events produced in two SEPARATE `run_cycle()` calls land in the published
      `lan/events/recent` array oldest-first: the cycle-1 event's index is lower than the
      cycle-2 event's index.
    - When more events accumulate than `events_max_entries`, the truncation
      `[-config.events_max_entries :]` drops the OLDEST entries and the surviving array is still
      oldest-first.
  </behavior>
  <read_first>
    - `tests/test_mqtt_poller.py` lines 734–1115 — every existing events test. Reuse the file's
      existing fixture/mock idioms verbatim (module-scope ALL_CAPS constants, the fake MQTT client
      that captures publishes, the Livestatus stub). No `conftest.py` exists in this repo; shared
      helpers are defined per-file — follow that.
    - `scripts/mqtt_poller.py` `run_cycle()`, `publish_events()`, and the `events_max_entries`
      config field.
  </read_first>
  <action>
Add exactly two tests to `tests/test_mqtt_poller.py`, placed next to the existing events tests, not
in a new file. Name them for the contract they pin, in the file's existing snake_case style — e.g.
`test_events_published_oldest_first_across_cycles` and
`test_events_truncation_drops_oldest_and_preserves_order`.

Test 1: drive two `run_cycle()` calls that each produce exactly one state-change event on two
different devices (device "a" then device "b"). Decode the payload published to
`lan/events/recent` after the second cycle and assert the FULL array order, not `[-1]`:
`assert [e["device_id"] for e in entries] == ["a", "b"]`. Assert the whole list in one comparison —
an `in` check or an `entries[-1]` check does not pin order and is what the existing tests already
do.

Test 2: set `events_max_entries` small (e.g. 2), drive three cycles producing events for devices
"a", "b", "c" in that order, and assert `[e["device_id"] for e in entries] == ["b", "c"]` — proving
the bound drops the oldest and leaves the remainder chronological.

Each test gets a comment naming the real contract it guards — that a browser consumer
(`render-shell.js` `renderEvents()`) reverses this array exactly once to display newest-first, so a
producer-side order flip would silently invert the event panel. This matches the repo's convention
of explaining the bug a regression test guards (see `test_delete_site_choice_value_survives_as_sentinel`
in `tests/test_wizard.py`).

Per CLAUDE.md, run everything through `uv run`. Do not add a dependency; `pytest` is already in the
dev group.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_mqtt_poller.py -k "oldest_first or truncation_drops_oldest" -q` exits 0 and collects exactly 2 tests
    - `uv run pytest tests/test_mqtt_poller.py -q` exits 0 (no pre-existing test regressed)
    - `grep -c 'device_id"\] == \[' tests/test_mqtt_poller.py` returns at least 2 (both tests assert a full ordered list, not membership)
    - `git diff --quiet scripts/mqtt_poller.py` still succeeds
    - Both new tests carry a comment containing the string `renderEvents`
    - `uv run pytest -q` exits 0 across the whole suite (the wizard's 7-phase tests are unaffected, per repo CLAUDE.md's compatibility constraint)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_mqtt_poller.py -q</automated>
  </verify>
  <done>Two ordering tests pass; the full suite still passes; the poller is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker → browser | `lan/events/recent` retained payload crosses from the poller into the dashboard; its ordering is a trusted contract this plan pins but does not otherwise change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-02 | Tampering | `renderEvents()` ordering contract | mitigate | Pin the producer-side order with two full-array assertions so a future poller refactor that flips order fails a test rather than silently inverting the operator's event panel |
| T-11.1-03 | Information Disclosure | live `mosquitto_sub` verification | accept | The broker is LAN-local with read-only WS ACLs (Phase 8 BRK-03); subscribing to a retained topic exposes nothing the dashboard does not already show |
| T-11.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed — `pytest` is already in `pyproject.toml`'s dev group; no `uv add` is performed |
</threat_model>

<verification>
`uv run pytest -q` green. `git diff --name-only` lists only `tests/test_mqtt_poller.py`.
</verification>

<success_criteria>
D-33 discharged: ordering confirmed (live if reachable), two regression tests pin it, and neither
`mqtt_poller.py` nor `renderEvents()` was changed.
</success_criteria>

<output>
Create `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-02-SUMMARY.md` when done
</output>
