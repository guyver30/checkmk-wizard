---
phase: 11.1-dashboard-layout-and-light-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-live-dashboard/11-CONTEXT.md
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "A future reader opening 11-CONTEXT.md sees D-22 marked superseded and reading light / tree-full-height / events-in-centre (D-36)"
    - "A future reader opening 11-CONTEXT.md sees D-24 marked superseded and reading stats strip above the centre's primary view (D-36)"
    - "D-11's colour meanings are explicitly recorded as unchanged (D-36)"
    - "REQUIREMENTS.md line 41 already carries the re-worded DASH-01 and its dated re-wording note (D-36)"
  artifacts:
    - path: ".planning/phases/11-live-dashboard/11-CONTEXT.md"
      provides: "Phase 11 decision record with D-22 and D-24 formally superseded"
      contains: "D-24 (SUPERSEDED"
  key_links:
    - from: ".planning/phases/11-live-dashboard/11-CONTEXT.md"
      to: ".planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md"
      via: "supersession pointer naming D-25/D-27/D-36"
      pattern: "11\\.1-CONTEXT\\.md"
---

<objective>
Formally supersede Phase 11's D-22 and D-24 inside Phase 11's own context file, and confirm
DASH-01's re-wording is already committed. D-36 says explicitly that editing 11.1-CONTEXT.md is
not enough — `11-CONTEXT.md` is the file a future reader opens.

Purpose: every later plan in this phase contradicts D-22 (dark, split sidebar) and D-24 (grouped
fleet overview). Those contradictions must be recorded, not silently created.
Output: an edited `11-CONTEXT.md`; a verified-unchanged `REQUIREMENTS.md`.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supersede D-22 and D-24 in 11-CONTEXT.md (D-36)</name>
  <files>.planning/phases/11-live-dashboard/11-CONTEXT.md</files>
  <read_first>
    - `.planning/phases/11-live-dashboard/11-CONTEXT.md` lines 74–200 — the decision list. Note the
      existing in-file supersession style: D-03 and D-22 already carry a `(REVISED)` suffix. Match
      that shape exactly; do not invent a new notation.
    - `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md` — D-36's verbatim
      "Becomes:" wording for both decisions, plus its D-11 note.
  </read_first>
  <action>
Edit `.planning/phases/11-live-dashboard/11-CONTEXT.md` in place, touching only the D-22, D-24 and
(if a D-11 clarification line is warranted) D-11 bullets. Nothing else in that file changes.

For D-22 (currently `- **D-22 (REVISED):**` at line 174): change the marker to
`- **D-22 (SUPERSEDED by Phase 11.1 D-25/D-27/D-36, 2026-09-16):**`. Keep the existing body text
intact as the historical record, then append a nested `**Becomes:**` sub-bullet carrying D-36's
wording: the layout is **light**, the **left column is ALL tree, full height**, and the
**event-history panel moves to the bottom of the centre**. Add a pointer to
`.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md` (D-25, D-27, D-31).
Note in the same sub-bullet that `docs/DMC-networkmap.png` / `docs/DMC-server.png` are history only
and that the contract is now `docs/dashboard1.png` / `docs/dashboard2.png`.

For D-24 (currently `- **D-24 (NEW):**` at line 184): change the marker to
`- **D-24 (SUPERSEDED by Phase 11.1 D-25/D-27/D-36, 2026-09-16):**`, keep the body, and append a
`**Becomes:**` sub-bullet: `index.html` is the stats-by-state strip above **the centre's primary
view**; that primary view is the topology map (DASH-07, Phase 13) and until it lands it is a sized,
labelled placeholder pane (D-27). State that the grouped fleet overview in `render-index.js` is
deleted by D-27 and that the per-state counts logic is kept for the strip.

For D-11: leave the decision body untouched. Append one sentence recording that Phase 11.1's light
palette re-derives the colour **values** only and that D-11's colour **meanings** are unchanged
(D-36). Do not restate the hex values here — they live in `dashboard.css` and plan 11.1-03.

Keep the file's existing prose voice and ~95-column wrapping. Surgical edits only, per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - `grep -c 'SUPERSEDED by Phase 11.1' .planning/phases/11-live-dashboard/11-CONTEXT.md` returns 2
    - `grep -n 'D-22 (SUPERSEDED' .planning/phases/11-live-dashboard/11-CONTEXT.md` matches exactly one line
    - `grep -n 'D-24 (SUPERSEDED' .planning/phases/11-live-dashboard/11-CONTEXT.md` matches exactly one line
    - The file contains the literal string `11.1-CONTEXT.md` at least once (the supersession pointer)
    - The file contains the literal string `full height` within the D-22 block
    - The file contains the literal string `placeholder` within the D-24 block
    - `git diff --stat .planning/phases/11-live-dashboard/11-CONTEXT.md` shows only that one file changed, and the D-23 and D-21 bullets are byte-identical to before (`git diff` shows no hunk touching them)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'SUPERSEDED by Phase 11.1' .planning/phases/11-live-dashboard/11-CONTEXT.md)" = "2" &amp;&amp; grep -q 'D-22 (SUPERSEDED' .planning/phases/11-live-dashboard/11-CONTEXT.md &amp;&amp; grep -q 'D-24 (SUPERSEDED' .planning/phases/11-live-dashboard/11-CONTEXT.md</automated>
  </verify>
  <done>D-22 and D-24 carry a SUPERSEDED marker with a "Becomes:" restatement and a pointer to 11.1-CONTEXT.md; D-11 records that only values changed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm DASH-01's re-wording is committed, do not re-edit (D-36)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 39–48 (the Dashboard block) and lines 114–120 (the dated
      split/re-wording notes).
    - D-36's DASH-01 paragraph in `11.1-CONTEXT.md`.
  </read_first>
  <action>
This is a verification task, not an edit task. Research already established that DASH-01's
re-wording landed: line 41 reads "above the centre's primary view ... until that lands it is a
sized, labelled placeholder pane", and a dated *Re-wording note (2026-09-16)* already sits with the
2026-09-12 split note.

Confirm both by reading the file. If — and only if — either is absent, apply D-36's exact text to
line 41 and add the dated note in the same italic style as the existing 2026-09-12 note. If both
are present, change nothing and record in the SUMMARY that D-36's REQUIREMENTS half was already
satisfied before this phase began. Do not re-word, re-format, or "tidy" the note that is there.
  </action>
  <acceptance_criteria>
    - `grep -c "the centre's primary view" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -q 'sized, labelled placeholder pane' .planning/REQUIREMENTS.md` succeeds
    - `grep -q 'Re-wording note (2026-09-16)' .planning/REQUIREMENTS.md` succeeds
    - `git diff --quiet .planning/REQUIREMENTS.md` succeeds (no change was needed), OR the SUMMARY states exactly which of the two elements was missing and was added
  </acceptance_criteria>
  <verify>
    <automated>grep -q "the centre's primary view" .planning/REQUIREMENTS.md &amp;&amp; grep -q 'sized, labelled placeholder pane' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'Re-wording note (2026-09-16)' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>DASH-01 line 41 and its dated re-wording note are both verified present; the file is unchanged unless something was genuinely missing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan edits planning markdown only. No runtime code, no input surface, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-01 | Repudiation | `.planning/phases/11-live-dashboard/11-CONTEXT.md` | mitigate | A later reader must not be able to claim D-22/D-24 were silently ignored — the supersession is written into the superseded file itself with a dated pointer (D-36) |
| T-11.1-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this phase installs no package of any kind; no `uv add`, no `pip install`, no `package.json` exists in this repo |
</threat_model>

<verification>
`git diff` touches at most `.planning/phases/11-live-dashboard/11-CONTEXT.md`. Both supersession
markers grep-visible. No `dashboard/` file is touched by this plan.
</verification>

<success_criteria>
D-36 is fully discharged: D-22 and D-24 superseded in Phase 11's own context file, DASH-01's
re-wording confirmed present in REQUIREMENTS.md.
</success_criteria>

<output>
Create `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-01-SUMMARY.md` when done
</output>
