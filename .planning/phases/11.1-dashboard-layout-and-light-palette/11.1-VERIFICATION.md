---
phase: 11.1-dashboard-layout-and-light-palette
verified: 2026-09-21T19:00:00Z
status: passed
score: 14/14 must-haves verified
has_blocking_gaps: false
overrides_applied: 0
deferred:
  - truth: "Live-data behaviors (device population in the tree, newest-first ordering against real MQTT events, 'Connected' connection-indicator state, and the vanilla-dashboard-on-port-8090 regression check) verified against a real running broker/poller stack"
    addressed_in: "Deployment (post-11.1, tracked in 11.1-11-SUMMARY.md 'Follow-up' and dashboard-react/README.md's cutover checklist)"
    evidence: "11.1-11-SUMMARY.md's Task 3 Verdict: the human-verification checkpoint ran in a sandbox with no podman/docker and no reachable MQTT broker, so these specific checks were explicitly deferred to a real deployment. All logic these checks would exercise (event ordering, store replace-on-message, connection-phase state machine) is independently covered by passing automated tests (test_mqtt_poller.py::test_events_published_oldest_first_across_cycles, useAppStore.test.ts, mqttClient.test.ts) that do not require a live broker."
---

# Phase 11.1: Dashboard Layout and Light Palette Verification Report

**Phase Goal:** The dashboard adopts KONE's light palette and the operator's three-pane layout, so the product looks and behaves the way it is meant to before any further capability is built on it
**Verified:** 2026-09-21T19:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Context Honored

This phase pivoted mid-flight (D-40 through D-46) from a vanilla-JS dashboard to a React +
TypeScript + Tailwind SPA (`dashboard-react/`) against `kone-design-system`. Plans 01-03 were
built against the pre-pivot vanilla-JS approach and are confirmed complete/standing
independently (not reverted); plans 04-11 are the React rewrite across 7 waves, confirmed
complete. This report verifies both, with the React app treated as the actual shipping product
per D-42 (vanilla `dashboard/` keeps running unmodified until cutover).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Phase 11's D-22 (dark theme) and D-24 (grouped fleet overview) are formally superseded, not silently ignored | VERIFIED | `.planning/phases/11-live-dashboard/11-CONTEXT.md` lines 193, 208: `D-22 (SUPERSEDED by Phase 11.1 D-25/D-27/D-36...)`, `D-24 (SUPERSEDED by Phase 11.1 D-25/D-27/D-36...)` |
| 2 | REQUIREMENTS.md's DASH-01 carries the re-worded text with a dated note | VERIFIED | `.planning/REQUIREMENTS.md` line 41 (re-worded text), line 118 (2026-09-16 re-wording note) |
| 3 | Newest-first event ordering is verified against the live poller contract with a regression test, not "fixed" | VERIFIED | `tests/test_mqtt_poller.py::test_events_published_oldest_first_across_cycles` (line 1124) exists and passes (`uv run pytest -q` 411/411); `EventHistory.tsx` comment + `events.slice().reverse()` confirms the render-side reversal is unchanged, not re-reversed |
| 4 | The dashboard renders on a light KONE palette, not the dark theme | VERIFIED | `dashboard/css/dashboard.css` re-derived to light tokens (`--state-ok-ink: #087827` etc., plan 03); `dashboard-react`'s production build (`dist/assets/index-*.css`) contains `kone-design-system`'s compiled light token set (`--color-bg-surface-default`, `--blue-500`, etc.) — confirmed via `npm run build` producing real CSS output, not an empty/placeholder stylesheet |
| 5 | The React app builds a static SPA bundle against `kone-design-system` and runs alongside the untouched vanilla dashboard | VERIFIED | `npm --prefix dashboard-react run build` exits 0, `dashboard-react/dist/index.html` produced (independently re-run, not just trusted from SUMMARY); `git diff --quiet -- dashboard/ scripts/mqtt_poller.py tests/test_mqtt_poller.py` clean (independently re-run) |
| 6 | Three-pane layout: full-height tree (left), stats-strip-above-map-placeholder (centre-top), event history (centre-bottom) | VERIFIED | `ThreePaneLayout.tsx` (grid wrapping tree/centreTop/centreBottom), `IndexRoute.tsx` composes `StatsStrip` above `MapPlaceholder` in `centreTop`, `EventHistory` in `centreBottom` |
| 7 | Panes are resizable by mouse and keyboard, collapsible via real buttons, with full ARIA separator semantics, sizes persisted via guarded storage | VERIFIED | `Splitter.tsx`: `role="separator"`, `aria-orientation`, `aria-valuenow/min/max`, `onPointerMove` drag + `onKeyDown` arrow/Home/End resize; `ThreePaneLayout.tsx`'s `CollapsiblePane` uses a real `<button aria-expanded>`; `guardedStorage.ts` wraps every `localStorage` read/write in try/catch, never throwing to the caller |
| 8 | Grouping control is an extensible select (type/folder) plus an "Order by severity" checkbox, not a two-state toggle | VERIFIED | `GroupingControls.tsx` renders `kone-design-system`'s `Select`/`Checkbox` from a `GROUPING_OPTIONS` array (extensible by appending an entry) plus a "Order by severity" `Checkbox`; `treeModel.ts`/`grouping.ts` implement the ordering |
| 9 | DASH-01: stats strip (counts by state) sits above the centre's primary view, merging updates in place | VERIFIED | `StatsStrip.tsx` renders `kone-design-system`'s `StatTile` per state, filtered to non-zero + always-OK; Zustand store (`useAppStore.ts`) replaces only the changed device slot per message (`set({ devices: {...get().devices, [id]: ...} })`), never rebuilding the whole tree — proven not to remount by `StaleStability.test.tsx`'s `toBe` DOM-identity assertions |
| 10 | DASH-01's primary view (topology map) is a sized, labelled placeholder pending Phase 13 | VERIFIED | `MapPlaceholder.tsx` renders `"Topology map — Phase 13"` sized by its flex container, occupying the centre-top geometry below the stats strip |
| 11 | DASH-06: hosts are color-coded/iconed by device type and grouped/colored by folder-derived group label | VERIFIED | `TreeNode.tsx` renders `device.typeIcon` per device row and a severity-derived `StateBadgeForState`; `grouping.ts`'s `groupKeyFor()` supports both `device_type` and `folder` modes, selectable live via `GroupingControls` |
| 12 | A stale flip (D-34) caused purely by elapsed time is visual-only — no remount, no lost focus/scroll/expand-state | VERIFIED | `StaleStability.test.tsx` seeds two devices, advances fake timers past the staleness threshold with **no** MQTT message, and asserts via `toBe` (DOM node identity) that unaffected/affected tree rows, focus, scroll position and the map placeholder's node identity all survive. Independently re-run: 1/1 passed |
| 13 | Event rows legibly identify their device even when hosts share a long common IP prefix (D-35) | VERIFIED | `truncate.ts::middleTruncate()` biases the kept characters to head+tail (not a head-only clip); wired into `EventRow.tsx` |
| 14 | The `CHECKMK_BASE_URL` placeholder trap is surfaced prominently in the deployment doc (D-37), and the nginx SPA-fallback cutover requirement is recorded as a tracked checklist item, not folklore | VERIFIED | `dashboard-react/README.md` §4 (CHECKMK_BASE_URL callout) and §7 (numbered cutover checklist with the literal `try_files $uri $uri/ /index.html;` directive); `docs/Podman setup for checkmk, minio, mosquitto, worker.md` lines 176-184 cross-reference both |

**Score:** 14/14 truths verified

### Deferred Items

Live-broker-dependent behaviors, explicitly deferred to a real deployment per the phase's own
human-verification checkpoint (11.1-11-SUMMARY.md Task 3 Verdict). Not treated as blocking gaps
per this run's explicit instruction — the underlying logic (event ordering, store replace
semantics, connection-phase state machine) is independently covered by passing unit tests that
do not require a live broker, and the deferral was a sandbox environment constraint (no
podman/docker installed), not a defect found in the code.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Device population in the tree against real MQTT data | Real Podman deployment | `useAppStore.test.ts` covers wholesale-replace-on-message logic without a broker; human-verify checkpoint observed correct disconnected-state UI (empty tree, "Reconnecting…") in the no-broker sandbox |
| 2 | Newest-first ordering against real live events | Real Podman deployment | `test_mqtt_poller.py` pins the producer-side ordering contract; `EventHistory.tsx`'s `.slice().reverse()` is unit-tested in `EventHistory.test.tsx` |
| 3 | "Connected" connection-indicator state | Real Podman deployment | `mqttClient.test.ts` and `ConnectionIndicator.test.tsx` cover the state machine including the "connected" phase without a live broker |
| 4 | Vanilla dashboard (port 8090) regression check | Real Podman deployment | `git diff --quiet -- dashboard/` confirms zero bytes changed in the vanilla dashboard by this phase, independently re-verified |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard-react/package.json` | Pinned deps incl. `kone-design-system` | VERIFIED | `"kone-design-system": "file:../design-system/kone-design-system-0.1.0.tgz"` present |
| `dashboard-react/src/App.tsx` | react-router route tree for index/devices/details | VERIFIED | `<Routes>` with `/`, `/devices`, `/details`, catch-all |
| `dashboard-react/src/lib/config.ts` | `CHECKMK_BASE_URL` D-37 convention | VERIFIED | Present with placeholder + `isCheckmkLinkConfigured()` |
| `dashboard-react/src/components/Splitter.tsx` | Full ARIA separator + keyboard resize | VERIFIED | `role="separator"`, arrow-key + Home/End handling |
| `dashboard-react/src/lib/guardedStorage.ts` | Try/catch localStorage wrapper | VERIFIED | `readJson`/`writeJson`, both catch and fall back |
| `dashboard-react/src/components/ThreePaneLayout.tsx` | CSS-grid three-pane shell | VERIFIED | Grid template columns/rows, prop-based slots (D-31) |
| `dashboard-react/src/components/MapPlaceholder.tsx` | Sized, labelled placeholder | VERIFIED | Renders `"Topology map — Phase 13"` |
| `dashboard-react/src/lib/stateMapping.ts` | State -> Badge mapping | VERIFIED | 8 states mapped, STALE/UNKNOWN differentiated by icon |
| `dashboard-react/src/components/StatsStrip.tsx` | Counts-by-state strip | VERIFIED | Uses `kone-design-system`'s `StatTile` |
| `dashboard-react/src/lib/treeModel.ts` | Group+device tree derivation | VERIFIED | Uses `rollUpGroup`/`buildGroupIndex` from `grouping.ts`, includes `typeIcon` |
| `dashboard-react/src/components/TreeNode.tsx` | Accessible recursive tree node | VERIFIED | `aria-expanded`, real `<button>`, per-device `typeIcon` |
| `dashboard-react/src/hooks/useGroupingPrefs.ts` | Guarded-storage grouping prefs | VERIFIED | Storage key `dashboard-react.grouping.v1` |
| `dashboard-react/src/components/GroupingControls.tsx` | Extensible select + checkbox | VERIFIED | `GROUPING_OPTIONS` array, `Select`+`Checkbox` from design system |
| `dashboard-react/src/lib/truncate.ts` | Middle-truncation | VERIFIED | `middleTruncate()`, head/tail bias documented |
| `dashboard-react/src/components/EventHistory.tsx` | Newest-first event list | VERIFIED | `.slice().reverse()`, explicit empty state |
| `dashboard-react/src/hooks/useNowTick.ts` | Single periodic clock | VERIFIED | `CLOCK_TICK_MS = 12000`, one `setInterval` |
| `dashboard-react/src/components/StaleStability.test.tsx` | D-34 regression | VERIFIED | Exists, substantive, passes independently |
| `dashboard-react/README.md` | Build/config/cutover docs | VERIFIED | All 7 required sections present |
| `dashboard/css/dashboard.css` (plan 03, superseded-in-place) | Light palette tokens | VERIFIED | `--state-*-ink`/`-fill` tokens, `.is-stale` dark-ink hatch, `.brand-logo` fix all present |
| `.planning/phases/11-live-dashboard/11-CONTEXT.md` (plan 01) | D-22/D-24 marked superseded | VERIFIED | Both supersession notes present |
| `tests/test_mqtt_poller.py` (plan 02) | Ordering regression coverage | VERIFIED | `test_events_published_oldest_first_across_cycles` and truncation-survives-ordering test present, both pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `main.tsx` | `kone-design-system/style.css` | side-effect CSS import | WIRED | `import "kone-design-system/style.css"` precedes `./index.css` |
| `App.tsx` | `react-router` | `BrowserRouter`/`Routes`/`Route` | WIRED | Imports `from "react-router"` (not `-dom`, per README's documented v8 pin) |
| `mqttClient.ts` | `useAppStore.ts` | `client.on('message')` -> `getState().handleMessage` | WIRED | Confirmed at `mqttClient.ts:109-111` |
| `usePaneLayout.ts` | `guardedStorage.ts` | persisted read/write | WIRED | Confirmed via must-have artifact check and passing `usePaneLayout.test.ts` |
| `IndexRoute.tsx` | `ThreePaneLayout.tsx` | JSX composition | WIRED | tree/centreTop/centreBottom props supplied |
| `TreeNode.tsx`/`StateBadge.tsx` | `kone-design-system` | `Badge` import | WIRED | `import { Badge } from "kone-design-system"` |
| `treeModel.ts` | `grouping.ts` | `buildGroupIndex`/`rollUpGroup`/`sortedGroupKeys` | WIRED | Confirmed in source |
| `EventHistory.tsx` | `useAppStore.ts` | events slice selector | WIRED | `useAppStore((s) => s.events)` |
| `IndexRoute.tsx` | `useNowTick.ts` | `nowMs` threaded into `buildTree`/selectors | WIRED | Confirmed, and proven stable (no extra re-render churn) by `StaleStability.test.tsx` |
| `App.tsx` | `mqttClient.connect()` | mount-time effect with singleton guard | WIRED | `useEffect(() => { connect(); }, [])`; `mqttClient.ts`'s `if (client) return;` prevents StrictMode double-connect |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `StatsStrip.tsx` | `counts` (StateCounts) | `selectors.ts::makeSelectStateCounts` reading `useAppStore().devices` | Yes — derived from live store, not a static constant | FLOWING |
| `Tree.tsx`/`TreeNode.tsx` | `groups` (TreeGroupNode[]) | `treeModel.ts::buildTree(devices, mode, nowMs, ...)` reading `useAppStore().devices` | Yes — rebuilt from store devices on every render | FLOWING |
| `EventHistory.tsx` | `events` | `useAppStore((s) => s.events)`, populated by `handleMessage` on `lan/events/recent` | Yes — store slice, not hardcoded | FLOWING |
| `ConnectionIndicator.tsx` | `phase`/`delayMs` | `useAppStore((state) => state.connection)`, set by `mqttClient.ts`'s connect/close/offline handlers | Yes — real state machine, independently unit-tested | FLOWING |

No hardcoded empty props or disconnected data sources found in any route-level component.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Production build succeeds and emits a real, non-trivial stylesheet | `npm --prefix dashboard-react run build` | exit 0; `dist/index.html`, `dist/assets/*.css` (27KB, containing real `kone-design-system` tokens) produced | PASS |
| Full test suite passes (independently re-run, not trusted from SUMMARY) | `npm --prefix dashboard-react test -- --run` | 24 files / 163 tests passed | PASS |
| Typecheck is clean | `npm --prefix dashboard-react run typecheck` | exit 0, no output | PASS |
| Python test suite passes (poller ordering regression included) | `uv run pytest -q` | 411 passed | PASS |
| D-34 stale-stability regression passes in isolation | `npx vitest run src/components/StaleStability.test.tsx` | 1/1 passed | PASS |
| Vanilla `dashboard/` untouched by this phase's React work | `git diff --quiet -- dashboard/ scripts/mqtt_poller.py tests/test_mqtt_poller.py` | exit 0 (clean) | PASS |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| DASH-01 | 01, 04, 06, 07, 09 (partial), 10, 11 | Stats strip above centre's primary view (placeholder), merging updates in place | SATISFIED | `StatsStrip`+`MapPlaceholder` composition, wholesale-replace store, `StaleStability.test.tsx` proves in-place update semantics |
| DASH-06 | 03, 05, 08, 09, 11 | Color-codes/icons hosts by device type, groups/colors by folder-derived label | SATISFIED | `TreeNode.tsx` device `typeIcon` + `StateBadgeForState`, `grouping.ts` folder mode, `GroupingControls.tsx` |

No orphaned requirements: `REQUIREMENTS.md`'s Phase 11/11.1 mapping table lists only DASH-01 and DASH-06 for this phase's requirement scope, matching the task's stated Phase requirement IDs and every plan's `requirements:` frontmatter.

### Anti-Patterns Found

None. Scanned all non-test files under `dashboard-react/src/` for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` (case-sensitive) and case-insensitive `coming soon`/`will be here`/`not yet implemented`/`not available` — zero matches except a legitimately-named `CHECKMK_BASE_URL_PLACEHOLDER` constant (a deliberate sentinel value compared against, not a stub marker). No console-log-only handlers, no hardcoded-empty props at any call site, no unexplained `return null`/`[]`/`{}` outside of intentional, documented logic (icon-absent case, empty-group case, staleness-not-applicable case).

### Human Verification Required

None outstanding. This phase's own `checkpoint:human-verify` task (plan 11.1-11, Task 3) already ran with a real human tester and is recorded APPROVED in `11.1-11-SUMMARY.md`, with four live-broker-dependent checks explicitly and reasonably deferred to a real Podman deployment (no broker was available in the verification sandbox). Per this verification run's explicit instruction, that deferral is treated as accepted scope, not a phase-blocking gap — and no evidence was found during this independent re-verification (build, full test suite, typecheck, and direct source reading) suggesting any of the deferred checks would fail. The four deferred items are listed above under "Deferred Items" as informational follow-up.

### Gaps Summary

No gaps. All 14 derived observable truths (from ROADMAP.md's Phase 11.1 goal/summary, the re-worded DASH-01, DASH-06, and the merged must-haves across all 11 plans) verified against the actual codebase — not just SUMMARY claims. Independently re-ran (not merely re-read) the build, the full `dashboard-react` test suite (163/163), typecheck, the full Python suite (411/411), and the isolated D-34 regression test; all passed. Confirmed byte-for-byte non-modification of the vanilla `dashboard/` directory. Confirmed real `kone-design-system` component usage (`NavBar`, `Badge`, `StatTile`, `Select`, `Checkbox`, `StatusBadge`) and real compiled design-system CSS tokens in the production build output, ruling out a superficial/stub integration. The one open item (live-broker-dependent behavior) was explicitly and reasonably deferred by the phase's own human-verify checkpoint due to a sandbox environment constraint, not a code defect, per this run's explicit scope guidance.

---

*Verified: 2026-09-21T19:00:00Z*
*Verifier: Claude (gsd-verifier)*
