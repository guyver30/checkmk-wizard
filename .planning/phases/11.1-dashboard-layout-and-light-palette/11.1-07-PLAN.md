---
phase: 11.1-dashboard-layout-and-light-palette
plan: 07
type: execute
wave: 3
depends_on: [11.1-05, 11.1-06]
files_modified:
  - dashboard-react/src/lib/stateMapping.ts
  - dashboard-react/src/lib/stateMapping.test.ts
  - dashboard-react/src/components/StateBadge.tsx
  - dashboard-react/src/components/StateBadge.test.tsx
  - dashboard-react/src/components/StatsStrip.tsx
  - dashboard-react/src/components/StatsStrip.test.tsx
  - dashboard-react/src/components/ConnectionIndicator.tsx
  - dashboard-react/src/components/ConnectionIndicator.test.tsx
  - dashboard-react/src/store/selectors.ts
  - dashboard-react/src/store/selectors.test.ts
  - dashboard-react/src/routes/IndexRoute.tsx
  - dashboard-react/src/App.tsx
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "The centre-top pane shows a counts-by-state strip ABOVE the topology-map placeholder"
    - "A single incoming device-status message updates the counts without remounting the map placeholder"
    - "Every device state (OK/WARN/CRIT/DOWN/UNREACH/UNKNOWN/PEND/STALE) renders a visually distinct badge"
    - "STALE and UNKNOWN are distinguishable by more than colour alone"
    - "The app header shows a connection-status indicator reflecting the MQTT connection phase"
    - "The MQTT client connects once when the app mounts"
  artifacts:
    - path: "dashboard-react/src/lib/stateMapping.ts"
      provides: "Single device-state -> Badge color/variant/icon mapping (D-44, D-11)"
      contains: "danger"
    - path: "dashboard-react/src/components/StatsStrip.tsx"
      provides: "Counts-by-state strip built on the design-system StatTile (DASH-01)"
      contains: "StatTile"
    - path: "dashboard-react/src/store/selectors.ts"
      provides: "Memo-friendly Zustand selectors for counts-by-state and connection phase"
      contains: "selectStateCounts"
  key_links:
    - from: "dashboard-react/src/components/StateBadge.tsx"
      to: "kone-design-system"
      via: "Badge import"
      pattern: "from \"kone-design-system\""
    - from: "dashboard-react/src/components/ConnectionIndicator.tsx"
      to: "kone-design-system"
      via: "StatusBadge import"
      pattern: "StatusBadge"
    - from: "dashboard-react/src/routes/IndexRoute.tsx"
      to: "dashboard-react/src/store/selectors.ts"
      via: "useAppStore(selectStateCounts)"
      pattern: "selectStateCounts"
---

<objective>
Map device states onto the design system's own status primitives (D-44), and deliver DASH-01's
stats strip above the map placeholder plus DASH-05's connection indicator.

Purpose: D-44 locks "trust the design-system" — no hand-rolled palette. Research answered the open
question: no component has a critical/down variant distinct from generic danger, and `Badge`'s
seven colours are the only surface with enough distinct slots for five device states. D-11's
colour *meanings* are unchanged; only where the values come from changes.
Output: one mapping module, three components, the store selectors they read, and the index/app
wiring.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md
@design-system/src/components/Badge.tsx
@design-system/src/components/Table.tsx
@dashboard-react/src/lib/display.ts
@dashboard-react/src/lib/staleness.ts
@dashboard-react/src/store/useAppStore.ts

<interfaces>
Badge({ color?: "neutral"|"black"|"success"|"warning"|"danger"|"info"|"purple",
        variant?: "outline"|"soft"|"solid", icon?: ReactNode, children })
StatusBadge({ status: "connected"|"low-signal"|"entrapment"|"unknown", children })
StatTile({ label: string, value: ReactNode, icon?: ReactNode, tone?: "neutral"|"danger" })

RESEARCH's answered D-44 mapping (use exactly this; no raw-token extension is needed):
  OK      -> color "success"
  WARN    -> color "warning"
  DOWN    -> color "danger", variant "solid"  (closest to "critical"; max emphasis)
  STALE   -> color "neutral" + a clock icon   (no health judgment implied)
  UNKNOWN -> color "black"  + a question icon (distinct from STALE by colour AND icon)
`purple` is the one colour left unused and is the documented headroom for a future state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device-state to Badge mapping and the StateBadge component</name>
  <files>dashboard-react/src/lib/stateMapping.ts, dashboard-react/src/lib/stateMapping.test.ts, dashboard-react/src/components/StateBadge.tsx, dashboard-react/src/components/StateBadge.test.tsx</files>
  <read_first>
    - design-system/src/components/Badge.tsx (the exact BadgeColor/BadgeVariant unions and the colorMap)
    - .planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md (the "D-44 Research Answer" section, including the recommended-mapping table)
    - dashboard-react/src/lib/display.ts (effectiveState, stateIcon — the locked icon class-name strings)
    - dashboard-react/src/lib/staleness.ts (isDeviceStale — staleness is derived, not a value of `state`)
  </read_first>
  <behavior>
    - `badgeForState("OK")` returns `{ color: "success", variant: "soft" }`.
    - `badgeForState("WARN")` returns `{ color: "warning", variant: "soft" }`.
    - `badgeForState("DOWN")` returns `{ color: "danger", variant: "solid" }`.
    - `badgeForState("CRIT")` returns a `danger` colour distinguishable from DOWN by variant, not identical to it.
    - `badgeForState("UNREACH")` returns a mapping distinct from `DOWN`'s.
    - `badgeForState("STALE")` returns `{ color: "neutral" }` with a non-null icon.
    - `badgeForState("UNKNOWN")` returns `{ color: "black" }` with a non-null icon different from STALE's.
    - `badgeForState("PEND")` returns a neutral mapping and does not throw.
    - `badgeForState(<any unrecognised string>)` falls back to the UNKNOWN mapping rather than throwing.
    - Rendering `<StateBadge device={<a stale device payload>} />` shows STALE, not the payload's own `state`, because staleness overrides.
    - Rendering `<StateBadge device={{host_state_raw:"UNREACH", state:"DOWN"}} />` shows UNREACH — it reads `effectiveState`, never the raw `state` key.
  </behavior>
  <action>
Create `src/lib/stateMapping.ts` exporting `type BadgeSpec = { color: BadgeColor; variant:
BadgeVariant; iconName: string | null; label: string }` and
`badgeForState(state: string): BadgeSpec`, keyed by the full state set OK, PEND, WARN, UNKNOWN,
CRIT, UNREACH, DOWN plus the derived pseudo-state STALE. Follow RESEARCH's table exactly for the
five named states; for the two states the table does not cover, assign CRIT `danger`/`soft` (so it
reads as severe but is visually separable from DOWN's `solid`) and UNREACH `danger`/`outline`, and
record in a comment that both share `danger` because the library has exactly one negative-severity
colour — variant is the only available differentiator, which is a documented library limitation
rather than a design choice. PEND maps to `neutral`/`outline`. Unrecognised input falls back to the
UNKNOWN spec.

`iconName` carries the locked UI-SPEC class-name strings already in `display.ts`'s `stateIcon`
table (`icon-good-filled`, `icon-warning-triangle-filled`, `icon-bad-filled`,
`icon-close-circle-filled`, `icon-question-circle-filled`, `icon-status-unavailable`,
`icon-clock`). Head the file with a comment stating that D-11's colour MEANINGS are unchanged from
Phase 11 — only the source of the values moved to `kone-design-system` (D-44) — and that STALE and
UNKNOWN are deliberately separated by icon as well as colour so the two are never distinguishable
by colour alone.

Create `src/components/StateBadge.tsx` taking `{ device: DevicePayload; nowMs?: number }`. It
computes the state itself: `isDeviceStale(device, nowMs)` first (staleness overrides the reported
state), otherwise `effectiveState(device)` — never `device.state` directly, because `state`
structurally cannot contain UNREACH. It renders the design system's `Badge` with the mapped
`color`/`variant`, an `icon` element carrying the mapped icon class name and `aria-hidden`, and the
state string as its text child. Also export a `StateBadgeForState({ state })` variant taking a
state string directly, for callers (the stats strip, the event rows) that have a state but no
device payload.

Write both test files covering every behavior bullet. Assert on rendered text and on the presence
of the icon class name, not on internal Tailwind class strings (those belong to the library).
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/lib/stateMapping src/components/StateBadge && npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior bullet has a corresponding passing test.
    - A test iterates all eight states (OK, PEND, WARN, UNKNOWN, CRIT, UNREACH, DOWN, STALE) and asserts the set of `${color}/${variant}` pairs contains 8 distinct values — no two states are visually identical.
    - A test asserts STALE's and UNKNOWN's `iconName` values differ AND their colours differ (the not-by-colour-alone requirement).
    - A test renders a device with `staleness: 5` and a `state: "OK"` and asserts the badge reads STALE.
    - `grep -q 'from "kone-design-system"' dashboard-react/src/components/StateBadge.tsx` succeeds and the file defines no hex colour literals (`grep -qE '#[0-9a-fA-F]{6}' dashboard-react/src/components/StateBadge.tsx` fails).
    - `git status --porcelain dashboard/` produces no output.
  </acceptance_criteria>
  <done>One authoritative state-to-visual mapping exists, sourced entirely from the design system's own primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counts-by-state selectors and the stats strip above the map placeholder</name>
  <files>dashboard-react/src/store/selectors.ts, dashboard-react/src/store/selectors.test.ts, dashboard-react/src/components/StatsStrip.tsx, dashboard-react/src/components/StatsStrip.test.tsx, dashboard-react/src/routes/IndexRoute.tsx</files>
  <read_first>
    - dashboard/js/render-index.js (the per-state counts logic D-27 explicitly KEEPS while deleting the grouped fleet overview around it — port the counting, not the cards)
    - dashboard-react/src/store/useAppStore.ts
    - dashboard-react/src/lib/staleness.ts
    - dashboard-react/src/routes/IndexRoute.tsx (plan 06's three-pane composition)
    - design-system/src/components/Table.tsx (the StatTile props and its neutral/danger tone)
  </read_first>
  <behavior>
    - `selectStateCounts` over a devices record of 17 OK, 3 DOWN returns `{OK:17, DOWN:3, WARN:0, CRIT:0, UNKNOWN:0, UNREACH:0, PEND:0, STALE:0}` — every key present, zeros included.
    - A stale device is counted under STALE and NOT under its reported state (staleness is the D-15/D-04 distinct visual state, separate from down).
    - A device whose `host_state_raw` is UNREACH counts under UNREACH, not DOWN.
    - `selectStateCounts` over an empty devices record returns all zeros and does not throw.
    - `selectConnectionPhase` returns the store's current connection phase.
    - The stats strip renders one tile per non-zero state plus OK, and renders the DOWN tile with the `danger` tone.
    - A device-status message that changes one device's state updates the strip's numbers while the map placeholder element keeps the same DOM node identity (merge-in-place, not re-render-from-scratch — DASH-01).
  </behavior>
  <action>
Create `src/store/selectors.ts` exporting `selectStateCounts(state: AppState):
Record<DeviceState | "STALE", number>` and `selectConnectionPhase(state: AppState)`. The count
function walks `state.devices`, applies `isDeviceStale` first (a stale device counts as STALE and
nothing else), then `effectiveState`, and increments. Always return every key with an explicit 0
default so the strip's tile set is stable across renders. Accept an optional `nowMs` via a
selector-factory (`makeSelectStateCounts(nowMs)`) so tests are not clock-flaky; the component
passes a value derived from its own render, not `Date.now()` captured at module load.

Note in a header comment that this is the per-state counts logic from
`dashboard/js/render-index.js`, deliberately separated from the grouped fleet overview that D-27
supersedes and deletes — the counting is KEPT, the cards are not ported.

Create `src/components/StatsStrip.tsx` taking `{ counts }` and rendering a horizontal, wrapping
row of the design system's `StatTile`, one per state, using the state name as `label`, the count as
`value`, and `tone="danger"` for DOWN and CRIT. Always render the OK tile even at zero; render a
non-OK tile only when its count is greater than zero, so the strip stays a thin strip (D-25) rather
than growing eight tiles wide on a healthy fleet. Give the strip a `role="status"` and an
`aria-label` of "Fleet state summary" so a screen reader announces the update.

Modify `src/routes/IndexRoute.tsx` so the `centreTop` slot renders `<StatsStrip counts={...} />`
ABOVE `<MapPlaceholder />` in a vertical flex column where the strip is content-sized and the
placeholder takes the remaining height. This is D-25's locked choice: both the at-a-glance numbers
and the topology area are visible with no control, and the strip is above — not a toggle, not
map-only. Read the counts with
`useAppStore(useCallback(makeSelectStateCounts(nowMs), [nowMs]))` or an equivalent stable selector;
do not compute counts inside the render body of a parent that also owns the placeholder.

Write both test files. For the merge-in-place behavior, render the index route, capture the
`MapPlaceholder` element node, dispatch a device-status message through
`useAppStore.getState().handleMessage(...)` inside `act`, and assert the counts text changed while
`screen.getByText("Topology map — Phase 13")` returns the SAME node object as before (`toBe`, not
`toEqual`).
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/store/selectors src/components/StatsStrip src/routes && npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior bullet has a corresponding passing test.
    - The merge-in-place test uses identity comparison (`toBe`) on the placeholder DOM node across a store update, not a text-equality check.
    - A test asserts a stale device is counted once, under STALE, and is absent from its reported state's count.
    - `screen.getByRole("status", { name: /fleet state summary/i })` resolves in the strip test.
    - `grep -q 'StatTile' dashboard-react/src/components/StatsStrip.tsx` succeeds.
    - The strip renders before the map placeholder in document order (assert via `compareDocumentPosition` or by index in `within(centreTop).getAllByTestId`).
    - `git status --porcelain dashboard/` produces no output.
  </acceptance_criteria>
  <done>DASH-01 is satisfied against the placeholder: a stats strip above the centre's primary view, merging updates in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Connection indicator and app-mount MQTT bootstrap</name>
  <files>dashboard-react/src/components/ConnectionIndicator.tsx, dashboard-react/src/components/ConnectionIndicator.test.tsx, dashboard-react/src/App.tsx</files>
  <read_first>
    - design-system/src/components/Badge.tsx (StatusBadge's four statuses and its dot-plus-text markup)
    - dashboard-react/src/store/mqttClient.ts (connect/disconnect and the singleton guard)
    - dashboard-react/src/store/selectors.ts
    - dashboard-react/src/App.tsx
  </read_first>
  <behavior>
    - Connection phase `"connected"` renders `StatusBadge` with status `connected` and the text "Connected".
    - Phase `"reconnecting"` renders status `low-signal` and text naming the retry delay in seconds.
    - Phase `"disconnected"` renders status `entrapment` and text "Disconnected".
    - Phase `"connecting"` renders status `unknown` and text "Connecting…".
    - The indicator exposes its text to assistive technology via `role="status"`.
    - Mounting `<App />` calls `connect()` exactly once even under StrictMode's double-invoked effects.
  </behavior>
  <action>
Create `src/components/ConnectionIndicator.tsx` reading `selectConnectionPhase` from the store and
rendering the design system's `StatusBadge`. RESEARCH identified `StatusBadge` as the incidental
near-exact fit for DASH-05's connection indicator specifically (a coloured dot paired with text) —
use it rather than reaching for `Badge` or hand-rolling the Phase 11 connection dot, whose ring
engineering (D-39) is explicitly dropped by D-43. Wrap it in an element with `role="status"` and
`aria-live="polite"` so a phase change is announced.

Modify `src/App.tsx` to render `<ConnectionIndicator />` in the `NavBar`'s `actions` slot, and to
call `connect()` from a `useEffect(() => { connect(); }, [])` at the top of the app. The effect
body is safe despite StrictMode's double-invocation because `mqttClient.connect()` carries its own
module-level idempotency guard (plan 05) — state that reason in a comment at the call site so a
future reader does not "fix" it by adding a cleanup that would tear the shared connection down.
Do NOT call `disconnect()` in the effect cleanup: the client is a module singleton intended to
outlive any one component, and a StrictMode cleanup would close it immediately after opening it.

Write `ConnectionIndicator.test.tsx` covering each phase by seeding the store via
`useAppStore.setState`, plus an App-level test that injects a spy `connectFn` through
`mqttClient`'s test seam (or spies on the exported `connect`) and asserts one call after a
StrictMode render.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test && npm --prefix dashboard-react run typecheck && npm --prefix dashboard-react run build</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior bullet has a corresponding passing test.
    - A test asserts the reconnecting state's rendered text contains the delay in seconds taken from the store, not a hardcoded string.
    - A test wrapping `<App />` in `<StrictMode>` asserts `connect` was called exactly once.
    - `grep -q 'StatusBadge' dashboard-react/src/components/ConnectionIndicator.tsx` succeeds.
    - `dashboard-react/src/App.tsx` contains no `disconnect()` call inside a `useEffect` cleanup.
    - The full suite (plans 04–07) passes and `npm --prefix dashboard-react run build` exits 0.
    - `git status --porcelain dashboard/ scripts/mqtt_poller.py` produces no output.
  </acceptance_criteria>
  <done>The app connects to the broker on mount and reports its connection state using the design system's own status primitive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store (MQTT-sourced) → rendered DOM | Device state strings and counts derived from untrusted payloads reach the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-08 | Tampering | `StateBadge` rendering a state string from the wire | mitigate | `badgeForState` falls back to the UNKNOWN spec for any unrecognised input rather than interpolating the raw value into a class name; the state text is rendered as a JSX text node (React escapes it) and never via `dangerouslySetInnerHTML` |
| T-11.1-09 | Denial of Service | `selectStateCounts` over a large devices record | accept | O(fleet) per render over ~21 hosts; the group-index memoisation note in `grouping.ts` already flags the shape to revisit before Phase 12/13 grow the fleet |
</threat_model>

<verification>
- `npm --prefix dashboard-react test` passes
- `npm --prefix dashboard-react run typecheck` exits 0
- `npm --prefix dashboard-react run build` exits 0
- `git status --porcelain dashboard/ scripts/mqtt_poller.py` is empty
</verification>

<success_criteria>
Device states render through the design system's own Badge primitives with STALE and UNKNOWN
separated by more than colour, the stats strip sits above the map placeholder and merges updates in
place (DASH-01), and the header reports the live MQTT connection phase.
</success_criteria>

<output>
Create `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-07-SUMMARY.md` when done
</output>
