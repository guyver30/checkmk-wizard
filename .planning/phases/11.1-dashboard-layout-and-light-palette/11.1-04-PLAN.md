---
phase: 11.1-dashboard-layout-and-light-palette
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - dashboard-react/package.json
  - dashboard-react/package-lock.json
  - dashboard-react/vite.config.ts
  - dashboard-react/vitest.setup.ts
  - dashboard-react/tailwind.config.js
  - dashboard-react/postcss.config.js
  - dashboard-react/tsconfig.json
  - dashboard-react/tsconfig.app.json
  - dashboard-react/tsconfig.node.json
  - dashboard-react/index.html
  - dashboard-react/src/main.tsx
  - dashboard-react/src/App.tsx
  - dashboard-react/src/index.css
  - dashboard-react/src/lib/config.ts
  - dashboard-react/src/routes/IndexRoute.tsx
  - dashboard-react/src/routes/DevicesRoute.tsx
  - dashboard-react/src/routes/DetailsRoute.tsx
  - dashboard-react/src/App.test.tsx
autonomous: false
requirements: [DASH-01]

must_haves:
  truths:
    - "A developer can run `npm --prefix dashboard-react run build` and get a static SPA bundle in dashboard-react/dist/"
    - "The built app renders at least one kone-design-system component with the library's own compiled styles applied"
    - "Navigating to /details?id=<hostname> renders the details route and reads the hostname from the query string"
    - "The existing vanilla dashboard/ directory is byte-for-byte unchanged"
  artifacts:
    - path: "dashboard-react/package.json"
      provides: "Pinned dependency set and build/test/dev scripts"
      contains: "kone-design-system"
    - path: "dashboard-react/vite.config.ts"
      provides: "Vite React SPA build config with react/react-dom dedupe and vitest jsdom environment"
      contains: "dedupe"
    - path: "dashboard-react/tailwind.config.js"
      provides: "Tailwind v3 config duplicating design-system's theme.extend token mapping"
      contains: "var(--color-fg-primary)"
    - path: "dashboard-react/src/App.tsx"
      provides: "react-router Declarative Mode route tree for index/devices/details"
      contains: "Routes"
    - path: "dashboard-react/src/lib/config.ts"
      provides: "Single per-deployment config module (D-37 CHECKMK_BASE_URL placeholder convention)"
      contains: "CHECKMK_BASE_URL"
  key_links:
    - from: "dashboard-react/src/main.tsx"
      to: "kone-design-system/style.css"
      via: "side-effect CSS import"
      pattern: "kone-design-system/style.css"
    - from: "dashboard-react/src/App.tsx"
      to: "react-router"
      via: "BrowserRouter/Routes/Route import"
      pattern: "from \"react-router\""
---

<objective>
Scaffold `dashboard-react/` — a Vite + React 19 + TypeScript + Tailwind-v3 SPA that consumes
`kone-design-system` from a packed tarball, routes with `react-router` v8 Declarative Mode, and
has a working vitest + jsdom test harness.

Purpose: every later Phase 11.1 plan builds inside this scaffold. Getting the version pins,
the design-system consumption path, and the test harness right once removes the whole class of
"Invalid hook call" / "Tailwind silently ignored my config" failures the research found (D-40,
D-41, D-42, RESEARCH Pitfalls 1/2/3).
Output: a buildable, testable, KONE-styled SPA shell with three empty routes and the
per-deployment config module.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md
@.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-CONTEXT.md
@design-system/USAGE.md
@design-system/package.json
@design-system/tailwind.config.js
@dashboard/js/config.js

<interfaces>
<!-- Contracts the executor needs. Use directly — no exploration. -->

kone-design-system public entry (design-system/package.json "exports"):
  "."            -> types dist/index.d.ts, import dist/index.js, require dist/index.cjs
  "./style.css"  -> dist/style.css
  peerDependencies: react >=18, react-dom >=18
  NOTE: the config is NOT exported — `tailwind.config.js` cannot be imported by a consumer.

Components confirmed present and used by later plans (design-system/src/components/):
  Badge({ color?: "neutral"|"black"|"success"|"warning"|"danger"|"info"|"purple",
          variant?: "outline"|"soft"|"solid", icon?: ReactNode, children })
  StatusBadge({ status: "connected"|"low-signal"|"entrapment"|"unknown", children })
  StatTile({ label: string, value: ReactNode, icon?: ReactNode, tone?: "neutral"|"danger" })
  Select({ label?, error?, options: {value,label}[], placeholder?, ...selectHTMLAttributes })
  Checkbox({ label?, size?: "sm"|"md", indeterminate?, ...inputHTMLAttributes })
  Banner({ status?: "info"|"success"|"warning"|"danger", message, icon?, onDismiss? })
  Tooltip({ content, position?: "top"|"bottom"|"left"|"right", children })

Registry versions verified live 2026-09-21 (re-verify with `npm view <pkg> version` before install):
  react 19.3.0 | react-dom 19.3.0 | react-router 8.4.0 | zustand 5.0.15 | mqtt 5.16.0
  vite 8.3.0 | @vitejs/plugin-react 6.1.1 | typescript 5.6.3 (pin; `latest` is 7.0.2 — DO NOT take it)
  tailwindcss 3.4.19 via the `v3-lts` dist-tag (`latest` is 4.3.3 — DO NOT take it)
  vitest 5.0.1 | jsdom 30.1.0 | @testing-library/react 16.3.3 | @testing-library/dom 10.x
  @testing-library/jest-dom 7.0.1
  @vitejs/plugin-react@6 peers vite ^8; vitest@5 peers vite ^6.4||^7||^8 — mutually compatible.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the test-harness packages</name>
  <what-built>
Nothing yet — this gate runs BEFORE any `npm install`. `11.1-RESEARCH.md`'s Package Legitimacy
Audit covers `vite`, `react-router`, `zustand`, `mqtt` and `@vitejs/plugin-react` (all Approved,
no postinstall hooks). It does NOT cover the test-harness packages this plan adds. Planning re-ran
the gate for them on 2026-09-21:

| Package | slopcheck | Postinstall | Repo |
|---------|-----------|-------------|------|
| `vitest` | **[SUS]** — "Suspiciously close to 'vite'. Could be a typosquat." | none | github.com/vitest-dev/vitest |
| `jsdom` | OK | none | github.com/jsdom/jsdom |
| `@testing-library/react` | OK | none | github.com/testing-library/react-testing-library |
| `@testing-library/dom` | OK | none | github.com/testing-library/dom-testing-library |
| `@testing-library/jest-dom` | OK | none | github.com/testing-library/jest-dom |

`vitest`'s [SUS] verdict is a name-proximity heuristic firing on the fact that Vitest is
deliberately named after Vite (it is the Vite team's own test runner). It is NOT auto-approvable —
policy requires a human to confirm it before install.
  </what-built>
  <read_first>
    - .planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md (the "Package Legitimacy Audit" section)
  </read_first>
  <how-to-verify>
1. Open https://www.npmjs.com/package/vitest — confirm the repository link is
   `github.com/vitest-dev/vitest`, weekly downloads are in the millions, and the package is
   published by the `vitest-dev` / `antfu` / `sheremet-va` maintainers (the Vite ecosystem team).
2. Confirm it is NOT a lookalike: the real Vite bundler is `vite` (already approved in research);
   `vitest` is a separate, intentionally-named sibling project, not a typosquat of it.
3. Spot-check https://www.npmjs.com/package/jsdom and
   https://www.npmjs.com/package/@testing-library/react resolve to the repos listed above.
  </how-to-verify>
  <action>Pause before running any `npm install`. Present the table above to the human, wait for an explicit approval of `vitest` (the `[SUS]`-flagged package), and record the verdict in the SUMMARY. This gate is never auto-approvable — `workflow.auto_advance` does not apply to package-legitimacy checkpoints. If the human rejects `vitest`, stop and report rather than substituting a different runner on your own initiative.</action>
  <verify>
    <automated>test ! -d dashboard-react/node_modules && echo "GATE_HELD: nothing installed yet"</automated>
  </verify>
  <acceptance_criteria>
    - The human has explicitly approved installing `vitest`, or named a replacement test runner.
    - No install command has been run before this approval.
  </acceptance_criteria>
  <done>The human has approved (or replaced) `vitest`, and the verdict is recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" to install these packages, or name an alternative test runner.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build and pack kone-design-system, scaffold dashboard-react, install the pinned dependency set</name>
  <files>.gitignore, dashboard-react/package.json, dashboard-react/package-lock.json, dashboard-react/tailwind.config.js, dashboard-react/postcss.config.js, dashboard-react/src/index.css, dashboard-react/tsconfig.json, dashboard-react/tsconfig.app.json, dashboard-react/tsconfig.node.json, dashboard-react/index.html</files>
  <read_first>
    - design-system/USAGE.md (the three consumption methods; method 3 is the one to use)
    - design-system/package.json (exports map, peerDependencies, build scripts)
    - design-system/tailwind.config.js (the full theme.extend block to duplicate)
    - design-system/src/index.css (import order: tokens.css before the @tailwind directives)
    - .gitignore (repo-root ignore list to extend)
    - .planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md (Pitfalls 1, 2, 3, 5)
  </read_first>
  <action>
Build the dependency first, then the consumer — `design-system/dist/` is gitignored and must not
be assumed to exist (RESEARCH Pitfall 2). Run `npm --prefix design-system install` (or `npm ci` if
a lockfile is present) then `npm --prefix design-system run build`, confirming
`design-system/dist/index.js`, `dist/index.d.ts` and `dist/style.css` all exist afterwards. Then
`npm --prefix design-system pack --pack-destination design-system` to produce
`design-system/kone-design-system-0.1.0.tgz`.

Scaffold the consumer with `npm create vite@latest dashboard-react -- --template react-ts` from the
repo root. Do NOT keep Vite's generated demo assets: delete `src/App.css`, `src/assets/`,
`public/vite.svg` and the counter demo body of `src/App.tsx` (Task 3 rewrites `App.tsx`).

Install the packed tarball rather than a relative-path local dependency (RESEARCH Pitfall 1 —
`npm install ../design-system` symlinks and can pull design-system's own React copy into the page,
producing "Invalid hook call"):
`npm --prefix dashboard-react install ../design-system/kone-design-system-0.1.0.tgz`
(note the tarball path is relative to `dashboard-react/`).

Install runtime deps: `react@^19.3.0 react-dom@^19.3.0 react-router@^8.4.0 zustand@^5.0.15
mqtt@^5.16.0`. **Do not install `react-router-dom`** — v8 dropped it; it is a frozen v7 compat shim.

Install dev deps: `typescript@^5.6.3` (pin — `latest` is the from-scratch TypeScript 7 rewrite and
is untested against this toolchain), `vite@^8.3.0`, `@vitejs/plugin-react@^6.1.1`,
`tailwindcss@^3.4.19 postcss@^8.4.47 autoprefixer@^10.4.20` (pin Tailwind to the v3 line — a bare
`npm i tailwindcss` installs v4, whose CSS-native `@theme` config silently ignores a
`tailwind.config.js` file, per RESEARCH Pitfall 3), plus `vitest@^5 jsdom@^30
@testing-library/react@^16 @testing-library/dom@^10 @testing-library/jest-dom@^7`.

Create `dashboard-react/tailwind.config.js` as an ES-module default export (the package is
`"type": "module"`) with `content: ["./index.html", "./src/**/*.{ts,tsx}"]`,
`darkMode: ["selector", '[data-theme="dark"]']`, and a `theme.extend` block copied **verbatim**
from `design-system/tailwind.config.js` (colors fg/bg/scrim/brand/success/warning/alert/secondary/
neutral/purple, borderRadius, boxShadow, fontFamily, fontSize — every entry references a
`var(--color-*)`/`var(--neutral-*)`/`var(--radius-*)`/`var(--shadow-*)`/`var(--font-*)` custom
property that `kone-design-system/style.css` already defines at `:root`). Add a header comment
naming `design-system/tailwind.config.js` as the source of truth and stating that this duplication
exists because the package's `exports` map does not expose the config (RESEARCH Assumption A3), so
the two files must be kept in step by hand. Create the matching `postcss.config.js`
(`tailwindcss` + `autoprefixer` plugins).

Create `dashboard-react/src/index.css` containing the three `@tailwind base; @tailwind components;
@tailwind utilities;` directives only — the design-system's own compiled CSS (with the `:root`
token definitions) is imported separately in `main.tsx` (Task 3) and must load first.

Set `index.html`'s `<title>` to "Checkmk Live Dashboard" and keep the `<div id="root">` mount point.

Extend the repo-root `.gitignore` with `dashboard-react/node_modules/`, `dashboard-react/dist/`,
and `design-system/*.tgz` — the tarball is a build artifact, not a committed dependency.
  </action>
  <verify>
    <automated>test -f dashboard-react/tailwind.config.js && test -f design-system/dist/style.css && grep -q '"kone-design-system"' dashboard-react/package.json && node -e "const p=require('./dashboard-react/package.json');const d={...p.dependencies,...p.devDependencies};if(d['react-router-dom'])throw new Error('react-router-dom must not be installed');if(!/\^?3\./.test(d.tailwindcss))throw new Error('tailwindcss must be pinned to v3: '+d.tailwindcss);if(!/\^?5\./.test(d.typescript))throw new Error('typescript must be pinned to v5: '+d.typescript);console.log('deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `design-system/dist/index.js`, `design-system/dist/index.d.ts` and `design-system/dist/style.css` all exist (the build ran; `dist/` was not assumed).
    - `dashboard-react/node_modules/kone-design-system/` is a real directory containing `dist/`, not a symlink (`test -d` succeeds and `test -L` fails).
    - `dashboard-react/package.json` `dependencies` contains `kone-design-system`, `react`, `react-dom`, `react-router`, `zustand`, `mqtt`, and does NOT contain `react-router-dom`.
    - `dashboard-react/package.json` `devDependencies` pins `tailwindcss` to a `3.x` range and `typescript` to a `5.x` range.
    - `grep -q 'var(--color-fg-primary)' dashboard-react/tailwind.config.js` succeeds, and the file's `theme.extend.colors` has the same top-level keys as `design-system/tailwind.config.js` (fg, bg, scrim, brand, success, warning, alert, secondary, neutral, purple).
    - `git status --porcelain dashboard/` produces no output (the vanilla dashboard is untouched).
    - `git check-ignore -q dashboard-react/node_modules` succeeds.
  </acceptance_criteria>
  <done>The scaffold exists with every dependency at its pinned version, consumed via the packed tarball, and Tailwind v3 is configured against the design-system's token names.</done>
</task>

<task type="auto">
  <name>Task 3: Router shell, per-deployment config module, and the vitest harness</name>
  <files>dashboard-react/src/main.tsx, dashboard-react/src/App.tsx, dashboard-react/src/routes/IndexRoute.tsx, dashboard-react/src/routes/DevicesRoute.tsx, dashboard-react/src/routes/DetailsRoute.tsx, dashboard-react/src/lib/config.ts, dashboard-react/vite.config.ts, dashboard-react/vitest.setup.ts, dashboard-react/package.json, dashboard-react/src/App.test.tsx</files>
  <read_first>
    - dashboard/js/config.js (the constant names and the CHECKMK_BASE_URL placeholder convention to port — D-37)
    - .planning/phases/11.1-dashboard-layout-and-light-palette/11.1-RESEARCH.md (Pattern 2, Assumption A1, and the `?id=` code example)
    - design-system/USAGE.md (the import form: named imports from "kone-design-system" plus "kone-design-system/style.css")
  </read_first>
  <action>
Write `vite.config.ts` exporting `defineConfig({ plugins: [react()], resolve: { dedupe: ["react",
"react-dom"] }, test: { environment: "jsdom", globals: true, setupFiles: ["./vitest.setup.ts"] } })`.
The `dedupe` entry is defensive belt-and-braces against RESEARCH Pitfall 1 even though the tarball
install already avoids the symlink. Add a `/// <reference types="vitest/config" />` triple-slash
directive at the top so the `test` key typechecks. Write `vitest.setup.ts` importing
`@testing-library/jest-dom/vitest`.

Add npm scripts to `dashboard-react/package.json`: `"test": "vitest run"`, `"test:watch":
"vitest"`, `"typecheck": "tsc --noEmit"`. Keep Vite's generated `dev`/`build`/`preview` scripts.

Write `src/lib/config.ts` porting every constant from `dashboard/js/config.js` as named exports:
`WS_PORT = 9002`, `WS_USERNAME = "wsreader"`, `WS_PASSWORD = "wsreader"`,
`POLL_INTERVAL_SECONDS = 60`, `STALENESS_FACTOR = 3`, `HISTORY_MAX_ENTRIES = 20`,
`CHECKMK_BASE_URL = "http://<HOST_IP>:8080"`, `CHECKMK_SITE = "dmc"`. Also export
`export const CHECKMK_BASE_URL_PLACEHOLDER = "http://<HOST_IP>:8080"` and a predicate
`isCheckmkLinkConfigured(): boolean` returning `CHECKMK_BASE_URL !== CHECKMK_BASE_URL_PLACEHOLDER`,
so the D-20 "disable the deep link while the placeholder is unedited" rule has one named home
instead of a string comparison scattered across views. Carry across the header comment explaining
(a) why the broker host is NOT configured here (derived at runtime from `location.hostname`, D-02),
(b) that `WS_USERNAME`/`WS_PASSWORD` are deliberately-committed disposable read-only credentials,
and (c) that `POLL_INTERVAL_SECONDS`/`HISTORY_MAX_ENTRIES` mirror `scripts/mqtt_poller.py`'s
defaults and must be kept in step with it. Note in the comment that this file replaces
`dashboard/js/config.js` for the React app and is the file D-37's deployment-doc callout points at.

Write `src/main.tsx`: import `"kone-design-system/style.css"` FIRST, then `"./index.css"` (order
matters — the library CSS defines the `:root` custom properties that this app's Tailwind utilities
resolve against), then `createRoot(document.getElementById("root")!).render(<StrictMode><App /></StrictMode>)`.

Write `src/App.tsx` using react-router v8 **Declarative Mode**: import `BrowserRouter`, `Routes`,
`Route` from `"react-router"` (NOT `"react-router-dom"`, NOT `"react-router/dom"` — RESEARCH
Assumption A1 flags conflicting docs here; this task's smoke test is the cheap insurance that
resolves it). Render `<BrowserRouter>` wrapping `<Routes>` with `path="/"` -> `IndexRoute`,
`path="/devices"` -> `DevicesRoute`, `path="/details"` -> `DetailsRoute`, and a `path="*"` catch-all
rendering IndexRoute. Add a minimal app header using the design-system's `NavBar` with `appName`
"Checkmk Live Dashboard" and nav items for Overview (`/`) and Devices (`/devices`) — Details is
deliberately NOT in the nav (Phase 11 removed it; the route stays bookmarkable, D-19/D-21).

Write the three route components as minimal placeholders that later plans fill:
`IndexRoute` renders a `<main>` with an `<h1>`; `DevicesRoute` likewise; `DetailsRoute` reads
`const [searchParams] = useSearchParams()` from `"react-router"` and renders the value of
`searchParams.get("id")` (falling back to a "No device selected" message) — this preserves D-19's
`?id=<hostname>` convention on a route instead of a second HTML file (D-41).

Write `src/App.test.tsx` with three tests using `@testing-library/react`'s `render` and `screen`:
(1) rendering `<App />` at `/` shows the index heading; (2) rendering a `MemoryRouter`-wrapped
route tree at `/details?id=sw-edge-01` shows `sw-edge-01`, proving `useSearchParams` reads the query
string; (3) rendering a design-system `Badge` produces an element whose `className` contains
`rounded-pill`, proving the library resolves and its compiled classes are the ones in use. Import
`MemoryRouter` from `"react-router"`.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react run typecheck && npm --prefix dashboard-react test && npm --prefix dashboard-react run build && test -f dashboard-react/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix dashboard-react test` reports 3 passing tests, 0 failing.
    - `npm --prefix dashboard-react run build` exits 0 and `dashboard-react/dist/index.html` exists alongside hashed `assets/*.js` and `assets/*.css` files.
    - `grep -q 'from "react-router"' dashboard-react/src/App.tsx` succeeds and `grep -q 'react-router-dom' dashboard-react/src/App.tsx` fails.
    - `grep -q 'kone-design-system/style.css' dashboard-react/src/main.tsx` succeeds.
    - `grep -q 'CHECKMK_BASE_URL_PLACEHOLDER' dashboard-react/src/lib/config.ts` and `grep -q 'isCheckmkLinkConfigured' dashboard-react/src/lib/config.ts` both succeed.
    - The details-route test asserts on the literal string `sw-edge-01` rendered from the query string, not on a hardcoded prop.
    - `git status --porcelain dashboard/ scripts/mqtt_poller.py` produces no output.
  </acceptance_criteria>
  <done>The SPA builds, all three routes resolve, the `?id=` convention is proven by test, the design-system renders with its own styles, and the deployment config module exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build machine | Third-party package code executes at install and build time |
| design-system tarball → dashboard-react bundle | Vendored internal library code is bundled into the served app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-SC | Tampering | npm installs in Task 2 | mitigate | Task 1 blocking-human legitimacy gate for the `[SUS]`-flagged `vitest` and the three un-audited testing-library packages; `npm view <pkg> scripts.postinstall` confirmed empty for all five at plan time |
| T-11.1-01 | Tampering | Duplicate React instance via symlinked local dependency | mitigate | Packed-tarball consumption (real directory copy) plus `resolve.dedupe: ["react","react-dom"]` in vite.config.ts |
| T-11.1-02 | Information Disclosure | `WS_USERNAME`/`WS_PASSWORD` committed in `src/lib/config.ts` | accept | Deliberate, documented precedent from `dashboard/js/config.js`: disposable credentials whose broker grant is read-only (`topic read lan/#` in `deploy/mosquitto.acl`); trusted-LAN deployment, multi-user auth explicitly out of scope per REQUIREMENTS.md |
</threat_model>

<verification>
- `npm --prefix dashboard-react run typecheck` exits 0
- `npm --prefix dashboard-react test` passes
- `npm --prefix dashboard-react run build` exits 0
- `git status --porcelain dashboard/ scripts/mqtt_poller.py` is empty
</verification>

<success_criteria>
A committed `dashboard-react/` scaffold that builds to static assets, renders KONE-styled
design-system components, routes `/`, `/devices` and `/details?id=` client-side, and runs a
passing vitest suite — with the vanilla `dashboard/` untouched.
</success_criteria>

<output>
Create `.planning/phases/11.1-dashboard-layout-and-light-palette/11.1-04-SUMMARY.md` when done
</output>
