---
phase: 14-fleet-intelligence
plan: 05
type: execute
wave: 4
depends_on: ["14-01", "14-04"]
files_modified:
  - docs/Podman setup for checkmk, minio, mosquitto, worker.md
  - dashboard-react/README.md
  - scripts/mqtt_poller.py
autonomous: false
requirements: [PLR-14, PLR-15, PLR-16, DASH-14, DASH-15]
tags: [docs, live-verification, checkpoint]

must_haves:
  truths:
    - "The deployment doc's MQTT topic contract lists lan/incidents/{incident_id}/status with trigger, QoS 1, retain true, payload keys and tombstone behaviour"
    - "An operator has confirmed on the live 2.4.0p36.cre site whether Livestatus exposes last_state_change, and the poller source records the dated result"
    - "An operator has seen, on the live site, a DOWN-root incident with UNREACH consequences collapse into one card with dimmed consequences, and an unmanaged-switch incident marked 'Inferred, not confirmed'"
    - "Evidence framing (PLR-14/15, DASH-14/15) is verified before any criticality/dependency editing work starts (D-06)"
  artifacts:
    - path: "docs/Podman setup for checkmk, minio, mosquitto, worker.md"
      provides: "incident topic contract row + 'Incident check (Phase 14)' section"
      contains: "lan/incidents/{incident_id}/status"
    - path: "dashboard-react/README.md"
      provides: "'5c. Incidents' section"
      contains: "## 5c. Incidents"
  key_links:
    - from: "docs topic contract"
      to: "scripts/mqtt_poller.py publish_incident()"
      via: "documented payload keys match compute_incidents() output"
      pattern: "worst_criticality"
---

<objective>
Document the incident contract and run the D-06 gate: evidence framing (root-cause collapse, inferred roots, incident cards, dimming) is verified live before criticality/dependency editing (plans 14-07, 14-08) begins. Includes the live `last_state_change` column probe that 14-RESEARCH.md A3 and Pitfall 2 flag.

Purpose: honour the project rule that docs match the implementation, and prove the grouping algorithm (14-RESEARCH.md A2) against a real topology rather than only invented fixtures.
Output: updated deployment doc and dashboard README; a dated live-verification comment in the poller.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.1/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.1/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/14-fleet-intelligence/14-CONTEXT.md
@.planning/phases/14-fleet-intelligence/14-01-SUMMARY.md
@.planning/phases/14-fleet-intelligence/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the incident topic contract and incident behaviour</name>
  <files>docs/Podman setup for checkmk, minio, mosquitto, worker.md, dashboard-react/README.md</files>
  <read_first>
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md (§6 "MQTT topic contract (poller)" table and the paragraphs under it, lines ~371-395; "Topology map check (Phase 13)" section ~506-526; poller smoke test ~437-490)
    - dashboard-react/README.md (sections 5, 5a, 5b)
    - scripts/mqtt_poller.py (compute_incidents docstring, publish_incident, publish_incident_tombstone as implemented by 14-01)
  </read_first>
  <action>
    1. In the deployment doc's topic contract table add a row: `lan/incidents/{incident_id}/status` | "Only when an incident opens, closes, or its root, consequence set, dependents or worst criticality change" | 1 | true | `id`, `root`, `root_state`, `inferred`, `confirmed_down`, `not_observable`, `dependents`, `worst_criticality`, `since`, `timestamp`. Also update the existing `lan/devices/topology` row's payload column to list `{id, parents, device_type, folder, alias, map_position, unmanaged}` (it currently omits the Phase 13 keys — fix while here).
    2. Below the table, add a Phase 14 paragraph: incident id is `incident-{root host id}`; a closed incident is cleared with an empty retained payload; the poller rebuilds incidents from Livestatus every cycle and, after a restart, clears incident topics that are no longer open (no poller-side file); grouping rules in one sentence each (DOWN host whose parent is not an unmanaged switch is a root; UNREACH hosts and DOWN hosts reachable through a contiguous chain of non-OK hosts join the topmost root; an unmanaged switch becomes an *inferred* root when at least two of its children are non-OK and one is DOWN, and then every consequence is reported as `not_observable`; a lone DOWN host under an unmanaged switch is its own incident); `since` comes from Livestatus `last_state_change` and is `null` if the column is absent; `worst_criticality` counts root, consequences and every host that transitively depends on them (host-level criticality only; defaults to `low` until labels are set — criticality/depends_on editing arrives with plan 14-08); known limitation: an unrelated host that is DOWN at the same time inside the same non-OK chain is folded into the incident.
    3. Add a section "### Incident check (Phase 14)" after "Topology map check (Phase 13)": (1) `podman exec mqtt-poller python -u /scripts/mqtt_poller.py --check-columns` should list `last_state_change`; (2) `mosquitto_sub -u wsreader -P wsreader -t 'lan/incidents/#' -v -C 1 -W 5` shows open incidents (times out with none open); (3) how to provoke one safely: in Checkmk, select a host and use Commands > "Fake check results" to set it DOWN (and its children UNREACH), then reverse with "Fake check results" UP / reschedule the host check; (4) expected dashboard result.
    4. dashboard-react/README.md: add "## 5c. Incidents" after 5b: card list above the stats strip, ordering (worst criticality, then longest-open), copy rules (never "not operating"; "not observable" vs "confirmed down"; "Inferred, not confirmed"), dimmed consequences in tree and map, "See incident" link and `/?incident={id}` deep link, empty state text, and that the list caps at 35% of viewport height.
    Every statement must match the code as implemented by plans 14-01..14-04 (read the code, do not paraphrase the plans).
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && grep -n "lan/incidents/{incident_id}/status" "docs/Podman setup for checkmk, minio, mosquitto, worker.md" && grep -n "### Incident check (Phase 14)" "docs/Podman setup for checkmk, minio, mosquitto, worker.md" && grep -n "## 5c. Incidents" dashboard-react/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Topic contract table contains a `lan/incidents/{incident_id}/status` row naming all ten payload keys
    - The `lan/devices/topology` row lists `map_position` and `unmanaged`
    - `grep -n "check-columns" "docs/Podman setup for checkmk, minio, mosquitto, worker.md"` shows the new Incident check step
    - `grep -n "## 5c. Incidents" dashboard-react/README.md` matches
    - `grep -rn "not operating" dashboard-react/README.md` only appears in a sentence stating the dashboard never says it
  </acceptance_criteria>
  <done>Deployment doc and dashboard README describe the incident contract and behaviour exactly as implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification of last_state_change and incident rendering</name>
  <files>scripts/mqtt_poller.py</files>
  <read_first>
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md ("Incident check (Phase 14)" written in Task 1)
  </read_first>
  <what-built>Poller incident engine (14-01), dashboard incident data layer (14-02), dimming (14-03) and incident cards (14-04), plus the docs from Task 1.</what-built>
  <how-to-verify>
    1. On the deploy host, pull this commit and restart the poller: `cd deploy && podman compose restart poller` (the poller mounts `../scripts` read-only, so a restart picks up the new code).
    2. Run `podman exec mqtt-poller python -u /scripts/mqtt_poller.py --check-columns` and report whether `last_state_change` is listed as present.
    3. Run the React dashboard the same way it was run for Phase 13 UAT (e.g. `cd dashboard-react && npm run build && npm run preview -- --host`) and open it.
    4. Managed-root case: pick a managed switch or host that has children (parents set in Phase 13). In Checkmk use Commands > "Fake check results" to set it DOWN and its children UNREACH. Within ~2 poll cycles expect ONE card "{switch} — {duration}", "N not observable", status colour warning/danger per rules; in the tree the children rows are dimmed with "See incident"; on the map the children are faded; clicking a faded map node jumps to `/?incident=...` and highlights the card. Duration should read a real value (not "duration unknown") if step 2 found the column.
    5. Unmanaged case: under an unmanaged switch (Phase 13 Add Node) with at least two children, fake two children DOWN. Expect ONE card rooted at the unmanaged switch with "Inferred, not confirmed" and the children under "not observable"; then restore one child and confirm the card splits into a single-host incident rooted at the remaining DOWN child (no "Inferred" marker).
    6. Restore every faked result; within ~2 cycles the cards disappear and `mosquitto_sub -u wsreader -P wsreader -t 'lan/incidents/#' -v -C 1 -W 5` times out (topics cleared).
    7. Restart the poller while an incident is open, and again after closing it while the poller is stopped; confirm no ghost card survives.
  </how-to-verify>
  <action>After the operator reports results: edit the `"last_state_change"` entry comment in `OPTIONAL_HOST_COLUMNS` (scripts/mqtt_poller.py) to record the dated live result, e.g. "live-verified present on 2.4.0p36.cre on YYYY-MM-DD via `--check-columns` (plan 14-05)" or, if absent, "live-verified ABSENT ... every incident's since is null; duration shows 'duration unknown'". If any grouping result in steps 4-7 differed from expectation, do NOT patch here — record the exact observation in the SUMMARY and stop for gap-closure planning.</action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && grep -n "plan 14-05" scripts/mqtt_poller.py && uv run pytest tests/test_mqtt_poller.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Operator reply recorded in the SUMMARY with the `--check-columns` result and pass/fail per step 4-7
    - `grep -n "plan 14-05" scripts/mqtt_poller.py` matches a dated comment on the last_state_change entry
  </acceptance_criteria>
  <done>Live evidence framing verified (or gaps recorded) and the last_state_change probe result is recorded in source.</done>
  <resume-signal>Type "approved" with the --check-columns result, or describe which step failed and what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → live Checkmk | verification uses Checkmk's own "Fake check results" command on real hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Denial of Service | faked DOWN results triggering real Checkmk notifications | mitigate | doc step tells the operator to restore every faked result and to use hosts whose notifications are acceptable (or schedule a short downtime first) |
| T-14-15 | Information Disclosure | credentials in docs | mitigate | docs reference the existing `wsreader` read-only credential only, as the Phase 13 section already does; no secret values added |
</threat_model>

<verification>
- Docs grep gates pass; poller test suite still passes after the comment edit
</verification>

<success_criteria>
The D-06 gate is passed: evidence framing works on the live site and is documented, so criticality/dependency editing can build on it.
</success_criteria>

<output>
Create `.planning/phases/14-fleet-intelligence/14-05-SUMMARY.md` when done
</output>
