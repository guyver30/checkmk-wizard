---
phase: 14-fleet-intelligence
plan: 09
type: execute
wave: 6
depends_on: ["14-06", "14-07", "14-08"]
files_modified:
  - docs/Podman setup for checkmk, minio, mosquitto, worker.md
  - dashboard-react/README.md
autonomous: false
requirements: [PLR-16, DASH-16, DASH-17]
tags: [docs, live-verification, checkpoint, kiosk, criticality]

must_haves:
  truths:
    - "The deployment doc's topology row lists criticality, service_criticality and depends_on, and a section documents the three host labels and their encoding"
    - "The dashboard README documents criticality/dependency editing and kiosk mode, including launching signage with chromium --kiosk"
    - "An operator has set a host tier, a per-service tier and a depends-on link from the dashboard on the live site, pressed Apply, and seen the labels in Checkmk and the values on lan/devices/topology"
    - "An operator has seen an incident's worst criticality rise to 'high' (one tier below) because a still-UP dependent host is critical (D-15)"
    - "An operator has run /?kiosk=1 and seen chrome-free rotation between incidents and topology"
  artifacts:
    - path: "docs/Podman setup for checkmk, minio, mosquitto, worker.md"
      provides: "Phase 14 label section + kiosk deployment note + updated topology row"
      contains: "service_criticality"
    - path: "dashboard-react/README.md"
      provides: "'5d. Criticality & dependencies' and '5e. Kiosk mode' sections"
      contains: "## 5e. Kiosk mode"
  key_links:
    - from: "docs label section"
      to: "scripts/mqtt_poller.py CRITICALITY_LABEL/SERVICE_CRITICALITY_LABEL/DEPENDS_ON_LABEL"
      via: "documented keys and delimiters match the code"
      pattern: "depends_on"
---

<objective>
Close Phase 14: document the criticality/dependency label model and kiosk mode, then verify DASH-16, the label half of PLR-16 and DASH-17 live, including the label value length spot-check 14-RESEARCH.md asked for and confirmation that the unchanged `topology_editor` role can write labels.

Output: updated deployment doc and dashboard README; recorded live verification.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.1/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.1/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/14-fleet-intelligence/14-06-SUMMARY.md
@.planning/phases/14-fleet-intelligence/14-07-SUMMARY.md
@.planning/phases/14-fleet-intelligence/14-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document criticality/dependency labels and kiosk mode</name>
  <files>docs/Podman setup for checkmk, minio, mosquitto, worker.md, dashboard-react/README.md</files>
  <read_first>
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md (topic contract table as updated by 14-05; "Incident check (Phase 14)"; "Topology map check (Phase 13)")
    - dashboard-react/README.md (sections 5-5c)
    - scripts/mqtt_poller.py (plan 14-07 constants, parsers, topology_nodes)
    - dashboard-react/src/lib/checkmkWrite.ts (plan 14-08 writers and codecs)
    - dashboard-react/src/components/CriticalityEditor.tsx
    - dashboard-react/src/routes/KioskView.tsx, dashboard-react/src/hooks/useKioskRotation.ts, dashboard-react/src/App.tsx
  </read_first>
  <action>
    1. Deployment doc: update the `lan/devices/topology` row payload to `{id, parents, device_type, folder, alias, map_position, unmanaged, criticality, service_criticality, depends_on}` and its trigger text to include a label change. Add a "Criticality and dependency labels (Phase 14)" paragraph under the table: label keys `criticality` (`low|medium|high|critical`, absent = `low`), `service_criticality` (`name=tier;name=tier`), `depends_on` (`host,host`, set on the dependent host), all written by the dashboard's edit mode with the existing `topology_editor` credential and live only after "Apply changes"; invalid values are ignored by the poller; caps of 50 depends_on ids and 200 service entries; hand-editing them in Checkmk (Setup > Hosts > host > Labels) works too; worst_criticality uses host-level tiers only.
    2. Deployment doc: add "### Kiosk / wall screen (Phase 14)": open `http://<HOST_IP>:<dashboard port>/?kiosk=1`; for permanent signage launch the browser itself in OS kiosk mode, e.g. `chromium --kiosk "http://<HOST_IP>:<port>/?kiosk=1"`, because browsers only allow the in-page "Enter full screen" button after a click; keep kiosk screens on the trusted LAN (the SPA bundle embeds the topology_editor secret, Phase 13 posture, even though kiosk renders no edit controls).
    3. dashboard-react/README.md: add "## 5d. Criticality & dependencies" (panel inside Edit topology, device picker + map click selection, Host criticality / Per-service criticality (Default removes the entry) / Depends on, auto-write per change with no per-field button, confirm on removing a dependency, pending count and single Apply, badge palette low/medium/high/critical) and "## 5e. Kiosk mode" (`?kiosk=1`, 20 s rotation, no nav/tree/history/edit controls, Enter full screen button hides after click or 10 s, chromium --kiosk recommendation).
    Every statement must match the implemented code.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && grep -n "service_criticality" "docs/Podman setup for checkmk, minio, mosquitto, worker.md" && grep -n "chromium --kiosk" "docs/Podman setup for checkmk, minio, mosquitto, worker.md" && grep -n "## 5d. Criticality & dependencies" dashboard-react/README.md && grep -n "## 5e. Kiosk mode" dashboard-react/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Topology row in the deployment doc lists `criticality`, `service_criticality`, `depends_on`
    - Deployment doc contains "### Kiosk / wall screen (Phase 14)" with a `chromium --kiosk` example
    - README contains sections 5d and 5e
  </acceptance_criteria>
  <done>Docs describe the label model and kiosk mode as implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification of criticality/dependency editing and kiosk mode</name>
  <files>dashboard-react/README.md</files>
  <read_first>
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md (sections written in Task 1)
  </read_first>
  <what-built>Poller label carriage (14-07), criticality/dependency editor (14-08), kiosk mode (14-06), docs (Task 1).</what-built>
  <how-to-verify>
    1. Restart the poller on the deploy host (`cd deploy && podman compose restart poller`) and run the React dashboard as in plan 14-05.
    2. Turn on "Edit topology", click a host on the map: the "Criticality & dependencies" panel shows it. Set Host criticality to Critical; confirm the pending banner count goes up by one.
    3. For a Linux agent host, set one systemd service to High; for a screen-like host, add two "Depends on" hosts; remove one and confirm the "Remove this dependency?" prompt appears (cancel once, then accept).
    4. Label length spot-check: add depends_on links to 10+ hosts on one host so the label value is long; press "Apply changes". In Checkmk (Setup > Hosts > host > Labels) confirm `criticality:critical`, `service_criticality:<name>=high`, and the full `depends_on:` list are stored unmodified (no truncation, no error), proving the unchanged `topology_editor` role can write labels.
    5. `mosquitto_sub -u wsreader -P wsreader -t lan/devices/topology -C 1 -W 5` shows the new keys on those nodes within about two poll cycles.
    6. Provoke an incident (Checkmk "Fake check results") on a host that the critical screen depends on; the incident card shows worst criticality "high" (the screen is still UP, so it counts one tier below "critical", D-15) and, under View devices, "Dependent devices: ..." lists the screen. Restore the faked result.
    7. Open `/?kiosk=1`: no nav bar, tree, event history or edit switch; the view alternates Incidents/Topology every ~20 s; "Enter full screen" works on click and disappears (or disappears after 10 s untouched).
  </how-to-verify>
  <action>Record the operator's per-step results in the SUMMARY. If step 4 shows any truncation or rejection, record the exact limit observed and stop for gap-closure planning rather than patching here.</action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard/dashboard-react && npm test && cd /home/kone/checkmk-wizard && uv run pytest tests/test_mqtt_poller.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Operator reply recorded in the SUMMARY with pass/fail for steps 2-7, including the observed depends_on label length from step 4
  </acceptance_criteria>
  <done>DASH-16, PLR-16 label carriage and DASH-17 verified live (or gaps recorded).</done>
  <resume-signal>Type "approved", or describe which step failed and what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Checkmk REST | live label writes with the topology_editor credential |
| public screen → dashboard | kiosk deployment guidance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-28 | Elevation of Privilege | topology_editor role | mitigate | step 4 verifies labels are written with the unchanged Phase 13 role; docs state no new permission is needed |
| T-14-29 | Information Disclosure | kiosk screens outside the trusted LAN | mitigate | docs instruct keeping kiosk screens on the trusted LAN and explain why |
| T-14-30 | Denial of Service | faked check results during UAT | mitigate | procedure restores every faked result (same guidance as plan 14-05) |
</threat_model>

<verification>
- Docs grep gates pass; full dashboard and poller test suites pass
</verification>

<success_criteria>
All seven Phase 14 requirements (PLR-14, PLR-15, PLR-16, DASH-14, DASH-15, DASH-16, DASH-17) are implemented, documented and live-verified.
</success_criteria>

<output>
Create `.planning/phases/14-fleet-intelligence/14-09-SUMMARY.md` when done
</output>
