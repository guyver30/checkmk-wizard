---
phase: 14-fleet-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/src/lib/types.ts
  - dashboard-react/src/lib/incidents.ts
  - dashboard-react/src/lib/incidents.test.ts
  - dashboard-react/src/store/useAppStore.ts
  - dashboard-react/src/store/useAppStore.test.ts
  - dashboard-react/src/store/mqttClient.ts
  - dashboard-react/src/store/mqttClient.test.ts
autonomous: true
requirements: [DASH-14, DASH-15]
tags: [dashboard, zustand, mqtt, incidents]

must_haves:
  truths:
    - "The dashboard subscribes to lan/incidents/+/status and receives every open incident as a retained message on connect"
    - "A tombstone (zero-length payload) on an incident topic removes that incident from the store"
    - "A malformed incident payload is dropped without throwing and without blanking the page"
    - "Open incidents are ordered by worst criticality (critical first), then by duration (longest-open first)"
    - "For any host id the dashboard can tell whether it is an incident root, a consequence, or neither, and which incident it belongs to"
  artifacts:
    - path: "dashboard-react/src/lib/incidents.ts"
      provides: "CRITICALITY_TIERS, CRITICALITY_RANK, normalizeIncident, selectOpenIncidents, sortIncidents, buildIncidentLookup, formatIncidentDuration, incidentStatus, consequenceSummary"
      exports: ["CRITICALITY_TIERS", "CRITICALITY_RANK", "normalizeIncident", "selectOpenIncidents", "sortIncidents", "buildIncidentLookup", "formatIncidentDuration", "incidentStatus", "consequenceSummary"]
    - path: "dashboard-react/src/store/useAppStore.ts"
      provides: "incidents: Record<string, IncidentPayload> slice"
      contains: "incidents"
    - path: "dashboard-react/src/store/mqttClient.ts"
      provides: "lan/incidents/+/status subscription"
      contains: "lan/incidents/+/status"
  key_links:
    - from: "dashboard-react/src/store/mqttClient.ts"
      to: "useAppStore.handleMessage"
      via: "SUBSCRIBE_TOPICS entry"
      pattern: "lan/incidents/\\+/status"
    - from: "dashboard-react/src/lib/incidents.ts"
      to: "scripts/mqtt_poller.py CRITICALITY_TIERS"
      via: "hand-kept identical tier tuple"
      pattern: "\"low\", \"medium\", \"high\", \"critical\""
---

<objective>
Build the dashboard-side incident data layer: payload types, the pure incident logic module, the Zustand `incidents` slice, and the MQTT subscription. This is the data foundation DASH-14 (card list) and DASH-15 (dimming) render from, kept a pure MQTT consumer per D-13.

Purpose: one source of truth for criticality ordering and incident membership on the client (14-RESEARCH.md Pitfall 4), built against the payload contract fixed in plan 14-01 so both run in wave 1 in parallel.
Output: `lib/incidents.ts` (+ tests), extended `types.ts`, store slice, subscription.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.1/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.1/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-fleet-intelligence/14-CONTEXT.md
@.planning/phases/14-fleet-intelligence/14-RESEARCH.md
@.planning/phases/14-fleet-intelligence/14-PATTERNS.md
@.planning/phases/14-fleet-intelligence/14-UI-SPEC.md

<interfaces>
Incident payload contract (published by scripts/mqtt_poller.py per plan 14-01):
Topic `lan/incidents/{incident_id}/status`, retained; zero-length payload = tombstone. incident_id = "incident-" + root host id.
JSON keys: id: string, root: string, root_state: string ("DOWN" | "UNREACH" | "UP"), inferred: boolean, confirmed_down: string[] (sorted), not_observable: string[] (sorted), dependents: string[] (sorted), worst_criticality: "low"|"medium"|"high"|"critical", since: ISO-8601 string | null, timestamp: ISO-8601 string.
In an inferred incident every consequence is in not_observable (plan 14-01 D-04 decision).

Topology node additions (published by plan 14-07, typed here so later plans need not touch types.ts): criticality: "low"|"medium"|"high"|"critical"; service_criticality: Record<string, tier>; depends_on: string[].

Existing (dashboard-react/src):
- store/useAppStore.ts: `AppState` (devices, history, services, serviceHistory, events, topology, pollerStatus, lastKnownPollerTimestamp, connection, handleMessage, setConnection); private `parsePayload(buf): {ok:true,value}|{ok:false}` (zero-length → {ok:true,value:null}); `isPlainObject(value)`; per-id wildcard handling at `if (parts[1] === "devices" && parts[3] === "status")`; never `set(..., true)`.
- store/mqttClient.ts: `export const SUBSCRIBE_TOPICS = ["lan/devices/+/status", "lan/devices/+/history", "lan/devices/+/services", "lan/devices/+/service_history", "lan/devices/topology", "lan/events/recent", "lan/poller/status"]`.
- lib/grouping.ts: `export const SEVERITY_RANK: Record<string, number>` with the "kept in step by hand with scripts/mqtt_poller.py" comment convention.
- lib/types.ts: `TopologyNode { id?, parents?, device_type?, folder?, alias?, map_position?, unmanaged? }`; every payload field optional and loosely typed (untrusted JSON).
Tests: vitest `describe`/`it`, `npm test -- <path>` runs `vitest run <path>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IncidentPayload/TopologyNode types and pure lib/incidents.ts</name>
  <files>dashboard-react/src/lib/types.ts, dashboard-react/src/lib/incidents.ts, dashboard-react/src/lib/incidents.test.ts</files>
  <read_first>
    - dashboard-react/src/lib/types.ts
    - dashboard-react/src/lib/grouping.ts (SEVERITY_RANK header-comment convention)
    - dashboard-react/src/lib/treeModel.ts (lines 1-15, "pure functions only" header convention)
    - dashboard-react/src/lib/display.ts (formatRelativeTime, lines 64-90)
    - dashboard-react/src/lib/grouping.test.ts (test style)
    - .planning/phases/14-fleet-intelligence/14-UI-SPEC.md (Copywriting Contract, Incident Card List)
  </read_first>
  <behavior>
    - normalizeIncident({id:"incident-a", root:"a", root_state:"DOWN", inferred:false, confirmed_down:["b"], not_observable:["c"], dependents:[], worst_criticality:"high", since:"2026-09-26T10:00:00+00:00"}) returns an Incident with camelCase fields and the same values.
    - normalizeIncident returns null for: non-object, missing/empty id, missing/empty root; non-array list fields become []; non-string list entries are dropped; unknown worst_criticality becomes "low"; non-string since becomes null; non-boolean inferred becomes false.
    - sortIncidents: [low/old, critical/new, critical/old] → [critical/old, critical/new, low/old]; null since sorts after any real since within the same tier; ties broken by id ascending.
    - selectOpenIncidents(record) normalizes every value, drops nulls, returns sorted.
    - buildIncidentLookup: root → {incidentId, role:"root", inferred}; every confirmed_down and not_observable host → {incidentId, role:"consequence", inferred}; dependents are NOT in the lookup; first incident (in sorted order) wins if a host appears twice.
    - formatIncidentDuration("2026-09-26T10:00:00Z", Date.parse("2026-09-26T10:12:30Z")) === "12 min"; 45 s → "45 s"; 3 h 5 min → "3 h 5 min"; 2 d 4 h → "2 d 4 h"; null or unparseable → "duration unknown"; future since clamps to "0 s".
    - consequenceSummary: N=2,M=4 → "2 confirmed down · 4 not observable"; N=0,M=4 → "4 not observable"; N=2,M=0 → "2 confirmed down"; N=0,M=0 → "" (empty string, root-only incident).
    - incidentStatus: "danger" when confirmedDown.length > 0 or (rootState === "DOWN" and !inferred); otherwise "warning".
    - CRITICALITY_RANK equals {low:0, medium:1, high:2, critical:3}; criticalityRank("bogus") === 0.
    - No exported string anywhere in incidents.ts contains "not operating" (D-04).
  </behavior>
  <action>
    1. types.ts: add `export interface IncidentPayload { id?: string; root?: string; root_state?: string; inferred?: boolean; confirmed_down?: unknown; not_observable?: unknown; dependents?: unknown; worst_criticality?: string; since?: string | null; timestamp?: string; [key: string]: unknown; }` with a doc comment citing plan 14-01's contract and "untrusted JSON; incidents.ts normalizeIncident() is the runtime guard". Extend `TopologyNode` with `criticality?: string; service_criticality?: unknown; depends_on?: unknown;` and a comment "Phase 14, PLR-16 — populated by the poller from Checkmk host labels (plan 14-07)".
    2. Create lib/incidents.ts with the treeModel.ts-style header ("pure functions only; no DOM, broker, storage or network") and exports:
       - `export const CRITICALITY_TIERS = ["low", "medium", "high", "critical"] as const;` and `export type CriticalityTier = (typeof CRITICALITY_TIERS)[number];` with the comment "Mirrors CRITICALITY_TIERS in scripts/mqtt_poller.py — two languages, kept in step by hand, no shared source (same convention as grouping.ts SEVERITY_RANK)".
       - `export const CRITICALITY_RANK: Record<CriticalityTier, number> = { low: 0, medium: 1, high: 2, critical: 3 };`, `export function isCriticalityTier(value: unknown): value is CriticalityTier`, `export function criticalityRank(value: unknown): number` (unknown → 0).
       - `export interface Incident { id: string; root: string; rootState: string; inferred: boolean; confirmedDown: string[]; notObservable: string[]; dependents: string[]; worstCriticality: CriticalityTier; since: string | null; }`
       - `normalizeIncident(raw: unknown): Incident | null`, `sortIncidents(incidents: Incident[]): Incident[]` (returns a new array; comparator uses CRITICALITY_RANK desc, then Date.parse(since) asc with null/NaN last, then id asc), `selectOpenIncidents(record: Record<string, IncidentPayload>): Incident[]`.
       - `export type IncidentRole = "root" | "consequence";` `export interface IncidentMembership { incidentId: string; role: IncidentRole; inferred: boolean; }` `export type IncidentLookup = Map<string, IncidentMembership>;` `buildIncidentLookup(incidents: Incident[]): IncidentLookup`.
       - `formatIncidentDuration(since: string | null, nowMs: number): string` per behavior (units "s", "min", "h", "d"; at most two units).
       - `consequenceSummary(incident: Incident): string` using the exact UI-SPEC copy with the middle dot separator " · ".
       - `incidentStatus(incident: Incident): "danger" | "warning"` per behavior (UI-SPEC Incident Card List status rule; enforces D-04 at the one place alarm weight could leak from pure UNREACH/inferred evidence).
    3. incidents.test.ts: cover every behavior bullet with `describe`/`it` blocks, plus a test reading the module source is NOT needed — instead assert `consequenceSummary` output never contains "operating".
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard/dashboard-react && npm test -- src/lib/incidents.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'export const CRITICALITY_TIERS = \["low", "medium", "high", "critical"\] as const' dashboard-react/src/lib/incidents.ts` matches
    - `grep -n "export function buildIncidentLookup" dashboard-react/src/lib/incidents.ts` matches
    - `grep -n "export interface IncidentPayload" dashboard-react/src/lib/types.ts` matches
    - `grep -n "depends_on?: unknown" dashboard-react/src/lib/types.ts` matches
    - `grep -c "not operating" dashboard-react/src/lib/incidents.ts` == 0
    - `npm test -- src/lib/incidents.test.ts` passes; `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>incidents.ts provides tier ranking, payload normalization, D-12 ordering, host→incident lookup, duration and summary copy, all unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: incidents store slice and lan/incidents/+/status subscription</name>
  <files>dashboard-react/src/store/useAppStore.ts, dashboard-react/src/store/useAppStore.test.ts, dashboard-react/src/store/mqttClient.ts, dashboard-react/src/store/mqttClient.test.ts</files>
  <read_first>
    - dashboard-react/src/store/useAppStore.ts (full file, esp. header comment lines 1-30 and handleMessage lines 111-237)
    - dashboard-react/src/store/useAppStore.test.ts (lines 1-60 for setup/reset style)
    - dashboard-react/src/store/mqttClient.ts (lines 20-40)
    - dashboard-react/src/store/mqttClient.test.ts (lines 50-60)
  </read_first>
  <behavior>
    - handleMessage("lan/incidents/incident-a/status", valid JSON object) → `incidents["incident-a"]` equals the parsed object; other slices unchanged.
    - A second message for the same id replaces the entry wholesale.
    - Zero-length payload → `incidents["incident-a"]` deleted.
    - Invalid JSON or an array payload → previous value kept, no throw.
    - `SUBSCRIBE_TOPICS` contains "lan/incidents/+/status".
  </behavior>
  <action>
    1. useAppStore.ts: import `IncidentPayload`; add `incidents: Record<string, IncidentPayload>;` to `AppState` (with a one-line Phase 14 / D-13 comment), initialize `incidents: {}`, and add a handleMessage branch `if (parts[0] === "lan" && parts[1] === "incidents" && parts[3] === "status")` placed before the devices branches, copying the per-id status branch shape: parse; `!parsed.ok` → return; `parsed.value === null` → copy `get().incidents`, delete `parts[2]`, `set({ incidents })`; non-plain-object → return; else `set({ incidents: { ...get().incidents, [parts[2]]: parsed.value as IncidentPayload } })`. Never `set(..., true)`.
    2. If the store test file has a reset helper that lists initial slices, add `incidents: {}` to it.
    3. mqttClient.ts: append `"lan/incidents/+/status"` to `SUBSCRIBE_TOPICS` (retained replay on SUBACK delivers every open incident, per the file's own header comment).
    4. Tests: add `describe("handleMessage - incidents", ...)` with the four store behaviors; in mqttClient.test.ts add `it("includes the Phase 14 incident wildcard topic", ...)` under the existing `describe("SUBSCRIBE_TOPICS")`.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard/dashboard-react && npm test -- src/store && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'parts\[1\] === "incidents"' dashboard-react/src/store/useAppStore.ts` matches
    - `grep -n "incidents: Record<string, IncidentPayload>" dashboard-react/src/store/useAppStore.ts` matches
    - `grep -n '"lan/incidents/+/status"' dashboard-react/src/store/mqttClient.ts` matches
    - `grep -n "set(.*, true)" dashboard-react/src/store/useAppStore.ts` returns nothing
    - `npm test -- src/store`, `npm run typecheck`, `npm run lint` all exit 0
  </acceptance_criteria>
  <done>Open incidents flow from the broker into a wholesale-replace, tombstone-aware, malformed-tolerant store slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mosquitto WS → browser | incident payloads are untrusted JSON crossing into the SPA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Denial of Service | useAppStore incident branch | mitigate | parsePayload never throws; non-object payloads dropped; last-known-good kept (tests cover invalid JSON and array payloads) |
| T-14-07 | Tampering | incident fields rendered later | mitigate | normalizeIncident type-checks every field, drops non-string list entries, clamps unknown tiers to "low"; React escapes rendered text (no dangerouslySetInnerHTML anywhere in this plan) |
| T-14-08 | Spoofing | fake incidents published by a browser | accept | broker ACL (Phase 8) already makes `wsreader` read-only; nothing in this plan publishes |
</threat_model>

<verification>
- `cd dashboard-react && npm test && npm run typecheck && npm run lint` all pass
</verification>

<success_criteria>
The dashboard holds a live, correctly ordered set of open incidents and a host→incident lookup, ready for plans 14-03 (dimming) and 14-04 (cards).
</success_criteria>

<output>
Create `.planning/phases/14-fleet-intelligence/14-02-SUMMARY.md` when done
</output>
