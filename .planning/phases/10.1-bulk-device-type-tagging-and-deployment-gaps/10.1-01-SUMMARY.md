---
phase: 10.1-bulk-device-type-tagging-and-deployment-gaps
plan: 01
subsystem: api
tags: [checkmk-rest-api, probe, merge-vs-replace, host-attributes]

# Dependency graph
requires:
  - phase: 10-checkmk-tag-group-onboarding-integration
    provides: device_type tag group live-verification precedent and the probe-script pattern (scripts/probe_checkmk_rest_shapes.py)
provides:
  - "scripts/probe_host_attribute_merge.py: a stdlib-only probe, with a dated in-source verdict, answering merge-vs-replace for a partial host_config PUT — VERDICT: REPLACE"
affects: [10.1-03-bulk-retag-implementation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Blocking live-verification checkpoint before implementation, mirroring Phase 10's probe-then-implement discipline"

key-files:
  created: [scripts/probe_host_attribute_merge.py]
  modified: []

key-decisions:
  - "PUT /objects/host_config/{name} with a partial {\"attributes\": {...}} body REPLACES the host's full attribute set — it does not merge. Live-verified 2026-09-12 against Checkmk CE, site id dmc: a two-key PUT (tag_device_type, alias) silently removed ipaddress, tag_agent and tag_snmp_ds."
  - "Plan 10.1-03's retag flow must GET each host's full extensions.attributes, merge tag_device_type/alias into that dict client-side, and PUT the whole dict back with the matching ETag — never send only the changed keys."
  - "The replace-safe echo-PUT fallback (send the full attribute dict back) was ACCEPTED (200) even without stripping meta_data first — this run did not establish any key that must be stripped."

patterns-established: []

requirements-completed: [TAG-04]

# Metrics
duration: 20min
completed: 2026-09-12
---

# Phase 10.1 Plan 01: Merge-vs-Replace Probe Summary

**Live-verified against a real Checkmk CE site (2026-09-12, site `dmc`): a partial `PUT /objects/host_config/{name}` REPLACES the host's attribute set rather than merging — `ipaddress`, `tag_agent` and `tag_snmp_ds` were silently stripped by a PUT naming only `tag_device_type` and `alias`. Plan 10.1-03's retag flow must GET-merge-PUT the full attribute dict.**

## Performance

- **Duration:** ~20 min total (15 min Task 1, ~5 min Task 3; Task 2 was an operator-run live probe)
- **Started:** 2026-09-12T03:25:00Z
- **Completed:** 2026-09-12
- **Tasks:** 3 of 3 completed
- **Files modified:** 1

## Accomplishments
- Wrote `scripts/probe_host_attribute_merge.py`, a stdlib-only (`urllib.request`, `urllib.error`, `json`, `os`, `sys`) diagnostic that creates a throwaway host (`gsd-probe-merge-host`), PUTs a two-key partial attribute update, diffs before/after attributes to determine MERGE vs REPLACE, tests the replace-safe full-attribute echo-PUT fallback, and deletes the throwaway host in a `finally` block.
- The operator ran the probe against the live Checkmk site (site id `dmc`, automation user) and supplied the verbatim `[P1]`..`[P7]` output.
- Recorded the dated, live-verified findings block in the probe's module docstring, replacing the `Findings: not yet run against a live site` placeholder — closing 10.1-RESEARCH.md's Open Question 1 / Assumption A1.
- Confirmed via automated checks: the file parses, contains none of `httpx`/`requests`/`checkmk_wizard` in its import set, contains the required literal strings, `uvx ruff check --no-cache src/ tests/ scripts/` reports exactly 7 findings (unchanged baseline, none in the new file), and `uv run pytest -q` passes 360/360.

## Live Probe Result (from the operator's run, 2026-09-12, site `dmc`)

- **VERDICT: REPLACE.** `device_type` tag group existed on the site, so P4's partial PUT used the strongest body shape (`{"tag_device_type": "other", "alias": "gsd-probe-after"}`). P5's diff: `removed: ['ipaddress', 'tag_agent', 'tag_snmp_ds']`, `changed: ['alias', 'meta_data']`, `added: ['tag_device_type']`. None of the three survival keys made it through.
- **ECHO-PUT: ACCEPTED.** Echoing the host's full `extensions.attributes` dict back — including `meta_data`, without stripping it — returned 200. No key was rejected by Checkmk in this run.
- **Cleanup:** P7's DELETE returned 204; the operator confirmed no `gsd-probe-merge-host` host remains on the site.
- **Not confirmed:** this run used one throwaway host in the root folder with four attributes. It did not exercise a host carrying `snmp_community`, a host in a nested folder, or a folder-inherited attribute — REPLACE semantics for those specific keys/placements remain inferred, not separately observed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the stdlib-only merge-vs-replace probe** - `e73c5e8` (feat)
2. **Task 2: Run the merge-vs-replace probe against the live Checkmk site** - operator-run checkpoint, no commit (verification only); output supplied verbatim above
3. **Task 3: Record the live verdict in the probe's docstring** - `cdb7935` (docs)

**Plan metadata:** pending (this SUMMARY commit)

## Files Created/Modified
- `scripts/probe_host_attribute_merge.py` - stdlib-only probe answering whether `update_host_attributes()`'s partial PUT merges or replaces a host's attribute set. Module docstring now carries the dated `Live-verified against a real Checkmk 2.4.0p36 CE site on 2026-09-12` findings block recording VERDICT: REPLACE, the ECHO-PUT: ACCEPTED result, and the consequence for plan 10.1-03.

## Decisions Made
- Followed the plan exactly for Task 1: env-var-only configuration, no `argparse`, literal `VERDICT:`/`ECHO-PUT:` print statements so the required literal strings are greppable in source.
- Rephrased two docstring lines during Task 1 to avoid the literal substring `checkmk_wizard` in the import-set claim (see Deviations below) — a Rule 3 blocking-issue fix, not a scope change.
- For Task 3, recorded the ECHO-PUT nuance precisely as observed: the fallback was accepted with `meta_data` still present, so no key strip list was established from this run. Deliberately did not claim `meta_data` must be stripped, since nothing was rejected.
- 10.1-RESEARCH.md's Assumption A1 explicitly flagged merge-vs-replace as an open question with both outcomes possible (not a prediction of MERGE) — the REPLACE result is not a contradiction of RESEARCH.md, so no post-mortem-style "predicted X, observed Y" framing was needed; the findings block states the verdict directly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed literal `checkmk_wizard` substring from two docstring lines**
- **Found during:** Task 1, running the acceptance-criteria grep checks
- **Issue:** `grep -c 'import httpx\|import requests\|checkmk_wizard' scripts/probe_host_attribute_merge.py` returned 2, not the required 0, because the docstring referenced `src/checkmk_wizard/api.py` and named `checkmk_wizard` as a package the script does not import — both are true statements, but the grep pattern matches the bare substring anywhere in the file, not just in an `import` line.
- **Fix:** Changed the path reference to `api.py:214-223` (matching this codebase's own convention of citing files by short name) and reworded "Does not import `httpx`, `requests`, or `checkmk_wizard`" to "Does not import `httpx`, `requests`, or the wizard's own installable package."
- **Files modified:** `scripts/probe_host_attribute_merge.py`
- **Verification:** `grep -c 'import httpx\|import requests\|checkmk_wizard' scripts/probe_host_attribute_merge.py` now returns 0
- **Committed in:** `e73c5e8` (part of Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Cosmetic docstring wording only; no change to the probe's behavior, structure, or any of the required literal markers. No scope creep.

## Issues Encountered
None. Task 2's live run required operator credentials this sandbox does not have (no podman, no live Checkmk site) — this was a normal checkpoint pause, not a blocker; the operator supplied the requested output and the plan resumed cleanly.

## Next Phase Readiness
- `scripts/probe_host_attribute_merge.py` now carries the dated, live-verified verdict plan 10.1-03 needs: **REPLACE**, with the GET-merge-PUT consequence stated as a direct instruction.
- Plan `10.1-03`'s retag implementation can now proceed — it must implement a GET-full-attributes → merge `tag_device_type`/`alias` client-side → PUT-full-dict flow, not a two-key partial PUT.
- No blockers carried forward.

---
*Phase: 10.1-bulk-device-type-tagging-and-deployment-gaps*
*Completed: 2026-09-12*

## Self-Check: PASSED

- FOUND: `scripts/probe_host_attribute_merge.py`
- FOUND: commit `e73c5e8`
- FOUND: commit `cdb7935`
