---
context: phase
phase: 09-poller-core
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-09-07T09:04:15.877Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking anti-patterns were discovered this session. Every issue hit during live verification was root-caused and fixed properly (see Decisions below and each fix's own commit message) rather than worked around, so there is nothing here that requires a structural prevention mechanism beyond what those commits already document._

## Critical Anti-Patterns

_None. Remove-this-section note kept per template: there is nothing to acknowledge before resuming._

<current_state>
Phase 9 (poller-core) is a 4-wave, 4-plan phase executed in strict dependency order: 09-01 -> 09-02 -> 09-03 -> 09-04. Waves 1-3 are fully complete, merged to `main`, and verified. Wave 4 (plan 09-04) is running: Task 1 is the blocking human-verify checkpoint, and this session did extensive real live verification on the developer's actual Podman/Checkmk deployment host — turn by turn, running the plan's prescribed commands and reporting verbatim output back. Along the way, 5 real bugs were found and fixed (all merged to `main`, not worked around). Two small pieces of live evidence are still outstanding before Task 1 can be called done and Task 2 (recording results) can start.
</current_state>

<completed_work>

Completed Tasks:
- Plan 09-01 (Wave 1): Poller core foundation — Done, merged (051fdb7).
- Plan 09-02 (Wave 2): MQTT wiring — Done, merged (277d85d).
- Plan 09-03 (Wave 3): Deployability (compose poller service, smoke_test_poller.py, operator docs) — Done, merged (8d77358).
- Plan 09-04 (Wave 4) Task 1: Live verification — In progress, most of it done this session:
  - `--check-columns` run on the real site: `parents`, `tags`, `filename` all confirmed **present** (closes RESEARCH.md Assumptions A1-A3).
  - `check_livestatus_columns`, `topology_quiet`, `poller_liveness` all confirmed `[PASS]` via `smoke_test_poller.py`.
  - Along the way, found and fixed 5 real bugs blocking verification (all merged to `main`):
    1. `deploy/compose.yaml` worker service's bind-mount path was broken by the 09-03 directory restructuring (`./app:/app` should have become `../..:/app` when compose.yaml moved inside the repo checkout) — commit `fff4eac`.
    2. `wizard.py` container-mode host/cmkadmin prompt defaults (now pre-fill `checkmk:5000` and `$CMK_PASSWORD`) — quick task `260907-lwe`, commits `fe710b3`/`a6a4628`.
    3. `CheckmkConnection`/GUI bootstrap helpers had no port support at all — added `host:port` parsing, needed because the official `checkmk/check-mk-raw` image serves on port 5000, not 80 — quick task `260907-m8w`, commits `840d28a`/`b127458`/`10f3404`.
    4. `_activate_pending_changes()` never passed `force_foreign_changes=True`, causing a recurring 401 "foreign changes" error (cmkadmin bootstraps users, `automation` user does everything else) — quick task `260907-mp2`, commits `1c5e375`/`ec4a55a`.
    5. Checkmk container lacked `CAP_NET_RAW` for `check_icmp` (RC 126 on every PING service), plus the deployment host's `net.ipv4.ping_group_range` sysctl (`1 0` default) silently blocked rootless Podman's ICMP relay even after the capability fix — commit `ba45197` + docs quick task `260907-nde`, commit `599efcb`.
  - Full test suite: 304 passed throughout (was 284 at session start).
</completed_work>

<remaining_work>

- Plan 09-04 Task 1: **Awaiting the developer** for two final pieces of live evidence:
  1. A clean re-run of `smoke_test_poller.py` now that hosts are onboarded (via `checkmk-wizard`) and activation/PING both work — the last recorded run showed `device_status_retained`/`topology_retained` as `[FAIL]` only because the site had zero hosts at that point.
  2. The manual tombstone test (delete a host in the Checkmk UI, activate, wait one poll interval, `mosquitto_sub` on `lan/#`) — not yet attempted this session.
- Plan 09-04 Task 2 (auto, blocked on the above): record the confirmed Livestatus column set with a dated "Live-verified against a real Checkmk 2.4.0p36.cre CE site" citation in `scripts/mqtt_poller.py` (following `api.py`'s citation convention), apply any corrections the final smoke-test/tombstone results reveal, correct the setup doc's "Manual tombstone test" paragraph if observed behavior differs, re-run `uv run pytest -q` and `uv run ruff check`, then create `09-04-SUMMARY.md`.
- After 09-04 completes: merge its worktree, re-run full test suite, `roadmap.update-plan-progress "09" "09-04" "complete"`, commit tracking update, then continue execute-phase's remaining steps — `code_review_gate` (`bm:code-review` skill on phase 09), `verify_phase_goal` (spawn `bm:gsd-verifier`), and `update_roadmap` to mark Phase 9 complete.
</remaining_work>

<decisions_made>

- Fixed all 5 bugs found during live verification properly (root cause, in the repo) rather than deferring or working around them on the host alone, since each was a real defect in this repo's own code or deploy config that would hit the next person following the same docs. Each has its own dated citation/why-comment in its commit per this repo's conventions.
- Did NOT auto-approve plan 09-04 Task 1's checkpoint despite `AUTO_MODE=true`, and will not mark it done without the final smoke-test SUMMARY line and tombstone-test result, even though every individual fix behind them is now confirmed working — the plan's stated philosophy is "nothing here is done until a live site says so," not inference from fixed components.
- `net.ipv4.ping_group_range` and the `NET_RAW` capability are both rootless-Podman properties, unrelated to VM-vs-bare-metal — confirmed this explicitly with the user after they initially suspected VMware NAT (which was ruled out earlier in the session by testing ping from the VM host itself, outside any container).
</decisions_made>

<blockers>
- Plan 09-04 Task 1 needs the developer to run 2 more commands on the real deployment host and report verbatim output — see Human Actions Pending below. No workaround; must be a human running these.
</blockers>

## Required Reading (in order)
1. `.planning/phases/09-poller-core/09-04-PLAN.md` — the full plan, especially Task 2's `<action>` for exactly what to do with the developer's reported output
2. `.planning/phases/09-poller-core/09-RESEARCH.md` Assumptions Log (A1-A4) — the specific claims Task 2 closes out
3. `src/checkmk_wizard/api.py` lines ~200-215 — the "Live-verified against a real Checkmk 2.4.0p35 CE site: ..." citation convention to match
4. `src/checkmk_wizard/site.py` lines ~191-202 — the dated post-mortem comment convention for corrected-after-the-fact behavior
5. This session's transcript (or `git log --oneline -20`) for the 5 bug-fix commits above — Task 2 should reference the real Checkmk version now confirmed live (`2.4.0p36.cre`, not `2.4.0p35` as RESEARCH.md assumed) when writing its citation

## Human Actions Pending (blocking)

1. Run `podman compose exec poller python /scripts/smoke_test_poller.py --host mosquitto --livestatus-host checkmk --skip-restart-checks` and report the full `[SUMMARY]` line plus any remaining `[FAIL]` lines.
2. Manually delete a host in the Checkmk UI, activate changes, wait one poll interval, then run `mosquitto_sub -h localhost -p 1883 -u poller -P poller -t 'lan/#' -v -C 20` and report whether the deleted host's status payload goes empty, drops from `lan/devices/topology`, and a `removed` event appears in `lan/events/recent`.

## Infrastructure State
- Deployment host (real, developer-owned): `checkmk`, `mosquitto`, `mqtt-poller`, `automation-worker` containers all up and working after this session's fixes; `minio` exited (unrelated, pre-existing, not touched). Checkmk site `dmc` has onboarded hosts (via `checkmk-wizard` run in container mode from the `worker` container) with activated changes and working PING/ICMP checks.
- Working tree has pre-existing, unrelated uncommitted state that predates this session and was left untouched throughout: `.planning/config.json` modified, `PROMPT_LOG.md` modified (this session's own prompt log entries), untracked `docs/src/`, `screen1.png`, `.planning/.pending-auth-captures.jsonl`.

<context>
This session resumed directly from an earlier `/bm:pause-work` handoff for this exact same plan/task (09-04 Task 1). Unlike that earlier session (sandboxed, no real deployment access), this session had the developer running every command live on their real Podman/Checkmk host and reporting output back turn by turn, which turned into an extended debugging chain: 5 separate real bugs surfaced and were fixed one at a time as each blocked the next verification step (worker mount path -> host/password prompt defaults -> REST port support -> activation 401 -> PING/ICMP). All are now merged to `main` with tests green. The only remaining gap is two pieces of live evidence the plan requires before Task 2 can honestly claim the site's behavior was confirmed rather than inferred.
</context>

<next_action>
Start with: Ask the user (or wait for them) to run the 2 remaining verification steps above on their real deployment host, then paste back the verbatim smoke-test `[SUMMARY]` line and the tombstone-test result. Do not proceed to Task 2 or infer these results from the other now-confirmed-working fixes.
</next_action>
