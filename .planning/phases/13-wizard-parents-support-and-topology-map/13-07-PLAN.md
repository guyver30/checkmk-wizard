---
phase: 13-wizard-parents-support-and-topology-map
plan: 07
type: execute
wave: 4
depends_on: [13-06]
files_modified:
  - dashboard-react/src/components/TopologyToolbar.tsx
  - dashboard-react/src/components/TopologyToolbar.test.tsx
  - dashboard-react/src/hooks/useEditIdleTimeout.ts
  - dashboard-react/src/hooks/useEditIdleTimeout.test.ts
  - dashboard-react/src/routes/IndexRoute.tsx
  - dashboard-react/src/routes/IndexRoute.test.tsx
autonomous: true
requirements: [DASH-12]
tags: [dashboard, edit-mode, activation, ux]

must_haves:
  truths:
    - "A kone-design-system Switch labelled 'Edit topology' sits between the stats strip and the map. It is off on every page load (D-02), and disabled while TOPOLOGY_EDITOR_SECRET is still the placeholder"
    - "While edit mode is on and at least one change is pending, a Banner reads '{N} change(s) not yet applied' and 'Apply changes' is enabled. In read-only mode the row shows only the Switch"
    - "Apply changes runs activateChanges() exactly once per press, shows 'Applying…' while in flight, then either shows 'Topology updated' and resets the count, or shows the persistent 'Saved, but not live yet' error and keeps the count"
    - "Edit mode switches itself off after 5 minutes without map interaction, and says so"
    - "Edit/write failures from the map surface as a visible Snackbar, never silently"
  artifacts:
    - path: "dashboard-react/src/components/TopologyToolbar.tsx"
      provides: "Switch + pending Banner + Apply Button row"
      exports: ["TopologyToolbar"]
    - path: "dashboard-react/src/hooks/useEditIdleTimeout.ts"
      provides: "Idle auto-exit timer with touch() reset"
      exports: ["useEditIdleTimeout", "EDIT_IDLE_TIMEOUT_MS"]
  key_links:
    - from: "IndexRoute Apply handler"
      to: "checkmkWrite.activateChanges"
      via: "onApply"
      pattern: "activateChanges\\("
    - from: "IndexRoute"
      to: "TopologyMap editMode/onEditSaved/onEditFailed"
      via: "props"
      pattern: "onEditSaved="
---

<objective>
Wire the edit-mode UI around the map: the D-02 toggle, the pending-changes indicator, the single batched "Apply changes" activation (RESEARCH Pitfall 3), success/error Snackbars, and an idle auto-exit.

Idle auto-exit decision: IMPLEMENT, at 5 minutes. UI-SPEC recommended it and RESEARCH's Security Domain names "a tab left in edit mode" as the residual risk after D-02, so the planner locks it in rather than deferring. It is small, local UI state and needs no new dependency.

Purpose: completes DASH-12. After this plan, an operator can toggle edit mode, draw edits (13-06) and make them live with one Apply.
Output: `TopologyToolbar.tsx`, `useEditIdleTimeout.ts`, IndexRoute wiring, and tests.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-06-SUMMARY.md

<interfaces>
From plan 13-06, dashboard-react/src/components/TopologyMap.tsx:
- TopologyMap({ topologyDevices, statuses, nowMs, editMode?: boolean, onEditSaved?: () => void, onEditFailed?: (f: { title: string; body: string }) => void })

From plan 13-05:
- checkmkWrite.activateChanges(): Promise<void>   (throws CheckmkWriteError)
- checkmkWrite.countPendingChanges(): Promise<number>
- config.isTopologyEditingConfigured(): boolean

From kone-design-system (design-system/src/components):
- Switch(props: InputHTMLAttributes & { label?: string; size?: "sm"|"md"|... })   // checked/onChange like a checkbox; role="switch"
- Banner({ status?: "info"|"success"|"warning"|"danger"; message: ReactNode; icon?; onDismiss? })
- Button({ variant?: "primary"|"secondary"|"tertiary"|"destructive"|"neutral"; size?; loading?: boolean; ...button attrs })
- Snackbar({ status?: "info"|"success"|"warning"|"danger"; message: ReactNode; actionLabel?: string; onAction?: () => void; icon? })  // static; our code owns timing

From dashboard-react/src/routes/IndexRoute.tsx: route-level useState pattern for UI-session state (openKeys), centreTop = flex-col gap-2 p-3 [StatsStrip, div.min-h-0.flex-1 > TopologyMap]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TopologyToolbar component and useEditIdleTimeout hook</name>
  <files>dashboard-react/src/components/TopologyToolbar.tsx, dashboard-react/src/components/TopologyToolbar.test.tsx, dashboard-react/src/hooks/useEditIdleTimeout.ts, dashboard-react/src/hooks/useEditIdleTimeout.test.ts</files>
  <read_first>
    - design-system/src/components/Switch.tsx, Banner.tsx, Button.tsx (prop shapes, loading prop)
    - dashboard-react/src/components/GroupingControls.tsx and GroupingControls.test.tsx (control-row component + test style)
    - dashboard-react/src/hooks/useNowTick.ts and useNowTick.test.ts (timer hook + fake-timer test style)
    - .planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md sections "Copywriting Contract", "Topology Map Rendering → Toolbar row", "Apply / Activation Flow", "Spacing Scale"
  </read_first>
  <behavior>
    - TopologyToolbar with editMode false renders only a switch named "Edit topology" (getByRole("switch", {name: "Edit topology"})), unchecked, with no Banner or Apply button in the DOM
    - with editMode true and pendingCount 0: the Apply button "Apply changes" is disabled and no Banner is shown
    - pendingCount 1 → Banner text "1 change not yet applied"; 3 → "3 changes not yet applied"; Apply is enabled with variant primary
    - applying true → the button shows "Applying…" and is in its loading state
    - toggling the switch calls onEditModeChange(!editMode); clicking Apply calls onApply once
    - editingConfigured false → the switch is disabled and the text "Editing is off until TOPOLOGY_EDITOR_SECRET is set in src/lib/config.ts." is shown
    - useEditIdleTimeout(active=true, onTimeout): after 5 min (fake timers) onTimeout fires once; calling touch() at 4 min restarts the window (no fire at 5 min, fires at 9 min); active=false never fires and clears any timer on unmount
  </behavior>
  <action>
Create `TopologyToolbar({ editMode, onEditModeChange, pendingCount, applying, onApply, editingConfigured })`. Layout: a single row, `flex items-center gap-2 p-3 bg-bg-subtle rounded-md` (UI-SPEC spacing sm/md; secondary-surface background). Contents:
- The kone-design-system `Switch` with label "Edit topology" (the locked wording), `checked={editMode}`, `disabled={!editingConfigured}`.
- When `editMode && pendingCount > 0`: a `Banner` with status "warning" and message `${pendingCount} change${pendingCount === 1 ? "" : "s"} not yet applied`.
- When `editMode`: a `Button` with variant "primary", `disabled={pendingCount === 0 || applying}`, `loading={applying}`, and label "Applying…" while applying, else "Apply changes".
- When `!editingConfigured`: a `text-xs text-fg-tertiary` span with the exact configuration hint from the behavior block. This hint is planner-authored; UI-SPEC does not cover the unconfigured state. Record it in the SUMMARY.

In read-only mode, render nothing else and reserve no space (UI-SPEC).

Create `dashboard-react/src/hooks/useEditIdleTimeout.ts`, exporting `EDIT_IDLE_TIMEOUT_MS = 5 * 60 * 1000` and `useEditIdleTimeout(active: boolean, onTimeout: () => void, timeoutMs = EDIT_IDLE_TIMEOUT_MS): { touch: () => void }`. It uses a single setTimeout held in a ref: armed when active becomes true, re-armed by touch(), cleared when active becomes false and on unmount. Keep the latest onTimeout in a ref. Its header comment cites UI-SPEC "Idle-timeout auto-exit" and RESEARCH's Security Domain residual-risk row as the reason it exists.

Write both test files first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/components/TopologyToolbar.test.tsx src/hooks/useEditIdleTimeout.test.ts && npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"Edit topology"' dashboard-react/src/components/TopologyToolbar.tsx` matches, and so do `grep -n "Apply changes"`, `grep -n "Applying…"` and `grep -n "not yet applied"`
    - `grep -n "EDIT_IDLE_TIMEOUT_MS = 5 \* 60 \* 1000" dashboard-react/src/hooks/useEditIdleTimeout.ts` matches
    - Both test files pass, with at least 7 toolbar and 3 hook `it(` cases; the typecheck exits 0
  </acceptance_criteria>
  <done>The toolbar row and idle timer exist as tested, self-contained units.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire edit mode, Apply, Snackbars and idle exit into IndexRoute</name>
  <files>dashboard-react/src/routes/IndexRoute.tsx, dashboard-react/src/routes/IndexRoute.test.tsx</files>
  <read_first>
    - dashboard-react/src/routes/IndexRoute.tsx (after 13-03)
    - dashboard-react/src/routes/IndexRoute.test.tsx
    - dashboard-react/src/components/TopologyMap.tsx (13-06 props)
    - dashboard-react/src/components/TopologyToolbar.tsx and dashboard-react/src/hooks/useEditIdleTimeout.ts (Task 1)
    - dashboard-react/src/lib/checkmkWrite.ts (activateChanges, countPendingChanges)
    - design-system/src/components/Snackbar.tsx
    - .planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md "Apply / Activation Flow" and "Copywriting Contract"
  </read_first>
  <behavior>
    - initial render: the switch is unchecked; TopologyMap receives editMode false
    - turning the switch on calls countPendingChanges once and shows its result as the pending count (e.g. mocked 2 → "2 changes not yet applied"); if countPendingChanges rejects, the count stays at the client value without an error Snackbar
    - an onEditSaved call from TopologyMap (invoke via the fake network's manipulation callbacks, or by rendering with a mocked TopologyMap) increments the count by 1
    - Apply: activateChanges resolved → Snackbar "Topology updated" is shown, the count resets to 0, the Banner disappears, and the Snackbar is gone after 3 s (fake timers)
    - Apply: activateChanges rejected → Snackbar containing "Saved, but not live yet" and "Your changes are stored but Activate Changes failed. Press Apply changes again, or finish activation directly in Checkmk." stays after 10 s; its "Dismiss" action removes it; the count is unchanged
    - onEditFailed({title, body}) → a danger Snackbar showing title and body, persistent until dismissed
    - 5 minutes without pointer/wheel/key interaction inside the map area while in edit mode → the switch turns off and an info Snackbar reads "Edit topology turned off after 5 minutes of inactivity"; a pointerdown inside the map area at 4 min postpones it
  </behavior>
  <action>
In IndexRoute.tsx, add route-level `useState` for `editMode` (initial false, never persisted: D-02 requires it to be off on every load), `pendingCount` (0), `applying` (false) and `snackbar` (`{ status: "success" | "danger" | "info"; message: ReactNode; autoDismissMs?: number } | null`). This follows the existing "UI-session state lifted into the route, not the Zustand store" rule (PATTERNS.md; the openKeys precedent).

- `onEditModeChange(next)`: set editMode. When turning on, call `countPendingChanges()` and set pendingCount from it; on rejection keep the current count (best-effort; the Banner count is informational).
- `onEditSaved`: `setPendingCount(c => c + 1)` and touch the idle timer.
- `onEditFailed({ title, body })`: a danger Snackbar whose message is title (font-semibold) plus body, persistent.
- `onApply`: guard against re-entry while applying. Set applying true and await `activateChanges()` exactly once. On success, set pendingCount 0 and show a success Snackbar "Topology updated" with autoDismissMs 3000. On failure, show a danger Snackbar with "Saved, but not live yet" and the Copywriting Contract body, persistent, with pendingCount unchanged. Finally set applying false. Never retry in the background (UI-SPEC "best-effort must be visible").
- Idle: `const { touch } = useEditIdleTimeout(editMode, () => { setEditMode(false); setSnackbar({ status: "info", message: "Edit topology turned off after 5 minutes of inactivity" }) })`. Attach `onPointerDown`, `onWheel` and `onKeyDown` to the div that wraps the toolbar and map, each calling touch().
- Snackbar rendering: when snackbar is set, render kone-design-system `Snackbar` inside `fixed bottom-4 right-4 z-50`, with actionLabel "Dismiss" and onAction clearing it. A `useEffect` clears it after `autoDismissMs` when that is set.
- Layout: centreTop becomes `flex h-full flex-col gap-2 p-3` > `StatsStrip` > the interaction-tracking wrapper `flex min-h-0 flex-1 flex-col gap-2` > `TopologyToolbar` (editingConfigured from `isTopologyEditingConfigured()`) > `div.min-h-0.flex-1` > `TopologyMap` with `editMode`, `onEditSaved` and `onEditFailed`. This matches UI-SPEC "toolbar row between StatsStrip and the map canvas". Change nothing in the tree or centreBottom.

Extend IndexRoute.test.tsx with the behavior cases. Mock `../lib/checkmkWrite` with vi.mock. For `isTopologyEditingConfigured`, mock `../lib/config` partially to return true, keeping the real constants via `vi.importActual`. Keep every existing IndexRoute test passing.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test && npm --prefix dashboard-react run typecheck && npm --prefix dashboard-react run lint && npm --prefix dashboard-react run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activateChanges(" dashboard-react/src/routes/IndexRoute.tsx` is exactly 1
    - `grep -n "Topology updated" dashboard-react/src/routes/IndexRoute.tsx` matches, and so do `grep -n "Saved, but not live yet"` and `grep -n "Edit topology turned off after 5 minutes of inactivity"`
    - `grep -n "useState(false)" dashboard-react/src/routes/IndexRoute.tsx` shows editMode initialised false, and there is no localStorage/guardedStorage use for editMode (`grep -n "editMode" dashboard-react/src/routes/IndexRoute.tsx | grep -i storage` returns nothing)
    - `grep -n "onEditSaved=" dashboard-react/src/routes/IndexRoute.tsx` matches, and so does `grep -n "onEditFailed="`
    - The full suite, typecheck, lint (oxlint) and build all pass
  </acceptance_criteria>
  <done>An operator can switch edit mode on, see the pending count, press Apply once to activate everything, and get clear success/failure feedback; edit mode never lingers unattended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser Apply action → Checkmk activation | One click promotes all pending config to live monitoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | edit mode left on at a kiosk | mitigate | Off on every load (not persisted); 5-minute idle auto-exit (tested); Apply is a separate deliberate click |
| T-13-17 | Elevation of Privilege | Apply activating others' changes | mitigate | activateChanges hard-codes force_foreign_changes false (13-05); a failure is surfaced as "Saved, but not live yet" pointing to Checkmk's own UI |
| T-13-19 | Denial of Service | repeated Apply presses | mitigate | Re-entry guard while applying, button disabled/loading, the activation poll is bounded (13-05) |
| T-13-23 | Repudiation / Information | silent write failures | mitigate | Every onEditFailed/Apply failure shows a persistent danger Snackbar; nothing is retried or swallowed silently |
</threat_model>

<verification>
- The full dashboard suite, typecheck, lint and build pass
</verification>

<success_criteria>
DASH-12 is complete end-to-end in the UI: D-02 toggle, client-side pending count, a single batched Apply with the UI-SPEC copy and states, visible failures, and an idle auto-exit.
</success_criteria>

<output>
Create `.planning/phases/13-wizard-parents-support-and-topology-map/13-07-SUMMARY.md` when done
</output>
