---
phase: 13-wizard-parents-support-and-topology-map
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - dashboard-react/src/components/TopologyMap.tsx
  - dashboard-react/src/components/TopologyMap.test.tsx
  - dashboard-react/src/test/fakeVisNetwork.ts
  - dashboard-react/vitest.setup.ts
  - dashboard-react/src/routes/IndexRoute.tsx
  - dashboard-react/src/routes/IndexRoute.test.tsx
  - dashboard-react/src/App.test.tsx
  - dashboard-react/src/components/StaleStability.test.tsx
  - dashboard-react/src/components/ThreePaneLayout.test.tsx
  - dashboard-react/src/components/MapPlaceholder.tsx
autonomous: true
requirements: [DASH-07]
tags: [dashboard, vis-network, react]

must_haves:
  truths:
    - "index route's centre shows a live vis-network topology map in the exact slot MapPlaceholder occupied, below the stats strip"
    - "Hosts render as state-coloured circularImage icons, with parent->child arrows derived from the topology payload's parents"
    - "Incoming topology/status updates patch the existing DataSets in place (DataSet.update/add/remove). The Network is constructed exactly once per mount and network.setData is never called"
    - "Physics stabilizes once, then freezes (Phase 11 D-09); nodes with a saved map_position are placed there and excluded from physics; all others start on the D-01 grid"
    - "Clicking a node while not in edit mode navigates to /details?id={id} (D-10)"
    - "With devices present but zero edges and edit mode off, a dismissible banner reads 'No connections drawn yet — turn on Edit topology to start.'"
  artifacts:
    - path: "dashboard-react/src/components/TopologyMap.tsx"
      provides: "Imperative vis-network mount + in-place DataSet sync + click navigation"
      exports: ["TopologyMap"]
    - path: "dashboard-react/src/test/fakeVisNetwork.ts"
      provides: "jsdom-safe Network test double registered globally in vitest.setup.ts"
  key_links:
    - from: "dashboard-react/src/routes/IndexRoute.tsx"
      to: "TopologyMap"
      via: "centreTop slot, fed by useAppStore topology.devices + devices + nowMs"
      pattern: "<TopologyMap"
    - from: "dashboard-react/src/components/TopologyMap.tsx"
      to: "buildMapModel / withGridPositions / nodeVisual"
      via: "import from lib/topologyLayout and lib/mapIcons"
      pattern: "buildMapModel\\("
    - from: "dashboard-react/src/components/TopologyMap.tsx"
      to: "vis-data DataSet.update"
      via: "sync effect"
      pattern: "\\.update\\("
---

<objective>
Replace MapPlaceholder with a live, read-only vis-network topology map that renders useAppStore's existing `topology` slot and merges every update in place. This delivers DASH-07.

Purpose: DASH-07 is the phase's mapped requirement. Edit mode (plans 13-06/13-07) builds on this component; it does not replace it.
Output: `TopologyMap.tsx` plus its tests, a shared jsdom-safe vis-network test double, the IndexRoute swap, and every placeholder-referencing test updated. `MapPlaceholder.tsx` is deleted.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-wizard-parents-support-and-topology-map/13-CONTEXT.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-PATTERNS.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-02-SUMMARY.md

<interfaces>
From plan 13-02, dashboard-react/src/lib/topologyLayout.ts:
- buildMapModel(topologyDevices: unknown[], statuses: Record<string, DevicePayload>, nowMs: number): { nodes: MapNode[]; edges: MapEdge[] }
- withGridPositions(nodes: MapNode[]): Array<MapNode & { x: number; y: number; saved: boolean }>
- MapNode { id: string; label: string; deviceType: string; state: string; position: {x,y} | null; unmanaged: boolean }
- MapEdge { id: string; from: string; to: string }   // id = `${parent}->${child}`

From plan 13-02, dashboard-react/src/lib/mapIcons.ts:
- nodeVisual(deviceType, state) → { image, color, borderWidth, shapeProperties }

From dashboard-react/src/store/useAppStore.ts:
- state.topology: TopologyPayload | null   // { devices?: unknown[]; timestamp?: string }
- state.devices: Record<string, DevicePayload>

From dashboard-react/src/hooks/useNowTick.ts: useNowTick(): number

From kone-design-system: Banner({ status?, message, icon?, onDismiss? })

Existing tests that assert the placeholder text "Topology map — Phase 13":
- src/App.test.tsx:11, src/routes/IndexRoute.test.tsx:22/31/44, src/components/StaleStability.test.tsx:93/136, src/components/ThreePaneLayout.test.tsx:3/23/28
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TopologyMap.tsx with a vis-network test double</name>
  <files>dashboard-react/src/components/TopologyMap.tsx, dashboard-react/src/components/TopologyMap.test.tsx, dashboard-react/src/test/fakeVisNetwork.ts, dashboard-react/vitest.setup.ts</files>
  <read_first>
    - dashboard-react/src/components/MapPlaceholder.tsx (wrapper classes to keep: `rounded-md border border-neutral-150 bg-bg-subtle`, sized-by-container rule)
    - dashboard-react/src/components/Splitter.tsx and Splitter.test.tsx (imperative ref pattern; "stub what jsdom lacks, comment why" test convention)
    - dashboard-react/src/components/TreeNode.tsx lines 80-101 (the `/details?id=${encodeURIComponent(id)}` navigation target)
    - dashboard-react/src/components/GroupingControls.test.tsx lines 1-46 (store reset / MemoryRouter test setup)
    - dashboard-react/vitest.setup.ts
    - dashboard-react/src/lib/topologyLayout.ts and dashboard-react/src/lib/mapIcons.ts (built in 13-02)
    - .planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md sections "Topology Map Rendering", "Empty / First-Load State", "Copywriting Contract"
    - .planning/phases/13-wizard-parents-support-and-topology-map/13-RESEARCH.md Patterns 1-2 and Anti-Patterns
  </read_first>
  <behavior>
    - The Network test double is constructed exactly once across several rerenders with changed statuses; the node DataSet passed to it is the same instance each time
    - After a status change from OK to CRIT for h1, the node DataSet's h1 item has a different `image` than before (updated in place)
    - A topology update that removes a device removes its node and its edges from the DataSets; one that adds a device adds it with x/y
    - Edges exist for every parent->child pair in the topology, with arrows "to"
    - A node with map_position "300,40" is added with x 300, y 40, physics false; a node without one gets its withGridPositions coordinates
    - Emitting "stabilizationIterationsDone" on the double results in a setOptions call containing physics false
    - Emitting "click" with {nodes:["h1"]} navigates to /details?id=h1 when editMode is false, and does NOT navigate when editMode is true
    - devices present + zero edges + editMode false → the text "No connections drawn yet — turn on Edit topology to start." is shown; dismissing it hides it; editMode true hides it
    - zero topology devices → heading "No connections drawn yet" with the Copywriting Contract body is shown, and no Network is constructed
    - an unmanaged node's DataSet item has title "Unmanaged switch (not monitored)"
    - the root element has data-testid="topology-map" in every state
  </behavior>
  <action>
Create `dashboard-react/src/test/fakeVisNetwork.ts`, a minimal `FakeNetwork` class implementing the surface TopologyMap uses: `constructor(container, data, options)`; `on(event, handler)`; `once(event, handler)`; `off`; `setOptions(opts)`; `destroy()`; `getPositions(ids)` (returns the node DataSet's x/y); `disableEditMode()`; `enableEditMode()`; `addEdgeMode()`; plus the test helper `emit(event, params)`. Also export a module-level `instances: FakeNetwork[]` array and a `resetFakeNetworks()` helper. Keep the real vis-data DataSet (it is pure JS and works in jsdom). In `vitest.setup.ts`, register `vi.mock("vis-network/peer", ...)` to return `{ Network: FakeNetwork }`, with a comment explaining that vis-network's canvas renderer calls `getContext("2d")`, which jsdom does not implement. This follows the Splitter.test.tsx convention of stubbing what jsdom lacks. Registering the mock globally is what keeps every test that renders IndexRoute (App, IndexRoute, GroupingControls, StaleStability) working.

Create `dashboard-react/src/components/TopologyMap.tsx`, exporting `TopologyMap(props: { topologyDevices: unknown[]; statuses: Record<string, DevicePayload>; nowMs: number; editMode?: boolean })`, where editMode defaults to false. The component takes the device list as a prop rather than reading the store, so Phase 15 can pass a filtered list (D-11). Import `Network` from `vis-network/peer` and `DataSet` from `vis-data/peer`, and import `vis-network/styles/vis-network.css` (needed by the manipulation toolbar in 13-06). Confirm these subpath entry points against context7's vis-network docs before writing the imports; if the installed version names them differently, use the documented names and note it in the SUMMARY.

- Model: `const model = useMemo(() => buildMapModel(topologyDevices, statuses, nowMs), [...])`.
- Mount once: a `useEffect` with an empty dependency list creates the node and edge DataSets and `new Network(containerRef.current, { nodes, edges }, options)`. It stores all three in refs and returns `network.destroy()`. Options: nodes `{ shape: "circularImage", size: 16, font: { face: "Inter", size: 12, color: "#111114" } }`; edges `{ arrows: "to", color: { color: "#96969f", highlight: "#1450f5", hover: "#1450f5" }, width: 1 }`; `physics: { stabilization: { iterations: 200 } }`; `interaction: { dragNodes: false, hover: true }`; `manipulation: { enabled: false }`; `layout: { randomSeed: 1 }`. Register `network.once("stabilizationIterationsDone", ...)`, which calls `network.setOptions({ physics: false })` (Phase 11 D-09). vis-network has no canvas-background option, so the white #ffffff canvas comes from the inner container div's class (e.g. `bg-white`), not from a Network option.
- Click: `network.on("click", ...)` reads `editModeRef.current`. A ref, not state, because the handler is registered once. When not in edit mode and `params.nodes[0]` exists, it calls `navigate(`/details?id=${encodeURIComponent(id)}`)` using `useNavigate` from `react-router` (D-10, UI-SPEC Click-mode gating).
- Sync: a `useEffect` on `[model]` diffs against the current DataSet ids. Existing nodes get `nodes.update({ id, label, title, ...nodeVisual(deviceType, state) })` with NO x/y, so pan/zoom and dragged positions are never disturbed (D-09). New nodes get `nodes.add` with the withGridPositions x/y plus `physics: false` when `saved` is true. Vanished nodes are removed. Edges are added and removed by their `${parent}->${child}` id the same way. `title` is `"Unmanaged switch (not monitored)"` for unmanaged nodes and undefined otherwise; this resolves UI-SPEC's open unmanaged-marker gap with a hover tooltip only, not a new visual state. Never call `network.setData` (RESEARCH Anti-Patterns).
- Chrome: the root `div` has `data-testid="topology-map"` and the MapPlaceholder wrapper classes plus `relative h-full w-full`. When `model.nodes.length === 0`, render the UI-SPEC empty state (heading "No connections drawn yet" in `text-xl font-semibold`, and the Copywriting Contract body verbatim) and do not mount the canvas div. When nodes exist, zero edges exist, editMode is false and the banner has not been dismissed (local useState), overlay a kone-design-system `Banner` with status "info" and message "No connections drawn yet — turn on Edit topology to start.", with `onDismiss` hiding it.

Write TopologyMap.test.tsx first (RED), covering every behavior bullet with the fake's `instances`, then implement (GREEN).
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/components/TopologyMap.test.tsx && npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "setData" dashboard-react/src/components/TopologyMap.tsx` returns no lines
    - `grep -n "stabilizationIterationsDone" dashboard-react/src/components/TopologyMap.tsx` matches, and so does `grep -n "physics: false"`
    - `grep -n 'circularImage' dashboard-react/src/components/TopologyMap.tsx` matches, and so does `grep -n '"Inter"'`
    - `grep -n "encodeURIComponent" dashboard-react/src/components/TopologyMap.tsx` matches the details navigation
    - `grep -n "vi.mock(\"vis-network/peer\"" dashboard-react/vitest.setup.ts` matches
    - The strings `No connections drawn yet — turn on Edit topology to start.` and `Unmanaged switch (not monitored)` appear in TopologyMap.tsx
    - TopologyMap.test.tsx passes with at least 10 `it(` cases, and the typecheck exits 0
  </acceptance_criteria>
  <done>TopologyMap renders a read-only, in-place-updating vis-network graph with frozen physics, saved or grid positions, click-through navigation and the UI-SPEC empty states, all tested against a jsdom-safe double.</done>
</task>

<task type="auto">
  <name>Task 2: Swap MapPlaceholder for TopologyMap in IndexRoute and update placeholder tests</name>
  <files>dashboard-react/src/routes/IndexRoute.tsx, dashboard-react/src/routes/IndexRoute.test.tsx, dashboard-react/src/App.test.tsx, dashboard-react/src/components/StaleStability.test.tsx, dashboard-react/src/components/ThreePaneLayout.test.tsx, dashboard-react/src/components/MapPlaceholder.tsx</files>
  <read_first>
    - dashboard-react/src/routes/IndexRoute.tsx (whole file; centreTop lines 85-94)
    - dashboard-react/src/routes/IndexRoute.test.tsx
    - dashboard-react/src/App.test.tsx
    - dashboard-react/src/components/StaleStability.test.tsx lines 80-140 (what the element-identity assertion is protecting)
    - dashboard-react/src/components/ThreePaneLayout.test.tsx
  </read_first>
  <action>
In IndexRoute.tsx, read `const topology = useAppStore((s) => s.topology)`. Derive `topologyDevices` with `useMemo`: `Array.isArray(topology?.devices) ? topology.devices : []` (stable reference while topology is unchanged). Replace `<MapPlaceholder />` inside the existing `<div className="min-h-0 flex-1">` with `<TopologyMap topologyDevices={topologyDevices} statuses={devices} nowMs={nowMs} />`. `devices` and `nowMs` are already in scope. Update the D-25 comment above centreTop so it says "map" instead of "map placeholder". Do not change ThreePaneLayout, the tree prop or centreBottom (UI-SPEC Layout Integration). Remove the MapPlaceholder import and delete `dashboard-react/src/components/MapPlaceholder.tsx` (the D-27 placeholder's stated purpose is fulfilled).

Update the tests that assert the placeholder text:
- App.test.tsx and IndexRoute.test.tsx: assert `screen.getByTestId("topology-map")` instead. Keep each test's original intent. For example, IndexRoute.test's before/after identity check becomes an identity check on the `topology-map` element.
- StaleStability.test.tsx:93/136: swap the text query for `getByTestId("topology-map")` and keep the element-identity (`toBe`) assertion. The test's point is that the centre map is not remounted by a staleness tick.
- ThreePaneLayout.test.tsx: replace the MapPlaceholder child with a plain `<div>centre top</div>` and assert that text. ThreePaneLayout does not depend on the map.
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test && npm --prefix dashboard-react run typecheck && npm --prefix dashboard-react run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e dashboard-react/src/components/MapPlaceholder.tsx`
    - `grep -rn "MapPlaceholder\|Topology map — Phase 13" dashboard-react/src` returns no lines
    - `grep -n "<TopologyMap" dashboard-react/src/routes/IndexRoute.tsx` matches, and it sits inside the `min-h-0 flex-1` wrapper
    - The full `npm --prefix dashboard-react test` passes, and `npm --prefix dashboard-react run build` exits 0
  </acceptance_criteria>
  <done>The index route shows the live topology map in the reserved slot; every test and the production build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT retained topology/status → browser canvas | Untrusted ids and aliases become node labels and navigation targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | click → navigate | mitigate | The node id is passed through encodeURIComponent into `/details?id=`, identical to TreeNode.tsx; this is an in-app route only, never an external URL |
| T-13-07 | Information Disclosure / XSS | node labels | accept | vis-network draws labels as canvas text (not HTML); `title` is a fixed literal string, never payload text |
| T-13-08 | Tampering | accidental edits on shared/kiosk display | mitigate | This plan ships with `dragNodes: false` and `manipulation.enabled: false`; editing only arrives behind the D-02 toggle in 13-06/13-07 |
</threat_model>

<verification>
- `npm --prefix dashboard-react test`, `typecheck` and `build` all pass
- No `setData` call and no `MapPlaceholder` reference remains
</verification>

<success_criteria>
DASH-07: index renders a live vis-network topology map with parent/child links, merging updates via DataSet.update rather than re-rendering.
</success_criteria>

<output>
Create `.planning/phases/13-wizard-parents-support-and-topology-map/13-03-SUMMARY.md` when done
</output>
