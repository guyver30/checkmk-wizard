---
phase: 13-wizard-parents-support-and-topology-map
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-react/package.json
  - dashboard-react/package-lock.json
  - dashboard-react/src/assets/icons/device-types/internet.svg
  - dashboard-react/src/assets/icons/device-types/api.svg
  - dashboard-react/src/assets/icons/device-types/secured.svg
  - dashboard-react/src/assets/icons/device-types/videocam.svg
  - dashboard-react/src/assets/icons/device-types/controls.svg
  - dashboard-react/src/assets/icons/device-types/circle.svg
  - dashboard-react/src/lib/mapIcons.ts
  - dashboard-react/src/lib/mapIcons.test.ts
  - dashboard-react/src/lib/topologyLayout.ts
  - dashboard-react/src/lib/topologyLayout.test.ts
  - dashboard-react/src/lib/types.ts
autonomous: true
requirements: [DASH-07]
tags: [dashboard, vis-network, pure-functions]

must_haves:
  truths:
    - "vis-network and vis-data are installed as npm dependencies of dashboard-react (not vendored UMD)"
    - "Every device_type maps to a vendored SVG whose #141414 fill is recoloured to the exact state hex from 13-UI-SPEC.md's State palette table, embedded as a data:image/svg+xml URI"
    - "A pure buildMapModel() turns the topology device list plus device statuses into nodes (label, device type, state, saved position, unmanaged flag) and parent->child edges, and never throws on malformed input"
    - "Hosts without a saved position get non-overlapping row-major grid positions (D-01)"
  artifacts:
    - path: "dashboard-react/src/lib/mapIcons.ts"
      provides: "STATE_HEX, deviceTypeSvg, recoloredDataUri, nodeVisual"
      exports: ["STATE_HEX", "deviceTypeSvg", "recoloredDataUri", "nodeVisual"]
    - path: "dashboard-react/src/lib/topologyLayout.ts"
      provides: "buildMapModel, initialGridPositions, parseMapPosition, formatMapPosition, label-key constants"
      exports: ["buildMapModel", "initialGridPositions", "parseMapPosition", "formatMapPosition", "MAP_POSITION_LABEL", "UNMANAGED_SWITCH_LABEL", "GRID_SPACING"]
    - path: "dashboard-react/src/assets/icons/device-types/internet.svg"
      provides: "Vendored KONE icon, byte-identical copy"
  key_links:
    - from: "dashboard-react/src/lib/mapIcons.ts"
      to: "dashboard-react/src/assets/icons/device-types/*.svg"
      via: "Vite ?raw imports"
      pattern: "device-types/.*\\.svg\\?raw"
    - from: "dashboard-react/src/lib/topologyLayout.ts"
      to: "display.ts effectiveState/displayName + staleness.ts isDeviceStale"
      via: "import"
      pattern: "isDeviceStale|effectiveState"
---

<objective>
Build the pure, DOM-free data layer the topology map renders from: npm dependencies, vendored device-type SVGs, the state-coloured icon builder (D-08/D-09), and the topology-to-graph model with initial grid placement (D-01, D-11).

Purpose: keeps every decision that can be unit-tested (colour mapping, edge derivation, grid math, position parsing) out of the canvas component. Plan 13-03 then only has to wire vis-network to these functions.
Output: `mapIcons.ts`, `topologyLayout.ts`, tightened `TopologyPayload` types, 6 vendored SVGs, and two test files.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-wizard-parents-support-and-topology-map/13-CONTEXT.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-PATTERNS.md

<interfaces>
From dashboard-react/src/lib/display.ts:
- displayName(devicePayload: DevicePayload | null | undefined): string — alias-or-id (D-18)
- effectiveState(devicePayload: DevicePayload | null | undefined): string
- deviceTypeIcon(deviceType): string — key set: other, E-link, ACS, Multimedia, NetworkDevice, GroupController, unknown; fallback "icon-circle"

From dashboard-react/src/lib/staleness.ts:
- isDeviceStale(devicePayload: DevicePayload | null | undefined, nowMs?: number): boolean

From dashboard-react/src/store/selectors.ts (the per-device state rule to mirror exactly):
- key = isDeviceStale(device, nowMs) ? "STALE" : effectiveState(device)

Poller topology node shape (scripts/mqtt_poller.py topology_nodes, extended by plan 13-04 / PLR-13):
{ id: string, parents: string[], device_type: string, folder: string, alias: string, map_position: string | null, unmanaged: boolean }

Vendored icon source: web_assets/kone-design-system-main/packages/kone-ds-assets/icons/{internet,api,secured,videocam,controls,circle}.svg. Every one uses fill="#141414" for its glyph and fill="none" on the root svg.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install vis-network/vis-data, vendor device-type SVGs, build mapIcons.ts</name>
  <files>dashboard-react/package.json, dashboard-react/package-lock.json, dashboard-react/src/assets/icons/device-types/{internet,api,secured,videocam,controls,circle}.svg, dashboard-react/src/lib/mapIcons.ts, dashboard-react/src/lib/mapIcons.test.ts</files>
  <read_first>
    - dashboard-react/package.json
    - dashboard-react/src/lib/display.ts (header comment lines 1-8 and deviceTypeIcon lines 62-76, the table and fallback convention to mirror)
    - dashboard-react/src/lib/display.test.ts (test style)
    - dashboard-react/src/lib/stateMapping.ts (state set; OK/PEND/WARN/UNKNOWN/CRIT/UNREACH/DOWN/STALE)
    - .planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md sections "State palette for map nodes" and "Topology Map Rendering"
    - .planning/phases/13-wizard-parents-support-and-topology-map/13-RESEARCH.md "Package Legitimacy Audit" (both packages [OK]) and Pitfall 5
  </read_first>
  <behavior>
    - STATE_HEX lookup: STATE_HEX.OK === "#22c55e", WARN "#f97316", CRIT/UNREACH/DOWN "#e5252a", UNKNOWN "#111114", PEND "#55555f", STALE "#55555f"
    - deviceTypeSvg("NetworkDevice") returns the internet.svg markup; deviceTypeSvg("E-link") the api.svg markup; deviceTypeSvg(undefined), deviceTypeSvg("unknown") and deviceTypeSvg("nonsense") return circle.svg markup (never throw)
    - recoloredDataUri(svg, "#22c55e") starts with "data:image/svg+xml;utf8," and, once decoded, contains fill="#22c55e" and no fill="#141414"; fill="none" is left untouched
    - nodeVisual("NetworkDevice", "DOWN") has borderWidth 4 with borderDashes false; UNREACH has borderWidth 3 and borderDashes [4,2]; CRIT has 2/false; STALE has 2/[2,2]; OK/WARN/PEND/UNKNOWN have 2/false; color.border equals the state hex; color.highlight.border is "#1450f5"
    - nodeVisual with an unrecognised state falls back to the UNKNOWN styling
  </behavior>
  <action>
Run `npm --prefix dashboard-react install vis-network@^10.1.2 vis-data@^8.0.5`. Both are legitimacy-audited [OK] in 13-RESEARCH.md; install exactly these two names and nothing else. This follows the CONTEXT.md Scope Revision (an npm dependency, not a vendored UMD). Commit the updated package-lock.json.

Copy the six SVGs byte-for-byte (cp, no edits) from `web_assets/kone-design-system-main/packages/kone-ds-assets/icons/` into the new directory `dashboard-react/src/assets/icons/device-types/`. Keep their lowercase-kebab filenames (D-09).

Create `dashboard-react/src/lib/mapIcons.ts` as a pure module. Its header comment is copied from display.ts's "No DOM access, no broker connection, no browser storage, no network calls" contract, plus one sentence explaining that canvas code cannot read CSS custom properties, which is why the hex values are transcribed from 13-UI-SPEC.md's State palette table. Import the six SVGs with Vite's `?raw` suffix (D-09). Export:
- `STATE_HEX: Record<string, string>` with exactly the eight values in the behavior block.
- `deviceTypeSvg(deviceType: string | null | undefined): string`, keyed with the same device_type keys as display.ts deviceTypeIcon: other→circle, E-link→api, ACS→secured, Multimedia→videocam, NetworkDevice→internet, GroupController→controls. Any other value falls back to circle.
- `recoloredDataUri(svgMarkup: string, hexColor: string): string`, which replaces every `fill="#141414"` with the given hex and returns `data:image/svg+xml;utf8,` plus encodeURIComponent(result) (D-08, RESEARCH Pitfall 5).
- `nodeVisual(deviceType, state)`, which returns the vis-network node style fields `{ image, color: { border, background: "#ffffff", highlight: { border: "#1450f5", background: "#ffffff" }, hover: { border, background: "#ffffff" } }, borderWidth, shapeProperties: { borderDashes } }` using the border table in the behavior block (UI-SPEC's accepted CRIT/UNREACH/DOWN tradeoff). Cache the generated data URI in a module-level Map keyed by `${deviceType}|${state}`, so a DataSet.update() of an unchanged node does not re-encode.

Write mapIcons.test.ts first (RED), covering every behavior bullet, then implement (GREEN).
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/lib/mapIcons.test.ts && npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./dashboard-react/package.json');process.exit(p.dependencies['vis-network']&&p.dependencies['vis-data']?0:1)"` exits 0
    - `cmp web_assets/kone-design-system-main/packages/kone-ds-assets/icons/internet.svg dashboard-react/src/assets/icons/device-types/internet.svg` exits 0, and the same holds for the other five files
    - `grep -c 'svg?raw' dashboard-react/src/lib/mapIcons.ts` is 6
    - `grep -E 'document\.|window\.|localStorage|fetch\(' dashboard-react/src/lib/mapIcons.ts` returns no lines
    - mapIcons.test.ts passes, and the typecheck exits 0
  </acceptance_criteria>
  <done>vis-network and vis-data are installed, the SVGs are vendored, and mapIcons.ts turns (device_type, state) into a recoloured data-URI node style with a tested border mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: topologyLayout.ts (buildMapModel, grid, position codec) and TopologyPayload types</name>
  <files>dashboard-react/src/lib/topologyLayout.ts, dashboard-react/src/lib/topologyLayout.test.ts, dashboard-react/src/lib/types.ts</files>
  <read_first>
    - dashboard-react/src/lib/types.ts (whole file, especially the header doctrine lines 1-6 and TopologyPayload lines 80-84)
    - dashboard-react/src/lib/treeModel.ts (pure-function module shape that takes devices/nowMs as parameters and never reads the store)
    - dashboard-react/src/lib/grouping.test.ts (test style)
    - dashboard-react/src/store/selectors.ts lines 25-42 (the STALE-overrides-state rule)
    - dashboard-react/src/lib/display.ts (displayName, effectiveState)
    - scripts/mqtt_poller.py lines 462-490 (topology_nodes/topology_signature node shape)
  </read_first>
  <behavior>
    - parseMapPosition("120,-40") → {x:120,y:-40}; parseMapPosition(" 1,2") → null; ("1.5,2") → null; ("abc") → null; (null/undefined/42) → null; ("1234567,1") → null (more than 6 digits)
    - formatMapPosition(119.6, -40.2) → "120,-40"
    - initialGridPositions(["a","b","c","d","e"]) → 3 columns, spacing 150: a(0,0) b(150,0) c(300,0) d(0,150) e(150,150); initialGridPositions([]) → {}
    - buildMapModel with topology [{id:"sw",parents:[]},{id:"h1",parents:["sw"]}] → 2 nodes, 1 edge {id:"sw->h1",from:"sw",to:"h1"}
    - a parent id that is not in the node list produces no edge; a non-array `parents` is treated as []; a duplicate id keeps only the first; a non-object entry or one with a non-string/empty id is skipped
    - node.state is "STALE" when isDeviceStale(status, nowMs) is true, otherwise effectiveState(status); a node with no status entry gets whatever isDeviceStale(undefined)/effectiveState(undefined) produce (no throw)
    - node.label uses displayName of the status payload when present, else displayName({id, alias}) from the topology entry
    - node.position is the parsed map_position or null; node.unmanaged is true only when the topology entry has unmanaged === true
    - withGridPositions(model.nodes): nodes with a saved position keep it; the rest get initialGridPositions over their own ids in input order, offset so the grid starts GRID_SPACING below the largest saved y (or at y=0 when none are saved)
  </behavior>
  <action>
In types.ts, add an exported `TopologyNode` interface whose fields are all optional, following the file's header doctrine: `id?: string; parents?: unknown; device_type?: string; folder?: string; alias?: string; map_position?: string | null; unmanaged?: boolean`. Change `TopologyPayload.devices` to `devices?: unknown[]`, kept as unknown[] on purpose (untrusted JSON). Add a doc comment pointing at topologyLayout.ts's buildMapModel as the runtime guard. Leave every other type untouched.

Create `dashboard-react/src/lib/topologyLayout.ts` as a pure module with the display.ts no-DOM header comment. Export:
- constants `MAP_POSITION_LABEL = "map_position"`, `UNMANAGED_SWITCH_LABEL = "unmanaged_switch"`, `UNMANAGED_SWITCH_VALUE = "yes"`, `GRID_SPACING = 150`. These are the Checkmk host-label keys that plans 13-04 (poller) and 13-05 (browser writer) both use; their comment names both consumers.
- `parseMapPosition(value: unknown): { x: number; y: number } | null`, validated by `^-?\d{1,6},-?\d{1,6}$`.
- `formatMapPosition(x: number, y: number): string`, which produces rounded integers joined by a comma.
- `initialGridPositions(ids: string[]): Record<string, {x, y}>`, a row-major grid with COLS = ceil(sqrt(n)) and GRID_SPACING spacing (RESEARCH Code Examples; UI-SPEC "Initial grid placement").
- `interface MapNode { id; label; deviceType; state; position: {x,y} | null; unmanaged: boolean }` and `interface MapEdge { id; from; to }`, where the edge id format is `${parent}->${child}` and the arrow runs parent to child (UI-SPEC Edge styling).
- `buildMapModel(topologyDevices: unknown[], statuses: Record<string, DevicePayload>, nowMs: number): { nodes: MapNode[]; edges: MapEdge[] }`.
- `withGridPositions(nodes: MapNode[]): Array<MapNode & { x: number; y: number; saved: boolean }>`.

D-11 forward-compatibility: buildMapModel takes an already-filtered device list as a plain parameter and never reads the store. Its doc comment says a later tag-filter pass (Phase 15) narrows the input list, and this function is not changed. Do not add a filter parameter now.

Write topologyLayout.test.ts first (RED), covering every behavior bullet, then implement (GREEN).
  </action>
  <verify>
    <automated>npm --prefix dashboard-react test -- src/lib/topologyLayout.test.ts && npm --prefix dashboard-react run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function buildMapModel" dashboard-react/src/lib/topologyLayout.ts` matches, and its signature has exactly the three parameters (topologyDevices, statuses, nowMs)
    - `grep -n 'MAP_POSITION_LABEL = "map_position"' dashboard-react/src/lib/topologyLayout.ts` and `grep -n 'UNMANAGED_SWITCH_LABEL = "unmanaged_switch"' dashboard-react/src/lib/topologyLayout.ts` both match
    - `grep -n "useAppStore\|document\.\|fetch(" dashboard-react/src/lib/topologyLayout.ts` returns no lines
    - `grep -n "export interface TopologyNode" dashboard-react/src/lib/types.ts` matches, and every field in it ends with `?:`
    - topologyLayout.test.ts passes with at least 12 `it(` cases; the full `npm --prefix dashboard-react test` still passes; the typecheck exits 0
  </acceptance_criteria>
  <done>buildMapModel/withGridPositions produce a tested, defensive node/edge model with saved-or-grid positions, ready for the map component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT retained `lan/devices/topology` → browser | Untrusted JSON (map_position, parents, ids) feeds the model builder |
| npm registry → dashboard-react build | New third-party packages enter the bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | buildMapModel / parseMapPosition | mitigate | A strict regex `^-?\d{1,6},-?\d{1,6}$`; non-string ids, duplicate ids, non-array parents and dangling parent ids are skipped, never thrown on (tested) |
| T-13-05 | Tampering | recoloredDataUri | mitigate | The only substitution into the SVG is a hex value from the fixed STATE_HEX table, never payload text; the output is encodeURIComponent-encoded into a data: URI |
| T-13-SC | Tampering | npm install vis-network/vis-data | mitigate | Both are [OK] in 13-RESEARCH.md's slopcheck audit, with no postinstall script; exact names and caret ranges are pinned in the committed package-lock.json |
</threat_model>

<verification>
- `npm --prefix dashboard-react test` and `npm --prefix dashboard-react run typecheck` both pass
- `npm --prefix dashboard-react run build` succeeds (the ?raw imports resolve in a production build)
</verification>

<success_criteria>
The map's colour, icon, edge and placement logic exists as pure, fully unit-tested functions, and vis-network/vis-data are installed.
</success_criteria>

<output>
Create `.planning/phases/13-wizard-parents-support-and-topology-map/13-02-SUMMARY.md` when done
</output>
