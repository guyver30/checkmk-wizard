---
phase: 13-wizard-parents-support-and-topology-map
plan: 08
type: execute
wave: 5
depends_on: [13-04, 13-07]
files_modified:
  - dashboard-react/README.md
  - docs/Podman setup for checkmk, minio, mosquitto, worker.md
  - .planning/PROJECT.md
autonomous: false
requirements: [DASH-07, DASH-12, DASH-13, PLR-13]
tags: [docs, live-verification, uat]

must_haves:
  truths:
    - "dashboard-react/README.md documents the topology map, edit mode, the Apply flow, the topology_editor credential setup, the /checkmk-api same-origin route (Vite proxy + CHECKMK_PROXY_TARGET, and the nginx location block for the cutover checklist), and how to remove a mistakenly added switch"
    - "The deployment doc tells an operator exactly how to provision the topology_editor credential and verify the map end-to-end"
    - "PROJECT.md's dashboard constraint and Out of Scope entries reflect that topology editing now writes to Checkmk's REST API from the browser (no new backend)"
    - "On the real deployment: a drawn edge goes live after one Apply and appears in a second browser; a dragged position survives reload in another browser; an added unmanaged switch shows UP with zero services in Checkmk and never WARN/CRIT (D-06)"
  artifacts:
    - path: "dashboard-react/README.md"
      provides: "Topology map & editing section + cutover nginx location"
      contains: "Edit topology"
    - path: "docs/Podman setup for checkmk, minio, mosquitto, worker.md"
      provides: "topology_editor provisioning + Phase 13 dashboard check"
      contains: "provision_topology_editor.py"
  key_links:
    - from: "dashboard-react/README.md cutover checklist"
      to: "nginx location /checkmk-api/"
      via: "documented config block"
      pattern: "checkmk-api"
---

<objective>
Bring the documentation in line with what Phase 13 built (a standing user instruction: update docs after every feature). Then prove the whole chain works on the real deployment, since no Checkmk site is reachable from the development machine.

Purpose: the write path (scoped credential → same-origin route → PUT → Apply → Livestatus → poller → every viewer) has several links that only a live run can exercise. So does D-06's "zero checks" claim.
Output: updated README, deployment doc and PROJECT.md, plus a recorded live UAT verdict.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.9.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-CONTEXT.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-UI-SPEC.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-04-SUMMARY.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-05-SUMMARY.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-06-SUMMARY.md
@.planning/phases/13-wizard-parents-support-and-topology-map/13-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README, deployment doc and PROJECT.md</name>
  <files>dashboard-react/README.md, docs/Podman setup for checkmk, minio, mosquitto, worker.md, .planning/PROJECT.md</files>
  <read_first>
    - dashboard-react/README.md (whole file; §1 "what it is not yet", §4 configuration, §7 cutover checklist)
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md §3 "First-time credential setup" (lines ~148-188), §6, §7 "Dashboard check (Phase 11)" (lines ~428-444)
    - .planning/PROJECT.md (Requirements bullet on the dashboard, Constraints "No new backend", Out of Scope)
    - dashboard-react/src/lib/config.ts, dashboard-react/vite.config.ts, scripts/provision_topology_editor.py, scripts/probe_topology_rest.py (the final implemented names and findings, so the docs match the code exactly)
    - README.md (root) lines 165-185 (Live dashboard section)
  </read_first>
  <action>
dashboard-react/README.md:
- §1: replace the "topology map placeholder" wording with the real map.
- §4: add rows for `CHECKMK_REST_ORIGIN`, `TOPOLOGY_EDITOR_USER` and `TOPOLOGY_EDITOR_SECRET`. The secret row says it is a write-capable, per-deployment value produced once by `scripts/provision_topology_editor.py`, that editing stays disabled while the placeholder is present, and that it should not be committed.
- New section "Topology map and editing": read-only behaviour (live colours, parent→child arrows, click → details; saved positions apply when a node first appears and never move nodes under a viewer). Edit mode: toggle, draw/reconnect/delete links, drag positions, add unmanaged switch, and deleting a mistakenly added switch in Checkmk's own UI (Setup > Hosts), since the dashboard deliberately cannot delete hosts. Apply: one activation; "Saved, but not live yet" appears when another user's changes are pending, because the scoped user never force-activates foreign changes. The 5-minute idle auto-exit.
- Dev/preview proxy: `CHECKMK_PROXY_TARGET` (default `http://localhost:8080`) and why it exists (browser same-origin policy, 13-01 V-CORS verdict).
- §7 cutover checklist: add a step with the nginx block `location /checkmk-api/ { proxy_pass http://checkmk:5000/; }` inside the dashboard service's config, stating it is a forwarding rule on the existing container, not a new service.

Deployment doc:
- Under §3 "First-time credential setup", add a "Note on the topology editor credential (Phase 13)". It gives the exact command `podman exec -it automation-worker bash -c "cd /app/checkmk-wizard && python3 scripts/provision_topology_editor.py"`, describes the manual WATO role step if 13-01's V-ROLE was MANUAL (copy the script's printed procedure), says where to paste the secret, and gives the blast-radius sentence (the credential can edit/add hosts and activate its own changes; it cannot manage users or rules).
- After "Dashboard check (Phase 11)", add a "Topology map check (Phase 13)" with the verification steps from Task 2, condensed.

PROJECT.md:
- Append a dated "(Revised 2026-09-23, Phase 13 ...)" note to the dashboard Requirements bullet (topology map built: DASH-07; edit mode: DASH-12/13).
- Amend the "No new backend for the dashboard" constraint with a dated note: still no server-side application, but topology edit mode writes directly to Checkmk's REST API from the browser with a scoped credential, reached via a same-origin forwarding rule (13-CONTEXT D-04).
- Amend the Out of Scope write-actions bullet so topology editing is the stated exception.

Root README.md: if its "Live dashboard" section describes capabilities that are now stale, add one sentence pointing to dashboard-react/README.md's new section. Otherwise leave it.
  </action>
  <verify>
    <automated>grep -q "Edit topology" dashboard-react/README.md && grep -q "checkmk-api" dashboard-react/README.md && grep -q "provision_topology_editor.py" "docs/Podman setup for checkmk, minio, mosquitto, worker.md" && grep -q "Phase 13" .planning/PROJECT.md && grep -q "CHECKMK_PROXY_TARGET" dashboard-react/README.md</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-react/README.md contains `Edit topology`, `Apply changes`, `TOPOLOGY_EDITOR_SECRET`, `CHECKMK_PROXY_TARGET`, `location /checkmk-api/` and `provision_topology_editor.py`
    - `grep -n "placeholder" dashboard-react/README.md` no longer describes the topology map as a placeholder (only the CHECKMK_BASE_URL/secret placeholder values may remain)
    - The deployment doc contains `provision_topology_editor.py` and a heading containing `Topology map check (Phase 13)`
    - PROJECT.md contains `Revised 2026-09-23, Phase 13` (or equivalent dated Phase 13 note) in both the dashboard bullet and the constraint
    - Every constant/file name mentioned in the docs exists in the code (spot check: `grep -n "CHECKMK_REST_ORIGIN" dashboard-react/src/lib/config.ts` matches)
  </acceptance_criteria>
  <done>The docs match the implemented feature and give an operator a complete setup path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end verification on the deployment host</name>
  <files>none (verification only)</files>
  <read_first>
    - dashboard-react/README.md "Topology map and editing"
    - docs/Podman setup for checkmk, minio, mosquitto, worker.md "Topology map check (Phase 13)"
  </read_first>
  <action>Pause and hand the steps below to the developer, who runs them on the real deployment host. This machine has no podman, broker or Checkmk site. Do not simulate or infer the results. Record the developer's verdict for every numbered step verbatim in the SUMMARY. A failed step becomes a gap with expected versus observed and the decision it relates to (D-xx / DASH-xx); fix nothing inside this task.</action>
  <what-built>
The full Phase 13 chain:
- The poller publishes `map_position` and `unmanaged` on every `lan/devices/topology` node (PLR-13).
- The dashboard's index route shows a live vis-network map in place of the placeholder (DASH-07).
- An "Edit topology" mode writes `parents`, `map_position` and new unmanaged-switch hosts straight to Checkmk's REST API, using a scoped `topology_editor` credential over a same-origin `/checkmk-api` route, and makes them live with one "Apply changes" (DASH-12/DASH-13).
  </what-built>
  <how-to-verify>
On the deployment host, from the repo checkout (pull first):
1. Provision the credential: `podman exec -it automation-worker bash -c "cd /app/checkmk-wizard && python3 scripts/provision_topology_editor.py"`. If it prints a manual WATO role procedure, do that, then re-run it. Paste the printed secret into `TOPOLOGY_EDITOR_SECRET` in `dashboard-react/src/lib/config.ts` (local edit, do not commit). Report whether the role step was REST or manual.
2. Restart the poller so it runs the new code: `cd deploy && podman compose up -d --force-recreate poller`. Then run `podman exec mosquitto mosquitto_sub -u wsreader -P wsreader -t lan/devices/topology -C 1 -W 5` and confirm each device node now has `map_position` and `unmanaged` keys. Paste one node.
3. Start the dashboard on this host so the broker (derived from the page's hostname) and Checkmk are both reachable: `npm --prefix design-system run build && CHECKMK_PROXY_TARGET=http://localhost:8080 npm --prefix dashboard-react run dev -- --host`. Open `http://<HOST_IP>:5173/` from a LAN browser (browser A) and a second browser or device (browser B).
4. DASH-07: confirm the map replaces the old placeholder under the stats strip, icons are coloured by state, and clicking a node opens `/details?id=...`. Confirm hosts start in a grid, not piled up.
5. Confirm "Edit topology" is off on load, and in read-only mode dragging a node does nothing.
6. In A, turn on Edit topology. Use the toolbar's "Add Edge" to draw from a switch/router host to a child host. Confirm "1 change not yet applied" (or a higher count if other changes were already pending). Press "Apply changes" and confirm "Applying…" then "Topology updated".
7. Within about 2 poll cycles (~2 minutes), confirm the arrow appears in browser B without reloading, and in Checkmk (Setup > Hosts > child > Parents).
8. In A, drag a node, Apply, then reload B and confirm the node sits where you dropped it.
9. In A, use "Add Node" to add an unmanaged switch named e.g. `sw-test`, then draw an edge from it to a host and Apply. In Checkmk, confirm `sw-test` exists, is UP, and has ZERO services (no PING), and that after a few minutes it never goes WARN/CRIT (D-06). In the dashboard, hovering it shows "Unmanaged switch (not monitored)".
10. Delete an edge: confirm the "Remove this connection?" dialog appears, then Apply, and the edge disappears in B after the next poll.
11. Failure path: in Checkmk, create any unrelated pending change as `cmkadmin` (e.g. edit a host alias without activating). Then make one map edit and press Apply in A. Confirm "Saved, but not live yet" appears and stays, because the scoped user must not activate foreign changes. Activate in Checkmk afterwards.
12. Leave A in edit mode untouched for 5 minutes and confirm it switches off with "Edit topology turned off after 5 minutes of inactivity".
13. Clean up: delete `sw-test` in Checkmk's own UI and activate.
  </how-to-verify>
  <acceptance_criteria>
    - The developer reported a result for each of steps 1-13
    - Steps 2, 4, 6, 7, 8 and 9 passed (the core DASH-07/12/13 and PLR-13 truths); any failure is recorded as a gap in the SUMMARY with expected vs observed
  </acceptance_criteria>
  <verify><human-check>Developer reports pass/fail for each of steps 1-13 on the live deployment</human-check></verify>
  <done>Per-step live results are recorded in 13-08-SUMMARY.md, with any failures captured as gaps.</done>
  <resume-signal>Type "approved" with per-step results, or describe which steps failed and what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator actions | Docs tell operators where secrets go and what they grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information Disclosure | TOPOLOGY_EDITOR_SECRET handling | mitigate | Docs say to paste it as a local per-deployment edit and not commit it; the checkpoint step 1 says the same; README states the write scope explicitly |
| T-13-09 | Elevation of Privilege | operator over-granting during a manual role step | mitigate | The deployment doc reproduces the provisioning script's exact permission list and names wato.activateforeign as excluded |
| T-13-17 | Elevation of Privilege | foreign-change activation | mitigate | UAT step 11 proves the scoped user cannot activate another user's pending change |
</threat_model>

<verification>
- The doc greps in Task 1 pass
- The Task 2 checkpoint verdicts are recorded in the SUMMARY
</verification>

<success_criteria>
Documentation matches the code, and a live run on the real deployment confirms DASH-07, DASH-12, DASH-13 and PLR-13 end-to-end, including D-06's zero-check switch.
</success_criteria>

<output>
Create `.planning/phases/13-wizard-parents-support-and-topology-map/13-08-SUMMARY.md` when done
</output>
