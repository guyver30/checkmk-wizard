---
context: phase
phase: 08-broker-infrastructure-hardening
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-05T10:36:55.603Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Do not delete or `git worktree remove` either live worktree — they hold unmerged, un-mergeable-elsewhere commits (08-01's tasks 1-2, 08-02's full completed work). Only remove them via the normal execute-phase worktree-cleanup step, after each plan is confirmed complete/mergeable.
- [ ] CONSTRAINT: Do not hand-write or hand-roll `deploy/mosquitto.passwd`. Mosquitto 2.x requires a real argon2id hash from the `mosquitto_passwd` binary (or a container running the mosquitto image); 08-RESEARCH.md explicitly forbids fabricating this hash. It must come from `./deploy/gen-mosquitto-passwd.sh` run on a machine with podman, docker, or `mosquitto_passwd` installed.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Premature worktree cleanup | Removing a paused/checkpointed executor's worktree before its checkpoint is resolved permanently loses uncommitted or unmerged work | blocking | Re-check `git worktree list` and cross-reference against this handoff's "Infrastructure State" section before running any worktree removal command |
| Hand-rolled password hash | Writing a plaintext or manually-computed hash into `deploy/mosquitto.passwd` instead of running the real `mosquitto_passwd` binary | blocking | Always invoke `./deploy/gen-mosquitto-passwd.sh`; if it fails, report the exact error rather than substituting a manual hash |

<current_state>
Executing `/bm:execute-phase 8` (broker-infrastructure-hardening). Wave 1 dispatched two parallel worktree executors off `main@d2a661e`:

- **08-01** (mosquitto.conf/acl/gen-script/passwd) — paused at a genuine checkpoint on task 3/3. Worktree: `/home/kone/checkmk-wizard/.claude/worktrees/agent-a06afbafce13e8f71`, branch `worktree-agent-a06afbafce13e8f71`, HEAD `6ef46ba`.
- **08-02** (compose.yaml + paho-mqtt dependency) — fully complete, all self-checks passed, 222/222 tests pass. Worktree: `/home/kone/checkmk-wizard/.claude/worktrees/agent-aeb0cd40745f7430d`, branch `worktree-agent-aeb0cd40745f7430d`, HEAD `bcf16af`.

Neither worktree branch has been merged into `main` yet — the orchestrator is holding the merge until the whole wave is resolvable (both plans done) to avoid partial STATE.md/ROADMAP.md writes.

Wave 2 (plan 08-03, live-broker smoke test — depends on both 08-01 and 08-02) has not been dispatched.
</current_state>

<completed_work>

Plan 08-01:
- Task 1: Write deploy/mosquitto.conf and deploy/mosquitto.acl — Done (`0cc7a7a`)
- Task 2: Write deploy/gen-mosquitto-passwd.sh — Done (`66686b7`)
- Task 3: Generate and commit deploy/mosquitto.passwd — In progress; script exists and was test-run (correctly refused to fabricate a hash and exited 1 with a clear error, since no podman/docker/mosquitto_passwd is available in this sandbox)

Plan 08-02 (complete, all 2 tasks):
- Task 1: Create deploy/compose.yaml as canonical 4-service stack — Done (`04399f1`)
- Task 2: Add paho-mqtt as a runtime dependency — Done (`62db29f`)
</completed_work>

<remaining_work>

- Plan 08-01, Task 3: Generate `deploy/mosquitto.passwd` on the real deployment host (see human action below), then commit it and finalize `08-01-SUMMARY.md`.
- Merge `worktree-agent-a06afbafce13e8f71` and `worktree-agent-aeb0cd40745f7430d` back into `main`, run the post-merge build/test gate, update STATE.md/ROADMAP.md for Wave 1.
- Dispatch Wave 2: plan 08-03 (live-broker smoke test, repoint Podman setup doc, verify on real deployment host).
</remaining_work>

<decisions_made>

- Held both worktree merges until the whole wave resolves, rather than merging 08-02 immediately, to keep STATE.md/ROADMAP.md writes atomic per wave (standard execute-phase orchestrator pattern).
</decisions_made>

<blockers>
- deploy/mosquitto.passwd generation: needs a real `mosquitto_passwd` binary or podman/docker running the mosquitto image — none present in this sandbox. Must be generated on the actual Checkmk/Podman deployment host. No workaround other than running the script there.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state (same info, structured)
2. `.planning/phases/08-broker-infrastructure-hardening/08-01-PLAN.md` — task 3 spec and verify steps
3. `.planning/phases/08-broker-infrastructure-hardening/08-RESEARCH.md` — "Don't Hand-Roll" section explaining why the password hash can't be fabricated

## Infrastructure State
- Worktree `agent-a06afbafce13e8f71` (branch `worktree-agent-a06afbafce13e8f71`, HEAD `6ef46ba`): live, holds plan 08-01's tasks 1-2 commits. Do not remove.
- Worktree `agent-aeb0cd40745f7430d` (branch `worktree-agent-aeb0cd40745f7430d`, HEAD `bcf16af`): live, holds plan 08-02's complete work. Do not remove.
- `main` at `d2a661e`, plus this session's uncommitted `.planning/STATE.md` (begin-phase marker) and pre-existing unrelated uncommitted changes (`.planning/config.json`, `PROMPT_LOG.md`, `.planning/.pending-auth-captures.jsonl`, `docs/src/`, `screen1.png`) — none of these are phase-08 work, leave as-is.

<context>
This is a standard `/bm:execute-phase` wave-based run. Wave 1 = plans 08-01 + 08-02 (no file overlap, dispatched in parallel worktrees). Wave 2 = plan 08-03 (depends on both). The pause happened mid-Wave-1 because plan 08-01's last task requires real infrastructure (a container runtime or the mosquitto_passwd binary) that doesn't exist in this execution sandbox — this is expected per 08-RESEARCH.md's "Environment Availability" finding, not a bug. The fix is purely a human action on the deployment host; no code/plan changes are needed.
</context>

<next_action>
Start with: confirm `deploy/mosquitto.passwd` has been generated on the deployment host (per the human action below) and pulled into this checkout, then resume agent `a06afbafce13e8f71` (or re-run its remaining task inline if the agent session is gone) to commit task 3 and write the final `08-01-SUMMARY.md`. Then resume `/bm:execute-phase 8` to merge Wave 1 and dispatch Wave 2 (plan 08-03).
</next_action>
