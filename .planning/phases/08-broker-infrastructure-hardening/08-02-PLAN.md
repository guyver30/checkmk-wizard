---
phase: 08-broker-infrastructure-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/compose.yaml
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [BRK-01, BRK-02]

must_haves:
  truths:
    - "deploy/compose.yaml is the canonical full 4-service stack (checkmk, mosquitto, minio, worker), not a mosquitto-only fragment"
    - "The mosquitto service mounts its config, ACL and password files under /mosquitto/config/, the only path the image's baked-in CMD reads"
    - "The WebSockets listener is published to the LAN on host port 9002 without colliding with MinIO's console on host 9001"
    - "The plain-MQTT listener stays published on host 1883 for debugging"
    - "The mosquitto_data named volume backs /mosquitto/data so retained messages survive a container restart"
    - "paho-mqtt is a declared runtime dependency resolvable from uv.lock at a version exposing CallbackAPIVersion"
  artifacts:
    - path: "deploy/compose.yaml"
      provides: "Canonical 4-service Podman Compose stack with the hardened mosquitto wiring"
      contains: "9002:9001"
    - path: "pyproject.toml"
      provides: "paho-mqtt declared as a runtime dependency"
      contains: "paho-mqtt"
  key_links:
    - from: "deploy/compose.yaml"
      to: "deploy/mosquitto.conf"
      via: "bind mount to the image's real config path"
      pattern: "./mosquitto.conf:/mosquitto/config/mosquitto.conf"
    - from: "deploy/compose.yaml"
      to: "deploy/mosquitto.acl"
      via: "bind mount"
      pattern: "./mosquitto.acl:/mosquitto/config/mosquitto.acl"
    - from: "deploy/compose.yaml"
      to: "deploy/mosquitto.passwd"
      via: "bind mount"
      pattern: "./mosquitto.passwd:/mosquitto/config/mosquitto.passwd"
    - from: "deploy/compose.yaml"
      to: "mosquitto_data named volume"
      via: "persistence volume mount"
      pattern: "mosquitto_data:/mosquitto/data"
---

<objective>
Promote the compose stack from a markdown snippet to a real checked-in `deploy/compose.yaml`, wiring the hardened mosquitto service correctly, and add `paho-mqtt` as a project dependency.

Purpose: BRK-01's WebSockets listener and BRK-02's persistence are only real once compose mounts the config at the path the image actually reads and publishes the WS port without colliding with MinIO. D-02 makes this file the single canonical stack definition that Phase 11 later extends with a `dashboard` service.
Output: `deploy/compose.yaml`, updated `pyproject.toml` + `uv.lock`.
</objective>

<execution_context>
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kone/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-broker-infrastructure-hardening/08-CONTEXT.md
@.planning/phases/08-broker-infrastructure-hardening/08-RESEARCH.md
@.planning/phases/08-broker-infrastructure-hardening/08-PATTERNS.md

<interfaces>
<!-- Fixed identifiers shared with Plan 01 and Plan 03. Do not rename. -->

Bind mounts the mosquitto service must declare (host path relative to deploy/ -> container path):
  ./mosquitto.conf   -> /mosquitto/config/mosquitto.conf    (:ro,z)
  ./mosquitto.acl    -> /mosquitto/config/mosquitto.acl     (:ro,z)
  ./mosquitto.passwd -> /mosquitto/config/mosquitto.passwd  (:ro,z)
  mosquitto_data     -> /mosquitto/data                     (:z, named volume)
  mosquitto_log      -> /mosquitto/log                      (:z, named volume)

Published host ports across the whole stack (must stay collision-free):
  8080, 8000, 6556 (checkmk) | 1883, 9002 (mosquitto) | 9000, 9001 (minio)

Mosquitto port mapping: host 9002 -> container 9001 (WebSockets); host 1883 -> container 1883 (plain MQTT)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/compose.yaml as the canonical 4-service stack</name>
  <files>deploy/compose.yaml</files>
  <read_first>
    - `docs/Podman setup for checkmk, minio, mosquitto, worker.md` lines 61-160 — the embedded 4-service compose block this file promotes, plus the `CMK_PASSWORD` prose note
    - `.planning/phases/08-broker-infrastructure-hardening/08-PATTERNS.md` — section "deploy/compose.yaml (config, infra/deployment)" listing the exact four deltas
    - `.planning/phases/08-broker-infrastructure-hardening/08-RESEARCH.md` — Pattern 3 (real config path), Pitfall 1 (mount path), Pitfall 2 (host 9001 collision)
  </read_first>
  <action>
Create `deploy/compose.yaml` by copying the entire embedded compose block from `docs/Podman setup for checkmk, minio, mosquitto, worker.md` (lines 65-157) — all four services `checkmk`, `mosquitto`, `minio`, `worker`, plus the top-level `volumes:` block (`checkmk_data`, `mosquitto_data`, `mosquitto_log`, `minio_data`) and the `networks:` block (`cmk_net`, `driver: bridge`). Per D-02 this is the full stack, not a mosquitto fragment. Preserve the existing numbered section comments (`# 1. Official Checkmk Raw`, `# 2. Mosquitto Broker`, `# 3. MinIO S3 Object Storage`, `# 4. Automation Worker`) and the inline `CMK_SITE_ID` rationale comment on the worker service.

Apply exactly four changes, all inside the `mosquitto` service. Leave `checkmk`, `minio`, and `worker` byte-for-byte identical to the doc:

1. Change the config bind mount from `./mosquitto.conf:/etc/mosquitto/mosquitto.conf:ro,z` to `./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro,z`. The `eclipse-mosquitto` image's baked-in CMD is `mosquitto -c /mosquitto/config/mosquitto.conf`, with no `/etc/mosquitto/` fallback or symlink — the current mount path means the custom config is never read (RESEARCH Pitfall 1).
2. Add two new read-only bind mounts alongside it: `./mosquitto.acl:/mosquitto/config/mosquitto.acl:ro,z` and `./mosquitto.passwd:/mosquitto/config/mosquitto.passwd:ro,z`.
3. Add `"9002:9001"` to the mosquitto `ports:` list for the new WebSockets listener. Do NOT use `9001:9001` — the `minio` service in this same file already publishes host 9001 for its web console, and the duplicate binding makes `podman compose up` fail (RESEARCH Pitfall 2). Only the host-side mapping differs; the container-side WS port stays 9001 to match `deploy/mosquitto.conf`.
4. Keep `"1883:1883"` published exactly as-is. This is deliberate per D-04 — the operator wants LAN reachability for debugging — and it is now credential-protected by `allow_anonymous false` (D-05) rather than anonymous. Do not remove or bind-scope it; internal-only scoping is explicitly deferred out of this phase.

Leave `mosquitto_data:/mosquitto/data:z` and `mosquitto_log:/mosquitto/log:z` unchanged — `mosquitto_data` is what makes `persistence_location /mosquitto/data/` durable across restarts (BRK-02).

Add a short `#` comment at each changed or added line recording WHY, in this repo's documented-why convention: the mount-path correction (the image CMD reads only `/mosquitto/config/`), the `9002` choice (avoids the minio console collision on host 9001), and that `1883` stays published deliberately for debugging while now requiring `poller` credentials.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && grep -q './mosquitto.conf:/mosquitto/config/mosquitto.conf' deploy/compose.yaml && grep -q './mosquitto.acl:/mosquitto/config/mosquitto.acl' deploy/compose.yaml && grep -q './mosquitto.passwd:/mosquitto/config/mosquitto.passwd' deploy/compose.yaml && grep -q 'mosquitto_data:/mosquitto/data' deploy/compose.yaml && grep -q '"9002:9001"' deploy/compose.yaml && grep -q '"1883:1883"' deploy/compose.yaml && test "$(grep -v '^\s*#' deploy/compose.yaml | grep -c '/etc/mosquitto/')" = "0" && test -z "$(grep -oE '\"[0-9]+:[0-9]+\"' deploy/compose.yaml | tr -d '\"' | cut -d: -f1 | sort | uniq -d)" && echo COMPOSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/compose.yaml` exists and parses as YAML with exactly the services `checkmk`, `mosquitto`, `minio`, `worker` — assert with `uv run --with pyyaml python -c "import yaml;d=yaml.safe_load(open('deploy/compose.yaml'));assert set(d['services'])=={'checkmk','mosquitto','minio','worker'};assert set(d['volumes'])=={'checkmk_data','mosquitto_data','mosquitto_log','minio_data'};assert d['networks']['cmk_net']['driver']=='bridge';print('YAML_OK')"` exiting 0
    - File contains the literal strings `./mosquitto.conf:/mosquitto/config/mosquitto.conf`, `./mosquitto.acl:/mosquitto/config/mosquitto.acl`, `./mosquitto.passwd:/mosquitto/config/mosquitto.passwd`
    - File contains `"9002:9001"` and `"1883:1883"`
    - `grep -v '^\s*#' deploy/compose.yaml | grep -c '/etc/mosquitto/'` returns 0 (stale mount path fully removed)
    - No published host port appears twice: `grep -oE '"[0-9]+:[0-9]+"' deploy/compose.yaml | tr -d '"' | cut -d: -f1 | sort | uniq -d` prints nothing
    - The `<verify>` command prints `COMPOSE_OK`
  </acceptance_criteria>
  <done>`deploy/compose.yaml` is the canonical 4-service stack whose mosquitto service mounts all three config artifacts under `/mosquitto/config/`, publishes host 9002 to container 9001 for WebSockets alongside 1883, and backs `/mosquitto/data` with the `mosquitto_data` named volume.</done>
</task>

<task type="auto">
  <name>Task 2: Add paho-mqtt as a runtime dependency</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - `pyproject.toml` — the existing `dependencies` list (lines 10-15) this addition joins
    - `.planning/phases/08-broker-infrastructure-hardening/08-RESEARCH.md` — "Package Legitimacy Audit" (paho-mqtt: PyPI, Eclipse Foundation, slopcheck status OK, Approved) and "Standard Stack" (version 2.1.0, `CallbackAPIVersion.VERSION2`)
  </read_first>
  <action>
Run `uv add paho-mqtt` from the repo root. Use `uv add` rather than hand-editing `pyproject.toml`, because it also resolves and updates `uv.lock`, which a manual edit would leave stale (D-08, and this repo's `uv`-only tooling convention).

This is a runtime dependency, not a dev-group one: Phase 9's poller will import it from `src/checkmk_wizard/`, and Plan 03's `scripts/smoke_test_broker.py` runs via `uv run` against the same environment. Do not pass `--dev` or `--group`.

Expected result: `dependencies` in `pyproject.toml` gains a `paho-mqtt>=2.1.0` entry (uv inserts it alphabetically between `httpx` and `questionary`), and `uv.lock` gains the resolved `paho-mqtt` package. No other dependency should change. If `uv add` re-resolves and bumps unrelated pinned versions in `uv.lock`, record that explicitly in the plan summary rather than silently accepting it.

Package legitimacy is already established by this phase's research — `paho-mqtt` is the Eclipse Foundation's reference Python MQTT client, registry-confirmed on PyPI at 2.1.0, slopcheck status OK with no flags, disposition Approved. No additional legitimacy checkpoint is required for this install.

Do not write any code importing `paho.mqtt` in this task; the smoke-test script is Plan 03's scope.
  </action>
  <verify>
    <automated>cd /home/kone/checkmk-wizard && uv sync --quiet && uv run python -c "import tomllib;d=tomllib.load(open('pyproject.toml','rb'));deps=d['project']['dependencies'];assert any(x.startswith('paho-mqtt') for x in deps), deps;import paho.mqtt.client as m;assert hasattr(m,'CallbackAPIVersion');print('DEP_OK')" && grep -q 'name = "paho-mqtt"' uv.lock && echo LOCK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[project].dependencies` contains an entry starting with `paho-mqtt` (asserted by the `<verify>` tomllib check)
    - `uv.lock` contains `name = "paho-mqtt"`
    - `uv run python -c "import paho.mqtt.client as m; print(m.CallbackAPIVersion.VERSION2)"` exits 0, proving a 2.x resolution (the VERSION2 callback API required by the smoke test and Phase 9's poller)
    - `paho-mqtt` does NOT appear under `[dependency-groups] dev` in `pyproject.toml`
    - Existing dev group (`pytest`, `pytest-asyncio`, `respx`) and the other four runtime dependencies are unchanged
    - `uv run pytest -q` still exits 0 (no regression in the existing mocked suite)
    - The `<verify>` command prints `DEP_OK` then `LOCK_OK`
  </acceptance_criteria>
  <done>`paho-mqtt` is a locked runtime dependency at a 2.x version exposing `CallbackAPIVersion.VERSION2`, and the existing pytest suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN → published container ports (1883, 9002) | Compose decides what the untrusted LAN can reach at all; this file is the network attack-surface definition |
| Host filesystem → container | Bind mounts decide which host files the broker process can read, and whether the hardened config is loaded at all |
| PyPI → project environment | `uv add` pulls third-party code into the runtime dependency set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | mosquitto config bind mount | mitigate | Mount to `/mosquitto/config/mosquitto.conf`, the only path the image's CMD reads; the stale `/etc/mosquitto/` path would silently no-op every auth/ACL control from Plan 01 (RESEARCH Pitfall 1) |
| T-08-08 | Denial of Service | Host port allocation (mosquitto WS vs minio console) | mitigate | Publish `9002:9001`; a `9001:9001` mapping collides with minio and prevents the stack from starting (RESEARCH Pitfall 2). Acceptance criteria assert no duplicate published host port anywhere in the file |
| T-08-09 | Information Disclosure | Host 1883 published to the LAN | accept | Explicit operator decision (D-04) for debugging reachability; residual risk reduced by D-05's credential requirement. Plaintext MQTT on a trusted LAN matches this project's existing plaintext-HTTP-for-Checkmk precedent. Internal-only scoping is a deferred follow-up, recorded in `.planning/STATE.md` deferred items |
| T-08-10 | Spoofing | LAN client reaching the new WS port 9002 | transfer | Enforcement lives in Plan 01's `password_file`/`acl_file`; compose only exposes the port. Plan 03's smoke test proves the control actually holds on the exposed port |
| T-08-SC | Tampering | `uv add paho-mqtt` (PyPI supply chain) | mitigate | RESEARCH "Package Legitimacy Audit" completed: registry-confirmed on PyPI, Eclipse Foundation source repo, `uvx slopcheck scan paho-mqtt --pkg pypi --json` returned status OK with no flags, disposition Approved. Version pinned via `uv.lock`. No `[ASSUMED]`/`[SUS]` packages in this phase, so no blocking legitimacy checkpoint is required |
</threat_model>

<verification>
1. `deploy/compose.yaml` parses as YAML with exactly four services, four named volumes, and the `cmk_net` bridge network.
2. All three mosquitto config artifacts are mounted under `/mosquitto/config/`; no `/etc/mosquitto/` path remains.
3. Published host ports are collision-free; `9002:9001` and `1883:1883` both present.
4. `paho-mqtt` importable via `uv run` with `CallbackAPIVersion` available; `uv run pytest -q` still green.
</verification>

<success_criteria>
- Roadmap Phase 8 success criterion 4 is satisfied: `compose.yaml` exposes the new WebSockets port to the LAN alongside the existing 1883 port.
- The hardened config from Plan 01 is actually loadable by the running broker (correct mount path), making BRK-01/BRK-02 real rather than nominal.
- `paho-mqtt` is available for Plan 03's smoke test and Phase 9's poller.
</success_criteria>

<output>
Create `.planning/phases/08-broker-infrastructure-hardening/08-02-SUMMARY.md` when done
</output>
