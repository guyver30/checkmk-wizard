# Quick 260925-k2x: shared device-type screen

Drop per-host device-type prompt in Phase 4 promotion; run the number-key screen after promotion over promoted (deferred write, stored on OnboardedHost), unpromoted scanned/pending, and onboarded hosts. Runs even if nothing promoted.
