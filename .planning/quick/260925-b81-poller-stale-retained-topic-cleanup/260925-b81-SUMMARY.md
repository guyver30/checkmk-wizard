# Quick 260925-b81: Poller stale retained-topic cleanup Summary

Poller now tombstones retained `lan/devices/{id}/*` topics of hosts absent from the site on startup, behind a confirmed-host-list gate, so a Checkmk rebuild no longer requires wiping `mosquitto_data`.

## Commits
- 23cb9c6: feat(quick-260925-b81): sweep stale retained device topics at poller startup (scripts/mqtt_poller.py, tests/test_mqtt_poller.py)
- 8aea7c8: docs(quick-260925-b81): blank-site reset no longer requires wiping mosquitto_data (Podman doc, section 8.5 and topic contract)

## What changed
- `PollerState.retained_ids`; `reconcile_state` collects ids from non-empty retained `status/history/services/service_history` topics (publishable ids only), subscribing the four wildcards before the topology topic.
- `run_cycle(..., allow_stale_sweep=False)`: tombstones `retained_ids - current_ids - previous_ids` via existing `publish_tombstone`, no "removed" event, then empties `retained_ids`. Gate off leaves the set intact.
- Gate in `run_forever` and `--once`: `bool(snapshots) or (rest_ok and not host_config)`; a LivestatusError skips the cycle so nothing is cleared.

## Verification (actually run)
- `uv run pytest tests/test_mqtt_poller.py -q`: 164 passed.
- `uvx ruff check scripts/mqtt_poller.py tests/test_mqtt_poller.py`: all checks passed.
- Task 2 verify command: exit 0, `deploy_checkmk_data` count 1.
- Not verified: the broker-ordering assumption (topology subscribed last acts as a barrier) was not tested against a live Mosquitto; failure mode is safe (an id is missed, never wrongly cleared). `--once` gate wiring is not covered by a dedicated test.

## Deviations
- [Process] Implementation was written before the tests, so the RED (failing-first) step was not observed for Task 1's TDD flow. Tests were added immediately after and pass.
- Worktree HEAD started at 96e7182 and was reset to a73e0e8 per the branch check.

## Known Stubs
None.

## Self-Check: PASSED
Commits 23cb9c6 and 8aea7c8 exist; modified files present.
