{
  "version": "1.0",
  "timestamp": "2026-09-13T08:39:21.882Z",
  "source": "auto-postool",
  "partial": true,
  "phase": "11",
  "phase_name": "live-dashboard (PLANNED, ready to execute)",
  "phase_dir": ".planning/phases/11-live-dashboard",
  "plan": "0",
  "task": null,
  "total_tasks": 9,
  "status": "auto-checkpoint",
  "completed_tasks": [],
  "remaining_tasks": [
    {
      "id": "01",
      "name": "Task 1: Add staleness and host_state_raw to the poller",
      "status": "not_started"
    },
    {
      "id": "02",
      "name": "Task 1: Vendor mqtt.js 5.15.2 browser UMD",
      "status": "not_started"
    },
    {
      "id": "03",
      "name": "Task 1: Write dashboard/css/dashboard.css from the KONE UI contract",
      "status": "not_started"
    },
    {
      "id": "04",
      "name": "Task 1: dashboard/js/staleness.js and dashboard/js/display.js",
      "status": "not_started"
    },
    {
      "id": "05",
      "name": "Task 1: dashboard/js/state-store.js",
      "status": "not_started"
    },
    {
      "id": "06",
      "name": "Task 1: dashboard/js/render-shell.js",
      "status": "not_started"
    },
    {
      "id": "07",
      "name": "Task 1: dashboard/js/render-index.js — stats strip and grouped fleet overview",
      "status": "not_started"
    },
    {
      "id": "08",
      "name": "Task 1: Detail panel header, flags and the Checkmk deep link",
      "status": "not_started"
    },
    {
      "id": "09",
      "name": "Task 1: Document the dashboard in the deployment doc and the repo READMEs",
      "status": "not_started"
    }
  ],
  "blockers": [
    {
      "description": "Phase 9 (Poller Core) and Phase 10 (Tag-Group Integration) flagged by research as needing a live-verification research pass during planning (Livestatus diff/QoS strategy; Checkmk REST API `tag_<group_id>` attribute shape) — see .planning/research/SUMMARY.md Research Flags.",
      "type": "technical",
      "workaround": ""
    },
    {
      "description": "Phase 8's read-only WS ACL was flagged by research as a critical, easy-to-retrofit-wrong risk if deferred — kept as v1 scope in this phase rather than pushed to v1.x.",
      "type": "technical",
      "workaround": ""
    }
  ],
  "human_actions_pending": [],
  "decisions": [
    {
      "decision": "Milestone: Poll Livestatus over TCP instead of Checkmk notification rules, to preserve the worker/checkmk filesystem boundary",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "Milestone: Redesign MQTT contract around per-device topics (`lan/devices/{id}/...`) rather than full-blob republishes",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "Milestone: Horizontal Layers build order chosen — broker (persistence + ACL from day one) → poller core → Checkmk tagging → dashboard (built last as a pure consumer)",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "[Phase 10.1]: D-10 (retag UX, operator-chosen 2026-09-12, SUPERSEDES 10.1-03-PLAN Task 1 step f prompts): the per-host device-type questionary.select is replaced by a numbered legend + single-digit entry. Print the device type legend once per folder, numbered by index into _load_device_types() (0=other guaranteed by D-06), then prompt each host as \"<host>  (<current>)  [<current index>]: \" accepting one digit; bare Enter keeps the current value. Before any PUT, print an \"N host(s) will be retagged, M unchanged\" summary and require a single Apply? confirm (default No) — this gate is new and protects a 20-host run from a mistyped digit. Rationale: the select-based flow cost 40 interactions for 20 hosts and the \"a few of each type\" pattern is the normal case. Numbering derives from the tag group source, never hand-maintained.",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "[Phase 10.1]: D-11 (retag alias handling, operator-chosen 2026-09-12, SUPERSEDES 10.1-03-PLAN Task 1 step f alias prompt): alias is NOT prompted per host during retag. After the device types are applied, ask once \"Also set aliases on any of these?\" (default No); on yes, questionary.checkbox over the just-retagged hosts, then a questionary.text alias prompt only for the checked ones. D-09 still governs the PUT body — a blank alias means the alias key is OMITTED entirely so Checkmk keeps what it has, never an explicit empty value that would clear an operator-set alias.",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "[Phase 10.1]: D-12 (retag scope, operator-chosen 2026-09-12, SUPERSEDES D-02): every already-onboarded host is a retag candidate regardless of its current device type. The old rule (\"attribute absent OR == other\") assumed a retag only ever corrects an untagged host, but an operator equally needs to fix a host tagged WRONGLY — a device typed ACS that is really a NetworkDevice was unreachable through the wizard, leaving the Checkmk UI as the only route. `_untagged_host_candidates` is renamed `_retaggable_hosts` and now filters only on `exclude_names` (this run's Phase 3 placeholders). A host with no attribute keeps device_type=None and is displayed as \"<first type> (implicit)\"; the absent-vs-explicit distinction is a display concern, not a filter one.",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "[Phase 10.1]: D-13 (retag UI, operator-chosen 2026-09-12, SUPERSEDES D-10's per-host digit prompt): one folder is edited in a full-screen prompt_toolkit list, not a sequence of per-host prompts. The screen shows the numbered device-type legend, every host in the folder with its current and pending type, and a footer menu. Keys: Up/Down (and j/k, PageUp/PageDown) move a clamped — never wrapping — cursor; a digit assigns that type to the row under the cursor; [A] applies and exits; [D] (or Ctrl-C) discards and exits. Only digits that index an existing type are bound, so a stray key is inert. Rationale: with 20 hosts a per-host prompt gave no view of the folder as a whole, no way to revise an earlier answer, and no sense of progress. \"Apply and exit\" writes the folder then asks about the next one; \"discard and exit\" restarts the whole retag flow from its first prompt (a full back-out, not \"skip this folder\"), which re-fetches the host list so a partially-written earlier pass is reflected rather than replayed stale. State lives in a pure RetagSelection dataclass so the cursor/assignment rules are unit-testable; the key bindings themselves are covered by driving the real Application through prompt_toolkit's create_pipe_input + DummyOutput. prompt_toolkit is now an explicit dependency (it was already transitive via questionary).",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "[Phase 12]: D-01 (Phase 12 disk scope, operator-chosen 2026-09-12): the device view shows `Filesystem /` as the headline gauge (`fs_used_percent`, warn/crit 80;90 read from perf_data), plus a badge carrying the worst `fs_used_percent` across all OTHER mount points on that host. Rationale: `/` alone was the first instinct, but Checkmk emits one `Filesystem <mount>` service per mount (live-verified on agent-test 2026-09-12: `Filesystem /` and `Filesystem /boot` are separate services), so a `/`-only gauge would render green while `/var` or `/data` sat at 98%. The badge closes that blind spot for one extra number and no layout change. Consequence: the poller's services query must return every `Filesystem *` service, not just `/`.",
      "rationale": "",
      "phase": "11"
    },
    {
      "decision": "[Phase 12]: D-02 (Phase 12 systemd scope, operator-chosen 2026-09-12): show ONLY the individual systemd services the operator selected during the wizard's Phase 5 \"Which running services should be actively monitored?\" prompt. The `Systemd Service Summary` roll-up (`Total: 163, Disabled: 5, Failed: 0`) is explicitly NOT wanted. Key implementation consequence: the poller has no access to wizard state (separate container; wizard state is only a local JSON snapshot), but it does not need it — Checkmk only creates a `Systemd Service <name>` service for services the wizard chose, so the chosen set is derivable from Livestatus alone by filtering `description ~ ^Systemd Service ` and excluding the literal `Systemd Service Summary`. Live-verified on agent-test 2026-09-12: the operator picked cron, ModemManager and open-vm-tools, and exactly those three appeared alongside the Summary. Per-service data available: state plus `cpu_time`, `mem_used`, `active_since`, `number_of_tasks`.",
      "rationale": "",
      "phase": "11"
    }
  ],
  "uncommitted_files": [],
  "next_action": "Ready to execute",
  "context_notes": "Status: Ready to execute\n\nStopped at: Phase 11 replanned (9 plans, checker passed, ready to execute)"
}
