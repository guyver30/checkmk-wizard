# `kone-ds-assets`

This package contains common digital assets. You can import them directly from this package or copy to your project. Direct import is recommended as it guarantees that assets remain up-to-date.

## Usage

- **/images**: common images and logos
- **/icons**: icons in SVG format. For equivalent icon font try `kone-ds-fonts` package