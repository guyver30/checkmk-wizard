{
  "name": "@kone-ds/fonts",
  "version": "1.1.0",
  "description": "Kone design system fonts",
  "keywords": [
    "fonts"
  ],
  "author": "Vasanthakumar Dharmalingam <vasanthakumar.dharmalingam@kone.com>",
  "homepage": "",
  "license": "SEE LICENSE IN LICENSE",
  "main": "lib/kone-ds-fonts.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://gitlab.prod.devops.kiot.fi/kone-ux/kone-design-system.git"
  },
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "clean": "rimraf dist",
    "copy": "copyfiles -f src/fonts/* ./dist/fonts/ && copyfiles -f src/*.html dist && copyfiles -f src/*.css dist",
    "sass": "npx sass src/fonts.scss dist/fonts.css && npx sass src/demo.scss dist/demo.css",
    "build": "npm run clean && npm run sass && npm run copy",
    "publish": "npm publish"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.1",
    "sass": "^1.63.6"
  }
}
